"""Stub tests for plan 02-10, Section 10.4 gate criterion 1 (peer round trip)."""

import pytest


def test_move_envelope_decoded_by_peer():
    """A move envelope sent by one peer is decoded correctly by the other."""
    pytest.skip("stub — implemented in plan 02-10")


def test_coordinates_survive_round_trip():
    """x/y coordinates survive a full send/receive round trip unchanged."""
    pytest.skip("stub — implemented in plan 02-10")
