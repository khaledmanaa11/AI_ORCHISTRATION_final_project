---
phase: 03-blind-strategy-module-rl-policy
plan: "23"
type: execute
wave: 6
depends_on: ["03-15", "03-21"]
files_modified:
  - training/preflight.py
  - training/probes.py
  - training/loop.py
  - tests/unit/training/test_preflight.py
  - tests/unit/training/test_probes.py
autonomous: true
requirements:
  - D-30
  - D-29
  - STRAT-06

must_haves:
  truths:
    - "D-30: all six checks are computable BEFORE episode 1 and run 1 failed FOUR of them at t=0. The entire 300,000-episode night was avoidable, and this gate is the reason it cannot happen again. It exits non-zero before the first episode rather than warning"
    - "D-30 check 1 — terminal reward defined and NON-ZERO for both outcomes for both roles. Run 1 failed this: `_reward` returned `reward_capture if role == 'cop' else 0.0`, so the thief's capture signal was literally zero. Asserted against 03-14's `terminal_reward`, which is a pure function and needs no episode to probe"
    - "D-30 check 2 — discounted terminal-value spread delta per role, failing if delta_role < 0.1 or delta_cop/delta_thief > 3. Run 1 failed this: thief 0.047 against cop 0.626, a ratio of 13.3. This is the check that would have caught the degenerate thief MDP in seconds"
    - "D-30 check 3 — epsilon_floor_episode <= 0.15 * episodes, and the same for alpha. Run 1 failed this: the floor was reached at episode 299,999 of 300,000, a ratio of 1.0 against a limit of 0.15"
    - "D-30 check 4 — the sampler yields >= min_distinct_starts distinct starts AND contains every eval start. Run 1 failed this: one start state, 150,000 visits on a single key, 5/20 eval starts covered for the cop and 4/20 for the thief. Asserted by CALLING 03-15's `distinct_start_count()` and `covers()`, never by re-deriving mu contains d"
    - "D-30 check 5 — every opponent pool contains at least one weak opponent, checked on the REALISED pool rather than the configured one. Run 1's configured mix silently renormalised to 0.375/0.625, so a check against configuration alone would have passed while the run was already wrong"
    - "D-30 check 6 — the four Jones probe environments recover their known-correct values (constant reward; random observations with predictable rewards; two timesteps; action-dependent rewards). NOTE: D-30's prose says 'six checks' and then names five; the sixth is section F.3's probe environments, and section F.3 is what D-30 cites. This discrepancy is recorded, not silently resolved"
    - "EVERY assertion must be SHOWN TO FIRE against a deliberately broken configuration — an assertion that cannot fail is worthless. Run 1's own configuration is the natural fixture for four of the six, and using it makes the tests self-documenting: the gate demonstrably catches the exact run it was built in response to"
    - "D-29: exactly one run. The gate is what makes a single run a reasonable bet rather than a gamble — it converts four failure modes from discovered-after-a-night into discovered-in-seconds"
  artifacts:
    - path: training/preflight.py
      provides: "The five config/sampler assertions, wired as a hard gate before episode 1"
      contains: "def preflight"
      min_lines: 70
    - path: training/probes.py
      provides: "The four Jones probe environments and their known-correct values"
      contains: "def run_probes"
      min_lines: 45
    - path: tests/unit/training/test_preflight.py
      provides: "Every assertion shown to fire, four of them against run-1's own configuration"
      min_lines: 60
  key_links:
    - from: training/preflight.py
      to: training/starts.py
      via: "check 4 calls 03-15's distinct_start_count() and covers() rather than re-deriving mu contains d"
      pattern: "covers|distinct_start_count"
    - from: training/preflight.py
      to: training/rewards.py
      via: "check 1 asserts 03-14's terminal_reward is non-zero for all four (role x outcome) pairs"
      pattern: "terminal_reward"
    - from: training/loop.py
      to: training/preflight.py
      via: "hard gate: non-zero exit before episode 1, never a warning"
      pattern: "preflight"
---

<objective>
The gate that makes run 1 unrepeatable.

Run 1 burned a full night on four defects that were **all computable before episode 1**: the thief's
terminal reward was zero, its discounted value spread was 13× weaker than the cop's, the ε/α floors
arrived at the final episode, and every episode started from one board. None of these needed a
single episode to detect.

This plan turns each into an assertion that runs in seconds and **exits non-zero before episode 1**.
Under D-29 there is exactly one run-2 attempt; this gate is what makes that a reasonable bet.

Output: `preflight.py`, `probes.py`, and a hard gate in `loop.py::main`.
</objective>

<execution_context>
@C:\Users\Hp\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Hp\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/03-blind-strategy-module-rl-policy/03-PLAN-OUTLINE-RUN2.md

- `03-CONTEXT.md` `<superseded>` — **D-30** (the six checks) and **D-29** (one run). Note that D-30's
  prose says "six" and names five; §F.3's probe environments are the sixth. **Record the
  discrepancy in the module docstring rather than resolving it silently.**
- `docs/phases/phase-3/RUN-1-POSTMORTEM.md` — Findings 1, 2, 3 supply the exact failing values each
  check must catch.
- Grep `docs/research/TRAINING-METHODOLOGY.md` for **§F.3** — the checks verbatim, including the
  Jones probe environments. Do not read it whole (77 KB).
- `03-15-PLAN.md` — `distinct_start_count()` and `covers()`, the surface check 4 asserts against.
- `03-14-PLAN.md` — `terminal_reward`, a pure function, the surface check 1 asserts against.
- `03-21-SUMMARY.md` — the hand-computed discounted terminal spread per role. **Check 2 must agree
  with it**; a disagreement means one of the two is wrong and must be resolved before the run.

**Sequencing — wave 6:** 03-21 set every magnitude; this plan measures them. It **sets nothing** and
**declares no config key** — the thresholds (`terminal_spread_min`, `terminal_spread_ratio_max`,
`floor_episode_fraction_max`, `min_distinct_starts`, `weak_opponent_floor`) were declared by 03-13
and given values by 03-21.
</context>

<task type="auto">
  <name>Task 1: The four Jones probe environments</name>
  <files>training/probes.py, tests/unit/training/test_probes.py</files>
  <read_first>
    docs/research/TRAINING-METHODOLOGY.md section F.3 (grep for the probe list — the four environments and their expected values)
    src/pursuit/strategy/linear_q.py (03-20: the learner the probes exercise)
  </read_first>
  <action>
    Tests first. `training/probes.py` (~45-90 code lines): four tiny environments whose correct
    value functions are known in closed form, run against the real learner:

    1. **Constant reward** — one state, one action, reward 1 every step. Recovers the discounted
       constant.
    2. **Random observations, predictable reward** — value must be independent of the observation.
    3. **Two timesteps** — catches a broken discount or a broken bootstrap.
    4. **Action-dependent reward** — catches an update that ignores which action was taken, which is
       exactly the semi-gradient indexing bug 03-20 could plausibly introduce.

    Each probe asserts the learner converges to its known value within a tolerance read from config.
    Keep them independent of the game engine — a probe that needs `GameState` tests the engine, not
    the learner.

    **This file is the plan's 150-line split:** the five config/sampler assertions live in
    `preflight.py`, the probe environments here, because they need their own tiny environments.
  </action>
  <verify>
    `uv run pytest tests/unit/training/test_probes.py -q` passes, covering: each probe recovers its
    known value; each probe FAILS when handed a deliberately broken learner (wrong discount, ignored
    action, dropped bootstrap) — a probe that cannot fail is worthless; probes run in well under a
    second so the gate stays cheap enough to run before every job.
  </verify>
  <done>Four probes that detect a broken learner without running a single game episode.</done>
</task>

<task type="auto">
  <name>Task 2: The five config and sampler assertions</name>
  <files>training/preflight.py, tests/unit/training/test_preflight.py</files>
  <read_first>
    training/rewards.py (03-14: terminal_reward)
    training/starts.py (03-15: distinct_start_count, covers)
    training/sparring.py (03-21: the realised pool)
    docs/phases/phase-3/RUN-1-POSTMORTEM.md (Findings 1, 2, 3 — the exact values each check must catch)
  </read_first>
  <action>
    Tests first. `training/preflight.py` (~70-130 code lines) exposing

      preflight(params, game_params, sampler, pools) -> list[Failure]

    returning **all** failures, not just the first — an operator fixing four defects wants to see
    four, not to rediscover them one run at a time.

    1. `terminal_reward` is defined and **non-zero** for all four (role × outcome) pairs.
       *Run-1 value: thief capture = 0.0.*
    2. Discounted terminal-value spread Δ per role: fail if `Δ_role < terminal_spread_min` or
       `Δ_cop / Δ_thief > terminal_spread_ratio_max`. *Run-1 values: 0.047 and 0.626, ratio 13.3.*
    3. `epsilon_floor_episode ≤ floor_episode_fraction_max × episodes`, and the same for α.
       *Run-1 value: 299,999 / 300,000 = 1.0.*
    4. `sampler.distinct_start_count() ≥ min_distinct_starts` **and**
       `sampler.covers(eval_starts)`. *Run-1 values: 1 distinct start; 5/20 and 4/20 covered.*
    5. Every **realised** pool contains at least one weak opponent. Check the realised pool, not the
       configured one — run 1's configured mix silently renormalised to 0.375/0.625, so a
       configuration-only check would have passed on an already-wrong run.

    Then run Task 1's probes as check 6.

    Wire into `training/loop.py::main` as a **hard gate**: run before episode 1, print every failure
    with its measured value and its threshold, and **exit non-zero**. Never a warning. Provide no
    `--skip-preflight` flag — a bypass would be used at 2am, which is exactly when it must not be.
  </action>
  <verify>
    `uv run pytest tests/unit/training/test_preflight.py -q` passes, covering: **each of the six
    checks fires against a deliberately broken configuration**, four of them driven by run-1's own
    values (thief capture reward 0.0; spread ratio 13.3; floor fraction 1.0; one distinct start) so
    the gate demonstrably catches the run it was built for; a valid run-2 configuration passes all
    six; `preflight` returns **all** failures rather than short-circuiting; `loop.py` exits non-zero
    before any episode when preflight fails, verified by asserting no curve row was written.
  </verify>
  <done>Six checks gate every run, each proven to fire, with run 1 as the worked example.</done>
</task>

<verification>
1. Run-1's exact configuration fails checks 1, 2, 3 and 4 — the gate catches the night it exists for.
2. A valid run-2 configuration passes all six.
3. Every assertion is shown to fire; none is decorative.
4. Check 2 agrees with 03-21's hand-computed spread; a disagreement blocks the run.
5. No bypass flag exists. The gate exits non-zero before episode 1.
6. This plan declares no config key and sets no magnitude (D-18).
7. Every touched file <=150 **code** lines: `bash scripts/check_line_limit.sh`.
8. `uv run ruff check .` -> 0; `uv run pytest --cov=pursuit --cov=training` green at >=85%.
</verification>

<success_criteria>
- No run can start with a zero terminal reward, a collapsed value spread, a floor at the final
  episode, a single start state, or a pool with no weak opponent.
- The four defects that cost run 1 a night are each caught in seconds.
- Every check is proven capable of failing, using run 1 as the fixture.
</success_criteria>

<output>
Create `.planning/phases/03-blind-strategy-module-rl-policy/03-23-SUMMARY.md` when done, recording
the measured value of each of the six checks against the final run-2 configuration — that record is
what 03-25's operator reads before committing the night.
</output>
