> # ⛔ SUPERSEDED — DO NOT EXECUTE
>
> This plan was written for the **run-2 design of 2026-08-03**: alpha-beta search bolted onto
> the sequential engine, with the Q-policy demoted to a learned evaluation.
>
> That design assumed a **sequential turn order**. The book does not permit one — §5.3.2 p.35
> makes the turn simultaneous, and the mandatory Commit-Reveal protocol exists to enforce it.
> The premise of this plan is therefore false, and executing it would rebuild the defect.
>
> **What was actually built instead**, on 2026-08-08:
> `docs/phases/phase-3/PRD.md`, `docs/PRD_matrix_mover.md`, and
> `docs/phases/phase-3/ENGINEERING-LOG.md` (why this plan set was abandoned).
> Landed in commits 8b30328, f3d9847, 2606efa, 7040d7a — merged to main as 6ca38fa.
>
> Kept unmodified below as the record of what was planned and why it was dropped.

---

---
phase: 03-blind-strategy-module-rl-policy
plan: "24"
type: execute
wave: 6
depends_on: ["03-21", "03-22"]
files_modified:
  - docs/phases/phase-3/PRD.md
  - docs/phases/phase-3/PLAN.md
  - docs/phases/phase-3/TODO.md
  - docs/TODO.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - DOC-01
  - D-27

must_haves:
  truths:
    - "Project CLAUDE.md makes the per-phase triplet mandatory and grader-facing: docs/phases/phase-3/{PRD,PLAN,TODO}.md must exist and describe what the phase ACTUALLY delivers. All three currently describe the run-1 tabular design, which the code no longer implements. Roadmap task 03-97 is this row"
    - "Rows 03-11..03-16 in docs/phases/phase-3/TODO.md are TODO-row numbers and plans 03-11..03-25 are PLAN-file numbers, and THE RANGES OVERLAP WITHOUT MATCHING. The mapping in the outline section 6 is authoritative and must be written into the TODO so no future reader assumes row 03-13 corresponds to plan 03-13 — it does not (row 03-13 maps to plans 03-11, 03-16, 03-18 and 03-19)"
    - "NO BOX IS TICKED IN THIS PLAN. Ticking is /gsd:verify-work 3 (roadmap task 03-99), and only after run 2 has been measured. A phase whose gate is unmet must not carry ticked completion rows — that is the misreporting rule 38 forbids"
    - "The withdrawn rows T4-followup-1 and T4-followup-2 STAY STRUCK THROUGH with their measured-false rationale intact. Deleting them would erase the record that two recorded follow-ups were measured and disproven, which is part of the phase's evidence"
    - "GATE-4 remains explicitly UNTICKED and its failing run-1 numbers stay visible: cop 0.250 against the 0.55 floor, thief 0.800 against a 0.900 baseline. The phase is not complete and the tracker must say so plainly"
    - "D-27: the triplet cross-references docs/phases/phase-3/STRAT-01-06-DEVIATION.md so a grader reading the phase PRD is routed to the deviation defence rather than discovering the mismatch alone"
    - ".planning/ROADMAP.md's Phase 3 plan list still names four stale placeholders (03-01 BrainBase, 03-02 state encoding, 03-03 harness, 03-04 PRD) that do not match the 26 plans that now exist. It is refreshed to the real list"
  artifacts:
    - path: docs/phases/phase-3/PRD.md
      provides: "Phase 3's deliverable restated as search over a learned evaluation, with the section 10.4 gate as acceptance criteria"
      contains: "STRAT-01-06-DEVIATION"
    - path: docs/phases/phase-3/TODO.md
      provides: "Rows 03-11..03-16 mapped to plan IDs; withdrawn rows preserved; nothing ticked"
      contains: "03-25"
  key_links:
    - from: docs/phases/phase-3/PRD.md
      to: docs/phases/phase-3/STRAT-01-06-DEVIATION.md
      via: "the phase PRD routes a grader to the deviation defence"
      pattern: "DEVIATION"
---

<objective>
Make the grader-facing trackers describe what the phase actually builds.

`docs/phases/phase-3/{PRD,PLAN,TODO}.md` all describe the run-1 tabular design. The code no longer
implements it. Project `CLAUDE.md` makes this triplet mandatory and explicitly grader-facing, so a
stale triplet is a visible defect rather than an internal inconsistency.

**What this plan does not do: tick anything.** Phase 3's gate is unmet until run 2 is measured
(plan 03-25), and `/gsd:verify-work 3` owns the ticking. A tracker showing completed rows for an
unmet gate is the kind of misreporting rule 38 treats as disqualifying.
</objective>

<execution_context>
@C:\Users\Hp\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Hp\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/03-blind-strategy-module-rl-policy/03-PLAN-OUTLINE-RUN2.md

**Outline §6 holds the authoritative row→plan mapping.** The namespaces collide:

| `docs/phases/phase-3/TODO.md` row | Plan(s) |
|---|---|
| row 03-11 thief safety rule | plan 03-12 |
| row 03-12 pre-flight assertions | plan 03-23 |
| row 03-13 cycle evaluation + alpha-beta | plans 03-11, 03-16, 03-18, 03-19 |
| row 03-14 barrier placement rewrite | plan 03-17 |
| row 03-15 run-2 training config | plans 03-20, 03-21 |
| row 03-16 `turns_remaining` | plan 03-13 |
| row 03-97 phase triplet | plan 03-24 (this one) |
| R1 / R2+R4 / R3+R5 / R6 | plans 03-15 / 03-14 / 03-21 / 03-25 (verify) |
| D-27 | plan 03-22 |
| GATE-4 (§10.4) | plan 03-25 |

- `docs/phases/phase-3/{PRD,PLAN,TODO}.md` — 113 / 152 / 93 lines, all describing run 1.
- `03-22-SUMMARY.md` — the deviation document this triplet must cross-reference.
- Project `CLAUDE.md` — the per-phase triplet rule and what each file must contain.
</context>

<task type="auto">
  <name>Task 1: Refresh the phase triplet</name>
  <files>docs/phases/phase-3/PRD.md, docs/phases/phase-3/PLAN.md, docs/phases/phase-3/TODO.md</files>
  <read_first>
    docs/phases/phase-3/PRD.md, PLAN.md, TODO.md (all three, whole — they are small)
    docs/phases/phase-3/STRAT-01-06-DEVIATION.md (03-22's output, cross-referenced from the PRD)
    CLAUDE.md (the per-phase triplet rule: what PRD, PLAN and TODO must each contain)
  </read_first>
  <action>
    **PRD.md** — restate the deliverable as search over a learned evaluation. Keep the §10.4
    milestone gate as the acceptance criteria and keep the REQ-ID coverage list. Add a prominent
    cross-reference to `STRAT-01-06-DEVIATION.md` so a grader reading the PRD is routed to the
    defence rather than discovering the mismatch on their own.

    **PLAN.md** — replace the component/file list with what waves 1-7 actually build:
    graph primitives, safety filter, state/config surface, terminal signal, randomised starts,
    features and evaluation, barrier rewrite, search, `SearchBrain`, linear learner, run-2 regime,
    the deviation defence, the pre-flight gate, this refresh, and the run. Record the phase-specific
    ADRs (D-26…D-31) by reference to `03-CONTEXT.md`'s `<superseded>` section rather than copying
    them — one authoritative source.

    **TODO.md** — add rows for plans 03-17…03-25, and annotate rows 03-11…03-16 with the plan IDs
    that implement them, using the §6 table above. **Write the namespace warning into the file
    itself**: row numbers and plan numbers overlap and do not correspond.

    Three preservation rules:
    - T4-followup-1 and T4-followup-2 stay **struck through** with their measured-false rationale.
    - GATE-4 stays **unticked**, with run-1's failing numbers visible.
    - **Nothing is ticked.** Status stays ☐ or ◐.
  </action>
  <verify>
    All three files describe search over a learned evaluation, not a tabular Q-policy. `grep -c "^| .*| ☑"`
    on TODO.md shows no NEW ticked row beyond those already true before this plan. The struck-through
    withdrawn rows are still present. GATE-4 is unticked with its failing numbers. PRD.md links
    `STRAT-01-06-DEVIATION.md`. Every plan 03-11…03-25 appears exactly once in TODO.md.
  </verify>
  <done>The triplet describes the built design; nothing is ticked; the withdrawn and failing records survive.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile the roadmap and root TODO</name>
  <files>.planning/ROADMAP.md, docs/TODO.md</files>
  <read_first>
    .planning/ROADMAP.md (the Phase 3 section — its plan list names four stale placeholders)
    docs/TODO.md (the root tracker's Phase 3 rows)
  </read_first>
  <action>
    `.planning/ROADMAP.md`: replace Phase 3's stale four-item plan list (`03-01` BrainBase,
    `03-02` state encoding, `03-03` harness, `03-04` PRD) with the real set — 03-00…03-10 complete,
    03-11…03-25 planned — keeping tasks `03-96`, `03-97` and `03-99` intact. Phase 3's status stays
    **executing**; its success criteria are unchanged (they are the book's §10.4 gate and are not
    ours to edit).

    `docs/TODO.md`: keep the Phase 3 rows consistent with the phase TODO. Same rule — **tick
    nothing.**

    Record in the SUMMARY that `/gsd:verify-work 3` (task 03-99) is what ticks both, and only after
    03-25 measures GATE-4.
  </action>
  <verify>
    ROADMAP.md's Phase 3 plan list matches the files in the phase directory. Tasks 03-96/03-97/03-99
    survive. Phase 3 is still marked executing. `docs/TODO.md`'s Phase 3 rows agree with the phase
    TODO, and no row gained a tick.
  </verify>
  <done>Roadmap and root tracker agree with the phase triplet and with the files on disk.</done>
</task>

<verification>
1. All three triplet files describe the search-based design; none still describes a tabular mover.
2. PRD.md routes the reader to STRAT-01-06-DEVIATION.md.
3. The row-vs-plan namespace collision is documented in TODO.md itself.
4. Withdrawn rows remain struck through; GATE-4 remains unticked with its failing numbers.
5. No box is ticked anywhere by this plan (rule 38; verify-work owns ticking).
6. ROADMAP.md's Phase 3 plan list matches the phase directory.
7. `uv run ruff check .` -> 0 and the test suite is unchanged — this plan touches no code.
</verification>

<success_criteria>
- A grader opening `docs/phases/phase-3/` sees a triplet describing what was actually built, routed
  to the deviation defence.
- The trackers are honest about an unmet gate rather than optimistic about it.
- Row numbers and plan numbers can no longer be confused by a future reader.
</success_criteria>

<output>
Create `.planning/phases/03-blind-strategy-module-rl-policy/03-24-SUMMARY.md` when done, noting that
ticking is deferred to `/gsd:verify-work 3` after 03-25.
</output>
