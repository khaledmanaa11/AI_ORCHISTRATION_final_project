---
phase: 03-blind-strategy-module-rl-policy
plan: "22"
type: execute
wave: 5
depends_on: ["03-19", "03-20"]
files_modified:
  - docs/PRD_rl_strategy.md
  - docs/phases/phase-3/STRAT-01-06-DEVIATION.md
  - README.md
autonomous: true
requirements:
  - D-27
  - DOC-02
  - STRAT-01
  - STRAT-06

must_haves:
  truths:
    - "D-27: this is the largest grading risk in the phase. STRAT-01 reads 'move selection uses a trained tabular Q-learning policy via BrainBase._pick_move' and STRAT-06 reads 'a trained Q-table ships'. Under D-26 both are met in SUBSTANCE but not in the LITERAL SHAPE the requirement text describes. An undocumented deviation from a book requirement reads to a grader as a missed requirement — the document is the deliverable, not a footnote in a SUMMARY"
    - "D-27: the defence states the deviation PLAINLY in its first paragraph. It does not bury it, does not argue the requirement means something other than what it says, and does not claim the deviation is not a deviation. The honest framing is the persuasive one: a measured negative result drove a design change, and the change is defensible on evidence"
    - "Every number in the document is one already measured and recorded: top 1% of cop keys hold 53.2% of all 3,525,039 visits; 5/20 eval start states cleared min_visits for the cop and 4/20 for the thief; median best-vs-second Q margin across the whole cop table 0.00000; cop 0.600 on trained starts against 0.133 on unseen; GATE-4 cop 0.250 against the 0.55 floor and thief 0.800 against a 0.900 heuristic baseline. NO NEW NUMBER IS PRODUCED BY THIS PLAN"
    - "The document shows what STILL SHIPS, concretely: a trained per-role weights file reached from `_pick_move`, learning curves from run 2, and run-1's curves retained as the historical record. STRAT-06's 'a trained table ships' is satisfied by a trained artifact that ships — its shape changed from 1.7M action entries to ~60 tuned evaluation weights"
    - "Every claim is checkable against code that has already landed — which is why this plan waits for wave 4. A defence citing code that does not exist yet is worse than no defence"
    - "The ablation pilot is NOT cited as support anywhere in the document. It ran, it is INCONCLUSIVE (every pairwise comparison insignificant: cop A-vs-C p=0.407, thief A-vs-D p=0.451), and the case rests on post-mortem Findings 1-4, which are direct measurements"
    - "The corrected statistics are used, not the inflated ones: eval replays are deterministic, so the true sample size is n=20, not 200. The honest recomputation gives cop p=0.250 and thief p=0.500 — NEITHER significant at alpha=0.05. The CLI's originally reported mcnemar_p ~ 0.0000 and z=3.95 were pseudo-replication artifacts and must not appear as evidence"
    - "DOC-02 / Segal versioning: `docs/PRD_rl_strategy.md` goes to v2.00 and describes search-over-learned-evaluation as the mechanism. Every number in it keeps its Source column marking it PARAMETERS.md versus engineering default. The rule-42 README section is updated to describe what run 2 actually produced"
    - "One earlier attribution error is corrected rather than quietly dropped: capture time on a grid is Mehrabian, Discrete Math 311:102-105, 2011 — an attribution to arXiv:1708.08255 made during the literature review was WRONG. Carrying the correction forward is part of the record"
  artifacts:
    - path: docs/phases/phase-3/STRAT-01-06-DEVIATION.md
      provides: "The grader-facing defence: what the requirements say, what was built, the measured reason, and what still ships"
      min_lines: 60
    - path: docs/PRD_rl_strategy.md
      provides: "v2.00 — search over a learned evaluation, replacing the tabular-policy mechanism description"
      contains: "2.00"
  key_links:
    - from: docs/phases/phase-3/STRAT-01-06-DEVIATION.md
      to: docs/phases/phase-3/RUN-1-POSTMORTEM.md
      via: "every measured claim cites the post-mortem finding it came from; no number is restated without its source"
      pattern: "RUN-1-POSTMORTEM"
    - from: docs/phases/phase-3/STRAT-01-06-DEVIATION.md
      to: src/pursuit/strategy/searchbrain.py
      via: "the 'what still ships' section points at code that has already landed"
      pattern: "searchbrain|SearchBrain"
---

<objective>
Write the deviation defence. This plan produces **no code** — and it exists as its own plan
precisely because it would otherwise become a paragraph in someone else's SUMMARY and be graded as
a missed requirement.

**The situation, stated plainly.** STRAT-01 requires move selection from *a trained tabular
Q-learning policy via `BrainBase._pick_move`*. STRAT-06 requires that *a trained Q-table ships*.
Under D-26 the mover is alpha-beta search and the learned artifact is ~60 evaluation weights. Both
requirements are satisfied in substance — a trained artifact is reached from `_pick_move` and it
ships — but not in the literal shape the requirement text describes.

**Why the honest framing is also the strongest one.** The deviation is not a shortcut; it is the
consequence of a measured negative result. Run 1 was a real 300,000-episode run, its failure was
diagnosed rather than explained away, no bar was lowered, and no table was promoted. That is a
better story than a tabular policy that quietly scored 0.250.
</objective>

<execution_context>
@C:\Users\Hp\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Hp\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/03-blind-strategy-module-rl-policy/03-PLAN-OUTLINE-RUN2.md

- `03-CONTEXT.md` `<superseded>` — **D-26** and **D-27** in full.
- `docs/phases/phase-3/RUN-1-POSTMORTEM.md` — the source of every measured number. **Cite it; do not
  re-derive anything from it.**
- `.planning/REQUIREMENTS.md` lines 39-45 — the exact STRAT-01…STRAT-07 wording being addressed.
- `docs/SEGAL_GUIDELINES.md` §2.3 and §2.5 — per-mechanism PRD requirements and versioning.
- The SUMMARYs of 03-18, 03-19 and 03-20 — the landed code this document points at.

**Scope fence.** STRAT-02, STRAT-03, STRAT-04, STRAT-05 and STRAT-07 are **not** deviated from and
the document must say so explicitly rather than leaving it ambiguous:
- STRAT-02 — the Bayes + BFS fallback still handles untrained states.
- STRAT-03 — pluggability is *strengthened*: three brains now register and resolve from config.
- STRAT-04 — shortest-path walking is unchanged (GATE-1 still green).
- STRAT-05 — the cop still selects barrier placement, now from the same argmax as its move.
- STRAT-07 — satisfied *more* strongly: alpha-beta is an algorithm, and
  `scripts/check_no_llm_in_strategy.py` gates every module on the decision path.
</context>

<task type="auto">
  <name>Task 1: The deviation document</name>
  <files>docs/phases/phase-3/STRAT-01-06-DEVIATION.md</files>
  <read_first>
    .planning/REQUIREMENTS.md (STRAT-01..STRAT-07 verbatim)
    docs/phases/phase-3/RUN-1-POSTMORTEM.md (Findings 1, 2, 4.3 and the Open Question section)
    docs/SEGAL_GUIDELINES.md section 2.3 (what a mechanism document must contain)
  </read_first>
  <action>
    Write `docs/phases/phase-3/STRAT-01-06-DEVIATION.md` with five sections, in this order:

    1. **What the requirements say** — STRAT-01 and STRAT-06 quoted verbatim, no paraphrase.
    2. **What was built instead** — alpha-beta over a learned linear evaluation; ~60 weights per
       role; pointers to `searchbrain.py`, `search.py`, `linear_q.py`, `weights.py`, all landed.
    3. **Why — the measurement.** Cite, do not re-derive: the cop reached 0.900 in training and
       0.250 at the gate; 0.600 on trained start states against 0.133 on unseen; only 5/20 eval
       starts cleared `min_visits`; top 1% of keys held 53.2% of 3,525,039 visits; median
       best-vs-second margin 0.00000. Then the structural reason: a tabular key over absolute
       coordinates shares nothing between positions, so a table trained from one start covers one
       reachable cone (Finding 1). Add Finding 4.3 — barrier layout was invisible to the policy, so
       the cop's ceiling was bounded by a hand-written heuristic no amount of training could move.
    4. **What still ships** — a trained per-role weights file, reached from `_pick_move`; learning
       curves from run 2; run-1's curves retained as the historical record. Say plainly that
       STRAT-06's "a trained table ships" is met by a trained artifact whose *shape* changed.
    5. **What is unchanged** — the STRAT-02/03/04/05/07 fence from `<context>` above, each with the
       test or gate that proves it.

    **Three discipline rules for this document:**
    - **Produce no new number.** Every figure is already measured and recorded. If a number is
      wanted that does not exist, say it does not exist.
    - **Do not cite the ablation pilot.** It is inconclusive and citing it would weaken a case that
      rests on direct measurement.
    - **Use the corrected statistics** — n=20, cop p=0.250, thief p=0.500, neither significant. The
      original mcnemar_p ~ 0.0000 and z=3.95 were pseudo-replication artifacts.

    Also correct, in passing, the one attribution error from the literature review: grid capture time
    is **Mehrabian, Discrete Math 311:102-105, 2011**; the earlier attribution to arXiv:1708.08255
    was wrong.
  </action>
  <verify>
    The document quotes STRAT-01 and STRAT-06 verbatim and states the deviation in its first
    paragraph. Every numeric claim traces to `RUN-1-POSTMORTEM.md` or to a landed SUMMARY — spot-check
    each against its source. Every file path it names exists. `grep -i "ablation"` returns nothing, or
    returns only an explicit statement that the pilot is inconclusive and not relied upon. No
    occurrence of `3.95` or `0.0000` as a supporting statistic.
  </verify>
  <done>A grader can read one document and see exactly what deviates, why, on what evidence, and what still ships.</done>
</task>

<task type="auto">
  <name>Task 2: PRD v2.00 and the rule-42 README section</name>
  <files>docs/PRD_rl_strategy.md, README.md</files>
  <read_first>
    docs/PRD_rl_strategy.md (whole file — v1.00, describes the tabular mechanism; section 8 holds D-14, the 0.55 bar)
    docs/SEGAL_GUIDELINES.md (versioning: starts at 1.00, tracked in version.py and config files)
    README.md (the existing rule-42 learning-curve section)
  </read_first>
  <action>
    `docs/PRD_rl_strategy.md` -> **v2.00**. Rewrite the mechanism as search over a learned
    evaluation: the feature vector, the per-role aggregation, the semi-gradient update, the search,
    and the fallback trigger's new meaning. Keep the **Source column** on every number, marking it
    `PARAMETERS.md` versus engineering default — that column is what makes the document auditable.

    **§8 and D-14 (the 0.55 bar) are reaffirmed, not edited.** Per D-28 the bar stays at 0.55 for
    run 2. Record that it is ours to re-argue — it appears nowhere in `docs/PARAMETERS.md`, checked
    across 142 lines with zero matches — but that it is not being moved now, and explicitly not
    because a run failed.

    `README.md`: update the rule-42 section to describe run 2's curves and mechanism. **Keep run-1's
    figures and its failing numbers** — deleting them would misrepresent the project's history, and
    rule 38 makes misreporting results a disqualification. The section should read as a record of a
    diagnosed failure followed by a redesign, because that is what happened.

    **Do not fill in run-2 result numbers here.** Run 2 has not happened — it is plan 03-25. Leave
    clearly marked placeholders and say in the SUMMARY that 03-25 fills them.
  </action>
  <verify>
    `docs/PRD_rl_strategy.md` reads v2.00, every number carries a Source column entry, and §8 states
    the 0.55 bar is unchanged for run 2 with its provenance. `README.md` retains run-1's three
    figures and failing numbers alongside the new mechanism description. No run-2 result number is
    stated as fact anywhere. `bash scripts/check_line_limit.sh` passes (docs are exempt from the
    code-line gate, but run it to confirm no code file was touched).
  </verify>
  <done>The per-mechanism PRD describes what the code actually does, and the README tells the true story.</done>
</task>

<verification>
1. The deviation is stated plainly in the document's first paragraph, not buried.
2. Every numeric claim traces to an existing measurement; this plan produces no new number.
3. The ablation pilot is not used as support; the corrected n=20 statistics are used throughout.
4. Every code path the document cites has already landed (waves 3-4).
5. STRAT-02/03/04/05/07 are explicitly marked unchanged, each with its proving test or gate.
6. Run-1's failing numbers survive in the README (rule 38 — never misreport results).
7. `uv run ruff check .` -> 0; `uv run pytest` unchanged and green — this plan touches no code.
</verification>

<success_criteria>
- A grader opening `docs/phases/phase-3/` finds a document that names the deviation, evidences it,
  and shows what still ships — with no number in it that cannot be traced.
- `docs/PRD_rl_strategy.md` v2.00 describes the mechanism the code actually implements (DOC-02,
  Segal section 2.3).
- The 0.55 bar is reaffirmed with its provenance, not quietly adjusted.
- The project's record stays honest: run 1's failure remains visible in the README.
</success_criteria>

<output>
Create `.planning/phases/03-blind-strategy-module-rl-policy/03-22-SUMMARY.md` when done, listing the
README placeholders 03-25 must fill after run 2.
</output>
