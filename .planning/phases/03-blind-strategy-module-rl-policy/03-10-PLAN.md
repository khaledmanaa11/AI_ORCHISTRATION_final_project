---
phase: 03-blind-strategy-module-rl-policy
plan: "10"
type: execute
wave: 7
depends_on: ["03-03", "03-06", "03-07", "03-09"]
files_modified:
  - tests/integration/test_shortest_path.py
  - tests/integration/test_policy_fallback.py
  - tests/integration/test_strategy_pluggable.py
  - tests/integration/test_beats_baseline.py
  - training/evaluate.py
  - scripts/check_no_llm_in_strategy.sh
  - docs/phases/phase-3/TODO.md
autonomous: true
requirements:
  - STRAT-01
  - STRAT-02
  - STRAT-03
  - STRAT-04
  - STRAT-05
  - STRAT-06
  - STRAT-07

must_haves:
  truths:
    - "GATE-1 maps to test_shortest_path.py: a known target is reached by a shortest barrier-aware walk with no manual intervention, on an open board AND through a barrier pocket (§10.4 criterion a, STRAT-04)"
    - "GATE-2 maps to test_policy_fallback.py: the Q-policy serves visited states and the fallback serves insufficiently-visited ones, with the min_visits boundary itself asserted (§10.4 criterion b, STRAT-01/02)"
    - "GATE-3 maps to test_strategy_pluggable.py: brains swap via config alone with zero diff under src/pursuit/network/, and no LLM is reachable from the decision path (§10.4 criterion c, STRAT-03/07)"
    - "GATE-4 maps to test_beats_baseline.py: the shipped Q-table beats HeuristicBrain per role over the configured game count at the configured margin, on HELD-OUT seeds disjoint from training (D-23)"
    - "The eval scenario seeds are provably disjoint from training seeds — asserted directly, because the heuristic is both sparring partner and eval opponent (D-23)"
    - "Both roles must pass GATE-4 independently; there is no shared done-flag that lets a strong cop mask a thief that never learned (D-25)"
    - "The statistical claim is honest: the win-rate margin is compared against the measured heuristic-vs-heuristic baseline for that role, not against an assumed 50%, since the game is not role-symmetric"
    - "A STRAT-01..STRAT-07 coverage audit maps every requirement to a named runnable test, and every row in docs/phases/phase-3/TODO.md is reconciled against what actually exists"
    - "The full-strength statistical gate runs as a checkpoint-promotion step, not on every commit; CI runs the deterministic dimensions plus a smaller smoke subset so the suite stays fast"
  artifacts:
    - path: tests/integration/test_beats_baseline.py
      provides: "GATE-4 — head-to-head evaluation of the shipped table against the heuristic baseline on held-out seeds, per role"
      contains: "held_out"
    - path: training/evaluate.py
      provides: "Evaluation CLI: three arms (baseline, Q-cop, Q-thief) over the scenario+seed grid, with --smoke and --assert-gate modes"
      contains: "assert_gate"
  key_links:
    - from: tests/integration/test_strategy_pluggable.py
      to: src/pursuit/strategy/registry.py
      via: "parametrized over both brain classes loaded from config, proving the swap needs no code edit"
      pattern: "build_brain"
    - from: training/evaluate.py
      to: src/pursuit/strategy/heuristic.py
      via: "the baseline arm — the opponent that defines whether the RL learned anything"
      pattern: "HeuristicBrain"
---

<objective>
Close the phase against its §10.4 milestone gate, and prove the central claim rather than
asserting it.

Three of the four gates are ordinary integration tests. The fourth — "the RL actually learned
something" — is the one that can be faked by accident, and `03-RESEARCH.md` §2 names how: the
heuristic is both the sparring partner and the eval opponent, so a table trained heavily against
it will beat it on the same start positions it trained on. The eval set therefore uses held-out
seeds, and the margin is measured against the *observed* heuristic-vs-heuristic baseline for
that role rather than an assumed coin flip, because cop and thief do not have symmetric odds.

Output: four gate tests, the evaluation CLI, the no-LLM CI check, and a reconciled phase TODO.
</objective>

<execution_context>
@C:\Users\Hp\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Hp\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/03-blind-strategy-module-rl-policy/03-PLAN-OUTLINE.md
@docs/phases/phase-3/PRD.md

The §10.4 gate wording and GATE-1…GATE-4 are in `docs/phases/phase-3/PRD.md` § Acceptance
criteria. `03-AI-SPEC.md` §5 holds the twelve eval dimensions; this plan implements the gate
subset (E1, E2, E3, E4, E5, E9, E10) as runnable tests.

Relevant decisions: **D-23** (held-out eval seeds), **D-25** (per-role independence).
</context>

<task type="auto">
  <name>Task 1: GATE-1, GATE-2, GATE-3 integration tests</name>
  <files>tests/integration/test_shortest_path.py, tests/integration/test_policy_fallback.py, tests/integration/test_strategy_pluggable.py, scripts/check_no_llm_in_strategy.sh</files>
  <read_first>
    docs/phases/phase-3/PRD.md (§ Acceptance criteria — the exact gate wording)
    tests/integration/ (Phase 2 house style, conftest helpers)
  </read_first>
  <action>
    `test_shortest_path.py` (GATE-1 / E1): drive a full agent through `sdk.engine` toward a known
    target on (a) an open board and (b) a board with a barrier pocket that defeats greedy
    Manhattan. Assert turns taken equals the BFS oracle and that no test-side intervention
    occurs — the "no manual intervention" clause is part of the gate.

    `test_policy_fallback.py` (GATE-2 / E3): with a fixture table, sweep visit counts across
    `min_visits` and assert `decision.source` routing is correct at every point, including both
    sides of the boundary. Add E4: on the pocket board, assert the fallback never picks a move
    that is Manhattan-closer but BFS-further.

    `test_strategy_pluggable.py` (GATE-3 / E10, E2): parametrize a full game over both brain
    classes selected purely by config, asserting no source edit is needed and that `git diff`
    over `src/pursuit/network/` is empty across the swap. Promote 03-02's structural import
    check into `scripts/check_no_llm_in_strategy.sh` so it runs in CI as a named gate, and call
    the same logic from the test.

    Reuse the existing `tests/integration/conftest.py` helpers rather than adding new fixtures
    that duplicate them (QUAL-02).
  </action>
  <verify>
    `uv run pytest tests/integration/ -q` passes. `bash scripts/check_no_llm_in_strategy.sh`
    exits 0, and exits nonzero when a forbidden import is temporarily introduced.
  </verify>
</task>

<task type="auto">
  <name>Task 2: Evaluation CLI and GATE-4</name>
  <files>training/evaluate.py, tests/integration/test_beats_baseline.py</files>
  <read_first>
    .planning/phases/03-blind-strategy-module-rl-policy/03-AI-SPEC.md (§5 E5 — three arms, the statistical test)
    .planning/phases/03-blind-strategy-module-rl-policy/03-RESEARCH.md (§2 — the train-on-test warning)
    training/harness.py (episode machinery to reuse)
  </read_first>
  <action>
    `training/evaluate.py` — three arms over one scenario+seed grid, per AI-SPEC E5:
    heuristic-vs-heuristic (the per-role baseline), Q-cop vs heuristic-thief, Q-thief vs
    heuristic-cop. Modes: `--smoke` (`eval_games_ci`) and `--full` (`eval_games`), plus
    `--assert-gate` which exits nonzero when the bar is not met.

    **Seeds must be held out** (D-23): derive eval start positions from
    `training_seed + eval_seed_offset` and assert in code that the resulting seed set is
    disjoint from the training set. Do this as an executable check, not a comment — this is the
    single assumption that, if wrong, makes the whole GATE-4 number meaningless.

    Statistical honesty: compare each arm to the **measured** baseline win rate for that role,
    require `min_win_rate_absolute` as a floor, and require the paired test to clear
    `significance_alpha`. Report the numbers even when the gate fails — a failing gate with a
    real number is useful; a failing gate with no number is not.

    `test_beats_baseline.py` runs the smoke subset in CI and asserts the machinery works and the
    seed sets are disjoint. Mark the full statistical run as a checkpoint-promotion step, not a
    per-commit test, so the suite stays fast.

    If the trained table does not yet exist when this plan runs, the test must **skip with a
    clear reason**, never pass vacuously — a green GATE-4 that never loaded a table is the worst
    possible outcome for this phase.
  </action>
  <verify>
    `uv run python training/evaluate.py --smoke` runs and reports per-role numbers.
    `uv run pytest tests/integration/test_beats_baseline.py -q` passes or skips with a stated
    reason; it never passes without having loaded a real table.
  </verify>
</task>

<task type="auto">
  <name>Task 3: STRAT coverage audit and TODO reconciliation</name>
  <files>docs/phases/phase-3/TODO.md</files>
  <read_first>
    .planning/REQUIREMENTS.md (STRAT-01..07 wording)
    docs/phases/phase-3/TODO.md
    docs/TODO.md
  </read_first>
  <action>
    Produce the coverage audit: a table mapping each of STRAT-01 … STRAT-07 to the named test(s)
    that exercise it, and QUAL-08/09/10/11/13 + DOC-02 to their gate commands. Any requirement
    without a named runnable test is an open gap — record it explicitly rather than marking the
    phase done.

    Then reconcile `docs/phases/phase-3/TODO.md`: tick rows that are genuinely complete, and for
    anything incomplete leave it unticked with a one-line note. Do not tick 03-99 here — that is
    `/gsd:verify-work 3`'s job.

    Add the operator steps `03-RESEARCH.md` §3 identified, as their own rows, because they are
    real and easy to lose: redirect training output to a file (console QuickEdit silently
    suspends the process on click), disable sleep for the run, and exclude the artifacts
    directory from Defender.
  </action>
  <verify>
    Every STRAT requirement maps to a named test that exists and runs. `docs/phases/phase-3/TODO.md`
    reflects reality — no row claims work that is not on disk.
  </verify>
</task>

<verification>
1. GATE-1…GATE-4 each map to a named runnable test file; all pass (or GATE-4 skips with a stated
   reason if no trained table exists yet).
2. Eval seeds are asserted disjoint from training seeds in code (D-23).
3. Both roles are evaluated independently; no shared pass flag (D-25).
4. The margin is measured against the observed per-role baseline, not an assumed 50%.
5. `scripts/check_no_llm_in_strategy.sh` runs in CI and fails when violated (STRAT-07).
6. STRAT-01…07 coverage audit complete, with any gap recorded rather than hidden.
7. `uv run ruff check .` → 0; `uv run pytest --cov=pursuit` → green at ≥85%;
   `bash scripts/check_line_limit.sh` → all files ≤150 lines.
</verification>

<success_criteria>
- The §10.4 milestone gate is demonstrably met, with each criterion tied to a runnable test.
- GATE-4 is honest: held-out seeds, per-role, measured baseline, stated significance — the
  phase's central claim survives scrutiny rather than resting on a favourable setup.
- STRAT-07 is enforced by a CI gate, not a convention.
- The phase TODO tells the truth about what is done.
</success_criteria>

<output>
Create `.planning/phases/03-blind-strategy-module-rl-policy/03-10-SUMMARY.md` when done.
</output>
