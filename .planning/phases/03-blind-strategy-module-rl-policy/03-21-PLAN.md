> # ⛔ SUPERSEDED — DO NOT EXECUTE
>
> This plan was written for the **run-2 design of 2026-08-03**: alpha-beta search bolted onto
> the sequential engine, with the Q-policy demoted to a learned evaluation.
>
> That design assumed a **sequential turn order**. The book does not permit one — §5.3.2 p.35
> makes the turn simultaneous, and the mandatory Commit-Reveal protocol exists to enforce it.
> The premise of this plan is therefore false, and executing it would rebuild the defect.
>
> **What was actually built instead**, on 2026-08-08:
> `docs/phases/phase-3/PRD.md`, `docs/PRD_matrix_mover.md`, and
> `docs/phases/phase-3/ENGINEERING-LOG.md` (why this plan set was abandoned).
> Landed in commits 8b30328, f3d9847, 2606efa, 7040d7a — merged to main as 6ca38fa.
>
> Kept unmodified below as the record of what was planned and why it was dropped.

---

---
phase: 03-blind-strategy-module-rl-policy
plan: "21"
type: execute
wave: 5
depends_on: ["03-15", "03-19", "03-20"]
files_modified:
  - training/sparring.py
  - training/pfsp.py
  - config/police/strategy.json
  - config/thief/strategy.json
  - tests/unit/training/test_sparring.py
  - tests/unit/training/test_pfsp.py
autonomous: true
requirements:
  - R3
  - R5
  - D-13
  - D-18
  - STRAT-06

must_haves:
  truths:
    - "D-13 superseded: PFSP sampling f(x) = x(1-x) replaces the fixed-mix renormalisation. Run 1's configured [0.30 heuristic, 0.50 past-self, 0.20 reference] NEVER RAN — `reference_impl_path` was empty, so `_available_weights` renormalised to 0.375/0.625 and the thief spent 62.5% of training against past-self cops while those cops climbed to a 0.90 win rate. A test asserts the realised mix equals the configured one, or fails loudly — silent renormalisation is what hid this for a whole run"
    - "D-13: every pool carries a PERMANENT weak-opponent floor (at least one scripted weak opponent and one random-walk). Our failure has a name — coevolutionary disengagement (Cartlidge & Bullock, Evolutionary Computation 12(2):193-222, 2004, verified independently) — and the sourced remedy 'reduce virulence' raises final quality on BOTH sides, not just the weaker one (Gleave et al. ICLR 2020)"
    - "R3: gamma differs by role — cop 0.99, thief 1.0. Discounting is the cop's capture-sooner incentive and actively harms the thief. Run 1's single gamma=0.95 gave an effective horizon of 1/(1-gamma) = 20 against a 35-turn task, collapsing the thief's usable value range to 0.047 against the cop's 0.626, a 13x weaker signal that sat below the noise floor at alpha=0.15"
    - "R3 / D-03: per-role gamma needs NO new key — each role's config file already holds its own `gamma`. `gamma_cop`/`gamma_thief` in one file is exactly the shared-state shape project rule 2 forbids, and 03-13 explicitly barred it"
    - "R5: the epsilon and alpha FLOOR VALUES sit at 0.10-0.15, and the decay LENGTH is set so each role reaches its floor at <= 15% of that role's episodes. Run 1 reached its floor at episode 299,999 of 300,000 — 100% of the way through — leaving no consolidation phase at all and producing the recorded final_slope of +0.094. The <=15% rule (TRAINING-METHODOLOGY section F.3) is stricter than the post-mortem's '~60% not 100%' and satisfies it"
    - "D-26: `episodes` is sized as a WEIGHT-TUNING budget, not a table-filling one. 60 weights per role need nothing like the 150,000 episodes per role that a 1.7M-entry table needed, and sizing it as if they did wastes the night"
    - "D-18 / project rule 1: this plan is the THIRD AND FINAL config owner (outline section 7) and sets every numeric magnitude 03-13 declared. Terminal rewards stay the PARAMETERS.md Table 17 fixed 20/5/5/10; everything this plan sets is an engineering default and is labelled as such in the config file"
    - "STOP-AND-ASK, not invent: if 03-23's pre-flight delta-ratio check (delta_cop/delta_thief > 3) and Jones' [-10, +10] target band cannot BOTH be satisfied, the only sourced remedy is a SINGLE scale divisor applied identically to both roles, preserving the PARAMETERS-sourced 20/5/5/10 ratio. If no common divisor satisfies both, this plan STOPS AND ASKS rather than picking a number. A value invented here would be a project-rule-1 violation"
    - "Sourced-but-flagged, carried honestly: TRAINING-METHODOLOGY's own 'No source found' section records that the thief-vs-cop epsilon-floor multiplier is an INFERENCE and that the exact AlphaStar PFSP exponent came from secondary sources only. The plan cites these as inferences, never as measured results"
  artifacts:
    - path: training/pfsp.py
      provides: "PFSP opponent sampling f(x) = x(1-x) with the weak-opponent floor"
      contains: "def pfsp_weights"
      min_lines: 30
    - path: training/sparring.py
      provides: "Pool construction on PFSP, with realised-mix assertion replacing silent renormalisation"
      contains: "pfsp"
    - path: tests/unit/training/test_sparring.py
      provides: "The realised mix equals the configured mix or fails loudly; the weak floor is never empty"
      min_lines: 40
  key_links:
    - from: training/sparring.py
      to: training/pfsp.py
      via: "opponent probabilities come from f(x)=x(1-x) over measured win rates, never a fixed renormalised weight list"
      pattern: "pfsp_weights"
---

<objective>
Set the run-2 regime: who the learner spars against, and every numeric magnitude 03-13 declared.

**Three run-1 failures converge here.** The sparring mix never ran as configured (0.375/0.625 by
silent renormalisation). A single γ=0.95 gave the thief a usable value range of 0.047 against the
cop's 0.626. ε and α reached their floors at episode 299,999 of 300,000, so there was no
consolidation phase.

**This is the last config owner.** After this plan, `config/{police,thief}/strategy.json` is final
for run 2 and 03-23's pre-flight gate measures it.

Output: `pfsp.py`, a rewritten `sparring.py`, and both role config files.
</objective>

<execution_context>
@C:\Users\Hp\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Hp\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/03-blind-strategy-module-rl-policy/03-PLAN-OUTLINE-RUN2.md

- `03-CONTEXT.md` `<superseded>` — the **D-13** and **D-18** overturn rows and the **R3**/**R5**
  defect rows. Where the top half conflicts, `<superseded>` wins.
- `docs/phases/phase-3/RUN-1-POSTMORTEM.md` — **Finding 2b** (the γ arithmetic, including the exact
  combined-effect table), **Finding 3** (the schedules), **Finding 4.2** (the renormalisation).
- Grep `docs/research/TRAINING-METHODOLOGY.md` for PFSP, per-role γ, and its **"No source found"**
  section. Do not read it whole — 77 KB.
- `training/sparring.py` — 142 raw lines, contains `_available_weights`, the renormaliser.
- The SUMMARYs of 03-18 (measured search depth per role at budget) and 03-20 (`learner_rule`
  recommendation) — **both feed values this plan sets.** Do not set them before reading those.

**Sequencing — wave 5 (outline §7):**

- Config: 03-13 declared the keys, 03-19 set two class-name strings, **this plan sets every
  number.** Nothing after it edits those files.
- 03-23 (wave 6) asserts what this plan configures. Design the values so its six checks pass at
  t=0 — run 1 failed four of them, and every one was computable before episode 1.
</context>

<task type="auto">
  <name>Task 1: PFSP sampling with a weak-opponent floor</name>
  <files>training/pfsp.py, training/sparring.py, tests/unit/training/test_pfsp.py, tests/unit/training/test_sparring.py</files>
  <read_first>
    training/sparring.py (whole file — `_available_weights` is the renormaliser being deleted)
    docs/phases/phase-3/RUN-1-POSTMORTEM.md Finding 4.2 (the measured 0.375/0.625)
  </read_first>
  <action>
    Tests first. `training/pfsp.py` (~30-50 code lines):

      pfsp_weights(candidates, win_rates, exponent) -> list[float]

    Prioritised fictitious self-play: weight each opponent by `f(x) = x(1 - x)` over the learner's
    measured win rate `x` against it, so the pool concentrates on opponents that are neither
    already-beaten nor hopeless. The exponent is a config key 03-13 declared; **its exact AlphaStar
    value reached us through secondary sources only** and must be labelled an inference, not a
    measured result.

    `sparring.py`: delete `_available_weights`'s silent renormalisation. Two rules replace it:

    1. **A missing pool arm is an error or an explicit opt-out, never a silent reweight.** Run 1's
       whole thief regime shifted because an empty `reference_impl_path` quietly moved 12.5% of the
       mix. Assert the realised mix matches the configured mix within tolerance, and fail loudly
       otherwise.
    2. **A permanent weak-opponent floor in every pool** — at least one scripted weak opponent and
       one random-walk, present for the whole run. This is the "reduce virulence" remedy for
       coevolutionary disengagement; it raises final quality on **both** sides, which is why it is
       not merely a handicap.

    **Split trigger:** if `sparring.py` would exceed 150 code lines, `pfsp.py` already carries the
    sampler; move pool construction to `training/pools.py` rather than compressing. Measure with
    `scripts/check_line_limit.sh`'s counter.

    D-21 stands: the reference implementation is **never vendored**, stays an opt-in local clone
    behind `reference_impl_path`, and its absence is now explicit rather than silently absorbed.
  </action>
  <verify>
    `uv run pytest tests/unit/training/test_pfsp.py tests/unit/training/test_sparring.py -q` passes,
    covering: `f(x)=x(1-x)` peaks at x=0.5 and goes to 0 at x=0 and x=1; an absent pool arm raises
    or requires an explicit opt-out flag and never silently reweights; the realised sampling
    distribution over many draws matches the configured one within tolerance; the weak floor is
    non-empty at every stage of a simulated run; sampling is reproducible under a seeded RNG (D-19).
  </verify>
  <done>Opponent sampling is PFSP with a permanent weak floor, and a mix that does not match its configuration fails loudly.</done>
</task>

<task type="auto">
  <name>Task 2: Set every run-2 numeric magnitude</name>
  <files>config/police/strategy.json, config/thief/strategy.json</files>
  <read_first>
    03-13-PLAN.md (the 15 keys declared and their types — set these, add none)
    .planning/phases/03-blind-strategy-module-rl-policy/03-18-SUMMARY.md (measured depth per role at budget)
    .planning/phases/03-blind-strategy-module-rl-policy/03-20-SUMMARY.md (the learner_rule recommendation)
    docs/phases/phase-3/RUN-1-POSTMORTEM.md ("What run 2 should change", R3 and R5 rows)
  </read_first>
  <action>
    Set, in both role files, with every value carrying an inline label marking it an **engineering
    default** rather than a `docs/PARAMETERS.md` value:

    - **γ: cop 0.99, thief 1.0** (R3). One `gamma` per file — never a two-role key in one file.
    - **ε and α floor *values* 0.10-0.15**, and decay lengths set so each role hits its floor at
      **≤15% of that role's episodes** (TRAINING-METHODOLOGY §F.3, the stricter reading of R5's
      "~60% not 100%"). Run 1 hit its floor at 100%.
    - **`search_depth_cap` and `max_decision_ms`** from 03-18's *measured* depth on this machine.
      D-26 says test 100/200 ms early. **Never encode ALG-COMPARISON's unverified 11-12 plies.**
    - **`learner_rule`** per role, from 03-20's recommendation and its citation.
    - **`episodes`** as a weight-tuning budget (D-26), not a table-filling one.
    - `min_distinct_starts`, the PFSP exponent, `weak_opponent_floor`, `barrier_candidate_min_degree`
      and the four `barrier_weight_*` magnitudes (03-17 fixed only their ordering).

    **Terminal rewards are NOT set here.** They are the Table 17 fixed 20/5/5/10 already in
    `GameParams` via 03-14.

    **The stop-and-ask rule is binding.** 03-14 raised terminal magnitudes from ~1.0 to 5-20, which
    changes the discounted value spread. If 03-23's check 2 (`delta_role >= 0.1` and
    `delta_cop/delta_thief <= 3`) and Jones' `[-10, +10]` target band cannot both be satisfied, apply
    a **single scale divisor identically to both roles** — that preserves the PARAMETERS-sourced
    20/5/5/10 ratio. If no common divisor satisfies both, **stop and ask.** Do not pick a number.
  </action>
  <verify>
    `uv run pytest tests/unit/ -q` passes with the new config. Both files load through the existing
    loader with no unknown key and no missing key. Every value this plan set is labelled an
    engineering default. `grep` finds no reward magnitude and no `-1.0` in either file.
    Hand-compute the discounted terminal spread per role and record both numbers in the SUMMARY —
    03-23 asserts them at t=0, and the point of this plan is that they are known **before** the run.
  </verify>
  <done>Run-2 configuration is final, per-role, and every magnitude is either PARAMETERS-sourced or a labelled engineering default.</done>
</task>

<verification>
1. The realised sparring mix equals the configured mix, or the run fails loudly (D-13, Finding 4.2).
2. Every pool keeps a weak opponent for the whole run (coevolutionary disengagement remedy).
3. gamma is per-role and lives one per file (R3, D-03).
4. Each role reaches its epsilon/alpha floor at <=15% of its episodes (R5, section F.3).
5. No invented numeric value: terminal rewards untouched from Table 17, every new magnitude labelled
   an engineering default, and the scale question stopped-and-asked rather than guessed (rule 1).
6. Every touched file <=150 **code** lines: `bash scripts/check_line_limit.sh`.
7. `uv run ruff check .` -> 0; `uv run pytest --cov=pursuit --cov=training` green at >=85%.
</verification>

<success_criteria>
- Opponent sampling is PFSP with a permanent weak floor, and cannot silently drift from its config.
- The thief's learning signal is no longer 13x weaker than the cop's.
- A consolidation phase exists: floors are reached early, not at the final episode.
- Every number in run-2's configuration traces to PARAMETERS.md, to a measurement, or to a labelled
  engineering default with a citation.
</success_criteria>

<output>
Create `.planning/phases/03-blind-strategy-module-rl-policy/03-21-SUMMARY.md` when done, recording
the final per-role values, **the hand-computed discounted terminal spread per role**, whether a
scale divisor was needed (and if so that it was applied identically to both roles), and any point
where the plan stopped and asked.
</output>
