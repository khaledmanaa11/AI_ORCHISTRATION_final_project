> # ⛔ SUPERSEDED — DO NOT EXECUTE
>
> This plan was written for the **run-2 design of 2026-08-03**: alpha-beta search bolted onto
> the sequential engine, with the Q-policy demoted to a learned evaluation.
>
> That design assumed a **sequential turn order**. The book does not permit one — §5.3.2 p.35
> makes the turn simultaneous, and the mandatory Commit-Reveal protocol exists to enforce it.
> The premise of this plan is therefore false, and executing it would rebuild the defect.
>
> **What was actually built instead**, on 2026-08-08:
> `docs/phases/phase-3/PRD.md`, `docs/PRD_matrix_mover.md`, and
> `docs/phases/phase-3/ENGINEERING-LOG.md` (why this plan set was abandoned).
> Landed in commits 8b30328, f3d9847, 2606efa, 7040d7a — merged to main as 6ca38fa.
>
> Kept unmodified below as the record of what was planned and why it was dropped.

---

---
phase: 03-blind-strategy-module-rl-policy
plan: "25"
type: checkpoint
wave: 7
depends_on: ["03-23", "03-24"]
files_modified:
  - README.md
  - docs/phases/phase-3/TODO.md
autonomous: false
requirements:
  - D-28
  - D-29
  - GATE-4
  - STRAT-06

must_haves:
  truths:
    - "This plan carries NO implementation. It is a human-operator checkpoint, the exact analogue of 03-10's Task 4: a person runs the overnight job on their own machine, inspects the curves, and measures GATE-4. Implementation and checkpoints never share a plan"
    - "D-30: `uv run python -m training.preflight` MUST exit 0 before the run starts. If it fails, the run does not start and the failures are fixed first — that is the entire point of 03-23. Run 1 failed four of these checks at t=0 and nobody knew until morning"
    - "D-28: GATE-4 is measured at the UNCHANGED min_win_rate_absolute = 0.55. The bar is not lowered for run 2, not before the measurement and not after it. It originates in docs/PRD_rl_strategy.md section 8 (D-14) and appears nowhere in docs/PARAMETERS.md (checked: 142 lines, zero matches), so it is ours to re-argue — but only on a second measurement, and never because a run failed"
    - "D-29: exactly ONE run. 03-11..03-24 all landed first and the pre-flight gate passed, which is what makes a single attempt reasonable"
    - "R6 is VERIFIED here, not implemented — it already landed in commit ced26d5. Eval replays are deterministic, so the true sample size is n=20, not 200. Any statistic this plan records uses n=20; the CLI's old mcnemar_p ~ 0.0000 and z=3.95 were pseudo-replication artifacts and must never be reported again"
    - "RULE 38 IS BINDING ON THE OUTCOME: whatever GATE-4 measures is what gets written down. No bar is lowered, no table is promoted that did not clear the gate, and no unmeasured number is written. Run 1 was reported as a failure and that record is why the project's numbers are credible — the same discipline applies whether run 2 passes or fails"
    - "If GATE-4 misses again, the miss becomes evidence ABOUT THE BAR rather than about the run — two independent measurements instead of one. Re-arguing 0.55 is then a separate decision this plan does not take and must not pre-empt"
    - "The three run-1 operator lessons are repeated because they are proven, not assumed: console QuickEdit suspends the process the moment the window is clicked (redirect to a file), machine-level sleep policy is not covered by the in-process SetThreadExecutionState guard, and Defender real-time scanning adds latency on every checkpoint rewrite"
  artifacts:
    - path: README.md
      provides: "The rule-42 section's run-2 placeholders filled with measured figures — and only measured ones"
  key_links:
    - from: docs/phases/phase-3/TODO.md
      to: README.md
      via: "GATE-4's row records the measured outcome; the README carries the curves and the same numbers"
      pattern: "GATE-4"
---

<objective>
Run 2, and measure GATE-4.

Everything 03-11…03-24 built exists to make this one night worth spending. The pre-flight gate
(03-23) must pass before it starts; the bar (D-28) does not move; there is exactly one attempt
(D-29).

**This plan is `autonomous: false`.** A human operator runs the job on their own machine. Nothing
here can be automated, exactly as with 03-10's Task 4.
</objective>

<execution_context>
@C:\Users\Hp\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Hp\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/03-blind-strategy-module-rl-policy/03-PLAN-OUTLINE-RUN2.md

- `03-CONTEXT.md` `<superseded>` — **D-28** (the bar) and **D-29** (one run).
- `03-23-SUMMARY.md` — the measured value of each of the six pre-flight checks against the final
  configuration. **Read this before committing the night.**
- `03-21-SUMMARY.md` — the final per-role values and the hand-computed terminal spread.
- `.planning/phases/03-blind-strategy-module-rl-policy/03-10-PLAN.md` Task 4 — the run-1 precedent
  this mirrors, including how a failing result was recorded.
- `docs/phases/phase-3/TODO.md` rows `03-10 op-1/2/3` — the three operator lessons below, each with
  its run-1 evidence.
</context>

<task type="human">
  <name>Task 1: Run 2 and measure GATE-4</name>
  <files>README.md, docs/phases/phase-3/TODO.md</files>
  <read_first>
    .planning/phases/03-blind-strategy-module-rl-policy/03-23-SUMMARY.md (all six checks against the final config)
    .planning/phases/03-blind-strategy-module-rl-policy/03-21-SUMMARY.md (final per-role values)
  </read_first>
  <action>
    **Operator sequence.** The first three lines are run-1 lessons and are not optional:

    ```
    powercfg /change standby-timeout-ac 0                       # op-2: machine-level sleep policy
    # op-3: add %LOCALAPPDATA%\pursuit\training to Defender exclusions
    uv run python -m training.preflight                          # D-30 gate -- MUST exit 0
    uv run python -m training.loop 2>&1 | tee run.log            # op-1: never click into the console
    uv run python -m training.evaluate --scenarios artifacts/eval_scenarios.json \
        --out artifacts/eval/<run_id>.csv --full --assert-gate
    ```

    - **op-1:** redirect to a file. Windows console QuickEdit silently suspends the process the
      moment the window is clicked — the single most common "the run just stopped" cause. The entry
      point is `training.loop`, **not** `training.harness` (which has no `__main__`).
    - **op-2:** the in-process `SetThreadExecutionState` guard covers sleep only while the process
      runs, not machine power policy.
    - **op-3:** real-time scanning adds latency to every checkpoint rewrite.

    **If pre-flight exits non-zero, stop.** Fix the reported failures and re-run it. Do not start
    the job on a failing gate and do not add a bypass.

    **Recording the result — the same discipline as run 1:**
    - Write down whatever GATE-4 measures, pass or fail (rule 38).
    - **Do not lower the bar** (D-28). **Do not promote a table that did not clear the gate.**
    - **Write no unmeasured number.** Fill the README's run-2 placeholders (listed in
      `03-22-SUMMARY.md`) with measured figures only, and keep run-1's figures alongside them as
      the historical record.
    - Use **n = 20** for every statistic (R6, already landed in `ced26d5`). The old
      `mcnemar_p ~ 0.0000` and `z = 3.95` were pseudo-replication artifacts.
    - Record the per-role search depth actually reached at the configured budget — 03-22's defence
      cites it as evidence the search is real.

    **If GATE-4 passes:** promote the blessed per-role weights files into `artifacts/`, which
    un-skips `test_beats_baseline_smoke_subset`, and note that `/gsd:verify-work 3` can now run.

    **If GATE-4 fails:** record it as a second measurement against the 0.55 bar and stop. Do not
    start a third run and do not re-argue the bar inside this plan — that is a separate decision,
    now backed by two data points instead of one.
  </action>
  <verify>
    `training/preflight.py` exited 0 before the run began, with its six measured values recorded.
    The run reached its configured episode count with no resume gap (`run_state.json` episode count
    matches, curve rows account for every interval). `training/evaluate.py --full --assert-gate` was
    run and its exit code recorded. Every number written to the README or the TODO traces to that
    output. No bar was changed in any config file or PRD — `git diff` on
    `config/{police,thief}/strategy.json` and `docs/PRD_rl_strategy.md` shows no threshold edit.
  </verify>
  <done>GATE-4 is measured at the unchanged bar and the true result is recorded, whichever way it went.</done>
</task>

<verification>
1. Pre-flight exited 0 before episode 1; its six measured values are in the SUMMARY.
2. GATE-4 was measured with `--full --assert-gate` and the exit code is recorded.
3. No threshold was edited anywhere (D-28) and no unblessed table was promoted.
4. Every reported statistic uses n=20 (R6); no `0.0000` p-value or `z=3.95` appears.
5. Run-1's figures and failing numbers survive alongside run 2's in the README (rule 38).
6. `uv run ruff check .` -> 0; `uv run pytest --cov=pursuit --cov=training` green at >=85%.
</verification>

<success_criteria>
- One run, gated by six pre-flight checks that all passed before it started.
- GATE-4 measured against the unchanged 0.55 bar, with honest n=20 statistics.
- The recorded outcome is the measured outcome — no bar moved, no table promoted that did not earn
  it, no number written that was not measured.
- If it failed, the project holds two independent measurements against the bar and can re-argue it
  on evidence rather than on disappointment.
</success_criteria>

<output>
Create `.planning/phases/03-blind-strategy-module-rl-policy/03-25-SUMMARY.md` when done, recording
the six pre-flight values, the run's episode count and wall-clock time, the per-role GATE-4 numbers
against the 0.55 bar, the per-role search depth reached, and whether tables were promoted. Then
update `.planning/STATE.md` — this is the last plan in Phase 3, and `/gsd:verify-work 3` runs only
if the gate is met.
</output>
