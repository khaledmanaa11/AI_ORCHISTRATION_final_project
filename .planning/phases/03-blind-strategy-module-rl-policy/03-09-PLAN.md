---
phase: 03-blind-strategy-module-rl-policy
plan: "09"
type: execute
wave: 7
depends_on: ["03-08"]
files_modified:
  - training/plot_curves.py
  - README.md
autonomous: true
requirements:
  - STRAT-06

must_haves:
  truths:
    - "training/curves.py already exists from 03-08; this plan CONSUMES it and must not recreate it (QUAL-02)"
    - "plot_curves.py is the only matplotlib importer in the repo, and nothing under src/ imports it, so a match never needs a plotting dependency (D-20)"
    - "It renders per-role figures — cop and thief separately, never averaged into one line, since averaging hides the asymmetric-convergence failure mode (D-25)"
    - "It verifies the AI-SPEC E6 convergence assertions against the CSV: final-decile mean win-rate at least win_rate_margin above the first decile, and final slope within convergence_tolerance over convergence_window"
    - "README.md gains the learning-curves section embedding the rendered PNGs — rule 42 makes this a graded deliverable, not optional telemetry"
    - "The README states the configured win-rate bar and the game count alongside the curves, because a curve without its evaluation bar does not tell the grader whether the phase goal was met"
    - "No number in the README is fabricated: if the overnight run has not happened yet, the section ships with clearly marked placeholders and 03-10 fills the measured values"
  artifacts:
    - path: training/plot_curves.py
      provides: "matplotlib renderer producing the per-role README learning-curve PNGs, plus the E6 convergence checks over the CSV"
      contains: "matplotlib"
  key_links:
    - from: README.md
      to: training/plot_curves.py
      via: "learning-curve section embeds the PNGs this script renders (rule 42)"
      pattern: "learning"
---

<objective>
Rule 42 makes learning curves a mandatory README section — a graded deliverable, not
diagnostics. The trap is timing: curves that are added "once training works" require re-running
the overnight job to produce them, and the run is the expensive part. So instrumentation ships
with the first episode of the first run.

The second trap is resume. `03-RESEARCH.md` §3 flags it: a resumed run that keeps appending
produces a CSV with a duplicated, rewound segment, and the PNG rendered from it shows a
learning curve that never happened. Truncate-on-resume is the fix.

Output: `training/plot_curves.py` (rendering + the E6 convergence checks) and the README
section. The curve **writer** (`training/curves.py`) was built in 03-08, because the harness
appends rows from the first episode; this plan reads what it wrote.
</objective>

<execution_context>
@C:\Users\Hp\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Hp\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/03-blind-strategy-module-rl-policy/03-PLAN-OUTLINE.md

Relevant decisions: **D-16** (curves from run 1), **D-20** (matplotlib dev-only), **D-24**
(truncate on resume), **D-25** (per-role curves).

`03-AI-SPEC.md` §5 dimension **E6** is the acceptance bar: the CSV schema, a final-decile mean
win-rate at least `win_rate_margin` above the first decile, a final slope within
`convergence_tolerance` over `convergence_window`, and PNGs embedded in the README.
</context>

<task type="auto">
  <name>Task 1: E6 convergence checks over the curve CSV</name>
  <files>training/plot_curves.py, tests/unit/training/test_curves.py</files>
  <read_first>
    .planning/phases/03-blind-strategy-module-rl-policy/03-AI-SPEC.md (§5 E6 — the acceptance thresholds)
    training/curves.py (created in 03-08 — read its schema, do NOT recreate the module)
  </read_first>
  <action>
    `training/curves.py` already exists from 03-08 and is the writer. This task adds the
    **reader-side analysis** that turns a CSV into a pass/fail answer on E6:

      decile_gain(rows, role) -> float     # final-decile mean win-rate minus first-decile mean
      final_slope(rows, role, window) -> float
      check_convergence(rows, params) -> dict[role, Verdict]

    Thresholds come from config (`win_rate_margin`, `convergence_window`,
    `convergence_tolerance`) — no literals (QUAL-11). Verdicts are computed **per role** and
    reported per role (D-25); a single averaged verdict can mark the phase converged while one
    role is still random.

    Put these in `plot_curves.py` alongside the rendering, or in a small sibling module if that
    file approaches 150 lines (QUAL-08) — but do not add them to `curves.py`, which must stay
    matplotlib-free and importable by the harness.

    Extend `tests/unit/training/test_curves.py` (it exists from 03-08) rather than creating a
    second test file for the same module.
  </action>
  <verify>
    `uv run pytest tests/unit/training/test_curves.py -q` passes, covering: a synthetic rising
    curve reports a positive decile gain and a flat final slope (converged); a flat-noise curve
    from episode 1 fails the decile-gain check; a still-climbing curve fails the slope check;
    verdicts are reported separately per role and one role failing does not mask the other.
  </verify>
</task>

<task type="auto">
  <name>Task 2: Plotting and the README section</name>
  <files>training/plot_curves.py, README.md</files>
  <read_first>
    README.md (existing structure and heading style)
    training/curves.py (the schema being read)
  </read_first>
  <action>
    `plot_curves.py`: read the CSV and render PNGs into `artifacts/curves/` — one figure per
    role showing win-rate vs baseline over episodes with the ε schedule on a secondary axis, and
    a mean-reward figure. Runnable as `uv run python training/plot_curves.py <csv> <outdir>`.

    This is the **only** matplotlib importer in the repo (D-20). Verify nothing under
    `src/pursuit/` imports it, and that it is not needed to run a match.

    Then add the README learning-curves section (rule 42): embed the PNGs, state what the axes
    mean, state the configured win-rate bar and the game count the head-to-head result used, and
    note the seed. A curve without its evaluation bar next to it does not tell the grader
    whether the phase goal was met.

    Do **not** fabricate results. If training has not been run yet at this point in the phase,
    write the section with the structure and a clearly marked placeholder for the figures, and
    leave the numbers to be filled by 03-10 after the real run. Never write a win-rate that has
    not been measured.
  </action>
  <verify>
    `uv run python training/plot_curves.py <fixture csv> <tmpdir>` produces PNG files.
    `grep -rl matplotlib src/` returns nothing.
    README renders and contains the learning-curves section with no fabricated numbers.
  </verify>
</task>

<verification>
1. `training/curves.py` is untouched by this plan — `git diff` shows no change to it (it is
   03-08's artifact; recreating it here would be the duplication QUAL-02 forbids).
2. The E6 checks read thresholds from config and return a verdict **per role**; one role's
   failure does not mask the other's (D-25).
3. A synthetic flat-noise curve fails the decile-gain check and a still-climbing curve fails the
   slope check — the checks provably discriminate rather than always passing.
4. Per-role PNGs render from a fixture CSV.
5. matplotlib appears only in `training/plot_curves.py`; nothing in `src/` imports it (D-20).
6. README has the rule-42 learning-curves section, stating the win-rate bar and game count
   alongside the figures, with clearly marked placeholders rather than invented numbers if the
   real run has not happened yet.
7. `uv run ruff check .` → 0; `uv run pytest --cov=pursuit --cov=training` → green at ≥85%;
   `bash scripts/check_line_limit.sh` → all files ≤150 lines.
</verification>

<success_criteria>
- The curves written from episode 1 (03-08) are turned into the graded README deliverable and
  into a pass/fail convergence verdict (rule 42, STRAT-06).
- Cop and thief curves are separable and separately judged, exposing asymmetric convergence
  instead of hiding it.
- The README carries the graded learning-curve section, with measured numbers only — never a
  win-rate that was not actually measured.
</success_criteria>

<output>
Create `.planning/phases/03-blind-strategy-module-rl-policy/03-09-SUMMARY.md` when done.
</output>
