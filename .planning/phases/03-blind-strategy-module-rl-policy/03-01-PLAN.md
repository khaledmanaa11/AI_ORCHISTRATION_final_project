---
phase: 03-blind-strategy-module-rl-policy
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/PRD_rl_strategy.md
autonomous: true
requirements:
  - DOC-02

must_haves:
  truths:
    - "docs/PRD_rl_strategy.md exists as the per-mechanism PRD for the Q-learning policy, required by CLAUDE.md and SEGAL_GUIDELINES.md §2.3 (DOC-02)"
    - "It carries Version 1.00 in its header, consistent with the project versioning rule"
    - "It is written in Wave 1, before the policy code it describes (03-04 through 03-08), satisfying SEGAL §2.5 step 5"
    - "It specifies the exact state-key composition: absolute (own, target) pair, agent-relative blocked-direction bitmask, barriers-used count, bucketed turn phase — and states explicitly that the full board barrier bitmap is excluded and why (D-04, D-05, D-06)"
    - "It specifies the reward function, the Q-update rule, and the epsilon-greedy schedule as equations, so the implementation has a single unambiguous source (D-01)"
    - "It specifies the fallback trigger as key-absent OR visits below threshold, and states the fallback's distance metric is BFS on the barrier-aware grid rather than raw Manhattan, with the barrier-pocket reason (D-08, D-09)"
    - "It documents the input contract as a believed target cell, so Phase 4's belief map replaces the source without retraining (D-11)"
    - "It documents the two-stage cop decision keeping the Q action space at 5, and the sparring pool composition (D-12, D-13)"
    - "Every number carries a Source column marking it either a PARAMETERS.md game value or a labelled engineering default — no number is unsourced (D-18)"
    - "It states that the algorithm chooses the move and the language model never does, naming rule 25 / STRAT-07 (STRAT-07)"
  artifacts:
    - path: docs/PRD_rl_strategy.md
      provides: "Per-mechanism PRD for the Q-learning policy: state encoding, reward, update rule, exploration schedule, fallback, barrier sub-policy, training regime, evaluation bar, parameter sourcing"
      contains: "Version:** 1.00"
      min_lines: 90
  key_links:
    - from: docs/PRD_rl_strategy.md
      to: docs/PARAMETERS.md
      via: "parameter table citing Table 13 row 1, Table 15 rows 2-4 in a Source column"
      pattern: "Table 15"
    - from: docs/PRD_rl_strategy.md
      to: ".planning/phases/03-blind-strategy-module-rl-policy/03-PLAN-OUTLINE.md decisions"
      via: "decision IDs D-01..D-20 cited inline next to the design statements they lock"
      pattern: "D-1[1248]"
    - from: docs/PRD_rl_strategy.md
      to: "src/pursuit/strategy/ (built later by 03-04..03-07)"
      via: "the state-key format and update rule the implementation must match"
      pattern: "min_visits"
---

<objective>
Write `docs/PRD_rl_strategy.md` — the per-mechanism PRD for the Q-learning policy — **before**
the policy code exists. Documentation-only: this plan creates exactly one markdown file and
touches no source, no config, and no other document.

CLAUDE.md and SEGAL §2.3 require a dedicated `docs/PRD_<mechanism>.md` for every algorithm or
central mechanism. The Q-learning policy is Phase 3's central mechanism, so DOC-02 is unmet
until this file exists, and §2.5 step 5 requires it approved before the code — which is why it
sits in Wave 1, ahead of 03-04 … 03-08.

It is also the anti-regression record for the three things the implementation will otherwise
get wrong: the state key silently growing to include the full barrier bitmap (which makes the
table never converge), the fallback quietly using raw Manhattan (which dead-ends in barrier
pockets), and a hyperparameter appearing as a literal in `src/` instead of config.
</objective>

<execution_context>
@C:\Users\Hp\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Hp\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/03-blind-strategy-module-rl-policy/03-PLAN-OUTLINE.md
@.planning/phases/03-blind-strategy-module-rl-policy/03-CONTEXT.md

The AI design contract at
`.planning/phases/03-blind-strategy-module-rl-policy/03-AI-SPEC.md` already holds the
framework decision, the critical failure modes, and the evaluation strategy. **Cite it; do not
duplicate it.** This PRD covers the *mechanism* — encoding, reward, update rule, fallback,
training regime — not the framework rationale.

House style to match: `docs/PRD_mcp_transport.md` (Phase 2's per-mechanism PRD) — header line,
blockquote stating what the document governs, tables over paragraphs, a Source column on every
number. Read it before writing.
</context>

<task type="auto">
  <name>Task 1: Write docs/PRD_rl_strategy.md §1-§6 (mechanism)</name>
  <files>docs/PRD_rl_strategy.md</files>
  <read_first>
    docs/PRD_mcp_transport.md (house style — match its shape exactly)
    docs/STRATEGY.md (the existing strategy design notes)
    docs/PARAMETERS.md (Table 13 row 1, Table 15 rows 2-4 — the game values)
    .planning/phases/03-blind-strategy-module-rl-policy/03-AI-SPEC.md (cite, do not duplicate)
    .planning/REQUIREMENTS.md (STRAT-01..07 wording)
  </read_first>
  <action>
    Header exactly in the house shape, `**Version:** 1.00 · **Status:** approved`, with a
    blockquote stating this is the per-mechanism PRD for the Q-learning policy, written before
    the code per §2.5 step 5, and that every number is either traced to PARAMETERS.md or
    labelled an engineering default.

    §1 Mechanism and scope — one paragraph, then a Requirements-covered table (STRAT-01…07 +
    DOC-02) and explicit in/out of scope (out: scent, hints, LLM, belief map → Phase 4).

    §2 State encoding (D-04, D-05, D-06) — give the exact key composition and a worked example
    of one encoded key. State the excluded alternatives and why: full barrier bitmap → state
    explosion; raw turn index → 35× sparsity. Give the resulting state-space size arithmetic.

    §3 Action space — the 5 movement actions, and why the barrier decision is deliberately
    *not* an action (D-12: two-stage decision keeps the space at 5).

    §4 Reward function — the terminal rewards and any shaping, as a table. Note that the
    scoring values in `game_params.json` are the *game's* scores and are not the same thing as
    the learner's reward signal; if the reward reuses them, say so explicitly.

    §5 Update rule and exploration — the Q-update as an equation, and the ε schedule shape with
    its start/floor/decay named as config keys (never as literals here). State D-19: seeded
    `random` for exploration, `secrets` reserved for Phase 6.

    §6 Fallback (D-08, D-09, D-10) — trigger is key-absent OR visits < `min_visits`; the Bayes
    motion-model prior (uniform, spread by the opponent's legal moves, prediction step only);
    the distance metric is BFS on the barrier-aware grid. State plainly that STRAT-02 names
    this "Manhattan" and that the implementation is barrier-aware BFS, with the reason — so the
    deviation is a recorded decision rather than an undocumented drift.
  </action>
  <verify>
    File exists with the §1-§6 sections and the exact header shape. No section beyond §6 yet.
  </verify>
</task>

<task type="auto">
  <name>Task 2: Append §7-§10 (training, evaluation, parameters, boundaries)</name>
  <files>docs/PRD_rl_strategy.md</files>
  <read_first>
    .planning/phases/03-blind-strategy-module-rl-policy/03-AI-SPEC.md (§5 evaluation strategy)
    .planning/phases/03-blind-strategy-module-rl-policy/03-RESEARCH.md (sparring pool mechanics, if present)
  </read_first>
  <action>
    §7 Training regime (D-13, D-15, D-17) — offline only; episodes step `pursuit.sdk.engine`
    directly and never the network layer, with the reason; the sparring pool composition and
    sampling rule; checkpoint cadence and resumability; seed logging.

    §8 Evaluation (D-14, D-16) — the success bar: `QLearningBrain` beats `HeuristicBrain`
    head-to-head over the fixed eval scenario set at the configured win-rate over the
    configured game count. Cite AI-SPEC §5 for the full rubric rather than restating it.
    Learning-curve instrumentation from episode 1 of run 1, and the README PNG obligation
    (rule 42).

    §9 Parameter table — every number used by the mechanism, each with a Source column:
      - board 7×7 → PARAMETERS.md Table 13 row 1 (minimum)
      - barrier quota 14 → Table 15 row 2 (minimum)
      - move ceiling 35 → Table 15 row 3 (minimum)
      - survival threshold 35 → Table 15 row 4 (minimum)
    Then a **separately labelled subsection** for the engineering defaults (D-18): α, γ, ε
    start/floor/decay, `min_visits`, turn-bucket boundaries, episode count, checkpoint
    interval, eval games, win-rate bar — each marked "engineering default, NOT a PARAMETERS.md
    value", with the config key it lives under. State the category distinction in prose: the
    first table may never be altered; the second is ours to tune.

    §10 Boundaries and honesty — the input contract is a *believed* target cell (D-11), so
    Phase 4 swaps the source without a retrain; the algorithm chooses the move and the LLM
    never does (rule 25 / STRAT-07); cop and thief hold separate tables in separate processes
    (D-03, project rule 2); barrier declarations are truthful and within quota (rules 16, 22).

    Then re-read the whole document once and confirm no number appears without a source.
  </action>
  <verify>
    Every numeric literal in the document appears in either the §9 game-value table or the §9
    engineering-defaults subsection. Grep the document for digits and confirm each hit is
    inside a sourced table row or is a section number.
  </verify>
</task>

<verification>
1. `docs/PRD_rl_strategy.md` exists, ≥90 lines, header carries `**Version:** 1.00`.
2. No source, config, or other doc modified — `git status --porcelain` shows exactly one path.
3. State-key composition documented with a worked example; full-bitmap exclusion stated with
   its reason (D-04, D-05, D-06).
4. Q-update rule and ε schedule present as equations; hyperparameters named as config keys,
   not literals.
5. Fallback trigger and the BFS-not-Manhattan deviation both documented with reasons
   (D-08, D-09).
6. §9 has both tables, and the engineering-defaults subsection is explicitly labelled as a
   different category from PARAMETERS.md values (D-18).
7. STRAT-07 / rule 25 stated in §10; D-11 input contract stated.
8. No unsourced number anywhere in the document.
</verification>

<success_criteria>
- The per-mechanism PRD for the Q-learning policy exists at v1.00, satisfying DOC-02.
- Written before the policy code (§2.5 step 5) — no file under `src/pursuit/strategy/` exists
  when this plan completes.
- Encoding, reward, update rule, exploration, fallback, training regime and evaluation bar are
  each specified precisely enough that 03-04 … 03-08 have no room to improvise.
- Every number is traced to PARAMETERS.md or labelled an engineering default (D-18).
- Cites `03-AI-SPEC.md` for framework rationale and evaluation rubric rather than duplicating.
- No secrets, no placeholder text, no Phase 4-7 behavior presented as a Phase-3 deliverable.
</success_criteria>

<output>
Create `.planning/phases/03-blind-strategy-module-rl-policy/03-01-SUMMARY.md` when done.
</output>
