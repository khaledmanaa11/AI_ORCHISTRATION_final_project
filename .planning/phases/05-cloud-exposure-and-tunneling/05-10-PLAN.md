---
phase: 05-cloud-exposure-and-tunneling
plan: "10"
type: execute
wave: 3
depends_on: ["05-04", "05-05", "05-09"]
files_modified:
  - src/pursuit/security/audit.py
  - src/pursuit/network/agent_audit_wiring.py
  - src/pursuit/network/deadline_errors.py
  - src/pursuit/network/deadline.py
  - tests/unit/test_audit_hostile_records.py
  - tests/unit/test_transport_failure_containment.py
  - tests/unit/test_agent_entrypoint.py
  - .planning/phases/05-cloud-exposure-and-tunneling/deferred-items.md
autonomous: true
gap_closure: true
requirements:
  - NET-06
  - CLOUD-02
  - SEC-05

must_haves:
  truths:
    - "ONE pattern has now produced FOUR separate defects in this phase, each found by a different plan and each with the same consequence -- an unhandled exception on PEER-CONTROLLED data escapes to main.py and kills the process, so WE publish no nonces (rule 36) and our own log carries no verdict at all. Found so far: httpx.ConnectError escaping call_with_retry (05-04, closed by 05-09); the wrapped RuntimeError connect shape (05-09, closed in flight); commit_pack.verify_reveal(**payload) raising TypeError (05-05, contained); and audit_peer_records raising on a malformed FINAL_REVEAL (05-VERIFICATION deferred #7, THIS plan). This plan closes the two still open and states the boundary rule once, in source, so a fifth instance is a review failure rather than a discovery"
    - "The rule, written where the boundary is: any function that reads a structure the PEER sent must treat malformed input as a MISMATCH with a named detail, never as an exception. Probed against the shipped code at HEAD -- audit_peer_records(observed_commits, observed_reveals, peer_records) raises TypeError on a records-is-a-string, TypeError on a string entry, and KeyError on an entry with no turn key. All three sites sit OUTSIDE 05-05's verify_reveal try/except, and agent_entrypoint guards only ToolError"
    - "A malformed peer FINAL_REVEAL is a MISMATCH, not a crash and not a free pass. A peer whose records we cannot parse has not published usable nonces, which is exactly what rule 36 sanctions -- so it must still lose. The distinction that matters: we record a named mismatch and reach a verdict, instead of dying before writing one. Do not silently treat unparseable records as an empty list; that would re-open the {'records': []} evasion 06-05 closed"
    - "An ngrok 502/503/504 or a 429 mid-game stops killing us (deferred #6). mcp/client/streamable_http.py calls raise_for_status() at five sites and fastmcp preserves HTTPStatusError unwrapped, so a transient gateway blip on the peer's side -- the single most likely real failure on league day, since ngrok returns 502 whenever the peer's local server is briefly unavailable -- currently escapes exactly like ConnectError did"
    - "The 403 contract is UNCHANGED and this is the constraint the whole change is shaped around: a 4xx is an application-level answer about OUR OWN request (a 403 from 05-02's SharedSecretMiddleware means OUR secret is wrong) and must still RAISE, unretried and unaccused. Only 5xx and 429 -- answers about the SERVER's availability, not about our credentials -- become retryable. tests/integration/test_secret_channel.py::test_wrong_secret_fails_every_call is the live anchor and must still pass unedited"
    - "board_outcome's production wiring gains a test. 05-VERIFICATION found it correctly wired at agent_entrypoint.py:110 but pinned by NOTHING -- its removal would ship green, and an earlier draft of 05-04 wired it through the harness only. The regression that plan was written to prevent must not be re-introducible silently"
  artifacts:
    - path: src/pursuit/security/audit.py
      provides: "audit_peer_records treating malformed peer records as named mismatches, never raising"
      contains: "malformed"
    - path: src/pursuit/network/deadline_errors.py
      provides: "retryable_status, the 5xx/429-vs-4xx split, with the 403 reasoning kept intact"
      contains: "429"
    - path: tests/unit/test_audit_hostile_records.py
      provides: "The hostile-shape matrix plus the control that an honest peer is unaffected and a withholding peer still loses"
      contains: "still"
  key_links:
    - from: src/pursuit/network/deadline.py
      to: src/pursuit/network/deadline_errors.py
      via: "the status split is asked for by name, never by branching on status codes inline"
      pattern: "retryable_status"
---

<objective>
Close the last two instances of this phase's recurring pattern — an unhandled exception on
peer-controlled data killing us before we write a verdict — and pin the one production wiring
that has no test.

Purpose: 05-08's remote round is expensive (two machines, two operators). Sending it into a
known crash path wastes the attempt and produces another unusable artifact.
Output: every peer-shaped input reaches a verdict; a gateway blip is survivable; a 403 still
is not an accusation.
</objective>

<execution_context>
@C:\Users\Hp\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Hp\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/05-cloud-exposure-and-tunneling/deferred-items.md
@.planning/phases/05-cloud-exposure-and-tunneling/05-VERIFICATION.md

- `src/pursuit/network/deadline_errors.py` — 05-09's taxonomy module. Read its docstring in
  full before touching it. It states the rule this plan extends: a transport failure is
  retried, an application answer is raised, a fault that is ours and deterministic is raised.
  The `HTTPStatusError`-is-not-retryable reasoning there is CORRECT for 4xx and must survive;
  this plan narrows it to 4xx rather than overturning it.
- `src/pursuit/security/audit.py` — 05-05 contained `verify_reveal(**payload)` at `_audit_one`.
  That containment is the shape to follow; it is simply not wide enough, because three sites
  outside it read peer structure first.
- 05-04's `record_audit_incomplete` and `record_technical_loss` are the two verdict sinks. A
  malformed-records peer must reach `record_technical_loss` (rule 36), never `audit_incomplete`
  — failing to PARSE what the peer sent is a fact about the peer, unlike failing to send our own.
</context>

<tasks>

<task type="auto">
  <name>Task 1: a malformed peer FINAL_REVEAL is a mismatch, not a crash</name>
  <files>src/pursuit/security/audit.py, src/pursuit/network/agent_audit_wiring.py, tests/unit/test_audit_hostile_records.py</files>
  <action>
    Make `audit_peer_records` total over peer-supplied input. The three raising sites the
    verifier probed are `audit.py:67`, `:118` and `:158` — re-probe them yourself rather than
    trusting those line numbers, since 05-05 split this module.

    Shape: a `peer_records` that is not a list, an entry that is not a dict, an entry with no
    `turn`, and a `turn` that is not an int each produce a named `AuditRecord` mismatch whose
    detail says what was wrong with the SHAPE — never an exception, and never a silent skip.
    State the reason in source: a peer whose records we cannot parse has not published usable
    nonces, which is what rule 36 sanctions, so it must still LOSE; what changes is that we
    reach a verdict instead of dying before writing one.

    Do NOT coerce unparseable records to an empty list. That would route them into
    `_missing_turns`' coverage check as "published nothing", which is the same verdict by
    accident — but it also re-opens the `{"records": []}` evasion path 06-05 closed, because
    the two cases would become indistinguishable in the record. Keep them distinct.

    Write the boundary rule ONCE, as a module-level comment in `audit.py`, naming all four
    instances this phase found. That comment is the deliverable a fifth instance gets caught by.
  </action>
  <verify>
    `tests/unit/test_audit_hostile_records.py` — the matrix (non-list; string entry; entry
    missing `turn`; non-int `turn`; a valid record mixed with a malformed one) plus two
    CONTROLS: an honest peer is entirely unaffected, and a peer sending `{"records": []}`
    still produces the rule-36 coverage mismatches 06-05's tests pin.
    `uv run pytest tests/unit/test_audit_coverage.py tests/unit/test_audit_turn_binding.py tests/unit/test_audit_state_binding.py -q` — all pass UNMODIFIED.
    Re-run the exact probe from 05-VERIFICATION and record before/after verbatim.
  </verify>
  <done>Every shape a peer can send reaches a verdict; the withholding sanction still bites.</done>
</task>

<task type="auto">
  <name>Task 2: a gateway blip is transient, a 403 is still ours</name>
  <files>src/pursuit/network/deadline_errors.py, src/pursuit/network/deadline.py, tests/unit/test_transport_failure_containment.py</files>
  <action>
    Add `retryable_status(exc) -> bool` to `deadline_errors.py`: True for an
    `httpx.HTTPStatusError` whose `response.status_code` is 5xx or 429, False for everything
    else. `call_with_retry` asks it by name; no status-code branching is written inline.

    The reasoning goes in the module docstring, extending 05-09's paragraph rather than
    replacing it:
      - 4xx stays RAISED. It is an application-level answer about OUR OWN request. The 403
        from `SharedSecretMiddleware` means OUR secret is wrong; retrying it burns the ladder
        and ends in a TechnicalWin accusing a peer that answered us correctly (rules 16/22).
      - 5xx and 429 are answers about the SERVER'S availability, not about our credentials —
        the same transient class as `ConnectError`. On league day this is the likeliest real
        failure of all: ngrok returns 502 whenever the peer's local server is briefly
        unavailable, which is exactly what a backoff exists for.
      - Note the WRAPPING asymmetry 05-09 measured, because it applies here in reverse:
        fastmcp preserves `HTTPStatusError` UNWRAPPED through its connect path, so unlike
        `ConnectError` this class needs no `__cause__` unwrapping. Verify that before relying
        on it; if it is wrapped in some path, `unwraps_to_retryable` must learn about it too.

    Order matters in `call_with_retry`: the raise-first clause still runs first, and the
    status check must not let a 4xx through it.
  </action>
  <verify>
    Extend `test_transport_failure_containment.py`: a 502, a 503 and a 429 are each retried
    and end in a TechnicalWin with measured evidence; a 403 and a 404 each RAISE, burn no
    backoff, and produce no TechnicalWin.
    `uv run pytest tests/integration/test_secret_channel.py -q` — all 3 pass UNMODIFIED; that
    is the live 403 anchor.
    Non-vacuity: the 502 case must FAIL against the pre-Task-2 code, and the 403 case must
    FAIL against a version that retries all of `HTTPStatusError`. Record both.
  </verify>
  <done>A peer's gateway hiccup costs a backoff; our own bad secret still costs us nothing but an honest error.</done>
</task>

<task type="auto">
  <name>Task 3: pin the wiring that has no test, and correct the record</name>
  <files>tests/unit/test_agent_entrypoint.py, .planning/phases/05-cloud-exposure-and-tunneling/deferred-items.md</files>
  <action>
    Add a test asserting `run_final_audit` is called from `run_agent` WITH
    `board_outcome` equal to `run_turn_loop`'s own return value. 05-VERIFICATION found this
    correctly wired but pinned by nothing — `test_agent_entrypoint.py`'s existing cases never
    assert the kwarg and `dev_launch.py` passes without it, so deleting it ships green. That
    is the exact regression 05-04 was written to prevent, and it is currently re-introducible
    in silence. Assert the VALUE identity, not merely the kwarg's presence.

    Then amend `deferred-items.md`: mark #1 and #6 and #7 closed by this plan or by 05-09 as
    applicable, and correct item #3's closing line — 05-VERIFICATION refuted its claim that
    "the containment above means nothing is exposed today", which was true of `verify_reveal`
    alone but not of the three sites outside it. Leave the original text and append the
    correction with its date; do not rewrite history.
  </action>
  <verify>
    The new test FAILS when the `board_outcome=outcome` kwarg is removed from
    `agent_entrypoint.py` — run that revert probe and record it. A test that passes without
    the wiring is testing nothing.
  </verify>
  <done>The production wiring cannot be removed silently, and the deferred record tells the truth.</done>
</task>

</tasks>

<verification>
1. `uv run ruff check .` -> 0 violations.
2. `uv run pytest tests/ --cov` -> all pass, coverage >= 85%. Baseline is **1327 passed /
   96.37%**, independently measured at HEAD. A DROP is a regression to explain.
3. `bash scripts/check_line_limit.sh` -> exit 0. `agent_lifecycle.py` is at 148/150 and
   `audit.py` was already split once — split a sibling rather than compressing.
4. `uv run python scripts/check_no_llm_in_strategy.py` -> OK.
5. `uv run python scripts/measure_gate6.py` -> **all three book Sec10.4 criteria PASS.**
6. `grep -rn "except Exception\|except BaseException" src/pursuit/security/audit.py src/pursuit/network/deadline*.py` -> NO match. Every widening names its classes.
7. `uv run python scripts/dev_launch.py` -> exit 0, both sides `audit_verdict matched=true`,
   one shared uid across both logs, both ledgers, both declarations.
</verification>

<success_criteria>
- No peer-supplied shape can prevent us from writing a verdict.
- A peer that sends unparseable records still loses; an honest peer is untouched.
- A 502/429 costs a backoff; a 403 still raises and accuses nobody.
- The board_outcome wiring is pinned by a test that fails without it.
</success_criteria>

<output>
Create `.planning/phases/05-cloud-exposure-and-tunneling/05-10-SUMMARY.md`. Record the
before/after of the 05-VERIFICATION hostile-shape probe verbatim, all revert-probe results,
the measured GATE-6 verdict, and `dev_launch.py`'s exit code and shared uid. State plainly
whether any FIFTH instance of the peer-data pattern was found while working — and if none was,
say that too, since the sweep's value is partly in the negative result.
</output>
