---
phase: 05-cloud-exposure-and-tunneling
plan: "18"
type: execute
wave: 10
depends_on: ["05-15", "05-17"]
files_modified:
  - src/pursuit/network/turn_commit.py
  - tests/unit/test_envelope_boundary_invariant.py
  - tests/unit/test_early_final_reveal.py
  - .planning/phases/05-cloud-exposure-and-tunneling/deferred-items.md
autonomous: true
gap_closure: true
requirements: [NET-06, CLOUD-02, SEC-05]

must_haves:
  truths:
    - "THE INITIATOR STOPS READING A PEER'S LEDGER AS AN ILLEGAL MOVE. Measured at HEAD (deferred #18): `turn_commit.await_and_respond`'s POLICE branch at `turn_commit.py:103` calls the pull primitive BARE -- `return await next_protocol_message(ctx)` -- with no type test, and hands whatever arrives to `turn_actions.await_opponent_turn` as if it were the opponent's REVEAL. With queue `[FINAL_REVEAL, REVEAL]` it returns `type=final_reveal`, and `await_opponent_turn` yields `Outcome.TECHNICAL_LOSS` with reason `'payload must be a dict, got NoneType'` through `turn_buffer.reject_peer_payload`. An honest peer's published ledger becomes our technical loss and a false declaration (rules 16/22). The four `wait_for_*` legs each check a type; this one alone does not."
    - "THIS IS THE FIFTH INSTANCE OF ONE CLASS, AND THE CLASS IS WHAT GETS CLOSED. 05-09, 05-10, 05-15, 05-17 and now #18 are all the same shape: an unexpected envelope type at a layer boundary is mishandled -- dropped, or misread as something else. Each was found only after the previous was fixed, every one by grepping outward rather than by review. A point fix buys one defect. This plan therefore ALSO pins the invariant across EVERY site that pulls from the queue, so instance six fails a test instead of a league game."
    - "THE INVARIANT, stated once and enforced by enumeration: every site that pulls an envelope either HANDLES its type, BUFFERS it for the layer that wants it, or SKIPS it -- and no site ever passes a non-move envelope into move handling. The test enumerates the pull sites by reading them out of the source rather than by a hand-written list, so a NEW pull site added later is covered on the day it lands and cannot silently opt out."
    - "05-17's routing is what makes the repair safe, and the two must not be confused. After 05-17 the peer's ledger is already SAFE in `ctx.commit_state.early_final_reveal` (measured `buffered = MessageType.FINAL_REVEAL` after the call, where pre-fix it was `None`), so the AUDIT already matches even while the turn loop mis-declares. #18 is therefore about the GAME OUTCOME, not the audit: we currently lose a game we did not lose."
    - "THE DESIGN DECISION IS NAMED, NOT SMUGGLED. Skipping a FINAL_REVEAL in that branch means deciding what the initiator does when the peer has demonstrably ended the game while we still await its REVEAL. The two candidates are: keep waiting (and burn a ladder we already know is hopeless), or treat the peer's FINAL_REVEAL as the end of the game and proceed to our own audit. The plan does not pre-judge it -- the executor MEASURES both against the existing turn-loop and audit behaviour and records why the chosen one is right. What is NOT acceptable is the outcome staying `TECHNICAL_LOSS` with a payload-parse reason string."
    - "Rule 36's sanction survives intact. A genuinely silent peer must STILL earn `technical_win{opponent_unresponsive}` -- 05-17's counter-control passed unchanged through six probes and must pass unchanged through this plan's too. A fix that stops accusing silent peers has broken the game."
    - "Zero new numeric values and no Table-19 value moved (CLAUDE.md rule 1). Routing and outcome attribution, not timing. 05-17 measured the wrong fix directly: widening the receive ladder to 21 attempts -- 10x the shipped value -- produced a BYTE-IDENTICAL accusation."
  artifacts:
    - path: src/pursuit/network/turn_commit.py
      provides: "the initiator's own wait stops treating a FINAL_REVEAL as a malformed move"
      contains: "a type test on the police branch, matching the four wait_for_* legs"
    - path: tests/unit/test_envelope_boundary_invariant.py
      provides: "the class-level guard: every pull site handles, buffers or skips"
      contains: "pull sites enumerated FROM SOURCE, not hand-listed; a new site is covered automatically"
  key_links:
    - from: src/pursuit/network/turn_commit.py
      to: src/pursuit/network/turn_commit_wait.py
      via: "the police branch adopts the same type discipline the four wait legs already have"
      pattern: "MessageType"
---

<objective>
Close the fifth instance of the envelope-boundary class, and pin the class so there is no sixth.

Purpose: an honest peer's published ledger currently becomes OUR technical loss with a
payload-parse reason string -- a false declaration under rules 16/22, reachable in a league game.
Output: the initiator handles a FINAL_REVEAL correctly, and every pull site in the codebase is
held to one stated invariant by a test that enumerates them from source.
</objective>

<context>
- `.planning/phases/05-cloud-exposure-and-tunneling/deferred-items.md` item **#18** -- the
  measurement, both ways, and why 05-17 did not fix it there.
- `05-17-SUMMARY.md` -- the routing fix this rests on, and its six probes.
- The four `wait_for_*` legs in `turn_commit_wait.py` -- the type discipline to match.
- Table 19 values live in config; `docs/PARAMETERS.md` is the only source for them.
</context>

<tasks>

<task type="auto">
  <name>Task 1: reproduce the initiator's false declaration</name>
  <files>tests/unit/test_early_final_reveal.py</files>
  <action>
    RED first. Police role, queue `[FINAL_REVEAL, REVEAL]`, per deferred #18's own measurement.
    Record the returned type, the resulting `Outcome`, and the reason string verbatim. Injected
    clock, zero real sleeps. Assert against a MATCHED audit over real hashes built through
    `commit_pack` -- 05-17 found that `records=[]` passes vacuously because `all_matched([])` is
    True, and that trap applies here unchanged.
  </action>
  <verify>The test fails at HEAD for exactly the stated reason, naming the honest peer.</verify>
  <done>The initiator's false declaration is reproduced in this repository's own numbers.</done>
</task>

<task type="auto">
  <name>Task 2: decide what the initiator does, and do it</name>
  <files>src/pursuit/network/turn_commit.py</files>
  <action>
    Give the police branch the type discipline the four `wait_for_*` legs already have. First
    MEASURE the two candidates named in the must_haves -- keep waiting, versus treat the peer's
    FINAL_REVEAL as the end of the game -- against how the turn loop and `run_final_audit`
    actually behave, and record which you chose and why in the source and in the SUMMARY. Change
    no numeric value. Correct any docstring this falsifies.
  </action>
  <verify>`bash scripts/check_line_limit.sh`, run tree-wide AND explicitly on any new file by path -- the no-arg form enumerates via `git ls-files` and passes vacuously on untracked files.</verify>
  <done>An honest peer that has ended the game no longer costs us a game we did not lose.</done>
</task>

<task type="auto">
  <name>Task 3: pin the class so there is no sixth instance</name>
  <files>tests/unit/test_envelope_boundary_invariant.py</files>
  <action>
    Enumerate every site that pulls an envelope from the queue -- BY READING THE SOURCE (AST or a
    grep over `src/pursuit/network/`), not from a hand-written list, so a new site added later is
    covered on the day it lands. For each, assert the invariant: it HANDLES the type, BUFFERS it,
    or SKIPS it, and never passes a non-move envelope into move handling. Drive each with every
    `MessageType` and confirm none produces a traceback, a technical loss with a parse-error
    reason, or a silent drop of a FINAL_REVEAL.

    Non-vacuity is mandatory: the enumeration must fail loudly if it finds ZERO sites (the empty
    `parametrize` trap -- pytest SKIPS silently), and the suite must fail with Task 2's fix
    reverted. Record both counts.

    Also include the counter-control unchanged: a genuinely silent peer STILL earns
    `technical_win{opponent_unresponsive}`.
  </action>
  <verify>Reverting Task 2 fails the suite; an emptied enumeration fails loudly rather than skipping.</verify>
  <done>The class has a standing guard, not a fifth point fix.</done>
</task>

</tasks>

<verification>
1. `uv run ruff check .` -> 0 violations.
2. `uv run pytest tests/ --cov` -> all pass, coverage >= 85% and not below the inherited baseline.
3. `bash scripts/check_line_limit.sh` -> exit 0, plus every new file checked explicitly by path.
4. `uv run python scripts/check_no_llm_in_strategy.py` -> OK.
5. `uv run python scripts/dev_launch.py` -> exit 0, one shared uid, both sides `audit_verdict
   matched=true`, zero `technical_win`, zero `watchdog_incident`.
</verification>

<success_criteria>
- An honest peer's FINAL_REVEAL never becomes our technical loss or a parse-error reason string.
- A genuinely silent peer is still accused -- rule 36 unchanged.
- Every queue-pull site is enumerated from source and held to the stated invariant.
- No Table-19 value moved.
</success_criteria>

<non_goals>
Deferred #16 (the linger's quiet interval is justified from our last ARRIVAL, while the peer
schedules its retry from its own FAILURE -- strictly later; 5 s quiet vs 35 s worst case) and #17
(a FINAL_REVEAL arriving DURING the linger is drained and discarded unaudited) stay OPEN with
their measurements. Both are game-end synchronisation questions deserving their own decision, and
neither is a false declaration against an honest peer.

**This is the last plan of phase 5.** Anything further is recorded and carried, not executed here.
</non_goals>

<output>
Create `.planning/phases/05-cloud-exposure-and-tunneling/05-18-SUMMARY.md` with the reproduced
declaration, the design decision and why, the enumeration's site count, and all probe counts.
</output>
