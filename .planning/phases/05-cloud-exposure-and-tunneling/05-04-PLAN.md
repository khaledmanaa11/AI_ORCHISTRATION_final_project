---
phase: 05-cloud-exposure-and-tunneling
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pursuit/network/agent_teardown.py
  - src/pursuit/network/agent_audit_wiring.py
  - src/pursuit/network/agent_audit_verdict.py
  - src/pursuit/network/agent_entrypoint.py
  - src/pursuit/network/agent_lifecycle.py
  - src/pursuit/network/event_log.py
  - tests/unit/test_agent_entrypoint.py
  - tests/unit/test_agent_audit_wiring.py
  - tests/unit/test_agent_teardown.py
  - tests/unit/test_audit_send_failure.py
  - tests/integration/test_step0_and_audit.py
  - tests/integration/test_late_peer_teardown.py
autonomous: true
gap_closure: true
requirements:
  - CLOUD-02
  - NET-06
  - NET-07
  - SEC-05

must_haves:
  truths:
    - "A failed OUTBOUND final-reveal push, when the turn loop already produced a board outcome, is recorded as evidence about US (a non-accusatory audit_incomplete) and the board outcome stands -- rules 16/22: our own send failing is not proof the peer was unresponsive. The 2026-08-13 round proves the point: machine A's peer_audit carries machine B's five ledger hashes byte-for-byte, so B's push WAS delivered at the exact moment B declared A unresponsive"
    - "The fix reaches PRODUCTION, not just the test harness: run_agent's own call site passes the turn loop's outcome as board_outcome. A grep must show the argument ARRIVING FROM run_agent, not merely a branch existing in agent_audit_wiring"
    - "The real sanctions survive intact: a genuine AUDIT_HASH_MISMATCH still returns Outcome.TECHNICAL_LOSS, a peer that genuinely withholds its nonces (recv_verdict, rule 36) still returns Outcome.TECHNICAL_LOSS, a push failure with NO board outcome (a turn loop that never resolved) still returns Outcome.TECHNICAL_LOSS, and the ToolError branch stays accusatory on purpose"
    - "A failed own push no longer aborts the audit: this side still waits for the peer's FINAL_REVEAL and still audits both directions. In the measured round this alone would have given machine B a matched audit_verdict instead of nothing at all"
    - "Every technical loss recorded after the turn loop appends a CORRECTED game_over record, so the log's last outcome and main.py's exit code can never disagree (the 2026-08-13 round has B's log ending on game_over=capture while the process exited 1)"
    - "Teardown no longer races a peer still mid-exchange: run_agent lingers on a bounded grace window before shutdown_cleanly cancels the server task and closes the listen socket (D-56/peer_runtime.py's documented bypass of uvicorn's graceful shutdown). Both bounds are existing NetworkParams values (PARAMETERS.md Table 19) -- response_timeout as the total cap, backoff_seconds as the quiet interval -- and no new numeric leaf lands in any config file (CLAUDE.md rule 1)"
    - "NET-07: no watchdog freeze can fire across the audit or the linger. Watchdog.touch() appears nowhere in the audit path today, its threshold is 60 s (Table 19 row 7) and its freeze action is os._exit(1) -- so the untouched window ALREADY spans the whole of run_final_audit. This plan closes that window rather than widening it"
    - "The proof is an integration harness that SEQUENCES the two sides and actually calls shutdown_cleanly (05-UAT.md G1 missing[4]). test_step0_and_audit.py:100 runs both turn loops in one asyncio.gather and never tears down, which is structurally why nothing caught this"
    - "Phase 6 does not regress: scripts/measure_gate6.py re-runs with all three book Sec10.4 criteria still PASS"
  artifacts:
    - path: src/pursuit/network/agent_teardown.py
      provides: "linger_for_peer(ctx) -- the bounded post-audit grace window that keeps this side's server answering a peer still retrying, without tripping the watchdog"
      contains: "response_timeout"
    - path: src/pursuit/network/agent_audit_wiring.py
      provides: "run_final_audit with a board_outcome parameter; a failed push records audit_incomplete and continues instead of accusing"
      contains: "board_outcome"
    - path: src/pursuit/network/agent_audit_verdict.py
      provides: "record_audit_incomplete (new, non-accusatory) and record_technical_loss now appending a corrected game_over"
      contains: "audit_incomplete"
    - path: tests/integration/test_late_peer_teardown.py
      provides: "The sequenced two-peer proof: an early finisher lingers and tears down while a late peer still completes with a matched verdict"
      contains: "shutdown_cleanly"
  key_links:
    - from: src/pursuit/network/agent_entrypoint.py
      to: src/pursuit/network/agent_audit_wiring.py
      via: "run_agent passes the turn loop's own outcome as board_outcome -- the production wiring, without which every new test passes while a real round still accuses"
      pattern: "board_outcome=outcome"
    - from: src/pursuit/network/agent_entrypoint.py
      to: src/pursuit/network/agent_teardown.py
      via: "run_agent's finally block lingers before shutdown_cleanly"
      pattern: "linger_for_peer"
    - from: src/pursuit/network/agent_teardown.py
      to: src/pursuit/shared/network_config.py
      via: "the grace bounds are NetworkParams fields, never literals"
      pattern: "ctx\\.net\\.(response_timeout|backoff_seconds)"
    - from: src/pursuit/network/agent_audit_wiring.py
      to: src/pursuit/network/agent_audit_verdict.py
      via: "a failed own push routes to audit_incomplete, not record_technical_loss"
      pattern: "record_audit_incomplete"
---

<objective>
Close G1: stop one side's own failed send from becoming an accusation against a peer that
had already answered it, and stop a zero-grace teardown from causing that failure in the
first place.

Purpose: the "verdicts agree" clause is the single thing GATE-5 criterion 2 failed on.
Output: an honest verdict path, a bounded grace window, and a durable last-outcome record.

Budget note: this plan runs at the upper end of the context target. Commit per task.
</objective>

<execution_context>
@C:\Users\Hp\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Hp\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/05-cloud-exposure-and-tunneling/05-UAT.md
@docs/phases/phase-5/GATE-5-MEASUREMENT.md

- `docs/phases/phase-5/remote-round-2026-08-13/` — the retained evidence. **Read it before
  writing code.** The load-bearing facts, already measured:
  - machine A's `audit_verdict` carries `matched: true` with all five of machine B's ledger
    turns in `peer_audit` — B's push attempt 1 WAS delivered and processed;
  - B's `technical_win` says `retries_attempted: 4`, `reason: opponent_unresponsive`, and
    B's last `game_over` still says `capture`;
  - B spent 17.4 s between its turn-4 REVEAL and its turn-4 hint (that half of G1 is closed
    by plan 05-06, which owns `turn_actions.py`);
  - **the two machines' clocks are ~72 s apart.** Never compare timestamps across the two
    logs; only within-machine deltas are meaningful.
- `src/pursuit/network/peer_runtime.py` `stop()` — documented as deliberately bypassing
  uvicorn's `Server.shutdown()`. Do NOT soften it; the linger is the fix, not a gentler stop.
- **A pre-existing NET-07 hazard this plan inherits, and must not be blamed for.**
  `grep -rn "watchdog.touch()" src/` returns only `turn_buffer.py:111,159`,
  `turn_commit_send.py:52,132`, `turn_commit_wait.py:70` — nothing in the audit path. So the
  untouched window already spans the whole of `run_final_audit`, whose receive ladder is
  bounded at `4x30 + 3x5 = 135 s`, against a `watchdog_threshold` of 60 s whose freeze action
  is `watchdog._default_exit` -> `os._exit(1)`. `shutdown_cleanly` stops the watchdog only
  AFTER all of that, and its own docstring already warns that a watchdog left running past
  GAME_OVER treats a clean shutdown as a freeze. Task 2 closes this; record in the SUMMARY
  that the pre-existing half was found here, not introduced here.
- `tests/unit/test_audit_turn_binding.py::test_an_honest_peer_is_still_matched` — this
  repo's established paired-fairness-control pattern. Every relaxation below owes one.
</context>

<tasks>

<task type="auto">
  <name>Task 1: a failed own push is evidence about us -- wired into production</name>
  <files>src/pursuit/network/event_log.py, src/pursuit/network/agent_audit_verdict.py, src/pursuit/network/agent_audit_wiring.py, src/pursuit/network/agent_entrypoint.py, tests/unit/test_audit_send_failure.py, tests/unit/test_agent_audit_wiring.py, tests/integration/test_step0_and_audit.py</files>
  <action>
    Add `EventType.AUDIT_INCOMPLETE = "audit_incomplete"` (additive, exactly how
    `AUDIT_VERDICT` was added in 06-03 — do not reshape the stable field set).

    `agent_audit_verdict.record_audit_incomplete(ctx, verdict)`: appends one
    AUDIT_INCOMPLETE record carrying the TechnicalWin's genuinely measured evidence
    (attempts, timeout_seconds, elapsed_seconds, last_error) and returns None. It must NOT
    use `EventType.TECHNICAL_WIN`, must NOT append a game_over, and its `reason` text must
    name OUR OWN send as the thing that failed — never the peer. Reuse
    `_technical_win_record`'s field-assembly shape rather than a second copy of it
    (no-duplication rule).

    `agent_audit_verdict.record_technical_loss`: ALSO append a corrected
    `turn_events.game_over_record(outcome=Outcome.TECHNICAL_LOSS)` after the technical_win
    record — the same two-record pattern `record_audit_verdict`'s mismatch path already uses
    at :93-100, and for the same stated reason (the log's LAST game_over is the authoritative
    one; the earlier board record is left standing as append-only evidence).

    `agent_audit_wiring.run_final_audit(ctx, *, board_outcome: Outcome | None = None)`.
    New control flow, and ONLY this:
      - push fails AND `board_outcome is None`  -> `record_technical_loss` (unchanged
        semantics: nothing else stands, the game never resolved);
      - push fails AND `board_outcome is not None` -> `record_audit_incomplete`, then FALL
        THROUGH to the receive+audit steps (do not return);
      - receive fails -> `record_technical_loss`, unchanged (rule 36: a peer that withholds
        its nonces still loses);
      - mismatch -> `record_audit_verdict`, unchanged (AUDIT_HASH_MISMATCH still loses).

    **THE PRODUCTION WIRING — this is the step without which the whole task is theatre.**
    `agent_entrypoint.py:85` currently reads `audit_outcome = await run_final_audit(ctx)`,
    and `outcome` from `run_turn_loop(ctx)` is already in scope from :78. Change it to pass
    `board_outcome=outcome`. Verification item 6 greps for the argument arriving FROM
    `run_agent`, not merely for a branch existing.

    **Leave the `except ToolError` branch at :86-89 accusatory, on purpose**, and say so in a
    `#` comment so nobody "simplifies" it into the new path: `deadline.call_with_retry`
    re-raises `ToolError` without retrying by design, because an application-level rejection
    is the PEER's own act, not a transport failure (that module's docstring states it, and
    06-06 shipped `PEER_PROTOCOL_ERROR` for exactly this). A peer that rejects our call is a
    different fact from our own send timing out, and the two must not collapse.

    Thread the real outcome through the existing harness: `test_step0_and_audit.py`'s
    `_run_audit_and_merge(outcome_a, outcome_b, ctx_a, ctx_b)` already holds both outcomes —
    pass them as `board_outcome`. `test_step0_and_audit_tamper.py` and the four
    `scripts/gate6_*.py` call sites go through that one helper and inherit it unchanged.
    That edit is purely additive (a new keyword threaded into an existing call), so a
    concurrent plan that only IMPORTS `_play_to_turn_loop_end` from this file is unaffected.

    `tests/unit/test_agent_audit_wiring.py:23,34` call `run_final_audit(ctx)` with no
    `board_outcome` and would therefore pass unchanged while no longer covering the
    production path. Re-specify both with a docstring saying they pin the
    `board_outcome is None` branch — the "turn loop never resolved" case — and are
    deliberately NOT the production shape, which `test_audit_send_failure.py` covers.

    The send-failure matrix belongs here, with the code it guards —
    `tests/unit/test_audit_send_failure.py`, four cases, each with its reason in its
    docstring. The first is the fix; the other three are the paired fairness controls this
    repo requires whenever a sanction is relaxed:
      1. push fails, `board_outcome` is a real board outcome -> an `audit_incomplete` record
         lands, NO `technical_win` record lands, `run_final_audit` does not return
         TECHNICAL_LOSS, and the receive+audit steps still ran;
      2. CONTROL — push fails, `board_outcome is None` -> still TECHNICAL_LOSS;
      3. CONTROL — push succeeds, receive fails (rule 36, the peer withheld its nonces) ->
         still TECHNICAL_LOSS, now with a corrected `game_over` beside it;
      4. CONTROL — a genuine AUDIT_HASH_MISMATCH -> still TECHNICAL_LOSS with the corrected
         `game_over`, unchanged from Phase 6.
    Non-vacuity: revert-probe case 1 against pre-fix code and confirm it FAILS; confirm
    cases 2-4 were already green pre-fix (they encode preserved behaviour). Record both in
    the SUMMARY. A case-1 test that passes against the old code is testing nothing.
  </action>
  <verify>
    `uv run pytest tests/unit/test_audit_send_failure.py tests/unit/test_agent_audit_wiring.py tests/integration/test_step0_and_audit.py tests/integration/test_step0_and_audit_tamper.py tests/unit/test_outcome_durability.py -q` passes.
    `uv run python scripts/measure_gate6.py` — all three criteria still PASS.
    `bash scripts/check_line_limit.sh` clean (agent_audit_wiring.py starts at 102/150,
    agent_audit_verdict.py at 86/150, event_log.py at 118/150, agent_entrypoint.py at 83/150
    — measured; re-measure, and if any lands over, split a sibling rather than compressing).
  </verify>
  <done>A failed own send after a real board outcome leaves the board outcome standing and accuses no one -- in run_agent, not only in tests; every genuine sanction still fires.</done>
</task>

<task type="auto">
  <name>Task 2: the bounded post-audit grace window, inside the watchdog's window</name>
  <files>src/pursuit/network/agent_teardown.py, src/pursuit/network/agent_entrypoint.py, src/pursuit/network/agent_lifecycle.py, tests/unit/test_agent_teardown.py, tests/unit/test_agent_entrypoint.py</files>
  <action>
    New sibling module `agent_teardown.py` (the established
    tunnel_wiring/secret_wiring/agent_entrypoint split precedent), holding
    `async def linger_for_peer(ctx) -> None`.

    Contract: keep this process's server task alive and drain `ctx.runtime.queue` until
    EITHER the queue has been quiet for one full `ctx.net.backoff_seconds` window OR
    `ctx.net.response_timeout` has elapsed in total. Pull through
    `deadline.wait_for_opponent(...)` and catch `DeadlineExpired` — `deadline._bounded` is
    the ONE place this codebase calls `asyncio.wait_for` (QUAL-02); do not add a second.

    Parameter provenance, stated in the module docstring — this is the whole point of the
    module and must not be paraphrased:
      - **total cap = `NetworkParams.response_timeout`** (PARAMETERS.md Table 19 row 6,
        "Deadline on any network request", negotiable, 30 s). The thing being waited out IS
        one inbound network request — the peer's own FINAL_REVEAL push. A peer still inside
        its own request deadline is by definition still within this window.
      - **quiet interval = `NetworkParams.backoff_seconds`** (Table 19 row 3, "Backoff
        before a retry", minimum, 5 s). If the peer were going to retry, it would have
        retried inside one backoff. This is the same row `deadline.py` already reuses for
        the same meaning (D-17).
      - `watchdog_threshold` (row 7) is DELIBERATELY not used as a bound: it answers "has
        this process stopped making progress", a different question, and borrowing it would
        double the teardown wait for no negotiated reason.
    Zero literals in this module; zero new keys in `config/*/network.json`.

    Draining is not answering — the tool handler already returned 200 by enqueuing. The
    drain exists to keep the queue bounded and to supply the quiet signal. Say so in the
    docstring so a later reader does not "optimise" it away.

    **NET-07, and this is not optional.** `Watchdog.check_once` fires when
    `clock() - last_activity > threshold_seconds` and its default `exit_action` is
    `os._exit(1)`; `touch()` is called nowhere in the audit path (see the pre-existing-hazard
    note in this plan's context). Close it by SPLITTING `agent_lifecycle.shutdown_cleanly`
    into its two existing halves — `ctx.watchdog.stop()` and `await ctx.runtime.stop()` —
    and having `run_agent` stop the watchdog BEFORE the linger and the runtime AFTER it.
    Prefer this over sprinkling `touch()` calls: the watchdog exists to escape a FROZEN turn
    loop, and by this point the turn loop is finished and the machine is terminal, so there
    is nothing left for it to rescue — whereas a `touch()`-per-iteration would keep a
    genuinely wedged process alive. Keep `shutdown_cleanly` itself present and idempotent for
    its existing callers; state the split's reason in both docstrings.

    Wire the linger into `agent_entrypoint.run_agent`'s `finally`, between the two halves,
    imported into `agent_entrypoint`'s own namespace (that module's tests monkeypatch at that
    namespace by design — see their file docstring). `run_with_tunnel` already stops the
    tunnel after `_play` returns, so the tunnel stays up across the linger; confirm that
    ordering rather than assuming it.

    `test_agent_entrypoint.py`: add the new steps to `_patch_common` with their own
    `order.append(...)`, and extend the expected orders. Note accurately what is there: TWO
    of the three tests assert an exact `order == [...]` list; the not-agreed test asserts
    `"run_turn_loop" not in order` and `order[-1] == "shutdown_cleanly"`. Keep the two exact
    lists exact, and convert the third to an exact list too — that is a strengthening, and
    its final element is no longer the last thing that runs.
  </action>
  <verify>
    Unit tests for `linger_for_peer` with an injected fake queue prove all three shapes:
    (a) an empty queue returns after one quiet interval, not after the full cap;
    (b) a queue that keeps delivering is drained until the total cap and then returns;
    (c) it never raises and never ends the game.
    NET-07 test: drive a real `Watchdog` (with an INJECTED `exit_action` and clock, never
    `os._exit`) across a full-cap linger and assert no freeze fires and `exit_action` is
    never called.
    `uv run pytest tests/unit/test_agent_entrypoint.py tests/unit/test_agent_teardown.py tests/unit/test_agent_lifecycle.py tests/unit/test_watchdog.py -q` passes.
    Measured: `uv run python scripts/dev_launch.py` still completes; report the added
    wall-clock cost per side in the SUMMARY (expected ~one backoff_seconds on a clean game).
  </verify>
  <done>A peer still retrying at us finds a live listener for a bounded, Table-19-sourced window, and no watchdog can kill the process inside it.</done>
</task>

<task type="auto">
  <name>Task 3: the sequenced two-peer proof (05-UAT.md G1 missing[4])</name>
  <files>tests/integration/test_late_peer_teardown.py</files>
  <action>
    This is the test that would have caught the bug that cost the round. `05-UAT.md` names
    its absence as the structural reason nothing did: `test_step0_and_audit.py:100` runs both
    turn loops in one `asyncio.gather` over in-memory clients and never calls
    `shutdown_cleanly`, so it cannot express a staggered or torn-down peer at all.

    Build a harness that SEQUENCES the two sides rather than gathering them, and that
    actually tears down. Reproduce the measured shape of the round:
      - side A reaches the end of its turn loop first and runs its audit;
      - A lingers, then tears down (watchdog stop, linger, runtime stop) — for real, via the
        production path, not a hand-rolled imitation;
      - side B arrives LATE and pushes its FINAL_REVEAL during A's grace window.

    Assert:
      1. B ends with a matched `audit_verdict` — not nothing at all, which is what the real
         round produced;
      2. neither side records a `technical_win` with `opponent_unresponsive`;
      3. both sides' final outcomes AGREE, and each side's LAST `game_over` record matches
         the outcome that side returns.

    Do not modify `tests/integration/test_step0_and_audit.py` from this task; import from it
    if useful (the established sibling-import precedent `test_step0_and_audit_tamper.py` and
    the gate6 scripts already use), or build the two contexts directly with
    `agent_lifecycle.default_context` as that file itself does.

    Non-vacuity, and report it: run this file against the pre-Task-2 teardown (no linger) and
    confirm assertion 1 or 2 FAILS. If it passes without the linger, the harness is not
    actually sequencing anything and must be rebuilt.
  </action>
  <verify>
    `uv run pytest tests/integration/test_late_peer_teardown.py -q` passes.
    The no-linger revert probe fails as described, with the failing assertion named in the
    SUMMARY.
    `bash scripts/check_line_limit.sh` clean — test files obey the 150-line limit too; split
    a sibling harness if it does not fit.
  </verify>
  <done>A late peer completes with a matched verdict against a peer that has already finished and torn down -- proven, not argued.</done>
</task>

</tasks>

<verification>
1. `uv run ruff check .` -> 0 violations.
2. `uv run pytest tests/ --cov` -> all pass, coverage >= 85%. Baseline is **1251 passed /
   96.26%**; expect **+9 to +14 tests** (4 send-failure, 4 teardown/NET-07, the sequenced
   harness, plus any coverage-closing test the new branches need) and coverage flat-to-up.
   Any DROP in the passing count is a regression to explain, never to accept.
3. `bash scripts/check_line_limit.sh` -> exit 0.
4. `uv run python scripts/check_no_llm_in_strategy.py` -> OK.
5. `uv run python scripts/measure_gate6.py` -> **all three book Sec10.4 criteria PASS**
   (constraint: this plan touches the audit path; Phase 6's gate must not regress).
6. **Production wiring, not just a branch:** `grep -n "board_outcome" src/` must show
   `agent_entrypoint.py` passing `board_outcome=outcome` into `run_final_audit`, with
   `outcome` coming from `run_turn_loop`. A branch in `agent_audit_wiring.py` with no caller
   supplying the argument is the exact failure this item exists to catch.
7. `grep -rn "audit_incomplete" src/` shows a real production caller, not a dead branch.
</verification>

<success_criteria>
- A side whose own final-reveal push fails, after a real board outcome, records
  non-accusatory evidence, still audits, and still reports the board outcome -- in run_agent.
- A genuine hash mismatch, a genuinely withholding peer, and a peer that rejects our call all
  still produce a technical loss, each with a corrected durable game_over.
- Teardown grace is bounded by two existing Table 19 values, adds no config key, and no
  watchdog freeze can fire inside it.
</success_criteria>

<output>
Create `.planning/phases/05-cloud-exposure-and-tunneling/05-04-SUMMARY.md`. Record the
measured GATE-6 re-run verdict, the measured linger cost on a clean loopback game, the
revert-probe results for Tasks 1 and 3, and — explicitly — that the pre-existing
watchdog-vs-audit window was FOUND by this plan, not introduced by it.
</output>
