---
phase: 05-cloud-exposure-and-tunneling
plan: "08"
type: execute
wave: 4
depends_on: ["05-04", "05-05", "05-06", "05-07"]
files_modified:
  - docs/phases/phase-5/REMOTE-ROUND-RUNBOOK.md
  - docs/phases/phase-5/GATE-5-MEASUREMENT.md
  - docs/phases/phase-5/remote-round-2026-08-14/
  - docs/phases/phase-5/TODO.md
  - docs/phases/phase-5/PRD.md
  - docs/phases/phase-5/PLAN.md
  - docs/TODO.md
  - .planning/ROADMAP.md
  - .planning/graphs/GRAPH_REPORT.md
autonomous: false
gap_closure: true
requirements:
  - CLOUD-02

must_haves:
  truths:
    - "GATE-5 criterion 2 is closed by a REAL round on two machines on two networks, or it is not closed. No script in this repository can produce it -- GATE-5-MEASUREMENT.md's own section says why, and same-machine-via-public-URL (criterion 1's smoke test) is never a substitute (the distinction 05-CONTEXT.md locks)"
    - "The re-run closes the exact clause attempt 1 failed: two AGREEING final verdicts, and ONE shared game UID across all four artifacts (both logs, both ledgers). Attempt 1 played a full 5-turn game to a real capture and still did not close the criterion"
    - "Attempt 1's evidence gaps are named and closed in the runbook BEFORE the round runs: machine B's full console output was never captured, and neither was the ngrok agent log -- the one artifact that would settle whether machine A's teardown or an independent tunnel drop killed B's push. Retaining them is the difference between diagnosing attempt 2 in an hour and re-running it"
    - "The two machines' clocks were ~72 s apart in attempt 1. The runbook says so and tells the operator to record each machine's UTC clock at start, because cross-machine timestamp comparison silently produced the wrong causal story for hours"
    - "Attempt 1 is not deleted or rewritten. GATE-5-MEASUREMENT.md keeps it as Attempt 1 with its honest verdict and adds Attempt 2 beside it -- the append-only evidence discipline this project applies to its own game logs applies to its own gate record (rule 38)"
    - "The phase triplet is complete and every row checked, per CLAUDE.md's per-phase-triplet rule: docs/phases/phase-5/{PRD,PLAN,TODO}.md cover the 05-04..05-08 gap-closure work, and 05-99 only ticks once the gate genuinely closes"
  artifacts:
    - path: docs/phases/phase-5/REMOTE-ROUND-RUNBOOK.md
      provides: "The amended operator procedure -- attempt-1 lessons, the expanded evidence list, the clock note"
      contains: "ngrok"
    - path: docs/phases/phase-5/GATE-5-MEASUREMENT.md
      provides: "Criterion 2 answered with the attempt-2 evidence, beside the retained attempt-1 record"
      contains: "Attempt 2"
    - path: docs/phases/phase-5/remote-round-2026-08-14/
      provides: "Both logs, both ledgers, both declaration pairs, both consoles, the ngrok agent logs"
      contains: "jsonl"
  key_links:
    - from: docs/phases/phase-5/GATE-5-MEASUREMENT.md
      to: .planning/ROADMAP.md
      via: "the measurement answers the section 10.4 criteria the roadmap records verbatim and does not edit them"
      pattern: "10.4"
    - from: docs/phases/phase-5/REMOTE-ROUND-RUNBOOK.md
      to: docs/phases/phase-5/remote-round-2026-08-13
      via: "the amended evidence list names exactly what attempt 1 lacked"
      pattern: "remote-round-2026-08-13"
---

<objective>
Re-run the genuine remote round against the fixed code and close GATE-5 criterion 2 --
or record honestly why it still did not close.

Purpose: this is the phase's one open gate item and the only thing standing between Phase 5
and /gsd:verify-work 5.
Output: four joinable artifacts, two agreeing verdicts, and a measurement document that
says so with numbers.
</objective>

<execution_context>
@C:\Users\Hp\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Hp\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@docs/phases/phase-5/GATE-5-MEASUREMENT.md
@docs/phases/phase-5/REMOTE-ROUND-RUNBOOK.md
@.planning/phases/05-cloud-exposure-and-tunneling/05-UAT.md

- `.planning/phases/05-cloud-exposure-and-tunneling/05-04-SUMMARY.md`,
  `05-05-SUMMARY.md`, `05-06-SUMMARY.md`, `05-07-SUMMARY.md` — what changed, and the
  measured linger cost, which the operator needs to expect at game end.
- `docs/phases/phase-5/remote-round-2026-08-13/` — attempt 1, retained permanently.
- `.planning/ROADMAP.md` Phase 5 — the two §10.4 criteria, verbatim. **Not ours to edit.**
</context>

<tasks>

<task type="auto">
  <name>Task 1: amend the runbook with what attempt 1 taught</name>
  <files>docs/phases/phase-5/REMOTE-ROUND-RUNBOOK.md</files>
  <action>
    The runbook is already good on setup and tunnels — do not rewrite it. Add what attempt 1
    proved was missing:

    1. **Evidence to retain (§5) gains the two artifacts attempt 1 lacked:**
       - machine B's FULL console output, stdout AND stderr, redirected to a file for the
         whole session (attempt 1 kept machine A's console but not B's, so B's side of the
         teardown is unreconstructable);
       - the **ngrok agent log from BOTH machines.** This is the single artifact that would
         have settled whether machine A's teardown or an independent tunnel drop killed B's
         push attempt 1. `pyngrok` surfaces the agent process's output on the console, so a
         full console redirect captures it; if the operator sets a `log:` path in ngrok's own
         config file, retain that file too. State both options — do not invent a path.
       Keep the existing four items unchanged.

    2. **A clock note, prominent.** In attempt 1 the two machines' clocks were ~72 s apart,
       and cross-machine timestamp comparison produced a confidently wrong causal story.
       Instruct: record each machine's UTC clock (`date -u` / `Get-Date -AsUTC`) immediately
       before starting, into the retained evidence. Compare timestamps only WITHIN a machine.

    3. **What is expected to be different this time**, so the operator can spot a regression
       live: both sides' logs now carry `message_received` + `hint` records; both sides'
       `language_turn` records show at least one `incoming_hint` that is not `no_hint`; both
       machines' log and ledger filenames share ONE stem, equal across the two machines; the
       process pauses for a bounded grace window at game end before exiting (name the
       measured figure from 05-04-SUMMARY.md).

    4. **A pre-flight line** that both machines are on the SAME post-fix commit, and the
       digest table's commit reference is refreshed to it — the existing table cites `0427137`
       and the runbook already says a mismatch is information, not something to edit away.
  </action>
  <verify>
    A reader with two machines can run the round and retain every artifact from this file
    alone. The two new evidence items are unambiguous. Nothing in §1-§4 was rewritten
    unnecessarily; `git diff` is additive plus the commit refresh.
  </verify>
  <done>The procedure now collects the evidence attempt 1 wished it had.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: run the round -- two machines, two networks, human-operated</name>
  <files>docs/phases/phase-5/remote-round-2026-08-14/ (evidence copied out of gitignored logs/ by the operator)</files>
  <action>
    HUMAN-RUN. Claude executes nothing in this task and must not simulate, approximate or
    substitute it. Present the <how-to-verify> steps below to the operator, then WAIT for the
    resume signal. Two physical machines on two different networks are required; there is no
    scriptable path from this repository, by the criterion's own construction.
  </action>
  <what-built>
    Every code fix from 05-04..05-07 is merged and green: the non-accusatory send-failure
    path, the bounded teardown grace, the negotiated game id adopted end to end with the
    audit validating the committed state record, inbound hint logging, a hint channel that
    delivers both ways, and an honest LLM declaration.
  </what-built>
  <how-to-verify>
    **This step CANNOT be scripted and CANNOT be run by Claude.** It requires a second
    physical computer on a second network, operated by a human. That is not a testing gap to
    close with more mocking — it is the literal content of book §10.4 criterion 2, and
    `GATE-5-MEASUREMENT.md` already says so in its own words. No amount of local automation
    substitutes for it.

    1. Follow `docs/phases/phase-5/REMOTE-ROUND-RUNBOOK.md` end to end, as amended by Task 1.
    2. Both machines on the SAME post-fix commit. Verify the two config digests print equal
       before starting (the runbook's own one-liner). A mismatch is information — do not edit
       config to force agreement.
    3. Record each machine's UTC clock before starting.
    4. Redirect BOTH machines' console output (stdout and stderr) to files for the whole
       session.
    5. Start both agents within a second or two of each other — `perform_handshake` makes
       exactly one attempt, with no retry.
    6. Play to a real outcome. A partial handshake does not close the criterion.
    7. Copy out of gitignored `logs/`: both `<uid>.jsonl`, both `<uid>.ledger.jsonl`, both
       declaration pairs, both consoles, both ngrok agent logs, both machines' recorded
       clocks and the machine/network description.

    **What a PASS looks like — check these before reporting back:**
    - both sides' final verdicts AGREE;
    - all four artifacts share ONE game UID stem, equal across the two machines;
    - both sides recorded an `audit_verdict`, and neither recorded a `technical_win` with
      `opponent_unresponsive`;
    - each side's LAST `game_over` record matches that side's process exit code;
    - both sides have `message_received` + `hint` records and at least one non-`no_hint`
      `incoming_hint`.

    If any of these fails, that is a real result. Report exactly what happened with the
    retained evidence — do not re-run until it looks good, and do not soften the criterion.
  </how-to-verify>
  <resume-signal>Report the outcome: "closed" with the evidence paths, or a description of what failed plus the retained artifacts.</resume-signal>
  <verify>The operator reports back against the five PASS checks above, with the retained evidence directory populated.</verify>
  <done>A real round has run on two machines on two networks against the fixed code, and its artifacts are on disk -- whatever the verdict.</done>
</task>

<task type="auto">
  <name>Task 3: record the result and close (or honestly re-open) the gate</name>
  <files>docs/phases/phase-5/remote-round-2026-08-14/, docs/phases/phase-5/GATE-5-MEASUREMENT.md, docs/phases/phase-5/TODO.md, docs/phases/phase-5/PRD.md, docs/phases/phase-5/PLAN.md, docs/TODO.md, .planning/ROADMAP.md, .planning/graphs/GRAPH_REPORT.md</files>
  <action>
    Commit the retained evidence under a NEW dated directory beside attempt 1's — never into
    or over `remote-round-2026-08-13/`.

    `GATE-5-MEASUREMENT.md`: add an **Attempt 2** section beside the retained Attempt 1.
    Follow the document's existing per-criterion discipline — method, run command, what a PASS
    looks like, then the measured evidence with every claim pointing at a named field or file.
    State the machine/network pair, both verdicts, both exit codes, the shared game UID, and
    the per-side hint decode counts. Record any observation that does not fit the happy path
    rather than smoothing it, exactly as the `.dev`-vs-`.app` and Windows-teardown-noise notes
    already do.
    - If the round closed the criterion: flip criterion 2 to PASS with the evidence links, and
      update the document's own header, which currently reads PENDING.
    - If it did not: leave it PENDING, add the new gaps, and STOP. Do not tick anything below.
      A gate that reads PASS without the evidence is the one failure mode rule 38 exists for.

    Only if criterion 2 genuinely closed:
    - `docs/phases/phase-5/TODO.md` — tick both §10.4 boxes and every row including 05-99,
      and add rows for the 05-04..05-08 gap-closure plans so the triplet matches the plan set
      (CLAUDE.md's per-phase-triplet rule).
    - `docs/phases/phase-5/PRD.md` / `PLAN.md` — extend for this gap-closure work: PRD.md's
      acceptance criteria gain the attempt-2 evidence pointer; PLAN.md's component table and
      wave graph gain the five new plans and the new modules (`agent_teardown.py`,
      `game_identity.py`, `turn_hint_buffer.py`) with the phase ADRs they rest on.
    - Root `docs/TODO.md` — tick the matching Phase-5 rows.
    - `.planning/ROADMAP.md` — tick the Phase 5 plan rows and add 05-04..05-08. The two
      §10.4 criteria themselves are quoted from the book and are NOT edited.
    - Roadmap task 05-96: refresh the knowledge graph
      (`graphify update . && cp graphify-out/{graph.json,graph.html,GRAPH_REPORT.md} .planning/graphs/`),
      commit only `GRAPH_REPORT.md`, and confirm the three new modules appear. Leave
      `graph.json`/`graph.html` unstaged — they are gitignored build artifacts.
  </action>
  <verify>
    `GATE-5-MEASUREMENT.md` contains both attempts; attempt 1's honest failure text is
    unchanged. Every attempt-2 claim points at a committed file or a named field.
    `git status` shows no `graph.json`/`graph.html` staged.
    Nothing is ticked anywhere if criterion 2 did not close.
  </verify>
  <done>The gate record states what actually happened, and the trackers agree with it.</done>
</task>

</tasks>

<verification>
1. `uv run ruff check .` -> 0 violations.
2. `uv run pytest tests/ --cov` -> all pass, coverage >= 85% (this plan changes no source; the
   number must equal 05-07's closing figure exactly — any drift is a finding).
3. `bash scripts/check_line_limit.sh` -> exit 0.
4. `uv run python scripts/check_no_llm_in_strategy.py` -> OK.
5. Secret discipline on the retained evidence, before committing anything: grep the new
   evidence directory and both console captures for the shared secret value, the ngrok
   authtoken and `ANTHROPIC_API_KEY` — consoles and ngrok logs are exactly where a token
   leaks (CLAUDE.md rule 4 / rules 39-40). Redact any hit, and say in the SUMMARY what was
   redacted and where.
6. `git ls-files docs/phases/phase-5/remote-round-2026-08-13/` still lists all of attempt 1.
</verification>

<success_criteria>
- A real remote round ran on two machines on two networks against the fixed code.
- Both sides' verdicts agree and all four artifacts share one game UID -- or the gate stays
  PENDING and says exactly why.
- The phase triplet and both trackers match the gate record, in either direction.
</success_criteria>

<output>
Create `.planning/phases/05-cloud-exposure-and-tunneling/05-08-SUMMARY.md` recording the
round's measured facts, the evidence paths, any redactions, and — if the criterion closed —
that Phase 5 is ready for `/gsd:verify-work 5`.
</output>
