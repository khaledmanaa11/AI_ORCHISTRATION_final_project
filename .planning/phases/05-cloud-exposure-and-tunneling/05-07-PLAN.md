---
phase: 05-cloud-exposure-and-tunneling
plan: "07"
type: execute
wave: 3
depends_on: ["05-05"]
files_modified:
  - src/pursuit/services/llm/client.py
  - src/pursuit/services/llm/bluff.py
  - src/pursuit/services/llm/bluff_prompt.py
  - src/pursuit/network/language_wiring.py
  - src/pursuit/network/agent_audit_wiring.py
  - tests/unit/test_llm_client.py
  - tests/unit/test_language_wiring.py
  - tests/unit/test_agent_audit_wiring.py
  - tests/unit/test_bluff_prompt.py
  - docs/PRD_deception.md
autonomous: true
gap_closure: true
requirements:
  - LANG-02
  - SEC-08

must_haves:
  truths:
    - "The keyless fallback BEHAVIOUR does not change. Phase 4 sanctioned a keyless run and the hint bank is the intended no-key path: a missing ANTHROPIC_API_KEY must keep degrading to the template bank and must NEVER become a hard startup failure. This plan only makes the existing behaviour legible"
    - "The operator is told once, at startup, at WARNING level -- there is no logging.basicConfig anywhere in this project, so only WARNING and above reaches stderr, and an INFO line would be invisible to the person who needs it"
    - "Key presence is probed, never read into a log line or a declaration (CLAUDE.md rule 4 / rules 39-40). The helper returns a bool and lives in client.py, which already owns the env var name -- the name is not duplicated anywhere"
    - "The declared llm_name is honest (rule 38): a template provider, or claude_api with no key, declares a template-fallback string; otherwise the configured model_id. Machine B declared claude-haiku-4-5 while making ZERO calls, and shipped a declaration byte-indistinguishable from machine A's, which made 8"
    - "The Step-0 declaration keeps EXACTLY its ten keys. The value changes; the field set does not. It is inside the HMAC'd envelope step0_sign.sign_declaration produces and handshake_step0 verifies against its own claimed digest"
    - "The compose SYSTEM prompt pins first person, and the STYLE_GUIDE block quoted in docs/PRD_deception.md is updated in the SAME commit so the doc and the shipped string never drift. NO post-compose person validator is added -- it would be fuzzy and would silently divert good hints to the bank"
    - "rule 38 is the absolute-DQ rule for games_played: config/*/games_played.json is not touched, reset or hand-edited by any task in this plan"
  artifacts:
    - path: src/pursuit/services/llm/client.py
      provides: "has_api_key() -- presence-only, beside the env var name it already owns"
      contains: "has_api_key"
    - path: src/pursuit/network/language_wiring.py
      provides: "The startup WARNING when the configured provider is claude_api and no key is present"
      contains: "_log.warning"
    - path: src/pursuit/network/agent_audit_wiring.py
      provides: "declare_step0's llm_name reflecting actual capability, not config aspiration"
      contains: "llm_name"
  key_links:
    - from: src/pursuit/network/language_wiring.py
      to: src/pursuit/services/llm/client.py
      via: "_build_provider probes key presence before constructing the Anthropic provider"
      pattern: "has_api_key"
    - from: src/pursuit/network/agent_audit_wiring.py
      to: src/pursuit/services/llm/client.py
      via: "the declared llm_name consults presence, not just cfg.language.model"
      pattern: "has_api_key"
    - from: src/pursuit/services/llm/bluff_prompt.py
      to: docs/PRD_deception.md
      via: "the STYLE_GUIDE string is quoted verbatim in the PRD and both change together"
      pattern: "STYLE GUIDE"
---

<objective>
Close G5: make the disabled LLM visible to the operator and honest in the declaration,
without changing one byte of what happens when the key is missing.

Purpose: rule 38 honesty, and an operator who can tell a live run from a template run.
Output: one startup warning, one honest declared name, one first-person prompt.
</objective>

<execution_context>
@C:\Users\Hp\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Hp\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/05-cloud-exposure-and-tunneling/05-UAT.md
@.planning/phases/05-cloud-exposure-and-tunneling/05-05-SUMMARY.md

- `docs/phases/phase-5/remote-round-2026-08-13/machineB-thief/declaration_074fc2b16888899e.json`
  — `llm_name: claude-haiku-4-5` on a machine whose five `language_turn` records all read
  `token_spend.calls = 0`. Compare it with machine A's; they are indistinguishable.
- `src/pursuit/security/step0_collect.py::collect_declaration` — the ten-key field set. This
  plan changes an argument's VALUE and nothing about the shape.
- `src/pursuit/services/llm/bluff.py` step 1 of the module docstring: no-key already routes
  through the same `LlmFailure` branch as any other failure. That routing is correct and
  stays; only its silence is the gap.
- `docs/PRD_deception.md:186` — where STYLE_GUIDE is quoted.
</context>

<tasks>

<task type="auto">
  <name>Task 1: the operator can tell the LLM is off</name>
  <files>src/pursuit/services/llm/client.py, src/pursuit/network/language_wiring.py, src/pursuit/services/llm/bluff.py, tests/unit/test_llm_client.py, tests/unit/test_language_wiring.py</files>
  <action>
    `client.has_api_key() -> bool` — `bool(os.environ.get(_API_KEY_ENV_VAR))`, presence only.
    It lives here because this module already owns `_API_KEY_ENV_VAR`; do not restate that
    name anywhere else. It must never return, log or interpolate the value.

    `language_wiring._build_provider`: when the configured provider is `claude_api` and
    `has_api_key()` is False, emit ONE `_log.warning` before constructing the provider,
    naming the env var NAME and stating plainly that every hint this game will come from the
    template bank. WARNING level is load-bearing: there is no `logging.basicConfig` in this
    project, so anything below WARNING never reaches the operator's stderr. Say that in the
    comment. Construct the provider exactly as before — the warning changes nothing about
    what is built or how compose degrades.

    `bluff.compose`'s `LlmFailure -> hint_bank` branch gains a `_log.debug` line (matching
    `_complete`'s existing debug line, and deliberately NOT warning: this fires per turn and
    would drown the one startup warning that matters). The operator-facing signal is the
    startup warning plus the declaration, not per-turn noise.
  </action>
  <verify>
    Unit tests with `monkeypatch.delenv`/`setenv`: `has_api_key()` is False when unset, False
    when set to empty string, True otherwise — and a control asserting the returned value is
    a bool, never the key.
    A `caplog` test: provider `claude_api` with no key logs exactly one WARNING that contains
    the env var NAME and does NOT contain any key-shaped value; with a key set, zero
    warnings; provider `template`, zero warnings regardless.
    `uv run pytest tests/ -q` green.
  </verify>
  <done>Starting an agent with no key says so, once, where the operator will see it.</done>
</task>

<task type="auto">
  <name>Task 2: an honest declaration and a first-person prompt</name>
  <files>src/pursuit/network/agent_audit_wiring.py, src/pursuit/services/llm/bluff_prompt.py, tests/unit/test_agent_audit_wiring.py, tests/unit/test_bluff_prompt.py, docs/PRD_deception.md</files>
  <action>
    `declare_step0`: gate `llm_name`. Provider is `template`, OR provider is `claude_api` and
    `has_api_key()` is False -> a NAMED module constant that says template fallback in plain
    words; otherwise `cfg.language.model[ModelKey.MODEL_ID]` exactly as today. Read the
    provider through `ModelKey.PROVIDER`, never a bare string. The ten declaration keys are
    unchanged — assert that in the test, because the declaration is HMAC-signed and any
    field-set change would break Step-0 verification against a peer.

    `bluff_prompt._SYSTEM_TEMPLATE`: pin FIRST PERSON. The current wording asks the model to
    phrase a claim "for a player", which is what produced machine A's turn-4 drift into
    "The player is currently positioned...". It must ask for a sentence the player speaks AS
    themselves. Do NOT add a post-compose person validator — it would be fuzzy and would
    silently divert perfectly good hints to the bank, which is a worse failure than an
    occasional third-person sentence.

    If the first-person instruction belongs in `STYLE_GUIDE` rather than the template body,
    update `docs/PRD_deception.md`'s quoted copy in the SAME commit — the PRD quotes that
    string verbatim and the two must never drift.

    Do not touch `config/*/games_played.json`, and do not touch anything else in
    `step0_collect.py` (rule 38: that counter is the absolute-DQ field).
  </action>
  <verify>
    Unit tests: with provider `claude_api` and no key, the declaration's `llm_name` names the
    template fallback; with a key set, it is the configured `model_id`; with provider
    `template`, the fallback string regardless of key. In all three, `len(declaration) == 10`
    and the key set is byte-identical to today's.
    `build_system_prompt` contains the first-person instruction; the STYLE_GUIDE string in
    `bluff_prompt.py` and the block quoted in `docs/PRD_deception.md` match exactly (assert
    it, or diff it and record the diff as empty).
    `uv run pytest tests/ -q` green.
  </verify>
  <done>A declaration states what the agent can actually do, and the prompt asks for the sentence the player would say.</done>
</task>

</tasks>

<verification>
1. `uv run ruff check .` -> 0 violations.
2. `uv run pytest tests/ --cov` -> all pass, coverage >= 85%. Against 05-05's closing count,
   expect **+6 to +9 tests**; coverage flat-to-up.
3. `bash scripts/check_line_limit.sh` -> exit 0. `agent_audit_wiring.py` is the file three
   plans in this set touch — re-measure it and report the number.
4. `uv run python scripts/check_no_llm_in_strategy.py` -> OK.
5. Secret check: `grep -rn "ANTHROPIC_API_KEY" src/` returns the env var NAME only, in
   `client.py`; no value anywhere, no second copy of the name.
6. Behaviour unchanged, measured not assumed: run the full suite with `ANTHROPIC_API_KEY`
   explicitly unset (the suite's own `monkeypatch.delenv` convention) and confirm every
   pre-existing keyless test still passes — the fallback still falls back.
</verification>

<success_criteria>
- A keyless run warns once at startup and declares itself honestly.
- The fallback path itself is byte-for-byte the behaviour Phase 4 sanctioned.
- The compose prompt asks for first person; no validator was added.
</success_criteria>

<output>
Create `.planning/phases/05-cloud-exposure-and-tunneling/05-07-SUMMARY.md`. Record the exact
declared `llm_name` strings for all three provider/key combinations, and the final line count
for `agent_audit_wiring.py`.
</output>
