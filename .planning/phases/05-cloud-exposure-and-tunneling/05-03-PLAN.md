---
phase: 05-cloud-exposure-and-tunneling
plan: "03"
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - scripts/gate5_tunnel_smoke.py
  - docs/phases/phase-5/GATE-5-MEASUREMENT.md
  - docs/phases/phase-5/LOCALTONET-FALLBACK.md
  - .planning/graphs/GRAPH_REPORT.md
autonomous: true
requirements:
  - CLOUD-01
  - CLOUD-02

must_haves:
  truths:
    - "The gate is the book's section 10.4 milestone 5, quoted verbatim from ROADMAP.md and not edited: each peer reachable through ngrok/Localtonet, and a remote agent plays a FULL round through the tunnel. Each criterion gets evidence, not a verdict - the Phase-3/Phase-4 measurement discipline"
    - "The smoke script is the scriptable half: it starts a peer with the tunnel up, round-trips a real HTTP request through the PUBLIC URL to the local FastMCP route, asserts a valid MCP response, asserts a request missing the shared-secret header dies with 403 THROUGH THE TUNNEL (not just loopback), and tears everything down. It needs NGROK_AUTHTOKEN and is therefore a manual script, not a CI test - stated in its docstring"
    - "The genuine remote round (CLOUD-02) inherently needs a second machine on a different network and a human to run it. GATE-5-MEASUREMENT.md records it as the phase's human-pending item with the exact procedure (start agent, share URL+secret, opponent config paste, full round, both event logs retained as evidence) - the GATE-4 live-run precedent, honestly applied. Rule 38: the phase is not fully measured until it runs"
    - "Same-machine-via-public-URL is recorded as what it is - a smoke test - and never presented as the remote round (CONTEXT locks this distinction)"
    - "D-57: LOCALTONET-FALLBACK.md is the rule-10 compliance runbook: install, authtoken, dashboard port mapping to the agent's local port, the 30-minute free-tier timeout stated plainly as the reason ngrok is primary, and what to re-establish if it is ever used on league day. Documentation only - no Localtonet code path exists"
    - "No CI test in this plan touches the internet: the smoke script is invoked by a human; pytest stays offline and green without any tunnel"
    - "Roadmap task 05-96: the knowledge graph is refreshed after the phase's code landed, GRAPH_REPORT.md committed, graph.json/html left gitignored"
  artifacts:
    - path: scripts/gate5_tunnel_smoke.py
      provides: "The one-command smoke run: tunnel up, public round-trip OK, 403 without secret, clean teardown, JSON evidence out"
      contains: "public_url"
    - path: docs/phases/phase-5/GATE-5-MEASUREMENT.md
      provides: "Both section 10.4 criteria with evidence; the remote round as the single human-pending item with its procedure"
      contains: "GATE-5"
    - path: docs/phases/phase-5/LOCALTONET-FALLBACK.md
      provides: "The rule-10 fallback runbook"
      contains: "authtoken"
  key_links:
    - from: scripts/gate5_tunnel_smoke.py
      to: src/pursuit/network/tunnel_manager.py
      via: "the smoke script drives the real TunnelManager, not a parallel reimplementation"
      pattern: "TunnelManager"
    - from: docs/phases/phase-5/GATE-5-MEASUREMENT.md
      to: .planning/ROADMAP.md
      via: "the measurement answers the section 10.4 criteria the roadmap records verbatim"
      pattern: "10.4"
---

<objective>
Make the gate measurable and honest: script everything scriptable, write the procedure for
the one step that needs a human and a second machine, and document the fallback rule 10
requires.
</objective>

<execution_context>
@C:\Users\Hp\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Hp\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/05-cloud-exposure-and-tunneling/05-PLAN-OUTLINE.md
@.planning/phases/05-cloud-exposure-and-tunneling/05-01-SUMMARY.md
@.planning/phases/05-cloud-exposure-and-tunneling/05-02-SUMMARY.md

- `docs/phases/phase-4/GATE-4-MEASUREMENT.md` — the measurement-document precedent,
  including how a pending human item is recorded without softening it.
- `.planning/ROADMAP.md` Phase 5 — the §10.4 criteria verbatim. Not editable.
- `05-RESEARCH.md` §Testing Strategy and §Localtonet — the smoke-script assertion list and
  the runbook content list, both already scoped.
- CLAUDE.md graphify section — the refresh command (05-96).
</context>

<task type="auto">
  <name>Task 1: the smoke script</name>
  <files>scripts/gate5_tunnel_smoke.py</files>
  <action>
    One command, run by a human with NGROK_AUTHTOKEN, PURSUIT_NGROK_DOMAIN and
    PURSUIT_TUNNEL_SECRET set. It: starts one peer with the tunnel via the real
    TunnelManager; asserts `public_url` is https and matches the configured domain; makes a
    real request through the public URL with the secret header and asserts a valid MCP-shaped
    response arrives at the local server; makes one without the header and asserts 403;
    stops everything; writes a JSON evidence file (url pattern, round-trip latency, both
    assertion outcomes, timestamp) into docs/phases/phase-5/.

    Split helper modules if it approaches the line limit (the gate4_* pattern). It reuses
    TunnelManager and the runtime as shipped — any behaviour it needs that they lack is a
    finding, not a workaround.
  </action>
  <verify>
    Reviewed logic + a unit-testable core: URL-pattern check and evidence-file writer are
    importable and covered offline. The script refuses to run with the env vars missing,
    naming them.
  </verify>
  <done>The scriptable half of the gate is one command with evidence output.</done>
</task>

<task type="auto">
  <name>Task 2: the measurement document and the remote-round procedure</name>
  <files>docs/phases/phase-5/GATE-5-MEASUREMENT.md</files>
  <action>
    Quote both §10.4 criteria verbatim. Criterion 1 (reachable): filled by the smoke
    evidence when the human runs the script — the document links the JSON and states what a
    pass looks like. Criterion 2 (remote round): PENDING — the full procedure: start agent,
    read the exchange block, deliver URL + secret env var to the remote operator, remote
    config paste (the Phase-2 opponent_url seam), play one full round, retain both peers'
    event logs and the final verdicts as the evidence, note the machine/network used.

    State plainly: the phase is not fully measured until both criteria carry evidence, and
    verify-work must not tick until then (rule 38). Mirror GATE-4's live-PENDING format so
    the grader sees one consistent discipline.
  </action>
  <verify>
    Both criteria quoted, neither paraphrased; the pending item names its exact commands and
    evidence artifacts; nothing ticked anywhere.
  </verify>
  <done>A human with two machines can complete the gate from this document alone.</done>
</task>

<task type="auto">
  <name>Task 3: the Localtonet runbook and the graph refresh</name>
  <files>docs/phases/phase-5/LOCALTONET-FALLBACK.md, .planning/graphs/GRAPH_REPORT.md</files>
  <action>
    The runbook per RESEARCH §Localtonet: install, `--authtoken`, dashboard mapping to the
    agent's local port, service flags, the 30-minute free-tier timeout stated as the reason
    it is the fallback, and the league-day note about re-establishing the tunnel per window.
    State that no code path exists for it and why that satisfies rule 10 (D-57).

    Then 05-96: `graphify update .` and copy the three outputs; commit only
    GRAPH_REPORT.md; sanity-check the new tunnel modules appear.
  </action>
  <verify>
    The runbook covers setup through league-day use without reference to any other document.
    GRAPH_REPORT.md names tunnel_manager and secret_guard; graph.json/html unstaged.
  </verify>
  <done>Rule 10's second provider is documented, and the graph knows Phase 5 exists.</done>
</task>

<verification>
1. `uv run ruff check .` → 0; `uv run pytest tests/ --cov` → ≥ 85%, 0 failures, fully offline.
2. `bash scripts/check_line_limit.sh` clean for the script and helpers.
3. GATE-5-MEASUREMENT.md quotes both criteria and marks criterion 2 PENDING with procedure.
4. Nothing ticked in any tracker.
5. `git status` shows no graph.json/graph.html staged.
</verification>

<success_criteria>
- One command produces reachability evidence; one document turns a human with two machines
  into a completed CLOUD-02 gate.
- The fallback provider is documented to rule-10 standard without a line of code.
</success_criteria>

<output>
Create `.planning/phases/05-cloud-exposure-and-tunneling/05-03-SUMMARY.md` stating what is
measured, what is pending on the human, and the exact commands — the phase's RESUME.md
equivalent for whoever runs the remote round.
</output>
