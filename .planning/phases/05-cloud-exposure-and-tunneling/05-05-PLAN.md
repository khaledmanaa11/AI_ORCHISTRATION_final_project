---
phase: 05-cloud-exposure-and-tunneling
plan: "05"
type: execute
wave: 2
depends_on: ["05-04"]
files_modified:
  - src/pursuit/network/game_identity.py
  - src/pursuit/network/agent_wiring.py
  - src/pursuit/network/agent_context.py
  - src/pursuit/network/agent_lifecycle.py
  - src/pursuit/network/agent_entrypoint.py
  - src/pursuit/network/agent_audit_wiring.py
  - src/pursuit/security/audit.py
  - tests/unit/test_game_identity.py
  - tests/unit/test_agent_entrypoint.py
  - tests/unit/test_audit_state_binding.py
  - tests/integration/test_step0_and_audit.py
autonomous: true
gap_closure: true
requirements:
  - CLOUD-02
  - SEC-05
  - SEC-08

must_haves:
  truths:
    - "D-61's negotiated game id governs the whole game, not just a declaration filename: after a handshake agrees, ctx.game_uid, the event log stem, the D-64 ledger stem (derived from log_path.stem), the declaration filenames and the committed state.game_id all carry ONE id. The 2026-08-13 round has 074fc2b16888899e on machine A and d50ceb00be724b93 on machine B for the same match -- four artifacts that cannot be joined"
    - "The adoption happens BEFORE run_turn_loop opens the ledger. Verified safe against the retained evidence: at that instant only the benign symmetric-handshake illegal_transition record exists in the log and the ledger file does not exist yet (CommitLedger.append first runs inside the turn loop, via turn_commit_ledger.commit_own_action)"
    - "The construction-time wiring survives the rename: make_transition_reporter and make_freeze_handler are bound before the AgentContext exists (design note 12) and must not keep writing to the pre-negotiation path afterwards -- they read a mutable GameIdentity, never a frozen string"
    - "D-60's state record stops being write-only. _audit_one validates the peer's committed state.role against the opponent's role and state.turn against the entry's own turn -- the anti-replay step-binding PRD_commit_reveal.md:121-123 says the record exists for. A probe against the shipped code reported a forged {game_id: OTHER-GAME, turn: 99, role: police} record as matched"
    - "state.game_id is validated ONLY when the handshake genuinely negotiated one from the peer. A league opponent that never publishes a game_id in its handshake payload is still matched -- validating unconditionally would turn every such honest round into a mutual technical loss (rules 16/22, the exact trap 06-03's Step-0 digest-equality check already fell into once)"
    - "This validation lands in the SAME plan as, and never before, the adoption (05-UAT.md G2, stated as correctness-critical): shipped alone it makes every honest remote round a mutual TECHNICAL_LOSS, because the two sides commit different game_ids today"
    - "Phase 6 does not regress: scripts/measure_gate6.py re-runs with all three book Sec10.4 criteria still PASS"
  artifacts:
    - path: src/pursuit/network/game_identity.py
      provides: "GameIdentity (the mutable game_uid/log_path binding), negotiated_game_id (the ONE D-61 policy definition), adopt_negotiated_game_id, and the two relocated JSONL sink closures"
      contains: "GameIdentity"
    - path: src/pursuit/security/audit.py
      provides: "_audit_one validating the committed state record's role/turn, and game_id when one was negotiated"
      contains: "expected_role"
    - path: tests/unit/test_audit_state_binding.py
      provides: "The replay-forgery cases plus the paired fairness controls (honest peer, and a peer that published no game_id)"
      contains: "still_matched"
  key_links:
    - from: src/pursuit/network/agent_entrypoint.py
      to: src/pursuit/network/game_identity.py
      via: "run_agent adopts the negotiated id after result.agreed and before run_turn_loop"
      pattern: "adopt_negotiated_game_id"
    - from: src/pursuit/network/game_identity.py
      to: src/pursuit/network/turn_commit_ledger.py
      via: "renaming ctx.log_path moves the D-64 ledger stem with it (ledger_path derives from log_path.stem)"
      pattern: "log_path"
    - from: src/pursuit/network/agent_audit_wiring.py
      to: src/pursuit/security/audit.py
      via: "run_final_audit supplies the expected role, and the negotiated game id only when there was one"
      pattern: "expected_game_id"
---

<objective>
Close G2: make the handshake-negotiated game id the id the whole game runs under, and make
the committed state record something the audit actually reads.

Purpose: criterion 2 needs four joinable artifacts across two machines; today it gets two
unrelated pairs. The audit half is the anti-replay binding the book asks for.
Output: one id per match, and a state record that is no longer decorative.
</objective>

<execution_context>
@C:\Users\Hp\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Hp\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/05-cloud-exposure-and-tunneling/05-UAT.md
@.planning/phases/05-cloud-exposure-and-tunneling/05-04-SUMMARY.md

- `docs/phases/phase-5/remote-round-2026-08-13/` — both `*.ledger.jsonl` files. Every record
  on machine B carries `state.game_id = d50ceb00be724b93`; every record on machine A carries
  `074fc2b16888899e`. Both declarations on BOTH machines are named `074fc2b16888899e`. That
  split is the gap, in one grep.
- `src/pursuit/network/agent_wiring.py` `make_handshake_responder` docstring, design note 12
  — why the sink closures cannot close over an AgentContext. The fix is a mutable value
  object passed as a plain parameter, not a deferred ctx reference.
- `06-05`'s own recorded decision (STATE.md): rejecting a disagreeing turn stamp *in-game*
  was considered and DECLINED because it risks a false accusation. The same caution applies
  here and is why `expected_game_id` is conditional — read that note before widening any
  check in this plan.
</context>

<tasks>

<task type="auto">
  <name>Task 1: GameIdentity -- a game id the wiring can follow</name>
  <files>src/pursuit/network/game_identity.py, src/pursuit/network/agent_wiring.py, src/pursuit/network/agent_context.py, src/pursuit/network/agent_lifecycle.py, tests/unit/test_game_identity.py</files>
  <action>
    New module `game_identity.py` holding:
      - `GameIdentity` — a MUTABLE dataclass with `game_uid: str` and `log_path: Path`.
      - `negotiated_game_id(role, own_uid, peer_game_id)` — the ONE definition of D-61's
        policy, moved verbatim from `agent_audit_wiring._declared_game_id`: police keeps its
        own; the thief adopts the initiator's, falling back to its own when the peer sent
        none. `agent_audit_wiring` imports it; no second copy survives (no-duplication rule
        — grep to confirm).
      - `make_transition_reporter` and `make_freeze_handler`, RELOCATED here from
        `agent_wiring.py` (which measures 148/150 and has no room) and re-exported from
        `agent_wiring.py` so `agent_lifecycle.make_transition_reporter` and every existing
        importer/test resolve unchanged. This is the same relocate-and-re-export move
        `secret_wiring.py` already made for the same reason (05-02).

    Both closures gain `identity: GameIdentity | None = None`, keyword-only. Supplied → the
    closure reads `identity.log_path` / `identity.game_uid` AT CALL TIME. Omitted (the
    default) → byte-identical to today, so every existing caller and test is unaffected.
    That default-None-preserves-today shape is this codebase's standing pattern
    (`local_scent_digest`, `shared_secret`, `expected_sender`).

    `AgentContext` gains two OPTIONAL fields, defaulted so no fixture needs an edit:
      - `identity: GameIdentity | None = None`
      - `negotiated_game_id: str | None = None` — set only when the peer genuinely supplied
        a game_id at handshake; it is what Task 3 keys its conditional check on.

    `agent_lifecycle.default_context` builds one `GameIdentity` from the game_uid/log_path it
    already computes, hands it to both closures, and puts it on the context. `build_context`'s
    signature stays unchanged (plain `game_uid` + `log_path` in, as today).
  </action>
  <verify>
    Unit tests: the reporter and the freeze handler each follow a `GameIdentity` mutated
    after construction (write, mutate, write again, assert two different files with two
    different game_uid values); and with `identity=None` both behave exactly as before.
    `uv run pytest tests/unit/test_agent_lifecycle.py tests/unit/test_agent_wiring.py tests/unit/test_game_identity.py -q` passes with the pre-existing files UNMODIFIED (they exercise the default path).
    `bash scripts/check_line_limit.sh` clean — agent_wiring.py measures 148/150 today and
    must come DOWN, not up.
  </verify>
  <done>The two JSONL sinks can follow a game id that changes once, and nothing else changed.</done>
</task>

<task type="auto">
  <name>Task 2: adopt the negotiated id before the ledger exists</name>
  <files>src/pursuit/network/game_identity.py, src/pursuit/network/agent_entrypoint.py, src/pursuit/network/agent_audit_wiring.py, tests/unit/test_agent_entrypoint.py, tests/integration/test_step0_and_audit.py</files>
  <action>
    `game_identity.adopt_negotiated_game_id(ctx, result) -> None`, called from
    `run_agent` immediately after `if not result.agreed: return None` and BEFORE
    `write_declaration` / `run_turn_loop`:
      - resolve via `negotiated_game_id(ctx.role, ctx.game_uid, result.peer_game_id)`;
      - set `ctx.negotiated_game_id = result.peer_game_id` (None when the peer sent none —
        Task 3 depends on this distinction, do not coerce it);
      - when the resolved id differs from `ctx.game_uid`: rename the existing log file with
        `Path.replace` to `<parent>/<negotiated>.jsonl`, then rebind `ctx.log_path`,
        `ctx.game_uid` AND `ctx.identity`'s two fields;
      - when it is the same (always true for police), no filesystem work at all.
    Edge cases to handle explicitly, not by accident: the source file may not exist (no
    illegal transition was logged), and source may equal target. Neither is an error.
    The D-64 ledger needs no separate handling — `turn_commit_ledger.ledger_path` derives
    from `ctx.log_path.stem`, and `commit_own_action` first runs inside the turn loop, after
    this point. State that dependency in the docstring so a later reordering breaks loudly.

    `agent_audit_wiring._declared_game_id` collapses: after adoption, `ctx.game_uid` IS the
    negotiated id for both roles, so `write_declaration` uses it directly and the private
    helper becomes a call to `negotiated_game_id` (or disappears entirely if it has no
    remaining caller — grep before deleting).

    `test_agent_entrypoint.py`: add the adoption step to `_patch_common` and to all three
    exact expected-order lists, in its correct position (after `perform_handshake`, before
    `write_declaration`). Keep the assertions EXACT lists.
  </action>
  <verify>
    A new integration case in `test_step0_and_audit.py` that hands the two sides
    DELIBERATELY DIFFERENT game_uids — today's harness gives both the same one by
    construction and its own docstring says so, which is precisely why nothing caught this —
    then asserts all four artifacts (both `<uid>.jsonl`, both `<uid>.ledger.jsonl`) share one
    stem, and that both ledgers' `payload.state.game_id` values are equal.
    Prove the rename did not lose the pre-handshake record: the negotiated-name log still
    contains the handshake `illegal_transition` line.
    `uv run pytest tests/ -q` green; `uv run python scripts/measure_gate6.py` all three PASS.
  </verify>
  <done>One match, one id, across both machines' logs, ledgers and declarations.</done>
</task>

<task type="auto">
  <name>Task 3: the audit reads the committed state record</name>
  <files>src/pursuit/security/audit.py, src/pursuit/network/agent_audit_wiring.py, tests/unit/test_audit_state_binding.py</files>
  <action>
    GATE: this task may only be written after Tasks 1-2 are green. Shipped alone it turns
    every honest remote round into a mutual TECHNICAL_LOSS, because both sides commit
    different game_ids until Task 2 lands.

    `audit.audit_peer_records(..., *, expected_game_id: str | None = None,
    expected_role: str | None = None)` — both default None (check skipped), so every
    existing test and fixture is untouched. `_audit_one` gains, placed AFTER the
    re-hash check and BEFORE the revealed-vs-played comparison, reading
    `entry["payload"]["state"]` through `StateRecordKey`, never bare strings:
      - `state.turn == entry["turn"]` — always enforced. Holds for every honest ledger by
        construction (`commit_own_action` passes one `turn` to both the record and the
        append). Closes the replay-across-turns variant.
      - `state.role == expected_role` — enforced whenever supplied. Closes replaying the
        OPPONENT's own past commits back at them, which every side's FINAL_REVEAL makes
        available.
      - `state.game_id == expected_game_id` — enforced ONLY when supplied. See below.
    A missing or malformed `state` key is a mismatch with a named detail, never a KeyError:
    `_audit_one` runs against peer-supplied data.

    `run_final_audit` supplies them per direction — `expected_role=opponent_role(ctx.role)`
    for `peer_audit`, `expected_role=ctx.role` for `self_audit` (symmetric honesty, the
    CONTEXT-locked property), and `expected_game_id=ctx.game_uid` **only when
    `ctx.negotiated_game_id is not None`**, otherwise None.

    That conditional is a deliberate fairness decision and must be argued in the source, not
    just implemented: a league opponent is another team's codebase. One that never publishes
    `game_id` in its handshake payload is honest, and after Task 2 the thief falls back to
    its own uid — so an unconditional check would accuse it on turn 1 of every game. Role and
    turn are locally derivable and stay unconditional; they alone close the cross-role and
    cross-turn replays. game_id additionally closes cross-GAME replay, and is enforceable
    exactly when the peer took part in negotiating one.
  </action>
  <verify>
    `tests/unit/test_audit_state_binding.py`, mirroring `test_audit_turn_binding.py`'s
    construction (hand-built ledger entries, honest `commit_pack` hashes so the re-hash check
    passes and the NEW check is provably what fires):
      - a record whose `state.game_id` names another game -> mismatch;
      - a record whose `state.role` is our own role replayed back at us -> mismatch;
      - a record whose `state.turn` disagrees with `entry["turn"]` -> mismatch;
      - CONTROL — an honest peer with agreeing state, role and game_id -> all matched;
      - CONTROL — an honest peer audited with `expected_game_id=None` (the peer published no
        game_id) -> all matched.
    Non-vacuity: confirm each of the first three FAILS against pre-Task-3 code and that both
    controls pass in both directions. Record it in the SUMMARY.
    `uv run python scripts/measure_gate6.py` -> all three criteria PASS.
  </verify>
  <done>The state record has production readers; the forged-record probe from 05-UAT.md no longer reports matched; no honest peer is accused.</done>
</task>

</tasks>

<verification>
1. `uv run ruff check .` -> 0 violations.
2. `uv run pytest tests/ --cov` -> all pass, coverage >= 85%. Against 05-04's closing count,
   expect **+8 to +12 tests**; coverage flat-to-up. A drop in the passing count is a
   regression to explain.
3. `bash scripts/check_line_limit.sh` -> exit 0. Watch `agent_wiring.py` (148/150 before this
   plan — must come down), `security/audit.py` (91), `agent_audit_wiring.py` (102 before
   05-04). Split a sibling if any lands over; never compress.
4. `uv run python scripts/check_no_llm_in_strategy.py` -> OK.
5. `uv run python scripts/measure_gate6.py` -> **all three book Sec10.4 criteria PASS.**
6. `grep -rn "_declared_game_id" src/` -> no orphan second definition of the D-61 policy.
7. A real loopback game (`uv run python scripts/dev_launch.py`) produces, per side, a log and
   a ledger sharing ONE stem, and the two sides' stems are equal.
</verification>

<success_criteria>
- Two machines' four artifacts join on one negotiated id.
- The committed state record is validated on both audit directions.
- No honest peer — including one that publishes no game_id — is ever accused by these checks.
</success_criteria>

<output>
Create `.planning/phases/05-cloud-exposure-and-tunneling/05-05-SUMMARY.md`. Record the
measured GATE-6 re-run verdict, the non-vacuity probe results for Task 3, and the exact
adoption call-site ordering inside `run_agent` (the next plan touching that function needs it).
</output>
