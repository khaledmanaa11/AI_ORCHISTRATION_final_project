---
phase: 05-cloud-exposure-and-tunneling
plan: "02"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/pursuit/network/secret_guard.py
  - src/pursuit/network/peer_runtime.py
  - src/pursuit/network/agent_wiring.py
  - .env-example
  - tests/unit/test_secret_guard.py
  - tests/unit/test_peer_runtime.py
  - tests/integration/test_secret_channel.py
autonomous: true
requirements:
  - CLOUD-01

must_haves:
  truths:
    - "D-56: the check lives at the ASGI boundary - one SharedSecretMiddleware passed via run_async(middleware=[...]) - so a request missing the header is rejected before FastMCP's session or tool machinery ever sees it. It is NEVER implemented inside individual @mcp.tool handlers: five copies drift, and malformed non-tool requests would still reach the server"
    - "D-56: the client side constructs StreamableHttpTransport(url, headers={...}) explicitly. A bare Client(url) string infers a transport with NO headers (verified in fastmcp 3.4.5 source) - the regression where the secret silently stops being sent is called out in a comment at the construction site and guarded by a test that inspects the transport's headers"
    - "The secret VALUE comes from PURSUIT_TUNNEL_SECRET (os.environ.get) on both sides; the header NAME comes from tunnel.json (05-01's block). No secret in source, config, or logs - the middleware's rejection log line records the remote address and the missing/mismatched fact, never the expected value"
    - "The comparison uses secrets.compare_digest, the same discipline the scent-digest handshake already established - a timing oracle on the interim token would be a silly gift"
    - "ngrok-skip-browser-warning: true rides in the same client headers dict - zero cost, defends against the free-tier interstitial without relying on Accept-header heuristics (RESEARCH pitfall 4)"
    - "D-57 guard: host_origin_protection stays OFF. A test or assertion pins that PeerRuntime does not enable it - in mode auto it would 421-reject every tunneled request because the peers bind loopback and the Host header arrives as the ngrok hostname (RESEARCH pitfall 2). The reason lives in a comment at the run_async call"
    - "When no secret is configured (env var unset), the middleware is NOT installed and the client sends no secret header - loopback dev and every existing test run exactly as before. Secret-on is opt-in per environment, not a hardcoded league switch"
    - "Phase-6 layering is stated, not implied: this header is transport hygiene that commit-reveal will sit on top of, and nothing in this plan touches the handshake or envelope shapes"
  artifacts:
    - path: src/pursuit/network/secret_guard.py
      provides: "SharedSecretMiddleware (pure ASGI callable) rejecting with 403 before MCP dispatch"
      contains: "compare_digest"
    - path: src/pursuit/network/peer_runtime.py
      provides: "run_async gains middleware=[...] when a secret is configured; client() builds the explicit transport with headers"
      contains: "StreamableHttpTransport"
  key_links:
    - from: src/pursuit/network/peer_runtime.py
      to: src/pursuit/network/secret_guard.py
      via: "the middleware is attached in the same run_async call that already passes sockets (D-56)"
      pattern: "SharedSecretMiddleware"
    - from: src/pursuit/network/peer_runtime.py
      to: src/pursuit/shared/tunnel_config.py
      via: "the header name is read from 05-01's tunnel block, one owner (D-55/D-56)"
      pattern: "tunnel"
---

<objective>
Put one lock on the public door, checked before anything else runs, and make the client
carry the key on every call.

CLOUD-01's second half: the exposed MCP tools are no longer an open scanner target, and the
protection is invisible to every loopback test.
</objective>

<execution_context>
@C:\Users\Hp\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Hp\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/05-cloud-exposure-and-tunneling/05-PLAN-OUTLINE.md
@.planning/phases/05-cloud-exposure-and-tunneling/05-RESEARCH.md
@.planning/phases/05-cloud-exposure-and-tunneling/05-01-SUMMARY.md

- `05-RESEARCH.md` §Architecture Patterns — both seams with source-verified snippets
  (middleware list on `run_async`; explicit transport construction in `client()`), and
  §Anti-Patterns for what not to do.
- `src/pursuit/network/peer_runtime.py` — `_run_http` and `client()`, the only two lines
  that change. Read the `handshake_handler` injection style; the secret follows it.
- `src/pursuit/network/config_hash.py` — the existing `secrets.compare_digest` precedent
  (`digests_match`).
- `.env-example` — gains `PURSUIT_TUNNEL_SECRET` and `PURSUIT_NGROK_DOMAIN` with dummy
  values (rule: committed with dummies, never real ones).
</context>

<task type="auto">
  <name>Task 1: the middleware</name>
  <files>src/pursuit/network/secret_guard.py, tests/unit/test_secret_guard.py</files>
  <action>
    A pure ASGI callable class: non-http scopes pass through; http scopes without the header,
    or with a value failing `secrets.compare_digest`, get a plain 403 and never reach the
    wrapped app. Constructor takes the header name and expected value. Log the rejection at
    warning with the peer address — never the expected value.
  </action>
  <verify>
    Driven via Starlette TestClient / httpx.ASGITransport with a trivial inner app: missing
    header → 403, wrong value → 403, correct value → inner app reached, non-http scope
    passes through. Log output carries no secret.
  </verify>
  <done>The lock exists and can be tested with no server at all.</done>
</task>

<task type="auto">
  <name>Task 2: wire both sides of PeerRuntime</name>
  <files>src/pursuit/network/peer_runtime.py, src/pursuit/network/agent_wiring.py, .env-example</files>
  <action>
    Server: `_run_http` passes `middleware=[Middleware(SharedSecretMiddleware, ...)]` in the
    same `run_async` call that passes `sockets` — only when a secret is configured. The
    D-57 comment (host_origin_protection must stay off, or gain allowed_hosts) sits at this
    call.

    Client: `client()` builds `StreamableHttpTransport(opponent_url, headers=...)` with the
    secret header (when configured) plus `ngrok-skip-browser-warning: true` always, and
    hands the transport to `Client(...)` with the unchanged timeout. The
    bare-string-drops-headers pitfall is a comment at this exact line.

    `agent_wiring` threads the tunnel params through, matching the handshake_handler
    injection style. `.env-example` gains the two new vars with dummy values.
  </action>
  <verify>
    Unit: with a secret configured, the built client transport's headers contain both
    entries (inspect the transport object, no network); without, no secret header and no
    middleware installed. Existing peer_runtime tests pass with secret-off defaults.
  </verify>
  <done>Both halves of the channel exist in the two places they belong, off by default.</done>
</task>

<task type="auto">
  <name>Task 3: prove it end-to-end in-process</name>
  <files>tests/integration/test_secret_channel.py</files>
  <action>
    Two loopback PeerRuntimes with the middleware active: a client built with the correct
    secret completes a handshake; a raw request without the header gets 403 and the server
    logs the rejection; a client with a wrong secret fails every call. No ngrok, no
    internet — this is the shape 05-03's smoke script repeats through the real tunnel.
  </action>
  <verify>
    All three cases pass deterministically offline. The 403 path shows the request never
    reached MCP routing (no session created, no tool invoked).
  </verify>
  <done>The channel is proven where CI can see it; only the tunnel itself remains.</done>
</task>

<verification>
1. `uv run ruff check .` → 0; `uv run pytest tests/ --cov` → ≥ 85%, 0 failures.
2. `bash scripts/check_line_limit.sh` clean; `uv run python scripts/check_no_llm_in_strategy.py` clean.
3. `grep -rn "PURSUIT_TUNNEL_SECRET" src/ | grep -v environ` finds no literal value use.
4. Existing integration tests pass unmodified with secret-off defaults.
5. No change to envelope shapes, handshake payloads, or tools.py signatures.
</verification>

<success_criteria>
- An unauthenticated request through the public URL dies at the boundary with 403; a peer
  holding the exchanged secret plays normally.
- Loopback dev and CI never notice the feature exists.
</success_criteria>

<output>
Create `.planning/phases/05-cloud-exposure-and-tunneling/05-02-SUMMARY.md` with the header
name, the env vars, and the middleware/transport seams as built — 05-03's smoke script and
runbook quote them.
</output>
