---
phase: 05-cloud-exposure-and-tunneling
plan: "16"
type: execute
wave: 8
depends_on: ["05-13"]
files_modified:
  - src/pursuit/network/turn_buffer.py
  - src/pursuit/network/turn_commit_wait.py
  - tests/unit/test_turn_loop_watchdog.py
  - .planning/phases/05-cloud-exposure-and-tunneling/deferred-items.md
autonomous: true
gap_closure: true
requirements: [NET-06, NET-07, CLOUD-02]

must_haves:
  truths:
    - "The TURN LOOP survives a bounded retry ladder against a stalled peer. Measured at HEAD: `next_protocol_message` (`turn_commit_wait.py:59`) touches the watchdog only at `:92`, AFTER `call_with_retry` returns, and its `on_attempt` hook defaults to `None`, which every turn-loop `wait_for_*` leg takes. So the whole ladder -- `response_timeout` x (`retry_count`+1) plus backoffs = 135 s at the shipped Table-19 values -- runs UNMARKED against `watchdog_threshold` = 60 s. `os._exit(1)` fires at t=60 s DURING PLAY, and we are the side with no verdict and no published nonces (rule 36 against US) while the peer records `opponent_unresponsive`. This is 05-13's deferred item #10, and it is the same defect 05-13 closed for the audit path arriving through the turn loop's door."
    - "This is closed by the mechanism 05-13 BUILT AND PROVED, not by a new one: the four turn-loop legs pass `on_attempt=ctx.watchdog.touch`, exactly as `agent_audit_exchange.receive_final_reveal` already does at `:103`. Each attempt is itself bounded by `response_timeout`, so the widest touch gap becomes `response_timeout + backoff_seconds` = 35 s < 60 s."
    - "NET-07 freeze detection is PRESERVED, not traded away. The touch marks a real BOUNDED attempt STARTING -- it is never a heartbeat on a dead loop, because a wedged event loop stops producing attempts and is still killed. A test must PROVE this, not assert it in prose: 05-13's own self-audit found its NET-07 control passed against exactly the wrong fix it existed to refute, because the fake never consulted the stop flag."
    - "IT IS NOT CLOSED BY WIDENING `watchdog_threshold`. 05-13 wrote that instruction down when it deferred this item, and Table 19 values are config, not ours to move (CLAUDE.md rule 1). A wrong-fix revert probe must refute both the widened threshold and a blanket `watchdog.stop()` across the turn loop."
    - "Zero real sleeps. The clock is injected through 05-13's `tests/unit/_fakes_watchdog.py` harness (real `Watchdog`, injected clock/`on_freeze`/`exit_action`, `check_once()` driven by hand). A test that actually waits 135 s is not acceptable."
  artifacts:
    - path: src/pursuit/network/turn_commit_wait.py
      provides: "the four turn-loop wait legs mark each bounded attempt"
      contains: "on_attempt=ctx.watchdog.touch at every wait_for_* leg; the :92 post-ladder touch retained"
    - path: tests/unit/test_turn_loop_watchdog.py
      provides: "the ladder-outlives-threshold case and its NET-07 counter-control"
      contains: "injected clock; a stalled peer still reaches a verdict; a genuinely frozen loop is still killed"
  key_links:
    - from: src/pursuit/network/turn_commit_wait.py
      to: src/pursuit/network/agent_audit_exchange.py
      via: "the same on_attempt hook 05-13 introduced, now used by the callers it deliberately left at the default"
      pattern: "on_attempt"
---

<objective>
Close deferred item #10: the turn loop carries the identical 135 s-vs-60 s watchdog exposure
that 05-13 closed for the audit path.

Purpose: 05-13 fixed the teardown door and measured, but deliberately did not fix, the same
hole in the main game loop -- where it kills us mid-game rather than at teardown.
Output: a stalled peer costs us a bounded ladder and an honest verdict, not a process death.
</objective>

<context>
- `.planning/phases/05-cloud-exposure-and-tunneling/05-13-SUMMARY.md` -- the mechanism, its
  four revert probes, and deferred #10 as logged there.
- `tests/unit/_fakes_watchdog.py` -- the harness this plan reuses. 05-13's self-audit finding
  about `check_once()` and the stop flag applies here verbatim; do not reintroduce it.
- Table 19 values live in config; `docs/PARAMETERS.md` is the only source for them.
</context>

<tasks>

<task type="auto">
  <name>Task 1: measure the exposure before changing anything</name>
  <files>tests/unit/test_turn_loop_watchdog.py</files>
  <action>
    Drive a turn-loop wait against a peer whose socket accepts but never answers, with the
    injected clock, and record: attempts, injected elapsed, which `check_once()` calls return
    frozen, and whether a verdict is reached. This is the RED half -- it must show the process
    would be killed before the ladder completes. Record the numbers verbatim.
  </action>
  <verify>The before-numbers show `fired=[True,...]` and no verdict, mirroring 05-13's own SEND probe table.</verify>
  <done>The hole is measured in this repository's own numbers, not inherited from 05-13's prose.</done>
</task>

<task type="auto">
  <name>Task 2: mark each bounded attempt on the four turn-loop legs</name>
  <files>src/pursuit/network/turn_commit_wait.py, src/pursuit/network/turn_buffer.py</files>
  <action>
    Pass `on_attempt=ctx.watchdog.touch` at the turn-loop `wait_for_*` legs. Keep the existing
    post-ladder touch at `:92`. Change no numeric value anywhere. Update the comment block at
    `:68-77`, which currently states as a fact that only `receive_final_reveal` passes a hook.
  </action>
  <verify>`bash scripts/check_line_limit.sh` exit 0; split rather than compress if a file crosses.</verify>
  <done>The widest unmarked window in the turn loop is one bounded attempt plus its backoff.</done>
</task>

<task type="auto">
  <name>Task 3: prove it, and prove the wrong fixes fail</name>
  <files>tests/unit/test_turn_loop_watchdog.py</files>
  <action>
    The GREEN half plus counter-controls: (a) the stalled-peer ladder now reaches a verdict with
    zero `watchdog_incident`; (b) NET-07 still fires for a genuinely wedged loop that stops
    producing attempts; (c) revert probes -- remove the hook, widen `watchdog_threshold`, and
    blanket-`stop()` the watchdog across the turn loop -- each recorded with real failure counts.
    Grep production callers for anything new. No `parametrize` without an anti-vacuity guard
    (pytest SKIPS an empty set silently -- 05-12's trap).
  </action>
  <verify>Every probe fails as recorded; the NET-07 control fails against the blanket-stop probe.</verify>
  <done>The fix is pinned and the two tempting wrong fixes are refuted by name.</done>
</task>

</tasks>

<verification>
1. `uv run ruff check .` -> 0 violations.
2. `uv run pytest tests/ --cov` -> all pass, coverage >= 85% and not below the inherited baseline.
3. `bash scripts/check_line_limit.sh` -> exit 0.
4. `uv run python scripts/check_no_llm_in_strategy.py` -> OK.
5. `uv run python scripts/dev_launch.py` -> exit 0, one shared uid, both sides `audit_verdict
   matched=true`, zero `technical_win`, zero `watchdog_incident`.
</verification>

<success_criteria>
- A turn-loop ladder outliving `watchdog_threshold` reaches an honest verdict instead of `os._exit`.
- NET-07 still kills a genuinely frozen agent, proven by a test that fails against the blanket-stop.
- No Table-19 value moved; deferred #10 marked closed with the measurement.
</success_criteria>

<non_goals>
Deferred #13 (the toggle-off MOVE envelope stamped 1..16 for turns 0..15) is NOT in scope. Its
repair changes `turn_commit.initiate`'s signature, and on the commit-reveal-ON path that `turn`
feeds `commit_own_action`'s D-59 hash input and the D-64 ledger join key -- rules 19/22
territory. It stays deferred WITH 05-14's measurement recorded, not silently.
</non_goals>

<output>
Create `.planning/phases/05-cloud-exposure-and-tunneling/05-16-SUMMARY.md` with the before/after
numbers, the three revert probes, and deferred #10 marked closed.
</output>
