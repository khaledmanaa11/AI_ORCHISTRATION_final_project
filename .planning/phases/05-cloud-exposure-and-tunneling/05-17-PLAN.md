---
phase: 05-cloud-exposure-and-tunneling
plan: "17"
type: execute
wave: 9
depends_on: ["05-13", "05-15", "05-16"]
files_modified:
  - src/pursuit/network/turn_commit_wait.py
  - src/pursuit/network/turn_buffer.py
  - tests/unit/test_early_final_reveal.py
  - .planning/phases/05-cloud-exposure-and-tunneling/deferred-items.md
autonomous: true
gap_closure: true
requirements: [NET-06, NET-07, CLOUD-02, SEC-05]

must_haves:
  truths:
    - "A peer's FINAL_REVEAL that arrives EARLY is never discarded. Measured at HEAD: the four turn-loop `wait_for_*` legs drop any unexpected envelope type as tolerated jitter -- `turn_commit_wait.py:176` says so in its own words ('a duplicate/unexpected arrival here is tolerated jitter -- dropped'), and the same shape sits at `:119-136` and `:148-155`. HINTs are exempt because they are explicitly buffered via `turn_buffer.record_hint`; FINAL_REVEAL has no such exemption and is consumed off the queue and thrown away."
    - "THE CONSEQUENCE IS A FALSE ACCUSATION, which is disqualification territory (rules 16/22). It is reachable whenever one side exits its turn loop early and pushes immediately -- the honest, ordinary case of a peer that finished first. We then burn our whole receive ladder (up to 135 s at shipped Table-19 values), and because our OWN push succeeded we take the accusatory branch at `agent_audit_wiring.py:97` and declare `record_technical_loss` / OPPONENT_UNRESPONSIVE against a peer that DEMONSTRABLY DID PUSH. It also costs us the 0-point technical loss."
    - "The premise of the existing comment is what breaks, and the fix must restore it rather than delete it. `agent_audit_wiring.py:90-93` reasons that 'a push that LANDED proves the peer's server accepted our envelope, so silence afterwards is the peer's own act'. That is sound ONLY if the silence is theirs. When WE consumed their FINAL_REVEAL off our own queue and dropped it, the silence is ours, and the inference is false. Do not weaken the accusation rule -- stop manufacturing the silence."
    - "05-15 hardened the MIRROR of this boundary and this side was left open. `receive_final_reveal` now checks WHICH envelope it got instead of reading records off whatever arrived first. The same envelope arriving one layer earlier -- in the turn loop rather than in the audit -- is still dropped. Closing it makes the pair symmetric, which is the actual invariant: a FINAL_REVEAL is never lost, wherever it lands."
    - "The mechanism ALREADY EXISTS in the same function and is reused, not reinvented: an interleaved HINT is buffered by `turn_buffer.record_hint` and collected later. An early FINAL_REVEAL is buffered the same way and the audit's receive leg checks that buffer BEFORE waiting on the queue, so a reveal that arrived during the turn loop short-circuits the ladder entirely instead of timing out behind it."
    - "Zero new numeric values, and no Table-19 value moved (CLAUDE.md rule 1). This is a routing fix, not a timing fix -- if the answer looks like a timeout change, it is the wrong answer."
  artifacts:
    - path: src/pursuit/network/turn_commit_wait.py
      provides: "an early FINAL_REVEAL is buffered, not dropped"
      contains: "the tolerated-jitter branch exempts FINAL_REVEAL the way it already exempts HINT"
    - path: tests/unit/test_early_final_reveal.py
      provides: "the false-accusation path, pinned"
      contains: "a peer that pushes early is MATCHED, never OPPONENT_UNRESPONSIVE; injected clock, zero real sleeps"
  key_links:
    - from: src/pursuit/network/turn_commit_wait.py
      to: src/pursuit/network/agent_audit_wiring.py
      via: "the buffered reveal reaches the audit's receive leg instead of being re-awaited"
      pattern: "FINAL_REVEAL"
---

<objective>
Stop dropping a peer's early FINAL_REVEAL, and with it stop declaring an honest peer
unresponsive.

Purpose: this is a rules-16/22 false-declaration path reachable by an honest opponent doing
nothing wrong -- the same defect class as 05-04, 05-09, 05-10 and 05-15, arriving one layer
earlier than any of them looked.
Output: a peer that finishes first and pushes immediately is MATCHED, not accused.
</objective>

<context>
Found during the 05-16/deferred-#4 diagnosis, by the angle asking whether a test race had a
production twin. It did, and this is a different and more serious one than the race that was
being investigated.

- `.planning/phases/05-cloud-exposure-and-tunneling/05-15-SUMMARY.md` -- the mirror fix inside
  `receive_final_reveal`, and the shape to match.
- `turn_buffer.record_hint` -- the buffering precedent to reuse.
- Table 19 values live in config; `docs/PARAMETERS.md` is the only source for them.
</context>

<tasks>

<task type="auto">
  <name>Task 1: reproduce the false accusation</name>
  <files>tests/unit/test_early_final_reveal.py</files>
  <action>
    RED first. Drive a peer that exits its turn loop early and pushes FINAL_REVEAL while we are
    still in a `wait_for_*` leg. Record what our side does: that the envelope is consumed and
    dropped, that our receive ladder then runs to exhaustion, and that we emit
    `record_technical_loss` / OPPONENT_UNRESPONSIVE against a peer whose push landed. Injected
    clock, zero real sleeps. Record the verdict and the log records verbatim.
  </action>
  <verify>The test fails at HEAD for exactly the stated reason, and the recorded verdict names the honest peer.</verify>
  <done>The false accusation is reproduced in this repository's own numbers.</done>
</task>

<task type="auto">
  <name>Task 2: buffer the early reveal instead of dropping it</name>
  <files>src/pursuit/network/turn_commit_wait.py, src/pursuit/network/turn_buffer.py</files>
  <action>
    Exempt FINAL_REVEAL from the tolerated-jitter drop, buffering it exactly as an interleaved
    HINT is already buffered, and have the audit's receive leg consult that buffer before waiting
    on the queue. Change no numeric value. Correct the three docstrings that currently assert an
    unexpected arrival is safely dropped -- they become false the moment this lands.
  </action>
  <verify>`bash scripts/check_line_limit.sh` exit 0; split rather than compress if a file crosses.</verify>
  <done>A FINAL_REVEAL is never lost, whichever layer it arrives in.</done>
</task>

<task type="auto">
  <name>Task 3: prove it, and keep rule 36 intact</name>
  <files>tests/unit/test_early_final_reveal.py</files>
  <action>
    GREEN plus the controls that matter:
    (a) the early-pushing peer is MATCHED with zero `technical_win`/`technical_loss`;
    (b) **a genuinely silent peer is STILL accused** -- rule 36's sanction must survive intact;
        this is the counter-control, and a fix that spares the silent peer has broken the game;
    (c) a duplicate FINAL_REVEAL does not double-count or re-drive the audit;
    (d) revert probes with real failure counts, including the wrong fix of widening the receive
        ladder instead of routing the envelope.
    Grep production callers for anything new. Anti-vacuity guard on any `parametrize` set.
  </action>
  <verify>Each probe fails as recorded; the silent-peer control fails against a fix that stops accusing unconditionally.</verify>
  <done>Honest peers are matched, silent peers still lose, and both are pinned.</done>
</task>

</tasks>

<verification>
1. `uv run ruff check .` -> 0 violations.
2. `uv run pytest tests/ --cov` -> all pass, coverage >= 85% and not below the inherited baseline.
3. `bash scripts/check_line_limit.sh` -> exit 0.
4. `uv run python scripts/check_no_llm_in_strategy.py` -> OK.
5. `uv run python scripts/dev_launch.py` -> exit 0, one shared uid, both sides `audit_verdict
   matched=true`, zero `technical_win`, zero `watchdog_incident`.
</verification>

<success_criteria>
- A peer that pushes its FINAL_REVEAL early is MATCHED, never OPPONENT_UNRESPONSIVE.
- A genuinely silent peer is still accused -- rule 36 unchanged.
- No Table-19 value moved; the fix is routing, not timing.
</success_criteria>

<non_goals>
Two narrower findings from the same diagnosis stay OPEN and are recorded, not fixed here:
(a) `agent_teardown.py:22-25` justifies the linger's quiet interval as "if the peer were going to
    retry, it would have retried inside one backoff" -- but the peer schedules its retry one
    backoff from its own FAILURE, strictly later than our last ARRIVAL. Narrow, but unsound as
    written; it is a comment/interval-derivation question, not this plan's routing bug.
(b) The linger drains via `queue.get()`, so a peer FINAL_REVEAL arriving DURING the linger is
    consumed and discarded unaudited. Documented as intentional; it deserves its own decision
    rather than being folded in here.
</non_goals>

<output>
Create `.planning/phases/05-cloud-exposure-and-tunneling/05-17-SUMMARY.md` with the reproduced
accusation, the probes, and (a)/(b) filed as deferred items with their measurements.
</output>
