---
phase: 07-reporting-and-visualization-shell
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - src/pursuit/network/agent_step0_wiring.py
  - src/pursuit/security/step0_collect.py
  - tests/unit/test_games_played_counter.py
  - tests/conftest.py
  - docs/phases/phase-6/GATE-6-MEASUREMENT.md
  - docs/phases/phase-7/GAMES-PLAYED-RECONSTRUCTION.md
autonomous: true
gap_closure: true
requirements: [REPORT-01, SEC-07, DOC-01]

must_haves:
  truths:
    - "THE COUNTER WE ARE ABOUT TO PUBLISH IS FALSE, AND PUBLISHING IT IS AN ABSOLUTE DISQUALIFICATION. Rule 38 (`docs/RULES.md:79`) -- 'Falsely declare the number of games played -> Absolute disqualification for an ethical and integrity breach'. Measured 2026-08-17: `config/police/games_played.json` = **1881**, `config/thief/` = **1874**, and ONE `uv run pytest tests/` run advances each by exactly **14** (1881->1895, 1874->1888, measured before/after). ~1881 is therefore on the order of 134 test runs of phantom games plus dev launches and gate measurements. The seven-count divergence between two agents that only ever played each other is independent proof the number does not count games."
    - "DEFECT 1 -- WRONG MOMENT, and the code says so itself. `step0_collect.record_game_played`'s docstring reads 'Rule 37/38: increment by exactly one, durably, **at game end only**'. It is called at `agent_step0_wiring.py:93`, inside the Step-0 DECLARATION path, which runs at game START. A handshake that aborts, a process killed by the watchdog, a game that never reaches an outcome -- all counted. The fix moves the increment to a genuine game end, once per completed game, matching the contract already written."
    - "DEFECT 2 -- WRONG SCOPE. The path is `cfg.config_dir / 'games_played.json'` and the test suite runs against the REAL `config/police/`, so every `pytest` run mutates production state. Tests must not be able to touch the shipped counter. Fix it at the seam, not by asking tests to be careful."
    - "A DOCUMENT CURRENTLY CERTIFIES THE BUG AS CORRECT, and that is what let it survive. `docs/phases/phase-6/GATE-6-MEASUREMENT.md:180-183` states the test-run inflation is 'the shipped, correct behavior of the counter, not a bug this script introduces'. That claim is FALSE and is corrected here, in place, with a dated note -- attempts and measurements are append-only in this project, but a statement of fact that is wrong gets corrected rather than left to mislead the next reader (rule 38 cuts both ways)."
    - "THE FINAL NUMBER IS NOT CLAUDE'S TO CHOOSE, AND INVENTING ONE WOULD ITSELF BE THE VIOLATION. What counts as a game played -- league games only, any real two-machine round, or local self-play too -- is a judgement about how the user is represented to the league, and being wrong in EITHER direction is a rule-38 problem. This plan therefore fixes the MECHANISM and produces an evidence-based RECONSTRUCTION with its derivation, and stops. The value is set by the human at the 07-10 checkpoint, before any live send. The counter file is gitignored, so git history cannot settle it."
    - "NOTHING THAT SENDS A DECLARATION SHIPS UNTIL THIS LANDS. This is wave 0 and everything in phase 7 that writes or transmits `declaration_<game_id>.json` depends on it. The gate is not 'the counter is a nice number' -- it is 'the counter means what the declaration says it means'."
  artifacts:
    - path: src/pursuit/security/step0_collect.py
      provides: "an increment that happens once, at game end, per completed game"
      contains: "the docstring's own 'at game end only' contract, now true"
    - path: tests/unit/test_games_played_counter.py
      provides: "the defect pinned in both directions"
      contains: "an aborted handshake increments NOTHING; a completed game increments exactly one; the suite cannot touch the shipped counter"
    - path: docs/phases/phase-7/GAMES-PLAYED-RECONSTRUCTION.md
      provides: "the evidence-based candidate and its derivation, for the human to decide from"
      contains: "what was counted, what was excluded, and why -- with the retained remote-round games enumerated"
  key_links:
    - from: src/pursuit/network/agent_step0_wiring.py
      to: docs/RULES.md
      via: "the declaration publishes games_played, and rule 38 makes a false one disqualifying"
      pattern: "games_played"
---

<objective>
Make the games-played counter mean what the declaration says it means, before Phase 7 sends it
anywhere.

Purpose: this is the only defect found so far whose sanction is ABSOLUTE DISQUALIFICATION, and
Phase 7 is the phase that puts the number in front of the lecturer.
Output: an honest mechanism, tests that cannot corrupt it, a corrected Phase-6 claim, and a
documented reconstruction for the human to set the value from.
</objective>

<context>
- `docs/RULES.md:79` -- rule 38, the sanction.
- `docs/PRD_commit_reveal.md:275-281` -- the declaration API and the counter helpers.
- `docs/phases/phase-6/GATE-6-MEASUREMENT.md:178-184` -- the claim being corrected.
- Retained real games for the reconstruction: `docs/phases/phase-5/remote-round-2026-08-13/`,
  `remote-round-2026-08-16/`, `remote-round-2026-08-16-attempt3/`,
  `remote-round-2026-08-16-attempt4/` (attempt 4 holds TWO games). Local games live in
  gitignored `logs/`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: pin the defect in both directions</name>
  <files>tests/unit/test_games_played_counter.py</files>
  <action>
    RED first, and prove BOTH halves: (a) a handshake that aborts before any outcome still
    increments the counter at HEAD; (b) the test suite mutates the shipped
    `config/police/games_played.json` at HEAD. Record the measured before/after for each.
    Zero real sleeps.
  </action>
  <verify>Both fail at HEAD for exactly the stated reasons, with numbers quoted.</verify>
  <done>The two defects are reproduced in this repository's own numbers, not argued.</done>
</task>

<task type="auto">
  <name>Task 2: increment once, at game end, and never from a test</name>
  <files>src/pursuit/network/agent_step0_wiring.py, src/pursuit/security/step0_collect.py, tests/conftest.py</files>
  <action>
    Move the increment out of the Step-0 declaration path to a genuine game end, once per
    completed game -- the contract `record_game_played`'s docstring already states. Decide and
    RECORD what "completed" means here (a recorded board outcome? an audit verdict?) by reading
    how the turn loop and `run_final_audit` actually end, and say why in the source.

    Then close the scope defect at the seam so the suite cannot reach the shipped counter.
    Prefer a mechanism that fails loudly if a test ever does, over one that quietly redirects.
    Do not ask tests to be careful; make it structural.
  </action>
  <verify>Task 1's two tests go green; `bash scripts/check_line_limit.sh` exit 0, and check any new file explicitly by path (the no-arg form enumerates via `git ls-files` and passes vacuously on untracked files).</verify>
  <done>The counter counts completed games, and only real ones.</done>
</task>

<task type="auto">
  <name>Task 3: correct the document that certified the bug, and reconstruct the evidence</name>
  <files>docs/phases/phase-6/GATE-6-MEASUREMENT.md, docs/phases/phase-7/GAMES-PLAYED-RECONSTRUCTION.md</files>
  <action>
    Correct `GATE-6-MEASUREMENT.md:178-184` in place with a dated note: the test-run inflation was
    NOT correct behaviour. Do not rewrite the surrounding measurement -- only the false claim.

    Then write `GAMES-PLAYED-RECONSTRUCTION.md`: enumerate every game with retained evidence
    (the four remote-round directories, attempt 4 holding two), state what is countable and what
    is not, and present the candidate value with its derivation and its uncertainty.

    **Do NOT set the counter, and do NOT pick the number.** Present the options and the evidence
    for each. State plainly that the value is set by the human at 07-10 before any live send, and
    that inventing one would be the rule-38 violation this plan exists to prevent.
  </action>
  <verify>No counter file is written by this task. The Phase-6 correction touches only the false claim.</verify>
  <done>The record is honest and the human has what they need to decide.</done>
</task>

</tasks>

<verification>
1. `uv run ruff check .` -> 0 violations.
2. `uv run pytest tests/ --cov` -> all pass, coverage >= 85% and not below baseline. **Then
   re-read both counter files and confirm the suite advanced them by ZERO** -- that is this
   plan's headline measurement.
3. `bash scripts/check_line_limit.sh` -> exit 0, plus every new file checked explicitly by path.
4. `uv run python scripts/check_no_llm_in_strategy.py` -> OK.
5. `uv run python scripts/dev_launch.py` -> exit 0; confirm it advances each counter by exactly
   ONE for the one game it plays, and say so with before/after numbers.
</verification>

<success_criteria>
- An aborted handshake increments nothing; a completed game increments exactly one.
- A full `pytest` run advances the shipped counters by ZERO (measured before/after).
- The false Phase-6 claim is corrected in place.
- The reconstruction exists, and the counter value is left for the human.
</success_criteria>

<non_goals>
Setting the counter value. Deciding what counts as a "game played". Both belong to the human at
07-10, and this plan's own must_haves explain why.
</non_goals>

<output>
Create `.planning/phases/07-reporting-and-visualization-shell/07-00-SUMMARY.md` with the
before/after numbers for both defects, the zero-delta suite measurement, and the reconstruction's
candidate range.
</output>
