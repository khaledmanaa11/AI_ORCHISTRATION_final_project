---
phase: 07-reporting-and-visualization-shell
plan: "05"
type: execute
wave: 2
depends_on: ["07-02"]
files_modified:
  - src/pursuit/services/reporting/artifact_log.py
  - src/pursuit/services/reporting/log_join.py
  - tests/unit/test_artifact_log.py
  - tests/integration/test_log_artifact_roundtrip.py
autonomous: true
requirements: [REPORT-06, REPORT-09]

must_haves:
  truths:
    - "JOINING ON THE PEER'S DECLARED TURN IS THE EXACT BUG 06-05 CLOSED, AND IT IS WRITTEN DOWN IN THE SOURCE. `agent_audit_observed.observed` (`agent_audit_observed.py:37-58`) keys both dicts on the RECORD's own top-level turn -- 'a number THIS side stamped ... never on the nested `envelope`'s turn, which on the received side is whatever the peer chose to claim' -- and states the measured consequence: keying on the peer's number let an adversary stamp COMMIT and REVEAL with disjoint turns, which emptied `audit.audit_peer_records`'s `set(commits) & set(reveals)` coverage intersection (re-opening the `{\"records\": []}` rule-36 evasion) and sent every entry down the trailing-commit exemption so the D-67 revealed-vs-played check never fired. **The `log_` builder joins on local turn truth, the same key, for the same reason.** The peer's claimed turn is still carried verbatim as evidence."
    - "A CRASHED GAME LEAVES A PARTIAL LAST LINE AND BOTH EXISTING READERS RAISE ON IT. Measured: `event_log.append_event` (`event_log.py:129-132`) writes one line then `flush()` + `os.fsync()`, so an interrupted process can leave a truncated tail. `agent_audit_observed._read_log` (`:31-35`) and `ledger.CommitLedger.read_all` (`ledger.py:66-67`) both do `[json.loads(line) for line in fh if line.strip()]` -- `read_all`'s docstring states the raise is deliberate fail-loud for the AUDIT path. The `log_` builder must NOT inherit that: a replay artifact that cannot be produced from a crashed game is useless precisely when it is needed. It drops the partial tail line, records `truncated_tail` explicitly in the artifact, and NEVER weakens `read_all`'s contract for the audit."
    - "THE HINT/INTENT RECORDS ARE NOT AN `EventType` MEMBER AND A TYPE-BASED FILTER SILENTLY MISSES THEM. Measured: `EventType` (`event_log.py:33-47`) has no `language_turn` and no `game_over` member; `turn_events.language_turn_record` and `game_over_record` assemble their four common fields directly and set `EventField.EVENT` to the bare strings `\"language_turn\"` / `\"game_over\"` -- both docstrings say so. A builder that filters on `EventType(record[EVENT])` raises `ValueError` on those two, and one that filters on `record[EVENT] in EventType._value2member_map_` drops them silently. Filter on the string, and pin both records' presence with a test."
    - "THE ARTIFACT MUST BE SELF-CONTAINED -- THE VIEWER NEVER OPENS THE JSONL OR THE LEDGER. Every turn carries: commitment, revealed move, intent, hint, verdict, nonce and hash (`PARAMETERS.md:167`). Prove it by constructing the artifact, DELETING both source files, and running 07-08's verification path over what remains. A test that leaves the sources on disk proves nothing about self-containment."
    - "RE-HASHING GOES THROUGH `commit_pack`, NEVER A FRESH `json.dumps`. `security/commit_pack.py:10-18` forbids a second `json.dumps(sort_keys=True, ...)` in this repository in as many words, and `verify_reveal` (`:117-123`) is the public entry that rebuilds via `build_commit_payload` and compares with `digests_match`. A second serializer reintroduces the canonical-JSON drift D-59 exists to prevent and produces FALSE `FAILED` verdicts on the one screen the grader inspects (07-08). 100% of turns must re-hash to the recorded `h_commit`."
    - "NONCE SEPARATION HOLDS DURING PLAY AND ENDS AT GAME END (D-73). `security/ledger.py:1-13` states the ledger 'is never read or written by anything on the wire path (D-64)' and that 06-02's integration test is what proves the wire log stays nonce-free. This builder runs AT GAME END, where SEC-04 makes the nonces public. It must not be callable from, or reachable by, anything inside the turn loop -- grep the production callers and say so."
    - "ZERO NEW NUMERIC VALUES. Turn indices, file names and the `<NN>` index come from 07-02; nothing here is a parameter."
  artifacts:
    - path: src/pursuit/services/reporting/log_join.py
      provides: "the JSONL x ledger join, on local turn truth, tolerant of a truncated tail"
      contains: "the peer's claimed envelope turn retained as evidence, never used as a key"
    - path: src/pursuit/services/reporting/artifact_log.py
      provides: "log_<game_id>_g<NN>.json -- one self-contained record per sub-game"
      contains: "commitment, revealed move, intent, hint, verdict, nonce and hash per turn"
    - path: tests/integration/test_log_artifact_roundtrip.py
      provides: "a two-peer recorded game -> artifact -> 100% re-hash, with both sources deleted"
      contains: "a truncated-tail fixture that yields an artifact plus an explicit marker"
  key_links:
    - from: src/pursuit/services/reporting/log_join.py
      to: src/pursuit/network/agent_audit_observed.py
      via: "the same local-turn key discipline 06-05 established, reused rather than re-derived"
      pattern: "turn"
---

<objective>
Derive one self-contained JSON per sub-game that a third party can verify offline, by joining the
wire log with the nonce ledger on local turn truth.

Purpose: rule 20 makes a verifying replay viewer a threshold condition for approving the project,
and this artifact is what it verifies.
Output: `log_<game_id>_g<NN>.json`, complete without either source file.
</objective>

<context>
- `07-PLAN-OUTLINE.md` Sec2 (D-73), Sec7 (07-05), Sec11 item 1 -- the journal already exists as
  `logs/<role>/<game_uid>.jsonl` plus a sibling `.ledger.jsonl`; this artifact is DERIVED from
  them, not a new sink.
- `src/pursuit/network/agent_audit_observed.py` -- read the whole `observed()` docstring before
  writing a line of the join. It is the specification for the join key.
- `src/pursuit/network/turn_commit_ledger.py:33-36` -- `ledger_path`, the `<log-stem>.ledger.jsonl`
  sibling convention.
- `src/pursuit/network/turn_events.py` -- every record builder and, crucially, which ones bypass
  `EventType`.
- `tests/integration/two_peer_game.py` -- the existing two-peer harness; reuse it rather than
  building a third.
</context>

<tasks>

<task type="auto">
  <name>Task 1: the join, on local turn truth, with a tolerated tail</name>
  <files>src/pursuit/services/reporting/log_join.py, tests/unit/test_artifact_log.py</files>
  <action>
    Read the JSONL and the sibling ledger; key both on the record's OWN top-level turn. Carry the
    peer's claimed `envelope.turn` into the output as evidence, in a field whose name makes clear
    it is a claim.

    Tail handling: drop a trailing partial line and set an explicit marker in the result. A
    malformed line that is NOT the last one is a hard error -- that is corruption, not an
    interrupted write, and the two must stay distinguishable.

    COUNTER-CONTROL, and it is the point of this task: build a fixture where the peer stamped its
    COMMIT and REVEAL envelopes with DISJOINT turns and assert the joined artifact still pairs them
    correctly by local turn. Then re-key the join on `envelope.turn` and record how many turns the
    artifact loses -- that is the revert probe, with a real count.
  </action>
  <verify>The disjoint-turn fixture joins correctly; the re-keyed probe loses a counted number of turns. The mid-file corruption case raises; the truncated-tail case does not.</verify>
  <done>The join cannot be steered by anything the peer chose.</done>
</task>

<task type="auto">
  <name>Task 2: the artifact, and the proof that it stands alone</name>
  <files>src/pursuit/services/reporting/artifact_log.py, tests/integration/test_log_artifact_roundtrip.py</files>
  <action>
    Assemble one record per turn carrying commitment, revealed move, intent, hint, verdict, nonce
    and hash, named by 07-02's namer. Filter records on the EVENT STRING -- `language_turn` and
    `game_over` are not `EventType` members.

    The integration proof, in this order and no other:
    1. run a recorded two-peer game through `tests/integration/two_peer_game.py`;
    2. build the artifact;
    3. DELETE the `.jsonl` and the `.ledger.jsonl`;
    4. re-hash every turn through `commit_pack.verify_reveal` from the artifact alone and assert
       100% of turns reproduce their recorded `h_commit`;
    5. assert the turn count is greater than zero before asserting 100% -- `all(...)` over an empty
       sequence is `True`, and `audit_record.all_matched([])` returning `True` (`audit_record.py:34-36`)
       is the canonical instance of this in this repository.
    Also assert the artifact carries at least one `language_turn`-sourced hint with its intent flag,
    and the game's `audit_verdict`.
  </action>
  <verify>Step 5's guard is present and its absence would let an empty artifact pass -- prove that by temporarily emptying the artifact and confirming the guard fires.</verify>
  <done>A third party with only this file can verify the game.</done>
</task>

<task type="auto">
  <name>Task 3: prove it cannot run during play</name>
  <files>src/pursuit/services/reporting/artifact_log.py</files>
  <action>
    Grep the PRODUCTION callers of the builder and of `CommitLedger.read_all` and record them. The
    builder must be reachable only from a game-end path (07-07 wires it), never from the turn loop
    -- D-64's nonce separation holds during play and only SEC-04's end-of-game publication makes
    the ledger readable. State this in the module docstring with the D-64/SEC-04 citation.
  </action>
  <verify>The recorded caller list contains no turn-loop module. A test-only caller is not evidence.</verify>
  <done>The nonces stay secret exactly as long as rule 18 requires.</done>
</task>

</tasks>

<verification>
1. `uv run ruff check .` -> 0 violations.
2. `uv run pytest tests/ --cov` -> all pass, coverage >= 85% and not below the inherited baseline.
3. `bash scripts/check_line_limit.sh` -> exit 0, PLUS every new file checked explicitly by path.
4. `uv run python scripts/dev_launch.py` -> exit 0; then build the artifact from that real game's
   files and report the turn count and the re-hash percentage.
5. Confirm no test or source in this plan mutates `logs/` outside a temp directory, and that
   `git status` shows no artifact accidentally staged from a real run.
</verification>

<success_criteria>
- Every turn carries commitment, revealed move, intent, hint, verdict, nonce and hash.
- 100% of turns re-hash to their recorded `h_commit`, on a non-zero turn count, with both source
  files deleted.
- The disjoint-turn adversarial fixture joins correctly; the `envelope.turn` revert probe loses a
  recorded number of turns.
- A truncated tail yields an artifact plus an explicit marker; mid-file corruption still raises.
- No production caller inside the turn loop.
</success_criteria>

<non_goals>
The viewer and the verdict banner (07-08). Sending the artifact (07-07). Any change to
`event_log.py`, `ledger.py` or `agent_audit_observed.py`.
</non_goals>

<output>
Create `.planning/phases/07-reporting-and-visualization-shell/07-05-SUMMARY.md` with the turn count
and re-hash percentage from a real `dev_launch` game, the revert probe's lost-turn count, and the
production-caller list.
</output>
