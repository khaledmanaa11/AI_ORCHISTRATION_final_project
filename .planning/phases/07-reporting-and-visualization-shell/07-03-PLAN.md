---
phase: 07-reporting-and-visualization-shell
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pursuit/sdk/local_view.py
  - src/pursuit/sdk/view_builder.py
  - scripts/check_local_truth.py
  - scripts/check_local_truth.sh
  - .github/workflows/quality-gate.yml
  - tests/unit/test_local_view_firewall.py
  - tests/unit/test_check_local_truth.py
autonomous: true
requirements: [REPORT-08]

must_haves:
  truths:
    - "THIS IS THE PHASE'S DISQUALIFICATION RISK AND AN IMPORT BOUNDARY IS NOT THE FIREWALL. Rule 9 (`docs/RULES.md:30`): 'Display the full objective board state in the live interface -> **Project disqualification** for illegal advantage', and `RULES.md:115` ranks it third among the cheapest ways to score zero. Measured: EVERY agent process legitimately holds BOTH true positions -- `GameState` (`shared/state.py:34-38`) is exactly `{cop, thief, barriers, barriers_placed, turn}`, and `turn_language.known_opponent_cell` (`turn_language.py:57-59`) reads `ctx.state.thief` on every cop turn from turn 1. So a `gui/` module could import nothing forbidden and still read `ctx.state.thief`. The leak is a FIELD READ, not an import."
    - "REDACTION LOGIC MUST LIVE OUTSIDE `gui/` OR THE GATE CANNOT SEE IT. Measured `pyproject.toml:38`: `omit = [\"*/main.py\", \"*/gui/*\", \"tests/*\"]`. Anything meaningful under `gui/` is invisible to `fail_under = 85` -- it would look like the gate passed. `LocalView` and `view_builder` therefore live under `src/pursuit/sdk/` (D-74). Equally, no logic may be parked under `scripts/` to dodge the size gate: `check_line_limit.sh:18` enumerates `src/**/*.py tests/**/*.py training/**/*.py` only, so `scripts/` is scanned by NEITHER gate."
    - "`LocalView` IS A CLOSED FIELD SET, NOT A FILTERED COPY. Its fields are: own position, declared barriers, the belief grid over the opponent, sensed scent, the hint log with intent flags, and turn/state/timer -- and nothing else. A `GameState`, an `AgentContext`, a `dict` of either, or an 'extras' field would each re-open the leak by construction. `view_builder` is the ONLY module in the repository permitted to read `ctx.state`, and the CI script proves it."
    - "THE FIELD-LEVEL SCAN IS THE LOAD-BEARING TEST AND IT IS VACUOUS UNLESS PAIRED. Three tests, all required: (a) a `GameState` whose opponent sits at a cell chosen to differ from own position, from every declared barrier AND from the belief argmax is built into a `LocalView`, and a deep scan of the SERIALISED view finds that coordinate nowhere; (b) THE COUNTER-CONTROL -- the same scan run against a deliberately leaky view MUST report the leak, asserted; (c) the structural CI script. Without (b), (a) passes against a scanner that always returns 'clean'. This is exactly the failure the phase-5 audit found six consecutive times."
    - "THE CI SCRIPT PASSES VACUOUSLY ON A DIRECTORY THAT DOES NOT EXIST YET, AND `src/pursuit/gui/` DOES NOT EXIST UNTIL 07-06. Measured: `src/pursuit/` today contains `network sdk security services shared strategy` and no `gui`, and the mould's own enumeration -- `check_no_llm_in_strategy.py:56`, `sorted(root.rglob('*.py'))` -- returns `[]` for a missing root, so `find_violations()` returns `[]` and `main()` prints OK. The script must FAIL LOUDLY when its root is absent or holds zero modules, and a test must pin that. A gate that reports OK because it scanned nothing is worse than no gate."
    - "THE HINT HISTORY 07-CONTEXT ASSUMES DOES NOT EXIST (07-PLAN-OUTLINE.md Sec11 item 3). Measured: `agent_context.py:116` documents `incoming_hints` as 'the LAST hint per sender -- unlike `pending_hints`, NOT cleared at resolve'; the turn-by-turn history exists only as `language_turn` records inside the JSONL (`turn_events.language_turn_record`). DECISION for this plan: `view_builder` accumulates its own append-only history in an EXPLICIT instance passed by the caller -- never a module-level global, never a class attribute (rule 2 / NET-02: two contexts in one interpreter share no field). The live GUI must not read the wire log; that would put file I/O and replay coupling into what has to stay a thin shell."
    - "A REVERT PROBE IS PART OF THE DELIVERABLE, NOT A NICETY. All three tests must FAIL against pre-plan code, with the failure counts recorded. A test written after the fix that would also have passed before it proves nothing -- six consecutive phase-5 executors shipped exactly that."
  artifacts:
    - path: src/pursuit/sdk/local_view.py
      provides: "the rules 8-9 read model -- a closed field set that structurally cannot carry an opponent cell"
      contains: "no GameState, no AgentContext, no free-form dict, no extras field"
    - path: src/pursuit/sdk/view_builder.py
      provides: "ctx -> LocalView, the ONLY module that reads ctx.state, plus an explicit hint-history accumulator"
      contains: "no module-level mutable state (NET-02)"
    - path: scripts/check_local_truth.py
      provides: "the structural gate: gui/ imports and attribute reads"
      contains: "a loud failure when the scanned root is missing or empty, and an overridable root so a test can point it at a synthetic tree"
    - path: tests/unit/test_local_view_firewall.py
      provides: "the field-level scan, its counter-control, and the recorded revert probe"
      contains: "an opponent cell distinct from own position, every barrier, and the belief argmax"
  key_links:
    - from: src/pursuit/sdk/view_builder.py
      to: docs/RULES.md
      via: "rules 8-9 make displaying the true board a project disqualification; this is the one place the projection happens"
      pattern: "local truth"
---

<objective>
Build one read model, outside `gui/`, that a view can render with no path to the opponent's true
cell -- and prove it with a scan that can fail.

Purpose: rule 9's sanction is project disqualification, and the data to trigger it sits in
`ctx.state` on every agent process by design.
Output: `LocalView`, `view_builder`, a CI structural gate, and three tests with a recorded revert
probe.
</objective>

<context>
- `07-PLAN-OUTLINE.md` Sec2 (D-74), Sec7 (07-03), Sec11 item 3.
- `docs/RULES.md:29-30` (rules 8-9) and `:115` (the ranked zero-scoring list).
- `scripts/check_no_llm_in_strategy.py` -- the mould. Copy its shape: a plain `ast` walk that never
  imports `pursuit` itself; `find_violations(root=...)` overridable so a test can point it at a
  synthetic `tmp_path` tree; a `.sh` wrapper; and the `test_strategy_pluggable.py` pattern of
  loading the script BY FILE PATH so the suite and CI provably run the same logic (QUAL-02).
- `src/pursuit/network/turn_language.py:121-142` -- `belief_snapshot` already computes exactly the
  belief entropy/argmax/reliability triple the sidebar wants, and its docstring already states the
  rules 8-9 discipline for the JSONL. Reuse the shape; do not re-derive it.
- `.github/workflows/quality-gate.yml` -- measured today: it runs `check_line_limit.sh`, `ruff` and
  `pytest --cov`, and does NOT run `check_no_llm_in_strategy.sh`. Wire the new gate in; note the
  pre-existing omission in the SUMMARY rather than silently fixing an unrelated thing.
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED -- the three tests, against pre-plan code</name>
  <files>tests/unit/test_local_view_firewall.py, tests/unit/test_check_local_truth.py</files>
  <action>
    Write all three before the read model exists, and record each failure with its message:

    (a) LEAK SCAN. Build a `GameState` whose opponent cell differs from own position, from every
        declared barrier and from the belief argmax -- state in the test WHY each exclusion is
        needed (a coincidence there would fail an honest view). Serialise the `LocalView` and deep
        scan the result for that coordinate as a VALUE, in every encoding the view could plausibly
        carry it (tuple, two-element list, and any flat board index the view actually uses). Assert
        it appears nowhere.
    (b) COUNTER-CONTROL. Run the identical scanner over a deliberately leaky object that DOES carry
        the opponent cell, and assert the scanner REPORTS it. Without this, (a) is satisfied by a
        scanner that always returns clean.
    (c) STRUCTURAL GATE. Point `find_violations` at a synthetic `tmp_path` tree containing one
        module that imports `pursuit.sdk.engine` and one that reads `ctx.state.thief`, and assert
        both are reported. Then assert the MISSING-ROOT case is an ERROR, not an OK -- this is the
        vacuity that would otherwise ship, because `src/pursuit/gui/` does not exist until 07-06.
  </action>
  <verify>All three fail at HEAD for the stated reasons. Quote the failure output in the commit message -- this is the revert probe, taken before the fix rather than reconstructed after it.</verify>
  <done>The firewall's absence is reproduced, and the scanner is proven able to fail.</done>
</task>

<task type="auto">
  <name>Task 2: LocalView and view_builder, outside gui/</name>
  <files>src/pursuit/sdk/local_view.py, src/pursuit/sdk/view_builder.py</files>
  <action>
    `local_view.py` -- a frozen dataclass with the closed field set: own position, declared
    barriers, the belief grid over the opponent, sensed scent, the hint log with intent flags, and
    turn/state/timer. Nothing else, and no field whose type could carry a `GameState`,
    an `AgentContext` or an unconstrained `dict`. Its docstring states rule 9's sanction and why
    this field set is the mitigation.

    `view_builder.py` -- `ctx -> LocalView`, the ONLY reader of `ctx.state` outside the turn loop.
    A `LocalView` with a `None` belief map (belief disabled this game, exactly the honest-`None`
    convention `turn_language.belief_snapshot` already uses) must build without fabricating one.
    The hint-history accumulator is an explicit object the caller owns and passes in -- never a
    module-level global and never a class attribute.

    Add nothing to `gui/`; that package does not exist in this plan.
  </action>
  <verify>Task 1's (a) and (b) go green. `bash scripts/check_line_limit.sh` exit 0, plus each new file checked explicitly by path. Grep confirms `ctx.state` is read in exactly one new place.</verify>
  <done>A view can be rendered from data that cannot express the opponent's position.</done>
</task>

<task type="auto">
  <name>Task 3: the CI gate, wired, and loud on an empty scan</name>
  <files>scripts/check_local_truth.py, scripts/check_local_truth.sh, .github/workflows/quality-gate.yml</files>
  <action>
    An `ast` walk over `src/pursuit/gui/` that fails on: any import of `pursuit.sdk.engine`,
    `pursuit.shared.state`, or `pursuit.network*`; and any attribute chain reaching `.state.cop`,
    `.state.thief` or `.state.barriers`. Also report any name bound from `pursuit.services.*`
    other than the reporting read path -- state the allowlist explicitly in source.

    FAIL LOUDLY when the root is missing or contains zero modules (Task 1c). Print the number of
    modules scanned on the OK line, so a human reading CI output can see it was not zero.

    Wire `check_local_truth.sh` into `.github/workflows/quality-gate.yml` beside
    `check_line_limit.sh`. Put NO logic in `scripts/` beyond the walk itself: `scripts/` is scanned
    by neither the size gate (`check_line_limit.sh:18`) nor coverage (`pyproject.toml:37`), so
    anything parked there is doubly unchecked.
  </action>
  <verify>`uv run python scripts/check_local_truth.py` -> exits NON-ZERO today, naming the missing root. Task 1c goes green. The workflow file's new step is the only edit to it.</verify>
  <done>The boundary is enforced by CI, and cannot report OK on an empty scan.</done>
</task>

</tasks>

<verification>
1. `uv run ruff check .` -> 0 violations.
2. `uv run pytest tests/ --cov` -> all pass, coverage >= 85% and not below the inherited baseline.
3. `bash scripts/check_line_limit.sh` -> exit 0, PLUS every new file checked explicitly by path.
4. `uv run python scripts/check_no_llm_in_strategy.py` -> OK (unchanged).
5. `uv run python scripts/check_local_truth.py` -> exits non-zero until `src/pursuit/gui/` exists;
   record the exact message so 07-06 knows what it must turn green.
6. Grep `src/` for every reader of `ctx.state` and confirm the new ones are exactly `view_builder`.
   A test-only reader proves nothing -- name the PRODUCTION callers.
</verification>

<success_criteria>
- An opponent cell distinct from every legitimately displayed cell appears nowhere in the
  serialised view.
- The same scanner run against a leaky view reports the leak -- asserted, not assumed.
- The CI script reports violations on a synthetic tree AND fails loudly on an empty/missing root.
- All three tests failed against pre-plan code, with the counts recorded.
- No redaction logic under `gui/` (coverage-omitted) and none under `scripts/` (gate-omitted).
</success_criteria>

<non_goals>
Any Tkinter code (07-06). Rendering decisions, colours or layout. Reading the wire log for hint
history -- the accumulator is in-memory by decision.
</non_goals>

<output>
Create `.planning/phases/07-reporting-and-visualization-shell/07-03-SUMMARY.md` with the three
pre-fix failure messages verbatim, the production-caller grep for `ctx.state`, and the recorded
note that `check_no_llm_in_strategy.sh` is not in the CI workflow today.
</output>
