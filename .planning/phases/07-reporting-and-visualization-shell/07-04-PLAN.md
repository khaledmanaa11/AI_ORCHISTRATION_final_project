---
phase: 07-reporting-and-visualization-shell
plan: "04"
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - src/pursuit/services/reporting/sink.py
  - src/pursuit/services/reporting/gmail_sink.py
  - src/pursuit/services/reporting/message.py
  - .env-example
  - pyproject.toml
  - tests/unit/test_mail_message.py
  - tests/unit/test_mail_sink_dry_run.py
  - tests/unit/test_gmail_sink.py
autonomous: true
requirements: [REPORT-01, REPORT-04, REPORT-05]

must_haves:
  truths:
    - "A `DryRunSink` THAT WRITES A FILE AND RETURNS SUCCESS MAKES EVERY TEST GREEN WHETHER OR NOT THE LIVE PATH WORKS. That is this plan's central vacuity risk and the reason for a hard rule: the 429/backoff test, the scope test and the MIME test ALL run against `GmailSink` with an INJECTED FAKE TRANSPORT, never against `DryRunSink`. `DryRunSink` gets its own narrow test (it wrote the two files) and is never the subject of a REPORT-04 or REPORT-05 assertion."
    - "THE BACKOFF ALREADY EXISTS IN THE GATEKEEPER, AND A SECOND ONE INSIDE `GmailSink` WOULD MAKE THE TEST PASS FOR THE WRONG REASON. Measured: `Gatekeeper._call_with_retry` (`gatekeeper.py:133-155`) retries on ANY exception, runs `total_attempts = retries_before_failure + 1`, and awaits `self._sleep(self._params.wait_after_error_seconds)` between attempts -- with `sleep` an injected constructor seam at `gatekeeper.py:82` put there for exactly this. So **`GmailSink` RAISES on HTTP 429; it does not sleep.** The assertion is on the INJECTED sleep having been called with the configured value (30 s per OQ-3), and it must be an assertion on the ARGUMENT, not on the call count alone -- a sink that slept 5 s twice would otherwise pass."
    - "OQ-3 IS RESOLVED AND APPLIES TO THIS INSTANCE ONLY (07-PLAN-OUTLINE.md Sec5, verbatim): backoff **30 s** for the mail gatekeeper -- `PARAMETERS.md:95` Table 19 row 3 gives 5 s as a MINIMUM (negotiable upward, never downward), `SEGAL_GUIDELINES.md:174` gives `retry_after_seconds: 30`, `:182` says take the stricter value, and 30 s satisfies both at once. Phase 4's LLM path keeps its shipped 5 s; measured on disk today in `config/{police,thief}/language.json`. This plan edits neither file."
    - "RULES 33-34 ARE ASSERTED BY PARSING THE MESSAGE BACK, NOT BY INSPECTING THE BUILDER. Rule 34 (`RULES.md:75`): 'Send a final report as free text -- only as an attached JSON file -> A non-JSON report is rejected in processing -> zero score'. The test re-parses the assembled message with the stdlib email parser and asserts: the JSON part carries `Content-Disposition: attachment` with a filename matching the PARAMETERS artifact name; the body part contains NO report content (assert on a distinctive value from the report, not on body length); and the attachment round-trips through `json.loads` to the original object."
    - "THE RECIPIENT IS FIXED AND COMES FROM CONFIG. `PARAMETERS.md:176-179`: `rmisegal+uoh26finalgame@gmail.com` is 'the single mandatory destination for game reports, and must be configured as the fixed recipient in both agents' mail-sending code'. It is a `reporting.json` leaf (07-01), asserted equal to that literal in a test, and never a default inside `src/`."
    - "THE SCOPE IS `gmail.send` AND NOTHING ELSE. Rule 30 (`RULES.md:66`): 'Use send-only permission scope for the mail interface -> Security breach that disqualifies the code'. A test must assert that constructing the client with any broader scope FAILS -- a test that only asserts the happy scope string is present would pass against a module that also requests `gmail.readonly`."
    - "`GmailSink` IS THE ONLY MODULE IMPORTING `google-*` (D-70), isolated exactly as `services/llm/anthropic_provider.py` isolates the LLM vendor SDK. Prove it with a grep over `src/` in the verification, not by assertion. No test constructs `GmailSink` against a real credential, and no test performs network I/O."
    - "CREDENTIALS ARE ENV-VAR NAMES IN CONFIG AND VALUES NOWHERE (rules 39-40; `RULES.md:80-81`). `.gitignore` already carries `credentials.json`, `client_secret*.json`, `token.json`, `token.pickle`, `*.token` and `.env`, with a comment stating 'These entries exist BEFORE the OAuth code does. Do not remove them.' -- written in anticipation of this plan. `.env-example` gains dummy entries in the existing house style (`ANTHROPIC_API_KEY=your-key-here`, `NGROK_AUTHTOKEN=...`); the real files are read through `os.environ.get()` only."
    - "A PERMANENTLY-FAILING SEND QUEUES; IT DOES NOT CRASH AND IT DOES NOT SILENTLY DROP. `Gatekeeper.submit` re-raises the last exception once retries exhaust (`gatekeeper.py:155`) -- 07-01's `chain.py` owns turning that into a bounded FIFO enqueue at Table-19 row 5's depth. This plan proves it ON THE MAIL PATH with an always-429 fake, and asserts the report is recoverable from the queue afterwards rather than merely 'no exception raised'."
  artifacts:
    - path: src/pursuit/services/reporting/message.py
      provides: "MIME assembly -- attached JSON, boilerplate body"
      contains: "rules 33-34 asserted by re-parsing, filename from the 07-02 artifact namer"
    - path: src/pursuit/services/reporting/gmail_sink.py
      provides: "the ONLY google-* importer; send-only scope; 429 raised, never slept on"
      contains: "an injected transport seam so no test needs a credential"
    - path: src/pursuit/services/reporting/sink.py
      provides: "the MailSink protocol and DryRunSink"
      contains: "dry_run writes .json + .eml under the configured artifact directory"
  key_links:
    - from: src/pursuit/services/reporting/gmail_sink.py
      to: src/pursuit/services/llm/anthropic_provider.py
      via: "one module owns the vendor SDK import; everything else talks to the protocol (D-70)"
      pattern: "provider"
---

<objective>
Get an attached-JSON report out through the Figure-13 chain -- to disk in `dry_run`, to a
send-only Gmail client in `live` -- with 429 handled by the gatekeeper's own backoff.

Purpose: rule 34 makes a free-text report a zero score and rule 30 makes a broad OAuth scope a
disqualification, so both are pinned by tests before a credential ever exists.
Output: `MailSink` + `DryRunSink` + `GmailSink` + MIME assembly, all provable offline.
</objective>

<context>
- `07-PLAN-OUTLINE.md` Sec2 (D-70), Sec5 (OQ-3), Sec7 (07-04).
- 07-01 delivered `reporting.json`, its loader and `chain.py`; 07-02 delivered the artifact namer.
  Consume both; duplicate neither.
- `src/pursuit/services/llm/anthropic_provider.py` -- the vendor-isolation precedent.
- `docs/RULES.md:64-66` (rules 28-30) and `:74-75` (rules 33-34).
- `.gitignore` lines 1-30 -- the secrets block, already written for this plan.
- `uv add` only (CLAUDE.md): `google-api-python-client`, `google-auth`, `google-auth-oauthlib`.
  Never `pip`. `pyproject.toml` is the only dependency source of truth.
</context>

<tasks>

<task type="auto">
  <name>Task 1: the MIME shape, asserted by parsing it back</name>
  <files>src/pursuit/services/reporting/message.py, tests/unit/test_mail_message.py</files>
  <action>
    Assemble a multipart message whose JSON report is an ATTACHMENT named by 07-02's artifact
    namer, with a fixed boilerplate body carrying no report content.

    Tests re-parse the rendered message with the stdlib email parser and assert: exactly one
    attachment part; its `Content-Disposition` is `attachment` with the expected filename; its
    payload round-trips through `json.loads` to the original object; and a DISTINCTIVE value from
    the report (e.g. the game_uid) does NOT appear in the body part. Pair the last one with a
    control that puts that value in the body and asserts the check FAILS.
  </action>
  <verify>All assertions are made on the re-parsed message. The body-leak control fails as recorded.</verify>
  <done>Rules 33-34 are pinned by the message that would actually be sent.</done>
</task>

<task type="auto">
  <name>Task 2: the sink protocol and the dry-run sink</name>
  <files>src/pursuit/services/reporting/sink.py, tests/unit/test_mail_sink_dry_run.py</files>
  <action>
    A `MailSink` protocol with one send operation and a named result type. `DryRunSink` writes the
    `.json` and the `.eml` under the artifact directory from `reporting.json` and returns success.

    Its test asserts only what it can honestly assert: both files exist, the `.eml` re-parses to
    the same message Task 1 built, and the `.json` equals the report. It makes NO claim about 429,
    scope, backoff or delivery -- those belong to Task 3, against `GmailSink`.
  </action>
  <verify>Nothing in this test file references 429, scope or backoff.</verify>
  <done>The autonomous path exists and its test does not overclaim.</done>
</task>

<task type="auto">
  <name>Task 3: GmailSink against an injected fake transport</name>
  <files>src/pursuit/services/reporting/gmail_sink.py, .env-example, pyproject.toml, tests/unit/test_gmail_sink.py</files>
  <action>
    `uv add` the three Google packages. `GmailSink` is the only module importing them, takes its
    transport as an injected seam, and reads credential/token locations from the env-var NAMES in
    `reporting.json` via `os.environ.get()` only.

    On HTTP 429 it RAISES a named retryable error -- it does not sleep. The gatekeeper's ladder
    owns the wait.

    The four tests that carry this plan:
    (a) a fake returning 429, 429, 200 SUCCEEDS, and the gatekeeper's INJECTED sleep was called
        exactly twice with the configured 30 s -- assert the ARGUMENT, not just the count;
    (b) a fake that always 429s exhausts `retries_before_failure` and the report ENDS UP IN THE
        QUEUE -- assert it is recoverable from the queue, not merely that nothing raised;
    (c) constructing the client with any scope beyond `gmail.send` FAILS -- include a broader-scope
        case that must be rejected, not only a happy-path assertion;
    (d) no test in the file constructs a real credential or touches the network.

    REVERT PROBES with real counts: (i) make `GmailSink` swallow 429 instead of raising -- (a) must
    fail; (ii) set the mail backoff to Table 19's bare 5 s -- (a)'s argument assertion must fail.
    Record both.
  </action>
  <verify>`grep -rn "google" src/` names exactly one module. `.env-example` gains dummy values only; `git diff` shows no real credential anywhere.</verify>
  <done>The live path is proven without a credential, and the proof cannot be satisfied by the dry-run sink.</done>
</task>

</tasks>

<verification>
1. `uv run ruff check .` -> 0 violations.
2. `uv run pytest tests/ --cov` -> all pass, coverage >= 85% and not below the inherited baseline.
   Zero network calls: the suite must pass with networking unavailable.
3. `bash scripts/check_line_limit.sh` -> exit 0, PLUS every new file checked explicitly by path.
4. `grep -rn "googleapiclient\|google.oauth2\|google_auth" src/ | cut -d: -f1 | sort -u` -> exactly
   `src/pursuit/services/reporting/gmail_sink.py`.
5. `git diff pyproject.toml` shows the three packages added via `uv add`; `uv lock` is current;
   no `requirements.txt` exists.
6. Secret scan before commit: grep the tree and `.env-example` for anything key-shaped; both
   shipped `reporting.json` files still read `mode: dry_run`.
</verification>

<success_criteria>
- The JSON is an attachment and the body carries no report content, asserted by re-parsing.
- The recipient equals `rmisegal+uoh26finalgame@gmail.com` and comes from config.
- Any scope beyond `gmail.send` fails a test.
- 429/429/200 succeeds with exactly two injected sleeps of 30 s; always-429 queues recoverably.
- `google-*` is imported in exactly one module; no test needs a credential or a network.
</success_criteria>

<non_goals>
Real OAuth consent, a real token file, and any live send -- all 07-10. Deciding WHEN a report is
sent (07-07). Building a second gatekeeper or a second backoff.
</non_goals>

<output>
Create `.planning/phases/07-reporting-and-visualization-shell/07-04-SUMMARY.md` with the two revert
probes' failure counts, the one-module google grep, and the exact sleep arguments observed.
</output>
