---
phase: 07-reporting-and-visualization-shell
plan: "06"
type: execute
wave: 2
depends_on: ["07-03"]
files_modified:
  - src/pursuit/sdk/view_publish.py
  - src/pursuit/network/turn_resolve.py
  - src/pursuit/gui/__init__.py
  - src/pursuit/gui/live_app.py
  - src/pursuit/gui/live_panels.py
  - src/pursuit/gui/live_sidebar.py
  - src/pursuit/gui/widgets.py
  - tests/unit/test_view_publish.py
  - tests/unit/test_gui_structural.py
autonomous: true
requirements: [REPORT-08]

must_haves:
  truths:
    - "D-76 (NEW, structural, zero numbers): THE LIVE GUI IS A SEPARATE PROCESS FED BY A PUBLISHED LOCAL-TRUTH SNAPSHOT. 07-PLAN-OUTLINE.md Sec7 leaves this open -- 'Drive Tk with `after()` off the turn loop, or run it in a separate process -- decide in the plan, not in the executor'. Decided: separate process. Measured reasons, all three from the tree: (1) `tk.mainloop()` blocks its calling thread, and `Watchdog` (`watchdog.py:116-123`) is a daemon thread polling `time.monotonic() - _last_activity` against `watchdog_threshold` = 60 (`config/*/network.json`) whose action is `os._exit(WatchdogExit.FREEZE)` (`watchdog.py:48-57`) -- blocking the asyncio loop stops every `touch()` and kills the agent mid-game, after which the peer declares us `opponent_unresponsive`; (2) Tk is not thread-safe, so 'run it in a thread' is not an alternative; (3) a separate process CANNOT hold `ctx.state` at all, which turns D-74's type-level firewall into a PROCESS-level one -- the strongest available answer to rule 9. Cost: one snapshot file per agent. It carries no shared runtime state -- each process writes only its OWN view to its OWN path, so CLAUDE.md rule 2 is untouched."
    - "THE SNAPSHOT PATH IS DERIVED, NOT CONFIGURED. `turn_commit_ledger.ledger_path` (`turn_commit_ledger.py:33-36`) already establishes the `<log-file-stem>.<kind>` sibling convention: `logs/<role>/<uid>.jsonl` -> `logs/<role>/<uid>.ledger.jsonl`. The view snapshot follows it exactly. No new config leaf, no env var, no invented path."
    - "**OQ-6 (NEW, RAISED NOT INVENTED) -- the live GUI's refresh interval has no source in any document.** `PARAMETERS.md` Table 19 has no UI row; `network.json`'s `watchdog_poll_seconds` = 1 is the watchdog's own D-18 engineering default and reusing it would be one number serving two purposes -- the practice `language_wiring.py:43-47` explicitly refuses ('A DIFFERENT constant from `strategy/registry.py`'s: this seeds a distinct stream, not the same number reused for two purposes'). RESOLUTION, structural, zero numbers in `src/`: the refresh interval is a REQUIRED keyword-only constructor argument with NO default in source -- the QUAL-11 pattern `TokenBucket.__init__` (`bucket.py:32-38`) and `Watchdog.__init__` (`watchdog.py:70-79`) both already use -- supplied by the single launcher call site as a REQUIRED argparse argument. The operator states it at launch; the repository states no number. 07-10 records the value used for the README screenshot."
    - "`gui/` IS COVERAGE-OMITTED, SO IT MUST CONTAIN NO LOGIC. Measured `pyproject.toml:38`: `omit = [\"*/main.py\", \"*/gui/*\", \"tests/*\"]`. Every derivation -- what colour a cell takes, how a probability maps to a shade, how a timer is formatted -- lives under `src/pursuit/sdk/` where the >=85% gate can see it. `gui/` holds widget construction and attribute reads only. Equally, nothing may be parked under `scripts/`: `check_line_limit.sh:18` enumerates `src/** tests/** training/**` and `pyproject.toml:37` sets `source = [\"src\", \"training\"]`, so `scripts/` escapes BOTH gates."
    - "THE FILE SPLIT IS PRE-AUTHORISED, NOT DISCOVERED AT COMMIT TIME. A Tk dashboard does not fit one file: `live_app.py` (root + refresh loop + argparse), `live_panels.py` (board / belief heatmap / scent), `live_sidebar.py` (hint log, intent flags, turn/state/timer), `widgets.py` (shared chrome, extracted at the SECOND copy per SEGAL Sec3:152-154, not the third). `check_line_limit.sh` with no arguments enumerates via `git ls-files`, so NEW UNTRACKED files pass VACUOUSLY -- check every new file explicitly by path."
    - "07-03's CI GATE CURRENTLY FAILS BECAUSE `src/pursuit/gui/` DOES NOT EXIST, AND THIS PLAN IS WHAT TURNS IT GREEN. Measured: `src/pursuit/` holds `network sdk security services shared strategy` and no `gui`. 07-03 made an empty/missing scan a LOUD failure precisely so this plan cannot satisfy it by scanning nothing. After this plan, `uv run python scripts/check_local_truth.py` must exit 0 AND print a non-zero module count."
    - "A HEADLESS ASSERTION MUST NOT BE A SKIP. `tk.Tk()` raises on a machine with no display, and a `pytest.mark.skipif` there converts the criterion-2 render check into a silent pass. So: the render-model mapping is tested with NO Tk at all (it lives in `sdk/`), and the actual widget construction is a SCRIPTED LAUNCH whose exit code 07-09 records as gate evidence on this machine. The PRD states Tk 8.6 is verified importable here."
    - "PUBLISHING MUST NEVER TOUCH THE OUTCOME, THE EXIT CODE OR THE TURN LOOP'S LIVENESS. The single call site is contained exactly as `capture_declaration.send_capture_declaration` is (`capture_declaration.py:34-40`: 'BEST-EFFORT and returns nothing on purpose'). Since 06-05 a non-zero exit code MEANS an audit mismatch (`main.py:25-29`), so an exception escaping the publisher would forge a technical-loss signal."
  artifacts:
    - path: src/pursuit/sdk/view_publish.py
      provides: "publish one LocalView snapshot beside the wire log, best-effort"
      contains: "the ledger_path sibling convention; durable_write_json; no raise into the caller"
    - path: src/pursuit/gui/live_app.py
      provides: "a standalone Tk dashboard over a published snapshot -- one window per agent process"
      contains: "a REQUIRED refresh-interval argument, no default in source (OQ-6)"
    - path: tests/unit/test_gui_structural.py
      provides: "07-03's gate run against the real gui/ package"
      contains: "an assertion that the scanned module count is non-zero"
  key_links:
    - from: src/pursuit/gui/live_app.py
      to: src/pursuit/sdk/local_view.py
      via: "the GUI process can only see what view_builder serialised -- it never holds ctx.state (D-74, D-76)"
      pattern: "LocalView"
---

<objective>
Put a live dashboard on screen that renders a `LocalView` and structurally cannot render anything
else, without ever blocking the agent's event loop.

Purpose: Sec10.4 criterion 2 -- the live GUI displays state, only local truth -- and rule 9 makes
the alternative a project disqualification.
Output: a snapshot publisher in the SDK layer and a standalone Tk app in `gui/`, four thin files.
</objective>

<context>
- `07-PLAN-OUTLINE.md` Sec2 (D-74), Sec7 (07-06 and its two traps), Sec11 items 3-4.
- 07-03 delivered `LocalView`, `view_builder` and `scripts/check_local_truth.py`. Consume them;
  add no second read model.
- `src/pursuit/network/watchdog.py` and `config/{police,thief}/network.json` -- the 60 s threshold
  and the `os._exit` action that make an in-process `mainloop()` fatal.
- `src/pursuit/network/turn_resolve.py` -- measured 83/150 code lines and the point at which a turn
  actually resolves; the intended single call site. Measure it again before editing and choose
  another single point WITH a recorded reason if it no longer has room.
- `src/pursuit/network/capture_declaration.py:93-141` -- the best-effort, never-raise, never-touch
  -the-outcome shape to copy.
</context>

<tasks>

<task type="auto">
  <name>Task 1: publish the snapshot, best-effort, from one place</name>
  <files>src/pursuit/sdk/view_publish.py, src/pursuit/network/turn_resolve.py, tests/unit/test_view_publish.py</files>
  <action>
    `view_publish.py` builds a `LocalView` through 07-03's `view_builder` and writes it beside the
    wire log using the `ledger_path` sibling convention and `shared/durable_write.durable_write_json`
    (no second persistence scheme -- QUAL-02).

    Wire ONE call site in the turn loop. It must not raise, must not await anything unbounded, and
    must not change any return value. Record in the source why it is contained, citing `main.py`'s
    exit-code meaning.

    Tests: a snapshot is written per resolved turn; a publisher whose write raises leaves the turn
    loop's return value and the log untouched (inject a failing writer and assert the caller is
    unaffected); and -- the rules 8-9 tie-in -- the WRITTEN FILE is run through 07-03's leak scan
    with the opponent placed at a cell distinct from own position, every barrier and the belief
    argmax. Pair it with the leaky-object counter-control so the scan is proven able to fail.
  </action>
  <verify>`bash scripts/check_line_limit.sh src/pursuit/network/turn_resolve.py` exit 0; new files checked explicitly by path. The failing-writer test asserts on the caller's observable behaviour, not on a log message.</verify>
  <done>The agent publishes what a viewer may see, and nothing more, and cannot be hurt by doing so.</done>
</task>

<task type="auto">
  <name>Task 2: the four thin GUI files</name>
  <files>src/pursuit/gui/__init__.py, src/pursuit/gui/live_app.py, src/pursuit/gui/live_panels.py, src/pursuit/gui/live_sidebar.py, src/pursuit/gui/widgets.py</files>
  <action>
    `live_app.py` -- a standalone entry point (`python -m pursuit.gui.live_app`) taking the snapshot
    path and a REQUIRED refresh interval (OQ-6: `required=True` in argparse, no default anywhere in
    source), driving `after()` off a Tk root it owns. It may block on `mainloop()`: this process has
    no turn loop and no watchdog.

    `live_panels.py` -- board, belief heatmap, scent. `live_sidebar.py` -- hint log with intent
    flags, turn/state/timer. `widgets.py` -- shared chrome, extracted at the second copy.

    ZERO derivation in `gui/`. Any mapping (probability -> shade, timer -> string, cell -> colour)
    lives in `sdk/` so coverage can see it -- add `sdk/view_render.py` if `local_view.py` has no
    room, and measure before deciding.

    A `LocalView` with a `None` belief map (belief disabled this game) must still render -- an
    honest empty panel, never a fabricated grid.
  </action>
  <verify>`uv run python -m pursuit.gui.live_app --help` exits 0 and shows the refresh argument as required. Each new file checked explicitly by path against the 150-line gate.</verify>
  <done>A window exists, and everything worth testing about it is testable.</done>
</task>

<task type="auto">
  <name>Task 3: turn 07-03's gate green, non-vacuously</name>
  <files>tests/unit/test_gui_structural.py</files>
  <action>
    Run `scripts/check_local_truth.py`'s `find_violations` against the REAL `src/pursuit/gui/`
    package -- loading the script by file path, the `test_strategy_pluggable.py` pattern, so the
    suite and CI provably run the same logic -- and assert both: zero violations, AND a non-zero
    count of modules scanned. The second assertion is the whole point; without it the test passes
    on an empty package.

    Add a synthetic-tree control: a temporary module under a `tmp_path` gui root that reads
    `ctx.state.thief` must be REPORTED. If it is not, the gate is not checking what it claims.
  </action>
  <verify>`uv run python scripts/check_local_truth.py` -> exit 0 with a non-zero module count printed. The synthetic violation is reported.</verify>
  <done>The rules 8-9 boundary is enforced against real code, not against an empty directory.</done>
</task>

</tasks>

<verification>
1. `uv run ruff check .` -> 0 violations.
2. `uv run pytest tests/ --cov` -> all pass, coverage >= 85% and not below the inherited baseline.
   Note in the SUMMARY that `gui/` is omitted from that figure, and state what percentage of the
   phase's new lines that omission covers.
3. `bash scripts/check_line_limit.sh` -> exit 0, PLUS every new file checked explicitly by path.
4. `uv run python scripts/check_local_truth.py` -> exit 0, non-zero module count.
5. `uv run python scripts/dev_launch.py` -> exit 0, both sides `audit_verdict matched=true`, zero
   `technical_win`, zero `watchdog_incident`. Then launch `python -m pursuit.gui.live_app` against
   one of the snapshots that run produced and record that it rendered and exited 0.
6. Grep `src/pursuit/gui/` for `ctx.state`, `GameState`, `engine` and `pursuit.network` -> no hits.
</verification>

<success_criteria>
- The agent never imports Tk and never blocks its event loop; `dev_launch` shows zero
  `watchdog_incident`.
- A published snapshot passes the leak scan, and the scan's counter-control still fails.
- `scripts/check_local_truth.py` exits 0 with a non-zero module count.
- No logic under `gui/` and none under `scripts/`.
- OQ-6 is recorded as an open question with its structural resolution; no interval number is
  written into `src/`.
</success_criteria>

<non_goals>
The replay viewer (07-08). Any screenshot or aesthetic judgement (07-10). A second read model, a
config leaf for the GUI, or an env-var toggle.
</non_goals>

<output>
Create `.planning/phases/07-reporting-and-visualization-shell/07-06-SUMMARY.md` recording D-76 with
its three measured reasons, OQ-6 with its structural resolution, the four files' line counts, and
the `check_local_truth.py` module count.
</output>
