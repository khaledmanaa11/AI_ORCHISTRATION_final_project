---
phase: 07-reporting-and-visualization-shell
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pursuit/services/reporting/artifacts.py
  - src/pursuit/services/reporting/artifact_config.py
  - src/pursuit/services/reporting/artifact_declaration.py
  - tests/unit/test_artifact_names.py
  - tests/unit/test_artifact_config.py
  - tests/unit/test_artifact_declaration.py
autonomous: true
requirements: [REPORT-06, REPORT-01]

must_haves:
  truths:
    - "EXTENDING THE SIGNED STEP-0 PAYLOAD ABORTS EVERY GAME, AND THE FAILURE LOOKS LIKE A BROKEN TEST SUITE (D-71). Measured: `agent_step0_wiring.declare_step0` builds the ten Sec5.5 keys via `step0_collect.collect_declaration` (`step0_collect.py:94-111`) and signs them with `step0_sign.sign_declaration`; `handshake_evaluate._compare_offer` then runs `_step0_verified(...)` and returns `HandshakeOutcome.STEP0_MISMATCH` on any disagreement, and `declare_step0`'s own docstring states 'a shape change here would break Step-0 against a peer'. So the `declaration_` artifact WRAPS the signed payloads verbatim and adds PARAMETERS' remaining content -- repo URLs, MCP server addresses, agreed token ceiling, start/end times -- OUTSIDE the signed envelope. Adding one field inside it is a phase-wide abort."
    - "THE DECLARATION ARTIFACT ALREADY EXISTS AND IS WRITTEN SOMEWHERE GIT CANNOT SEE IT. Measured: `agent_step0_wiring.write_declaration` writes `ctx.log_path.parent / f'declaration_{declared_game_id}.json'`, and `agent_lifecycle.py:86` fixes `log_path = Path('logs') / cfg.role / f'{game_uid}.jsonl'`. `.gitignore` ignores `logs/` wholesale -- WHILE CARRYING A COMMENT that says 'the four required JSON artifacts (declaration_/config_/log_/result_) MUST be committed per rule 50 and Appendix F rule 4 -- keep them out of this ignore list'. The comment is true and the tree does not honour it. This plan writes the four artifacts into the non-ignored artifact directory 07-01 names in `reporting.json` and NEVER assumes a path under `logs/` is committable."
    - "`<NN>` IS NOT THE RULE-37 COUNTER, AND THE TWO MUST NOT TOUCH (D-72). `PARAMETERS.md:159-168` names `config_<game_id>_g<NN>.json` and `log_<game_id>_g<NN>.json` with `<NN>` the match number, and `result_<game_id>.json` / `declaration_<game_id>.json` with NO suffix. `<NN>` is a per-`game_id` sub-game index, 01-based, zero-padded to two digits, derived from what already exists for THIS `game_id` in the artifact directory. `config/police/games_played.json` -- five digits today, and disqualification-bearing under rule 38 -- is never read here. 07-00 fixed that counter's mechanism and 07-10 owns its value; this plan does not touch it in either direction."
    - "ONE SERIALIZER, NOT TWO, AND THE PACKAGE BOUNDARY IS ALREADY DECIDED. `canonical_json`/`digests_match` live in `src/pursuit/network/config_hash.py:25-59`, NOT in `pursuit.security` -- `security/commit_pack.py:10-18` imports them from there as a documented, deliberately narrow package-boundary exception and forbids a second `json.dumps(sort_keys=True, ...)` in as many words. 07-CONTEXT.md points at the wrong package (07-PLAN-OUTLINE.md Sec11 item 2). Import from `network/config_hash.py`; never create a third copy."
    - "THE `config_` DIGEST HAS AN EXACT, TESTABLE CONTRACT. `config_hash.config_digest` (`config_hash.py:36-45`) computes `sha256(canonical_json(json.loads(game_params.json)))`. The `config_` artifact embeds `game_params.json`'s content, and the SHA-256 of that embedded sub-object recomputed through the same helper MUST equal `config_digest(config_dir/'game_params.json')` byte for byte -- the same digest `agent_entrypoint.py:80` puts on the handshake wire. If those two disagree, the artifact and the game were locked to different configs (rule 11)."
    - "A TWO-ROLE FIXTURE MUST PRODUCE BYTE-IDENTICAL `config_` ON BOTH SIDES (rule 11: 'identical byte-for-byte on both sides'). `config/police/` and `config/thief/` legitimately differ in `network.json` (port, opponent_url -- D-04, and `config_hash.py:13-16` says hashing it 'would abort every game'), so the artifact carries the AGREED game configuration and never the per-agent network file. Prove it with a fixture that builds both sides and compares bytes -- not by asserting the builder is careful."
    - "ZERO NUMERIC VALUES ARE INTRODUCED BY THIS PLAN. Names, zero-padding width and JSON shapes are structural; every parameter VALUE inside `config_` is copied from the already-loaded shipped config, never re-typed."
  artifacts:
    - path: src/pursuit/services/reporting/artifacts.py
      provides: "the four PARAMETERS names, the <NN> index, the shared game_uid, and the one canonical-signing entry point"
      contains: "_g<NN> on config_/log_ only; canonical_json imported from network/config_hash.py"
    - path: src/pursuit/services/reporting/artifact_config.py
      provides: "config_<game_id>_g<NN>.json"
      contains: "an embedded game_params whose recomputed digest equals config_hash.config_digest's"
    - path: src/pursuit/services/reporting/artifact_declaration.py
      provides: "the D-71 wrapper around own + peer signed Step-0 payloads"
      contains: "the signed envelopes verbatim, with PARAMETERS' remaining content strictly outside them"
  key_links:
    - from: src/pursuit/services/reporting/artifact_declaration.py
      to: src/pursuit/network/agent_step0_wiring.py
      via: "the wrapper embeds what write_declaration already persists, and adds nothing inside the signature"
      pattern: "declaration"
---

<objective>
Fix the naming, joining and signing rules for all four artifacts in one place, and produce the two
that need no completed game.

Purpose: `PARAMETERS.md`'s four filenames are a spec surface a grader will diff against, and the
`declaration_` half sits one field away from aborting every game at the handshake.
Output: `artifacts.py` (names, `<NN>`, `game_uid`, canonical signing), `config_<game_id>_g<NN>.json`,
and the `declaration_` wrapper.
</objective>

<context>
- `07-PLAN-OUTLINE.md` Sec2 (D-71, D-72), Sec7 (07-02), Sec11 items 1-2.
- `.planning/phases/07-reporting-and-visualization-shell/07-00-SUMMARY.md` -- wave 0. The
  games-played VALUE is unset and belongs to the human at 07-10. Nothing here reads it, defaults
  it, or reads around it.
- `docs/PARAMETERS.md:157-168` -- the four names, verbatim. `:143-153` -- the five mandatory rules,
  including 'a different configuration file name per game' and 'every game requires an email
  carrying the GitHub commit hash'.
- `src/pursuit/network/config_hash.py`, `src/pursuit/security/commit_pack.py:10-18` -- the one
  canonicalisation and the boundary exception that already exists for it.
- 07-01 owns `reporting.json` and its artifact-directory leaf; this plan is in the same wave, so
  take the directory as a CONSTRUCTOR ARGUMENT and let the wiring supply it. Do not import
  `reporting_config` and do not duplicate its default.
</context>

<tasks>

<task type="auto">
  <name>Task 1: the names and the index, pinned against PARAMETERS</name>
  <files>src/pursuit/services/reporting/artifacts.py, tests/unit/test_artifact_names.py</files>
  <action>
    One module owning: the four filename builders; the `<NN>` derivation (per-`game_id`, 01-based,
    zero-padded to two, computed from what already exists for that `game_id` in the artifact
    directory); the shared `game_uid`; and the single canonical-signing entry point delegating to
    `network/config_hash.canonical_json`.

    Test the names against `PARAMETERS.md:163-168` transcribed into the test as literals -- present
    `_g<NN>` on `config_`/`log_`, ABSENT on `declaration_`/`result_`. Include the negative: a helper
    that puts `_g<NN>` on `result_` must fail the same assertion (otherwise the check is shape-only).

    ANTI-VACUITY: no `parametrize` list may be built from a directory glob. An empty `parametrize`
    SKIPS silently and the whole file reads green. Build the list from literals and assert its
    length.
  </action>
  <verify>Each of the four names matches PARAMETERS character for character. `<NN>` advances 01 -> 02 for a second sub-game of the same `game_id` and restarts at 01 for a different one.</verify>
  <done>The filenames a grader will diff are pinned to the document that defines them.</done>
</task>

<task type="auto">
  <name>Task 2: config_<game_id>_g<NN>.json, digest-equal to the handshake's</name>
  <files>src/pursuit/services/reporting/artifact_config.py, tests/unit/test_artifact_config.py</files>
  <action>
    Write the agreed configuration -- the parameters both sides locked -- and never the per-agent
    `network.json` (`config_hash.py:13-16` states why hashing it would abort every game).

    The load-bearing test: recompute the SHA-256 of the embedded `game_params` content through
    `config_hash.canonical_json` and assert it equals `config_hash.config_digest(config_dir /
    'game_params.json')` -- the exact string `agent_entrypoint.py:80` puts on the wire. Then the
    symmetry test: build the artifact from `config/police/` and from `config/thief/` and compare
    the two files BYTE for byte.

    COUNTER-CONTROL: mutate one value in a copied `game_params.json` and assert the digest test
    FAILS. A digest assertion that cannot fail is not an assertion.
  </action>
  <verify>Both sides byte-identical; the digest equals the handshake's; the mutation control fails as recorded.</verify>
  <done>The artifact and the game are provably locked to the same configuration (rule 11).</done>
</task>

<task type="auto">
  <name>Task 3: the declaration wrapper -- add nothing inside the signature</name>
  <files>src/pursuit/services/reporting/artifact_declaration.py, tests/unit/test_artifact_declaration.py</files>
  <action>
    Embed BOTH signed envelopes verbatim -- this side's (the dict `declare_step0` returns:
    `{"declaration": {...ten Sec5.5 keys...}, **signature}`) and the peer's when they sent one
    (`HandshakeResult.peer_step0_declaration`; `write_declaration` already persists it to
    `declaration_<game_id>_peer.json`). A digest-only peer contributes no content and the wrapper
    says so honestly rather than fabricating an empty declaration.

    Add PARAMETERS' remaining declaration content STRICTLY OUTSIDE the signed envelopes: repo URLs,
    MCP server addresses, agreed token ceiling, start/end times.

    THE TEST THAT MATTERS: re-verify each embedded envelope's signature THROUGH
    `step0_sign.verify_declaration` after the wrapper has been written and read back. If the wrapper
    perturbed one byte of a signed payload, this fails. Pair it with a counter-control that injects
    one extra key INSIDE the signed dict and asserts verification FAILS -- that is the exact
    mistake D-71 exists to prevent, and the test must be able to catch it.

    Do not modify `agent_step0_wiring.py`, `step0_collect.py` or `step0_sign.py`.
  </action>
  <verify>`git diff src/pursuit/security/step0_*.py src/pursuit/network/agent_step0_wiring.py` is EMPTY. Both signature re-verifications pass; the injected-key control fails.</verify>
  <done>The declaration is completed without being touched.</done>
</task>

</tasks>

<verification>
1. `uv run ruff check .` -> 0 violations.
2. `uv run pytest tests/ --cov` -> all pass, coverage >= 85% and not below the inherited baseline.
   Every Phase-6 Step-0 / handshake test passes UNMODIFIED -- one of them needing an edit means the
   signed payload moved, which is the D-71 abort.
3. `bash scripts/check_line_limit.sh` -> exit 0, PLUS every new file checked explicitly by path.
4. `uv run python scripts/dev_launch.py` -> exit 0, both sides `audit_verdict matched=true`, zero
   `technical_win`, and NO `STEP0_MISMATCH` anywhere in either log.
5. Grep the produced artifacts for the shared secret, `ANTHROPIC_API_KEY` and any ngrok token
   before committing any sample (rules 39-40).
</verification>

<success_criteria>
- All four names match `PARAMETERS.md` exactly, including `_g<NN>`'s presence and absence.
- The `config_` artifact's embedded digest equals `config_hash.config_digest`'s, and the two roles
  produce byte-identical files.
- Every embedded Step-0 signature still verifies after the wrap, and the injected-key control fails.
- `games_played.json` is neither read nor written.
</success_criteria>

<non_goals>
`log_` (07-05) and `result_` (07-07). Sending anything (07-04). Any edit to the signed Step-0 field
set, the `Envelope` shape or the commit-reveal payload.
</non_goals>

<output>
Create `.planning/phases/07-reporting-and-visualization-shell/07-02-SUMMARY.md` with the digest
equality measured on both roles, the counter-control failures, and the artifact directory actually
used.
</output>
