---
phase: 07-reporting-and-visualization-shell
plan: "07"
type: execute
wave: 3
depends_on: ["07-04", "07-05"]
files_modified:
  - src/pursuit/services/reporting/artifact_result.py
  - src/pursuit/services/reporting/result_agreement.py
  - src/pursuit/services/reporting/end_of_game.py
  - src/pursuit/network/agent_entrypoint.py
  - tests/unit/test_result_agreement.py
  - tests/unit/test_artifact_result.py
  - tests/integration/test_end_of_game_reporting.py
autonomous: true
requirements: [REPORT-01, REPORT-06, REPORT-07]

must_haves:
  truths:
    - "THE SERIES TOKEN TOTAL CANNOT COME FROM `budget.report()`, AND THE OUTLINE SAYS IT CAN. 07-PLAN-OUTLINE.md Sec7 states the series total is 'fed from `ctx.language.gatekeeper.budget.report()`'. MEASURED: `Gatekeeper.__init__` (`gatekeeper.py:91-95`) constructs a FRESH `TokenBudget` per instance; `language_wiring.build_language_runtime` (`language_wiring.py:160`) constructs the gatekeeper once per PROCESS; and `budget.py:8-11` says 'one instance = one series'. Two games are two processes and therefore two budgets -- `report()` returns THIS GAME's spend and nothing more. **The series total is the previous `result_` file's series total plus this game's `report()`**, which is exactly why OQ-4 makes `result_` one durable file per series. Rule 54 (`RULES.md:103`) requires both numbers; a per-game figure printed in the series slot is a false report."
    - "OQ-4 IS RESOLVED (07-PLAN-OUTLINE.md Sec5, verbatim): 'one file per SERIES, rewritten and emailed after every sub-game'. `PARAMETERS.md:168` names `result_<game_id>.json` with NO `_g<NN>` suffix and defines it as 'Final results summary across all sub-games. **This is the mandatory report emailed to the lecturer.**'; rule 32 (`RULES.md:73`) sanctions PER GAME -- 'No report -> the points from that game are disqualified'. So: one series file, durably rewritten with `.prev` rotation (`durable_write.py:34-57`) after each sub-game, and emailed each time -- which is what makes rule 32's per-game sanction unreachable. Recorded as an INTERPRETATION with both citations, because neither document states the rewrite-and-resend shape in as many words."
    - "`agreed` IS THREE-VALUED, AND DEFAULTING IT IS A FALSE DECLARATION. Measured: `capture_declaration.declares_capture` (`capture_declaration.py:69-80`) sends `GAME_OVER` only when `outcome is Outcome.CAPTURE` AND this side is the cop -- 'The thief stays silent; it has nothing to declare'. So on a SURVIVAL game NO peer claim exists on either side, and `peer_outcome` is legitimately absent. An executor writing `agreed = own == peer` with `peer` defaulted to `own` produces `agreed: true` on every silent game -- a fabricated agreement, under a rule (35, `RULES.md:76`) whose sanction is 0 for BOTH teams. `agreed` is `true` / `false` / `null-with-a-stated-reason`, never inferred."
    - "A REPORTING FAILURE MUST NOT FORGE A TECHNICAL LOSS. Measured: `agent_entrypoint.py:132-133` runs `record_completed_game(cfg, outcome)` then `return outcome`, INSIDE the try whose `finally` (`:153-157`) runs `stop_watchdog(ctx)` -> `linger_for_peer(ctx)` -> `stop_runtime(ctx)`. An exception raised at `:132` propagates out of `_play`, through `run_with_tunnel`, out of `run_agent`, to `main.py:58`'s `asyncio.run` -- and since 06-05 a non-zero exit code MEANS an audit mismatch (`main.py:25-29`). So the hook is contained at its own boundary, exactly as `capture_declaration` swallows `ToolError`, and never touches `outcome` or the exit code."
    - "THE WATCHDOG IS STILL ARMED AT THE HOOK'S CALL SITE AND THE MAIL LADDER OUTLIVES IT. Measured: `ctx.watchdog.start()` at `agent_entrypoint.py:77`, `stop_watchdog(ctx)` at `:153` -- the hook at `:132` sits inside that window, with `watchdog_threshold` = 60 s (`config/*/network.json`). The mail ladder at the OQ-3 mail values is `response_timeout` 30 s x (`retries_before_failure` 3 + 1) plus 30 s backoff x 3 = **up to 210 s**, three and a half times the threshold. This is 05-13's defect arriving through a new door. The hook MUST touch the watchdog once per bounded attempt -- the shape `capture_declaration.py:116-118,132` already uses inside the same armed window and states the reason for -- or be bounded under the threshold. Prove which, with an injected clock and a real watchdog, exactly as `tests/unit/test_audit_send_failure.py` does; zero real sleeps."
    - "`ctx.language` IS OPTIONAL AND A MISSING BUDGET IS REPORTED HONESTLY, NEVER AS ZERO. `agent_context.py:115` types `language: LanguageRuntime | None = None`. A game played with the language layer off has no token spend to report; the artifact says so with an explicit absent marker, following the `belief_snapshot` convention ('all None when belief is disabled this game -- an honest \"not run\", never fabricated', `turn_language.py:134-136`). Zeros presented as a measurement are a rule-38-class misstatement."
    - "THE COMMIT HASH IS REUSED, NOT RE-DERIVED. `PARAMETERS.md:152-153` mandatory rule 5: 'every game requires an email to the lecturer carrying the GitHub commit hash the code ran on for that game'. `step0_collect._git_commit_hash()` (`step0_collect.py:87-91`) already collects it into the Step-0 declaration's `commit_hash` field and 'raises loudly on failure -- never a blank hash'. `result_` carries THAT value, read from the declaration this game actually ran under. A second `git rev-parse` could report a different commit than the one declared."
    - "THIS PLAN TRANSMITS NOTHING IN `live` MODE AND SETS NO GAMES-PLAYED VALUE. Both shipped `reporting.json` files stay `dry_run` (07-01). 07-00 left the rule-38 counter VALUE deliberately unset and 07-10 owns it; nothing here reads it, defaults it or reads around it."
  artifacts:
    - path: src/pursuit/services/reporting/result_agreement.py
      provides: "rule 35's agreement record, derived from data already on the wire (D-75)"
      contains: "own_outcome, peer_outcome, audit_verdict and a three-valued agreed"
    - path: src/pursuit/services/reporting/artifact_result.py
      provides: "result_<game_id>.json -- one durable file per series, .prev-rotated"
      contains: "this game's AND the series' token totals, plus the declared commit hash"
    - path: src/pursuit/services/reporting/end_of_game.py
      provides: "the single game-end hook: build log_, build result_, send through the chain"
      contains: "watchdog touches per bounded attempt; contained so no failure reaches the exit code"
  key_links:
    - from: src/pursuit/network/agent_entrypoint.py
      to: src/pursuit/services/reporting/end_of_game.py
      via: "one call beside record_completed_game, inside the window where the gatekeeper is still alive"
      pattern: "end_of_game"
---

<objective>
At game end, have each agent independently write `result_<game_id>.json` and send it -- without
touching the outcome, the exit code, or the watchdog's honesty.

Purpose: rule 35 zeroes BOTH teams when one fails to report, and rule 32 sanctions per game, so
this hook is the single most submission-critical call site in the codebase.
Output: the result artifact, the agreement record, and a bounded, contained game-end hook.
</objective>

<context>
- `07-PLAN-OUTLINE.md` Sec2 (D-75), Sec5 (OQ-4), Sec7 (07-07 and its trap).
- 07-04 delivered the chain-facing sink; 07-05 delivered the `log_` builder. Call both.
- `src/pursuit/network/agent_entrypoint.py` -- read `:125-157` in full before editing. The comment
  block at `:125-131` explains exactly why `record_completed_game` sits where it does; the new call
  belongs beside it, not in the `finally`.
- `src/pursuit/network/capture_declaration.py` -- the contained, best-effort, watchdog-touching
  shape, in the same armed window, already shipped.
- `.planning/phases/05-cloud-exposure-and-tunneling/05-13-PLAN.md` -- the ladder-outlives-watchdog
  defect and the injected-clock test technique that pinned it.
- `src/pursuit/network/agent_audit_verdict.py` -- where the `audit_verdict` record is written and
  what it carries.
</context>

<tasks>

<task type="auto">
  <name>Task 1: the agreement record, with its counter-control pair</name>
  <files>src/pursuit/services/reporting/result_agreement.py, tests/unit/test_result_agreement.py</files>
  <action>
    Derive `own_outcome` (this side's resolved `Outcome`), `peer_outcome` (the peer's `GAME_OVER`
    claim, logged by `capture_declaration.record_received_declaration` as a `message_received`
    record carrying `payload["outcome"]`), `audit_verdict` (the Phase-6 record), and `agreed`.

    No new `MessageType`, no new tool, no protocol change (D-75).

    The four cases, each a test:
    (a) honest agreeing capture -> `agreed: true`;
    (b) FABRICATED DISAGREEMENT -> `agreed: false` AND a report is still produced. Rule 35 needs
        the disagreement REPORTED, not suppressed. This is the counter-control: a fix that
        smooths disagreements passes (a) and fails here;
    (c) survival game, no peer claim exists at all -> `agreed: null` with a stated reason, and
        assert it is NOT `true`;
    (d) a peer claim that is malformed or of an unexpected type is evidence, not a crash -- follow
        `security/audit.py`'s boundary rule (`audit.py:56-124`): a peer-controlled value that
        cannot be parsed becomes a NAMED non-agreement, never an exception.
  </action>
  <verify>Case (c) fails against an implementation that defaults `peer_outcome` to `own_outcome`. Record that revert probe with its failure count.</verify>
  <done>The agreement claim is true, false, or honestly unknown -- and never invented.</done>
</task>

<task type="auto">
  <name>Task 2: result_<game_id>.json -- per-game and per-series</name>
  <files>src/pursuit/services/reporting/artifact_result.py, tests/unit/test_artifact_result.py</files>
  <action>
    One file per series (OQ-4), rewritten via `durable_write_json` so the `.prev` generation
    survives an interrupted rewrite. It carries: the agreement record from Task 1; this game's
    token totals from `gatekeeper.budget.report()`; the SERIES totals accumulated from the previous
    file's series figure plus this game's; and the `commit_hash` read from this game's Step-0
    declaration.

    THE TEST THAT CARRIES REPORT-07: run two games in sequence against the same series file and
    assert the series total is STRICTLY GREATER than either individual game's -- and that it equals
    their sum. An implementation that writes `report()` into both slots passes a
    'series >= game' assertion and fails this one; write the strict version.

    Also: a game with `ctx.language is None` produces an explicit absent marker, and a test asserts
    the field is NOT `0`.
  </action>
  <verify>The two-game sum assertion fails against a single-budget implementation. The `.prev` file exists after the second write.</verify>
  <done>Rule 54's two numbers are both real measurements.</done>
</task>

<task type="auto">
  <name>Task 3: the hook -- bounded, contained, and beside the counter</name>
  <files>src/pursuit/services/reporting/end_of_game.py, src/pursuit/network/agent_entrypoint.py, tests/integration/test_end_of_game_reporting.py</files>
  <action>
    `end_of_game.py` owns the sequence: build `log_` (07-05), build `result_` (Task 2), send through
    07-01's chain in `dry_run`. `agent_entrypoint.py` gains ONE call beside
    `record_completed_game` at `:132` -- a new module rather than growth in that file, following
    the `agent_audit_wiring -> agent_audit_exchange -> agent_audit_verdict` precedent. Measure
    `agent_entrypoint.py` before and after (103/150 at HEAD).

    Decide and RECORD which containment the watchdog needs: a touch per bounded attempt (the
    `capture_declaration.py:116-118` shape) or a total bound under `watchdog_threshold`. Say why in
    the source, with the 210 s vs 60 s arithmetic.

    The three integration proofs:
    (a) a normal game writes both artifacts and produces a `dry_run` send, and the process exit
        code is unchanged from the pre-plan run;
    (b) a sink that ALWAYS fails leaves the outcome, the last `game_over` record and the exit code
        untouched, and the report is recoverable from the queue;
    (c) a reporting path that outlives `watchdog_threshold` under an INJECTED clock still reaches
        its end without a `watchdog_incident` -- and the paired control: a genuinely frozen agent
        is STILL killed, so NET-07 is not traded away. Zero real sleeps, no 60-second tests.
  </action>
  <verify>Remove the containment and record which of (a)-(c) fail, with counts. Remove the watchdog touch and record that (c) dies on `watchdog_incident` while the frozen-agent control still passes.</verify>
  <done>The report goes out, and nothing about the game changes because it did.</done>
</task>

</tasks>

<verification>
1. `uv run ruff check .` -> 0 violations.
2. `uv run pytest tests/ --cov` -> all pass, coverage >= 85% and not below the inherited baseline.
3. `bash scripts/check_line_limit.sh` -> exit 0, PLUS every new file checked explicitly by path;
   quote `agent_entrypoint.py`'s before/after counts.
4. `uv run python scripts/check_no_llm_in_strategy.py` and `scripts/check_local_truth.py` -> OK.
5. `uv run python scripts/dev_launch.py` -> exit 0, both sides `audit_verdict matched=true`, zero
   `technical_win`, zero `watchdog_incident`. Confirm BOTH counters advanced by exactly ONE
   (07-00's invariant) and that both agents wrote their own `result_` and `log_`.
6. `git diff config/police/reporting.json config/thief/reporting.json` -> empty; both still
   `dry_run`. No live send happens in this plan.
</verification>

<success_criteria>
- `result_` carries this game's AND the series' token totals, and the two-game sum assertion holds.
- An honest agreeing game records `agreed: true`; a fabricated disagreement records `agreed: false`
  and still reports; a silent-peer game records `null`, never `true`.
- A send failure changes neither the outcome, the last `game_over` record, nor the exit code.
- The reporting window cannot trip the freeze watchdog, and a genuinely frozen agent is still
  killed.
- The commit hash equals the one this game's Step-0 declaration carries.
</success_criteria>

<non_goals>
Any live send or OAuth (07-10). Setting the games-played value (07-10). Changing `Outcome`,
`main.py`'s exit-code meaning, or the audit's verdict policy.
</non_goals>

<output>
Create `.planning/phases/07-reporting-and-visualization-shell/07-07-SUMMARY.md` with the two-game
token measurement, the three revert probes' failure counts, the watchdog containment chosen with
its arithmetic, and `agent_entrypoint.py`'s before/after line counts.
</output>
