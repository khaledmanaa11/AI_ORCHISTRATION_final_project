---
phase: 07-reporting-and-visualization-shell
plan: "09"
type: execute
wave: 4
depends_on: ["07-01", "07-02", "07-03", "07-04", "07-05", "07-06", "07-07", "07-08"]
files_modified:
  - scripts/measure_gate7.py
  - scripts/gate7_common.py
  - scripts/gate7_mail.py
  - scripts/gate7_localtruth.py
  - scripts/gate7_replay.py
  - scripts/gate7_report.py
  - docs/PRD_gatekeeper.md
  - docs/phases/phase-7/GATE-7-MEASUREMENT.md
  - docs/phases/phase-7/OAUTH-RUNBOOK.md
  - .planning/graphs/GRAPH_REPORT.md
autonomous: true
requirements: [REPORT-01, REPORT-02, REPORT-08, REPORT-09, DOC-02]

must_haves:
  truths:
    - "CRITERION 1 IS REPORTED AS `dry_run` PASS + LIVE PENDING, NEVER AS A BLANKET PASS. Sec10.4 criterion 1 requires a game summary SENT BY MAIL. Everything up to the send is measured here with zero credentials; the one live send needs a human at Google's consent screen and belongs to 07-10. A gate document that reads PASS without the delivered-message evidence is the failure mode rule 38 exists for -- and `docs/phases/phase-5/GATE-5-MEASUREMENT.md`'s retained PENDING/Attempt-1 record is this project's own precedent for how to write it."
    - "`scripts/` IS SCANNED BY NEITHER GATE, SO IT IS NOT A PLACE TO PUT LOGIC. Measured: `check_line_limit.sh:18` enumerates `git ls-files 'src/**/*.py' 'tests/**/*.py' 'training/**/*.py'` -- `scripts/` is absent; `pyproject.toml:37` sets `source = [\"src\", \"training\"]` -- `scripts/` is absent again. The `gate6_*.py` siblings were split for real size reasons, not to hide code, and `gate7_*.py` follows that precedent: measurement ORCHESTRATION only, with every behaviour it measures already living under `src/` and already covered."
    - "THE MEASUREMENT SCRIPT MUST NOT IMPORT `gui/` AT MODULE SCOPE. `tkinter.Tk()` raises on a machine with no display, so a top-level `import pursuit.gui.replay_app` would fail the WHOLE gate for a reason unrelated to any criterion. Import inside the function that needs it, or drive the app as a subprocess and record its exit code. `pyproject.toml:38` also omits `*/gui/*` from coverage, which is exactly why nothing measured here may depend on logic living there."
    - "EVERY NUMBER IN `PRD_gatekeeper.md` CARRIES ITS SOURCE, AND THE TWO UNSOURCED ONES ARE NAMED AS SUCH. Table 19 rows come from `PARAMETERS.md:93-99`; `requests_per_hour: 500` comes from `SEGAL_GUIDELINES.md:173`; the mail backoff of 30 s is OQ-3's resolution with `PARAMETERS.md:95` + `SEGAL_GUIDELINES.md:174,182` behind it; the DOS detector carries NO number at all (OQ-2's structural latch); and NO daily ceiling is written (OQ-1). If a derived daily bound appears anywhere it is labelled derived (`24 x requests_per_hour`) and never presented as a book value."
    - "THE RUNBOOK IS WRITTEN HERE AND RUN BY A HUMAN AT 07-10, AND IT MUST BE FOLLOWABLE START TO FINISH BY SOMEONE WHO DID NOT WRITE IT. `docs/phases/phase-5/REMOTE-ROUND-RUNBOOK.md` is the house precedent: numbered steps, the exact evidence to retain, and what a PASS looks like stated BEFORE the run. It must say plainly that Claude does not enter credentials and does not click consent, and that `dry_run` is restored afterwards."
    - "THE GRAPH REFRESH IS ROADMAP TASK 07-96 AND IT IS NOT OPTIONAL FROM PHASE 3 ONWARD. `graphify update . && cp graphify-out/{graph.json,graph.html,GRAPH_REPORT.md} .planning/graphs/`; commit ONLY `GRAPH_REPORT.md` -- `graph.json`/`graph.html` are gitignored build artifacts (`.gitignore`, the graphify block). Confirm the phase's new modules appear in the report."
    - "THIS PLAN MEASURES; IT DOES NOT FIX. If a criterion fails, it is recorded as a FAIL with its evidence and a gap is filed -- exactly as `GATE-5-MEASUREMENT.md` retained Attempt 1's honest failure beside Attempt 2. Editing code to make a measurement pass, inside the plan whose job is to measure, is how a gate stops being evidence."
  artifacts:
    - path: scripts/measure_gate7.py
      provides: "one command, zero credentials, an honest PASS/FAIL per Sec10.4 criterion"
      contains: "criterion 1 as dry_run PASS + live PENDING; no gui import at module scope"
    - path: docs/PRD_gatekeeper.md
      provides: "the per-mechanism PRD for rate limiting and reporting (ROADMAP row 07-04, SEGAL Sec2.3)"
      contains: "the Figure-13 chain, every number, and the source of every number"
    - path: docs/phases/phase-7/GATE-7-MEASUREMENT.md
      provides: "the three criteria answered with measured evidence"
      contains: "criterion 1 PENDING on its live half, with the exact evidence 07-10 must attach"
    - path: docs/phases/phase-7/OAUTH-RUNBOOK.md
      provides: "the operator procedure 07-10 follows"
      contains: "consent, one live send, the two screenshots, the OQ-5 decision, and restoring dry_run"
  key_links:
    - from: docs/phases/phase-7/GATE-7-MEASUREMENT.md
      to: .planning/ROADMAP.md
      via: "the measurement answers the Sec10.4 criteria the roadmap records verbatim and does not edit them"
      pattern: "10.4"
---

<objective>
Produce real evidence for everything machine-measurable in this phase, the per-mechanism PRD the
ROADMAP requires, and the runbook the human will follow.

Purpose: two of the three Sec10.4 criteria are fully measurable on this machine with no
credentials, and the third is measurable except for one send -- so the gate should be almost
entirely closed before a person is involved.
Output: `measure_gate7.py` + siblings, `docs/PRD_gatekeeper.md`, `GATE-7-MEASUREMENT.md`,
`OAUTH-RUNBOOK.md`, and a refreshed knowledge graph.
</objective>

<context>
- `07-PLAN-OUTLINE.md` Sec7 (07-09 and its two traps), Sec9 (the criterion-to-plan map), Sec10
  (ROADMAP row 07-04 -> this plan).
- `scripts/measure_gate6.py` + `gate6_*.py` -- the shape to imitate: idempotent, temp-directory
  games, one JSON evidence file, a printed per-criterion verdict.
- `docs/phases/phase-5/GATE-5-MEASUREMENT.md` and `REMOTE-ROUND-RUNBOOK.md` -- how a PENDING
  criterion and a human runbook are written in this project.
- `.planning/ROADMAP.md` Phase 7 -- the three Sec10.4 criteria, verbatim. **Not ours to edit.**
- `docs/phases/phase-7/GAMES-PLAYED-RECONSTRUCTION.md` (07-00) -- the runbook's OQ-5 step points at
  it and must not pre-empt its decision.
</context>

<tasks>

<task type="auto">
  <name>Task 1: measure_gate7 + siblings</name>
  <files>scripts/measure_gate7.py, scripts/gate7_common.py, scripts/gate7_mail.py, scripts/gate7_localtruth.py, scripts/gate7_replay.py, scripts/gate7_report.py</files>
  <action>
    One command, zero env vars, zero credentials, writing one JSON evidence file plus a printed
    per-criterion summary -- `measure_gate6.py`'s shape.

    Criterion 1 (`gate7_mail.py`): a full end-to-end `dry_run` report through the chain against an
    injected fake client -- the attachment re-parsed, the recipient, the scope rejection, and the
    429/429/200 backoff arguments. Report the verdict as `dry_run` PASS + live PENDING, as two
    named fields, never collapsed into one boolean.

    Criterion 2 (`gate7_localtruth.py`): run `check_local_truth.find_violations` against the real
    `src/pursuit/gui/` and record BOTH the violation count and the module count -- a zero/zero
    result is a FAIL, not a PASS. Then record the live-app launch's exit code, driving it as a
    SUBPROCESS so no `gui` import happens at this script's module scope.

    Criterion 3 (`gate7_replay.py`): a recorded game -> artifact -> `Verified OK`; a tampered
    artifact -> FAILED naming the turn; an empty artifact -> neither. Three fields, three verdicts.

    Keep every script to orchestration. If one grows real logic, that logic belongs under `src/`
    where both gates can see it.
  </action>
  <verify>`uv run python scripts/measure_gate7.py` runs twice in a row with identical output (idempotent, temp directories, never the real `logs/` tree) and exits 0. Every gate7 file is checked explicitly against the 150-line gate by path.</verify>
  <done>Every machine-checkable claim in this phase has a number behind it.</done>
</task>

<task type="auto">
  <name>Task 2: docs/PRD_gatekeeper.md -- every number, and its source</name>
  <files>docs/PRD_gatekeeper.md</files>
  <action>
    The per-mechanism PRD the ROADMAP's row 07-04 and SEGAL Sec2.3 require. Document: the one
    gatekeeper class and its two instances (D-68); the Figure-13 chain order (D-69) and why the
    bucket stays inside `submit()`; the token-bucket law quoted from `PARAMETERS.md:101-102`; the
    refusal contract (queue, never crash, never bare rejection -- SEGAL Sec4:175-176); and a table
    of EVERY number with its file-and-line source.

    Name the two that have no book source and say what was done instead: OQ-1 (no daily ceiling
    exists anywhere, so none is written; SEGAL's hourly 500 is used because it IS sourced) and OQ-2
    (a structural latch, so the DOS detector carries no number at all). State OQ-3's resolution and
    that it applies to the mail instance only, with Phase 4's shipped 5 s unchanged.
  </action>
  <verify>Every row of the number table resolves to a file and line that actually says it. Spot-check three by opening them.</verify>
  <done>A grader can audit every limit in the project from one page.</done>
</task>

<task type="auto">
  <name>Task 3: the gate record and the runbook</name>
  <files>docs/phases/phase-7/GATE-7-MEASUREMENT.md, docs/phases/phase-7/OAUTH-RUNBOOK.md</files>
  <action>
    `GATE-7-MEASUREMENT.md` -- the three criteria quoted from the ROADMAP verbatim (never edited),
    each with: method, run command, what a PASS looks like, and the measured evidence with every
    claim pointing at a named field or file. Criterion 1's live half is PENDING and states exactly
    what 07-10 must attach to flip it. Record any observation that does not fit the happy path
    rather than smoothing it.

    `OAUTH-RUNBOOK.md` -- numbered, followable by someone who did not write it, covering exactly:
    (1) create the OAuth client restricted to `gmail.send`, complete consent, and place the
    credential/token files at the env-var paths `reporting.json` NAMES -- with the explicit line
    that **Claude must not enter credentials and must not click consent**;
    (2) flip ONE config to `reporting.mode = live`, run one game, confirm arrival at
    `rmisegal+uoh26finalgame@gmail.com` with the JSON ATTACHED, then flip it BACK to `dry_run`;
    (3) capture the two README assets -- the live GUI and the replay viewer showing `Verified OK`
    (Sec9.4.2 item 5, rule 42), recording the OQ-6 refresh interval used;
    (4) decide OQ-5 from `GAMES-PLAYED-RECONSTRUCTION.md` and record the decision IN WRITING before
    any live send.
    Add the evidence-retention and secret-scan steps `REMOTE-ROUND-RUNBOOK.md` learned the hard way.
  </action>
  <verify>The runbook names no number this repository does not already source, and pre-empts none of OQ-5's options. The measurement document's criterion 1 reads PENDING.</verify>
  <done>The human has a short, scripted session ahead of them and nothing to improvise.</done>
</task>

<task type="auto">
  <name>Task 4: refresh the knowledge graph (ROADMAP 07-96)</name>
  <files>.planning/graphs/GRAPH_REPORT.md</files>
  <action>
    `graphify update . && cp graphify-out/{graph.json,graph.html,GRAPH_REPORT.md} .planning/graphs/`.
    Commit ONLY `GRAPH_REPORT.md`. Confirm this phase's new modules appear:
    `services/reporting/*`, `sdk/local_view.py`, `sdk/view_builder.py`, `sdk/view_publish.py`,
    `gui/*`, `shared/reporting_config.py`, `shared/gatekeeper_params.py`.
  </action>
  <verify>`git status` shows no `graph.json`/`graph.html` staged. The named modules appear in the report.</verify>
  <done>The map matches the tree.</done>
</task>

</tasks>

<verification>
1. `uv run ruff check .` -> 0 violations.
2. `uv run pytest tests/ --cov` -> all pass, coverage >= 85%; quote the exact figure.
3. `bash scripts/check_line_limit.sh` -> exit 0, PLUS every gate7 file checked explicitly by path
   (they live in `scripts/`, which the no-arg form does not scan -- that is the point).
4. `uv run python scripts/check_no_llm_in_strategy.py` and `scripts/check_local_truth.py` -> OK,
   the second with a non-zero module count.
5. `uv run python scripts/measure_gate7.py` -> exit 0, run twice, output identical.
6. `uv run python scripts/dev_launch.py` -> exit 0, both sides `audit_verdict matched=true`, zero
   `technical_win`, zero `watchdog_incident`, both counters +1.
7. Secret scan across every new doc and evidence file before committing (rules 39-40).
</verification>

<success_criteria>
- `measure_gate7.py` emits an honest per-criterion verdict; criterion 1 is `dry_run` PASS + live
  PENDING, never a blanket PASS.
- Criterion 2's evidence records a NON-ZERO module count alongside a zero violation count.
- Criterion 3 records all three verdicts, including the empty-artifact case.
- `PRD_gatekeeper.md` sources every number and names the two that have no book source.
- `OAUTH-RUNBOOK.md` is followable start to finish and states what Claude must not do.
- The graph is refreshed and only `GRAPH_REPORT.md` is committed.
</success_criteria>

<non_goals>
Fixing anything a measurement finds -- record it and file a gap. Any live send, any credential, any
screenshot judgement (all 07-10). Editing the Sec10.4 criteria, which are quoted from the book.
</non_goals>

<output>
Create `.planning/phases/07-reporting-and-visualization-shell/07-09-SUMMARY.md` with the per-criterion
verdicts as measured, the coverage figure, any gaps filed, and the graph's node/edge counts.
</output>
