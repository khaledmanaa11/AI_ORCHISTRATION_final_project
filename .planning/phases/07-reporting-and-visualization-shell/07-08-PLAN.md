---
phase: 07-reporting-and-visualization-shell
plan: "08"
type: execute
wave: 3
depends_on: ["07-05", "07-06"]
files_modified:
  - src/pursuit/services/reporting/replay_verify.py
  - src/pursuit/gui/replay_app.py
  - src/pursuit/gui/replay_panels.py
  - tests/unit/test_replay_verify.py
  - tests/integration/test_replay_roundtrip.py
autonomous: true
requirements: [REPORT-09]

must_haves:
  truths:
    - "`all_matched([])` IS `True` AND THIS IS THE ONE SCREEN THE GRADER LOOKS AT. Measured: `security/audit_record.py:34-36` -- 'True only when every record matched (vacuously True for an empty list)'. Python's `all(...)` over an empty sequence is the same trap, and `audit.audit_peer_records`'s own docstring calls a turn-less game 'vacuously matched, correctly'. For the AUDIT that is right; for the VIEWER it is a lie on screen. **An empty or zero-turn log must show an explicit 'nothing to verify' state -- never `Verified OK`.** Assert the turn count is greater than zero BEFORE any aggregate verdict, and pin the empty case with its own test."
    - "A SECOND SERIALIZER PRODUCES FALSE `FAILED` VERDICTS, WHICH IS THE WORST POSSIBLE FAILURE HERE. `security/commit_pack.py:10-18` forbids adding a second `json.dumps(sort_keys=True, ...)` in this repository in as many words, and `network/config_hash.canonical_json` (`config_hash.py:25-33`) is the one canonicalisation. `replay_verify` recomputes through `commit_pack.verify_reveal` (`:117-123`), which rebuilds via `build_commit_payload` and compares with `digests_match` -- never a fresh `json.dumps`, and never `commit_pack._hash_payload`, which is private."
    - "`verify_reveal` RAISES ON MALFORMED INPUT AND A TAMPERED ARTIFACT IS EXACTLY MALFORMED INPUT. Measured: `security/audit.py:159-165` wraps it in `except (TypeError, KeyError, ValueError)` and the boundary-rule comment block at `:56-124` records SIX separate production defects from this one pattern, including instance 5 -- 'ValueError: build_commit_payload: intent must be one of [\"lie\", \"truth\"], got \"maybe\"' escaping a narrower clause. A viewer that crashes on a tampered file shows no verdict at all. `replay_verify` catches the same three types and reports FAILED NAMING THE TURN."
    - "THE VERDICT NEEDS A PRODUCTION CALLER, GREP-PROVEN. `security/audit.py:150-151` records the precedent for stating this ('This is the one production call site that sees peer data (grep: the only other caller is scripts/gate6_tamper.py, over records it built itself)'). A verdict function reachable only from tests proves nothing about the screen. Name the production callers in the SUMMARY."
    - "THE BANNER STRINGS LIVE OUTSIDE `gui/`, IN `replay_verify.py`. `pyproject.toml:38` omits `*/gui/*` from coverage, so a literal defined in the app is untested; and the test must assert the SAME string the screen renders, not a parallel copy. Sec10.4 criterion 3 quotes `Verified OK` verbatim -- treat it as a fixed literal, not a message to reword."
    - "THE REPLAY APP HAS NO WATCHDOG PROBLEM, UNLIKE THE LIVE ONE. It loads a file and owns its process, so `tk.mainloop()` is correct here -- there is no asyncio loop to starve and no `os._exit(1)` freeze action in scope (contrast `watchdog.py:48-57`, which is what forced D-76 on the live GUI). Say so in the module docstring so a later reader does not 'fix' it into the live app's shape."
    - "`gui/` STAYS LOGIC-FREE AND `scripts/` IS NOT A HIDING PLACE. Verification, stepping state and the verdict all live in `services/reporting/replay_verify.py` where coverage sees them; `replay_app.py`/`replay_panels.py` construct widgets and read attributes. `check_line_limit.sh:18` scans `src/** tests/** training/**` only and `pyproject.toml:37` covers `src` and `training` only, so anything moved to `scripts/` escapes BOTH gates."
    - "TAMPER DETECTION IS PROVEN FIELD BY FIELD, NOT ONCE. A single-bit change in ANY of `state`, `move`, `intent` or `nonce` must produce FAILED naming the turn -- four cases, because a verifier that only re-hashes `move` passes a one-case test. The tamper harness `scripts/gate6_tamper.py` already exists for the audit; reuse its mutation shapes rather than inventing new ones."
  artifacts:
    - path: src/pursuit/services/reporting/replay_verify.py
      provides: "per-turn verification and the whole-file verdict, with the banner literals"
      contains: "commit_pack.verify_reveal only; (TypeError, KeyError, ValueError) contained; a non-zero-turn guard before any aggregate"
    - path: src/pursuit/gui/replay_app.py
      provides: "file open, step / play / pause, and a prominent verdict banner"
      contains: "no verification logic -- it renders replay_verify's result"
    - path: tests/unit/test_replay_verify.py
      provides: "clean -> OK, four single-field tampers -> FAILED naming the turn, empty -> neither"
      contains: "the empty-log case asserting NOT Verified OK"
  key_links:
    - from: src/pursuit/services/reporting/replay_verify.py
      to: src/pursuit/security/commit_pack.py
      via: "one payload builder and one canonicalisation -- D-59, or the verdict drifts to false FAILED"
      pattern: "verify_reveal"
---

<objective>
Load a `log_` artifact, verify it, and show the verdict prominently while stepping through turns.

Purpose: rule 20 (`RULES.md:51`) makes a verifying replay viewer 'a threshold condition for
approving and submitting the project', and Sec10.4 criterion 3 names the words on the screen.
Output: `replay_verify` plus a two-file Tk viewer that renders its result and computes nothing.
</objective>

<context>
- `07-PLAN-OUTLINE.md` Sec2 (D-73 verifier side), Sec7 (07-08 and its two traps).
- 07-05 delivered the artifact and proved it self-contained with both sources deleted. This plan
  consumes it and must never open the JSONL or the ledger.
- 07-06 delivered `gui/widgets.py` and the panel shapes; `replay_panels.py` reuses them rather than
  re-drawing a board (SEGAL Sec3:152-154 -- extract at the second copy).
- `scripts/gate6_tamper.py` -- the existing mutation shapes.
- `src/pursuit/security/audit.py:56-124` -- read the boundary-rule block before writing the
  try/except. It is a list of the exact ways this goes wrong.
</context>

<tasks>

<task type="auto">
  <name>Task 1: the verifier, and the three verdicts it must be able to reach</name>
  <files>src/pursuit/services/reporting/replay_verify.py, tests/unit/test_replay_verify.py</files>
  <action>
    Per turn: rebuild through `commit_pack.verify_reveal` and compare against the recorded
    `h_commit`. Contain `(TypeError, KeyError, ValueError)` into a FAILED record naming the turn --
    a tampered artifact is malformed input by construction.

    Three whole-file verdicts, and the third is not optional: OK, FAILED (naming the first failing
    turn), and NOTHING-TO-VERIFY for an empty or zero-turn artifact. Define the `Verified OK`
    literal here and export it.

    Tests: clean artifact -> OK; four separate single-field tampers (`state`, `move`, `intent`,
    `nonce`) -> FAILED naming the turn; empty artifact -> NOT OK, and assert on the specific
    nothing-to-verify state rather than merely `!= OK`.

    REVERT PROBES, with counts: (i) replace `verify_reveal` with a fresh `json.dumps(sort_keys=True)`
    re-hash and record how many CLEAN turns now report FAILED -- that is D-59's drift, measured;
    (ii) drop the non-zero-turn guard and confirm the empty artifact reports OK.
  </action>
  <verify>Both probes produce the recorded failures. The four tamper cases are four tests, not one parametrized case built from a glob (an empty `parametrize` SKIPS silently).</verify>
  <done>The verdict can be right, can be wrong, and cannot be vacuous.</done>
</task>

<task type="auto">
  <name>Task 2: the viewer -- two thin files, rendering only</name>
  <files>src/pursuit/gui/replay_app.py, src/pursuit/gui/replay_panels.py</files>
  <action>
    `replay_app.py` -- open a `log_` artifact by path, step / play / pause through turns, and show
    the verdict banner prominently (Sec10.4 criterion 3 is what a grader will screenshot).
    `replay_panels.py` -- per-turn rendering, reusing 07-06's `widgets.py` and panel shapes.

    Zero verification, zero stepping arithmetic and zero formatting in these files; they read
    `replay_verify`'s result. `mainloop()` is correct here -- record why in the docstring, so the
    contrast with the live app's D-76 is on the page.

    Both files must pass 07-03's `check_local_truth.py`: the artifact is a published record, but the
    same import/attribute rules apply to every module under `gui/`.
  </action>
  <verify>`uv run python -m pursuit.gui.replay_app --help` exits 0. `uv run python scripts/check_local_truth.py` exits 0 with a module count that grew by two.</verify>
  <done>The screen shows the verdict, and the screen decides nothing.</done>
</task>

<task type="auto">
  <name>Task 3: the round trip, end to end, and the production caller</name>
  <files>tests/integration/test_replay_roundtrip.py</files>
  <action>
    Play a recorded two-peer game (`tests/integration/two_peer_game.py`), build the `log_` artifact
    (07-05), DELETE the `.jsonl` and the `.ledger.jsonl`, and verify from the artifact alone:
    -> `Verified OK`, with the turn count reported and asserted greater than zero.

    Then tamper one bit in the artifact and assert FAILED naming the correct turn.

    Finally, grep the PRODUCTION callers of the verdict function and record them. `replay_app.py`
    must be one of them; a test-only reachability proves nothing about the shipped screen.
  </action>
  <verify>The clean run reports a non-zero turn count with OK; the tamper names the turn actually mutated, not turn 0.</verify>
  <done>The artifact a third party receives verifies on its own, and the screen says so honestly.</done>
</task>

</tasks>

<verification>
1. `uv run ruff check .` -> 0 violations.
2. `uv run pytest tests/ --cov` -> all pass, coverage >= 85% and not below the inherited baseline.
3. `bash scripts/check_line_limit.sh` -> exit 0, PLUS every new file checked explicitly by path.
4. `uv run python scripts/check_local_truth.py` -> exit 0, module count grew by two.
5. `uv run python scripts/dev_launch.py` -> exit 0; build the artifact from that real game and run
   the viewer against it, recording the verdict and the turn count.
6. `grep -rn "json.dumps" src/pursuit/services/reporting/` -> no `sort_keys=True` hash-input call
   outside the one that goes through `config_hash.canonical_json`.
</verification>

<success_criteria>
- A clean recorded log shows `Verified OK` on a non-zero turn count.
- A single-bit tamper in each of `state`/`move`/`intent`/`nonce` shows FAILED naming the turn.
- An empty or zero-turn log shows an explicit nothing-to-verify state, never `Verified OK`.
- The fresh-`json.dumps` revert probe produces a recorded number of FALSE FAILED verdicts.
- The verdict function has a named production caller.
</success_criteria>

<non_goals>
Screenshots and aesthetic judgement (07-10). Any change to `commit_pack.py`, `config_hash.py` or
the `log_` artifact's shape. Reading the JSONL or the ledger from the viewer.
</non_goals>

<output>
Create `.planning/phases/07-reporting-and-visualization-shell/07-08-SUMMARY.md` with the two revert
probes' counts (especially the number of false FAILED verdicts the second serializer produced), the
verified turn count from a real `dev_launch` game, and the production-caller list.
</output>
