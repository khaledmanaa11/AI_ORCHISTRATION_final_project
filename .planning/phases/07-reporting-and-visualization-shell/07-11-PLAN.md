---
phase: 07-reporting-and-visualization-shell
plan: "11"
type: execute
wave: 2
depends_on: ["07-03"]
blocks: ["07-06", "07-08"]
files_modified:
  - src/pursuit/strategy/beliefadapter.py
  - src/pursuit/sdk/view_builder.py
  - src/pursuit/sdk/local_view.py
  - config/police/belief.json
  - tests/unit/sdk/local_view_fixtures.py
  - tests/unit/sdk/test_local_truth_recovery.py
autonomous: true
gap_closure: true
requirements: [REPORT-02, STRAT-05, QUAL-11]

must_haves:
  truths:
    - "THE COP'S BELIEF IS NOT A BELIEF -- IT IS THE ENGINE'S GROUND TRUTH, AND PUBLISHING IT IS AN ABSOLUTE DISQUALIFICATION. Rules 8-9. Measured end to end through the shipped path with the real `config/police/*.json`: `turn_language.py:57` returns `ctx.state.thief` as `known_cell` on every turn>0; `choose_destination` (`turn_language.py:86-88`) passes it to `BeliefAdapter.decide`; `beliefadapter.py:126-128` calls `observe_exact(known_cell)` AND `emit_opponent(known_cell)`; `belief.py:57` sets a hard 1.0/0.0 delta on that cell. Probe output: `ctx.state.thief (engine truth): (5, 3)` / `known_opponent_cell returned: (5, 3)` / `identical?: True`."
    - "THE OBVIOUS FIX IS A TRAP AND IT WAS MEASURED. Deleting `BeliefView.argmax` makes the shipped scanner `local_view_fixtures.coordinate_hits` return `hits with argmax REDACTED: []` -- a clean verdict with the leak fully intact. The true cell stays uniquely recoverable from `belief.rows` BY GEOMETRY: `observe_exact` gives a delta, `belief_motion.spread:52-66` disperses it only over that cell's legal destinations, and `belief.py:80` multiplies pointwise so a zeroed cell never reopens -- so the published support is exactly the legal-move PLUS centred on the true pre-move cell, and the centre of a plus is uniquely recoverable. Measured: support `[(4,3),(5,2),(5,3),(5,4),(6,3)]`, 5 of 49, P(true)=0.5556, inversion returned `[(5,3)] == truth? True`. **A fix validated by a coordinate-absence test will look successful and will not be.**"
    - "`scent.opponent` LEAKS INDEPENDENTLY, so fixing only the belief is a half fix. `emit_opponent(known_cell)` stamps the kernel at weight 1.0 and `view_builder.py:127` publishes it dense. Measured: `scent.opponent argmax: (5, 3) == truth? True value 0.9` -- and 0.9 is EXACTLY `\"source\": 0.9` in `config/police/scent.json`, i.e. the unmixed kernel centre on the true cell, not a decayed trace. Because decay is a uniform scalar, two consecutive published snapshots subtract to recover the fresh deposit, so even an animate-only GUI leaks every turn."
    - "THE SEALED-THIEF ENDGAME PUBLISHES A ONE-PIXEL HEATMAP ON THE TRUTH, AND IT IS THE COP'S WIN CONDITION, NOT A CORNER CASE. When the thief's only legal action is STAY, `spread` is the identity and the delta never disperses. Measured with the thief at (0,0) behind barriers {(0,1),(1,0)}: `argmax: (0, 0)` / `entropy: -0.0` / `lit cells: [(0, 0, 1.0)]`. That cost 2 barriers against `\"barrier_quota\": 14` in `config/police/game_params.json`, so it WILL occur in league games."
    - "COP SEAT ONLY -- DO NOT DEGRADE THE THIEF. `turn_language.py:58` returns `ctx.pending_cop_action.move`, which is None at the thief's decide point on EVERY turn under commit-reveal (`turn_language.py:47-55` states this; `\"commit_reveal\": true` in BOTH `config/police/security.json` and `config/thief/security.json`). The thief never calls `observe_exact`, so its belief is genuinely multi-modal and legal to draw. A symmetric fix would destroy the one panel that is honestly impressive."
    - "NOBODY OWNED THIS BECAUSE BOTH LAYERS DELEGATED IT. `beliefadapter.py:120-123` says in its own docstring that state keeps carrying the true joint position because \"rule 3's 'local truth' is a display-layer concern, not this one's\" -- the strategy layer explicitly handed rule 9 to the display layer, and the display layer published the value unredacted. This plan makes ONE layer own it, in writing. The provenance is protocol-legitimate (the thief's own honest Reveal, folded in by `turn_resolve`) -- rule 9 governs the DISPLAY, not the provenance, so the strategy layer keeps using what it legitimately learned."
    - "`scripts/check_local_truth.py` CANNOT EVER CATCH THIS and must not be cited as evidence about these panels. It checks forbidden imports and `<x>.state.{cop,thief,barriers}` attribute chains, scoped to `src/pursuit/gui/`. Verified by running the real gate via its own `find_violations(root=...)` against a synthetic panel that markers `belief.argmax` and labels the `scent.opponent` peak: `violations: []`, `OK: 1 module(s) scanned`, `exit code: 0`. It is an import/attribute gate, not a disclosure gate."
    - "THE FIREWALL'S OWN FIXTURES WERE VACUOUS, so 07-03's thoroughness proves less than it appears. `local_view_fixtures.scent_field` (`:71-76`) never calls `emit_opponent`, so `view.scent.opponent` was an ALL-ZERO grid in every one of 07-03's 30 probes -- the single field carrying the true cell at 0.9 strength was empty in every test written to protect it. `honest_context` (`:93`) likewise does not seed belief the way production does. Fix the fixtures FIRST; every assertion in this area is untrustworthy until they model production."
  artifacts:
    - path: src/pursuit/strategy/beliefadapter.py
      provides: "the root-cause fix: what is published is no longer what observe_exact seeded"
      contains: "the chosen option (a) or (b), with the reason written down"
    - path: tests/unit/sdk/test_local_truth_recovery.py
      provides: "a RUNTIME recovery test, not another absence test"
      contains: "geometric inversion, scent argmax, support size and entropy floor -- all four"
    - path: tests/unit/sdk/local_view_fixtures.py
      provides: "fixtures that model production instead of an all-zero grid"
      contains: "emit_opponent actually called; belief seeded as production seeds it"
  key_links:
    - from: src/pursuit/strategy/beliefadapter.py
      to: src/pursuit/sdk/view_builder.py
      via: "the display belief is a different object from the strategy belief, and only one is published"
      pattern: "observe_exact"
---

<objective>
Stop the cop's GUI from displaying the thief's exact cell.

Purpose: rules 8-9 are an absolute disqualification, this was reproduced end to end through the
shipped code, and both the CI gate and every existing firewall test pass while it happens.
Output: a display belief that is genuinely a belief, and a test that proves it by trying to
invert it.
</objective>

<context>
Found by an adversarial pass over 07-03's firewall, asking the question the coordinate scanner
structurally cannot answer: can the true position be RECOVERED from what a rules-compliant GUI
would draw? It can.

- `07-03-SUMMARY.md` -- the syntactic firewall this builds on. Its `LeakyLocalView` control is
  good and stays; it simply tests a different question.
- `beliefadapter.py:120-134`, `belief.py:57,80`, `belief_motion.spread:52-66`,
  `view_builder.py:127`, `turn_language.py:47-58` -- the measured chain.
- **07-06 (live GUI) and 07-08 (replay viewer) MUST NOT execute before this lands.**
</context>

<tasks>

<task type="auto">
  <name>Task 1: make the fixtures model production, then reproduce the recovery</name>
  <files>tests/unit/sdk/local_view_fixtures.py, tests/unit/sdk/test_local_truth_recovery.py</files>
  <action>
    Fix the fixtures FIRST -- `scent_field` must actually call `emit_opponent`, and
    `honest_context` must seed belief the way production does. Then write the RED recovery test
    against a `LocalView` built through the PRODUCTION path (a real `decide(..., known_cell=
    ctx.state.thief)`), asserting all four:
      1. `belief.argmax != ctx.state.thief`;
      2. argmax of `scent.opponent` != `ctx.state.thief`;
      3. **the geometric inversion**: the belief support has no unique cell within L1<=1 of every
         support cell equal to `ctx.state.thief`;
      4. support size and published entropy above a floor.
    Plus the sealed-thief case (thief walled at a corner with 2 barriers). Record every failure
    verbatim -- these are the numbers the SUMMARY rests on.
  </action>
  <verify>All four fail at HEAD, and the sealed case shows entropy -0.0 with one lit cell.</verify>
  <done>The leak is reproduced in this repository's own numbers, on fixtures that model production.</done>
</task>

<task type="auto">
  <name>Task 2: fix the root cause, and write down which option you chose</name>
  <files>src/pursuit/strategy/beliefadapter.py, src/pursuit/sdk/view_builder.py, config/police/belief.json</files>
  <action>
    Fix at `beliefadapter.py:126-128`, NOT at the view. Decide explicitly between:
      (a) a display-only `BeliefMap` never fed `ctx.state.thief` -- fed only from hints and from
          scent attributable to declared reveals -- published in place of the strategy belief; or
      (b) publish the belief only when `observe_exact` did not fire this turn, rendering a stated
          placeholder otherwise.
    Record WHICH and WHY in the source and the SUMMARY. The strategy layer has already delegated
    rule 9 to the display layer (`beliefadapter.py:120-123`); if this plan also declines it, nobody
    owns it.

    `scent.opponent` must be fixed too -- it leaks independently of the belief. Either drop it from
    the cop's view or emit it from the display belief.

    Any entropy/support floor is a CONFIG value, never a literal (CLAUDE.md rule 1).
    **Do not touch the thief's path.** Prove it: the thief's published belief must be byte-identical
    before and after.
  </action>
  <verify>Task 1's tests go green; `check_line_limit.sh` exit 0, new files checked explicitly by path.</verify>
  <done>What is published is no longer what observe_exact seeded.</done>
</task>

<task type="auto">
  <name>Task 3: correct the three false docstrings, and prove the thief is untouched</name>
  <files>src/pursuit/sdk/local_view.py</files>
  <action>
    Three docstrings currently assert the opposite of the measured behaviour and will mislead both
    the next reader and the grader: `:13-14` ("cannot express an opponent's true cell"), `:51-52`
    ("argmax ... is routinely wrong; that is exactly why it is legal to draw"), and `:62-64`
    ("our own RECONSTRUCTION ... not a live reading of where it is now"). Correct them to what is
    true after Task 2, and say what they used to claim.

    Add the thief-unchanged control, and revert-probe the fix with real failure counts -- including
    the argmax-only "fix", which must still FAIL the geometric test.
  </action>
  <verify>The argmax-only probe fails the geometric assertion; the thief control passes byte-identical.</verify>
  <done>The record matches the behaviour, and the fix is provably not the fake one.</done>
</task>

</tasks>

<verification>
1. `uv run ruff check .` -> 0 violations.
2. `uv run pytest tests/ --cov` -> all pass, coverage >= 85% and not below the inherited baseline.
3. `bash scripts/check_line_limit.sh` -> exit 0, plus every new file explicitly by path.
4. `uv run python scripts/check_no_llm_in_strategy.py` -> OK.
5. Counters: full suite advances `config/{police,thief}/games_played.json` by ZERO; one real game
   by exactly ONE. Quote all four numbers.
6. `uv run python scripts/dev_launch.py` -> exit 0, both sides `audit_verdict matched=true`.
</verification>

<success_criteria>
- The true thief cell is NOT recoverable from a published cop `LocalView` -- by argmax, by scent,
  or by geometric inversion of the belief support.
- The sealed-thief endgame does not publish a one-pixel heatmap on the truth.
- The thief's published belief is byte-identical before and after.
- The argmax-only "fix" provably still fails.
- Three false docstrings corrected.
</success_criteria>

<non_goals>
Hardening `check_local_truth.py` against attribute-chain indirection (alias/getattr/asdict), the
`rglob('*.py')` blind spot for `.pyw`, and the empty-`__init__.py` anti-vacuity hole. All real,
all filed, none of them the disqualifying leak -- which flows through fields the gate reads as
legal. 07-06 gets them as instructions.
</non_goals>

<output>
Create `.planning/phases/07-reporting-and-visualization-shell/07-11-SUMMARY.md` with the
reproduced recovery, the option chosen and why, the argmax-only counter-probe, the thief-unchanged
control, and the four counter numbers.
</output>
