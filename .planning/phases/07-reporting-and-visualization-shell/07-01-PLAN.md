---
phase: 07-reporting-and-visualization-shell
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pursuit/shared/gatekeeper_params.py
  - src/pursuit/shared/reporting_config.py
  - src/pursuit/services/llm/gatekeeper.py
  - src/pursuit/services/reporting/__init__.py
  - src/pursuit/services/reporting/quota.py
  - src/pursuit/services/reporting/dos.py
  - src/pursuit/services/reporting/chain.py
  - config/police/reporting.json
  - config/thief/reporting.json
  - tests/unit/test_gatekeeper_params.py
  - tests/unit/test_reporting_quota.py
  - tests/unit/test_reporting_dos.py
  - tests/unit/test_reporting_chain.py
  - tests/unit/test_reporting_config.py
autonomous: true
requirements: [REPORT-02, REPORT-03, REPORT-04]

must_haves:
  truths:
    - "THE OUTLINE'S LINE BUDGET FOR THIS PLAN IS WRONG AND THE EXTRACTION CANNOT LAND IN PLACE. 07-PLAN-OUTLINE.md Sec5 says '`gatekeeper.py` is at 89 counted lines with room for the `GatekeeperParams` extraction'. MEASURED 2026-08-17 with the gate's own awk (`MAX_LINES=134 bash scripts/check_line_limit.sh src/pursuit/services/llm/gatekeeper.py` -> VIOLATION at 135): `gatekeeper.py` is **135/150**, `shared/language_config.py` is **139/150**. Fifteen and eleven lines of headroom. `GatekeeperParams` therefore lands in a NEW file, `shared/gatekeeper_params.py` -- split, never compress (Segal Table 5). Re-measure both hosts before every commit."
    - "MAKING `budget` OPTIONAL IS AN EDIT TO THE LLM PATH, AND ONE REORDERING SILENTLY BREAKS D-35. Measured: `gatekeeper.py:112` calls `self.budget.reserve(estimated_tokens)` UNCONDITIONALLY and BEFORE the queue-depth check at `:113-116`, and `settle()` at `:126` runs only on the success path. So today an overflow (`GatekeeperOverflow`) and a retry-exhausted call BOTH leave `_reserved` permanently inflated, which is exactly D-35's stated intent -- `budget.py:8-12` and `gatekeeper.py:14-16` say 'the degrade level reflects every QUEUED call immediately, not just completed ones'. An executor who 'tidies' the None-guard by moving `reserve()` below the overflow check makes the degrade ladder count completed calls only. NO EXISTING TEST SAYS SO. Keep the LLM instance's statement order provably identical and pin it with a test that asserts `_reserved` survives a `GatekeeperOverflow`."
    - "OQ-1 IS RESOLVED AND NO DAILY NUMBER IS WRITTEN (07-PLAN-OUTLINE.md Sec5, verbatim). `docs/SEGAL_GUIDELINES.md:173-174` supplies `requests_per_minute: 30`, `requests_per_hour: 500`, `max_retries: 3` for the generic API gatekeeper. Those are sourced and are what `QuotaManager` enforces. **No document anywhere gives a daily figure, so none is written.** If the chain must express a daily bound at all it is DERIVED as `24 x requests_per_hour` and labelled derived in both config and source -- never presented as a book value. No engineering default is used here."
    - "OQ-2 IS RESOLVED STRUCTURALLY AND `DosDetector` CONTAINS ZERO NUMBERS (07-PLAN-OUTLINE.md Sec5, verbatim): 'the detector latches when the token bucket has been continuously empty across `retries_before_failure` + 1 attempts. Rule 29 is satisfied, and nothing is invented. Prefer this over a labelled default whenever a structural definition exists -- a number nobody can source is a number nobody can defend.' `retries_before_failure` is Table 19 row 4 (`docs/PARAMETERS.md:96`, minimum 3), already loaded and floor-checked."
    - "OQ-3 IS RESOLVED FOR THE MAIL INSTANCE ONLY (07-PLAN-OUTLINE.md Sec5, verbatim): backoff **30 s** for the new mail gatekeeper -- `docs/PARAMETERS.md:95` Table 19 row 3 gives 5 s as a MINIMUM (negotiable upward, never downward), `docs/SEGAL_GUIDELINES.md:174` gives `retry_after_seconds: 30`, and `:182` instructs 'where the two documents differ, take the stricter value'. Concurrency stays **2**: `PARAMETERS.md:94` gives 2 as a minimum, SEGAL gives `concurrent_max: 5`, and fewer concurrent requests is the stricter reading. **PHASE 4's LLM PATH IS UNTOUCHED** -- `config/{police,thief}/language.json` keeps `wait_after_error_seconds: 5` and `parallel_requests: 2`, measured on disk today, and this plan must not edit either file."
    - "THE BUCKET IS PRIVATE, SO THE DOS OBSERVATION SEAM IS A REAL DESIGN STEP, NOT A DETAIL. `Gatekeeper._bucket` (`gatekeeper.py:86`) is name-mangled-by-convention private and `_call_with_retry` (`:143-146`) is the only reader. `DosDetector` sits OUTSIDE `submit()` (D-69) and cannot see it. Add ONE read-only public seam on `Gatekeeper` exposing bucket readiness, measure the 15 lines of headroom first, and split rather than compress if it does not fit. Do NOT reach into `_bucket` from `chain.py`."
    - "A REFUSAL IS NEVER A CRASH AND NEVER A BARE REJECTION (rules 28-29, SEGAL Sec4:175-176: 'On overflow: FIFO queue, not rejection and not a crash'). Quota exhaustion, a latched DOS lock and a retry-exhausted send all return a caller-handled outcome in `GatekeeperOverflow`'s documented mould (`gatekeeper.py:43-48`: 'Callers MUST catch this ... it is not a crash and must never propagate unhandled into the turn loop'). The send queue's depth is Table 19 row 5 (`PARAMETERS.md:97`, minimum 100), read from `reporting.json`. The drain is an explicit caller-driven method -- never a background thread, which would be a second liveness owner beside the freeze watchdog."
    - "THE QUOTA COUNTER IS DURABLE ACROSS A PROCESS RESTART and reuses `shared/durable_write.py`'s existing write/rotate/replace + `.prev` fallback pair (`durable_write.py:34-57,73-93`), the same crash-safe scheme `step0_collect.record_game_played` and `QTable.save()` already use. No second persistence scheme (QUAL-02)."
    - "ZERO NEW BOOK NUMBERS. `reporting.json` re-declares the Table-19 rows so the mail gatekeeper is CONFIGURED, not hardcoded (SEGAL Sec4:172), and adds structural strings only: `mode` (`dry_run`|`live`), the recipient, the artifact directory, and credential/token env-var NAMES. Every numeric leaf is floor-checked against the same Table-19 minima `shared/language_config.py:84-90` already enforces."
  artifacts:
    - path: src/pursuit/shared/gatekeeper_params.py
      provides: "the seven Table-19 rows as one type both LanguageParams and ReportingParams satisfy"
      contains: "no numeric literal -- floors stay where language_config.py already keeps them"
    - path: src/pursuit/services/llm/gatekeeper.py
      provides: "one class, two instances: budget injected and optional, LLM call order unchanged"
      contains: "a public read-only bucket-readiness seam for DosDetector; reserve() still before the queue-depth check"
    - path: src/pursuit/services/reporting/chain.py
      provides: "the Fig-13 composition: QuotaManager -> Gatekeeper.submit [bucket inside] -> DosDetector -> sink"
      contains: "every refusal a caller-handled outcome; a bounded FIFO send queue with an explicit drain"
    - path: config/police/reporting.json
      provides: "the mail instance's configured limits and structural strings"
      contains: "wait_after_error_seconds 30 (OQ-3), parallel_requests 2, queue_depth 100, requests_per_hour 500 labelled to its SEGAL source"
  key_links:
    - from: src/pursuit/services/reporting/chain.py
      to: src/pursuit/services/llm/gatekeeper.py
      via: "the chain wraps the ONE gatekeeper class rather than building a second one (D-68, SEGAL Sec4:179-182)"
      pattern: "Gatekeeper"
---

<objective>
Make one gatekeeper class serve both the LLM and the mail path, with the book's Figure-13 stages
in front of it and every limit read from config.

Purpose: rules 28-29 require a token bucket and a DOS detector on the outgoing mail path, and
SEGAL Sec4 forbids a second gatekeeper -- so this is an extension of shipped, tested Phase-4 code,
which is precisely what makes it risky.
Output: `GatekeeperParams`, an injected-optional budget, `QuotaManager`, `DosDetector`, `chain.py`,
`reporting.json` and its loader -- with Phase 4's LLM behaviour provably byte-identical.
</objective>

<context>
- `07-PLAN-OUTLINE.md` Sec2 (D-68, D-69), Sec3, Sec5 (OQ-1/OQ-2/OQ-3 resolutions) -- carry them
  verbatim, do not re-derive.
- `src/pursuit/services/llm/gatekeeper.py` -- read the whole module docstring first; it already
  anticipates this plan ("Phase 7 hangs Gmail off this SAME class").
- `src/pursuit/shared/language_config.py` -- the loader convention: a `*Key` str-Enum beside its
  own loader, a frozen params dataclass, floors named in a module-level table, fail-loud errors
  naming the offending key. `reporting_config.py` follows it exactly.
- `docs/SEGAL_GUIDELINES.md:158-182` -- the generic gatekeeper contract and the stricter-value rule.
- `src/pursuit/shared/durable_write.py` -- the only persistence scheme this plan may use.
</context>

<tasks>

<task type="auto">
  <name>Task 1: measure first, then extract GatekeeperParams into its own file</name>
  <files>src/pursuit/shared/gatekeeper_params.py, src/pursuit/services/llm/gatekeeper.py, tests/unit/test_gatekeeper_params.py</files>
  <action>
    FIRST, measure and record: run `MAX_LINES=1 bash scripts/check_line_limit.sh` against
    `gatekeeper.py`, `language_config.py` and `language_wiring.py` and quote the three counts in
    the commit message. They decide where code may land.

    Extract the seven Table-19 fields into `shared/gatekeeper_params.py` as the type both
    `LanguageParams` and the new `ReportingParams` satisfy. `load_language_config()`'s public
    return type does not change and `config/*/language.json` is not edited. Carry NO numeric
    literal into the new file -- the floors stay in `language_config.py:84-90`.

    Then retype `Gatekeeper.__init__`'s `params` and make `budget` an injected optional. The LLM
    instance must still hold a real `TokenBudget` with the same three thresholds under the same
    attribute name `gatekeeper.budget` (`language_wiring.py:160` builds it, `bluff` refreshes
    `degrade_level` off `budget.level`). Add ONE read-only public seam reporting whether the
    bucket can admit now -- `DosDetector` needs it and `_bucket` is private.
  </action>
  <verify>Every pre-existing Phase-4 test passes UNMODIFIED. `bash scripts/check_line_limit.sh` exit 0, plus each new file checked explicitly by path (the no-arg form enumerates via `git ls-files` at `check_line_limit.sh:18` and passes vacuously on untracked files).</verify>
  <done>One class, two shapes of caller, and Phase 4 cannot tell the difference.</done>
</task>

<task type="auto">
  <name>Task 2: pin the call order that has no test today</name>
  <files>tests/unit/test_gatekeeper_params.py</files>
  <action>
    D-35's contract is unpinned. Add the tests that would have caught a reordering:
    (a) a call that raises `GatekeeperOverflow` has ALREADY reserved -- the degrade level reflects
        it, and a later successful call does not un-reserve it;
    (b) a call whose retries exhaust raises the last exception and likewise never settles;
    (c) a mail-shaped call with `budget=None` and `input_tokens=output_tokens=0` completes and
        touches no `TokenBudget` at all (assert via a budget double that fails the test if ANY
        method is called);
    (d) the bucket admits floor(C) immediate sends then blocks, and `time_until_available()`
        equals `(1 - tokens) / r`.

    REVERT PROBE, recorded with real failure counts: move `reserve()` below the queue-depth check
    and record which of (a)-(d) fail. If none fail, the tests are vacuous and are not done.
    Injected clock and injected `sleep` (`gatekeeper.py:81-82`) -- zero real sleeps.
  </action>
  <verify>The revert probe fails with a named, counted failure. Every assertion names a measured value, not a shape.</verify>
  <done>The order that D-35 depends on can no longer be changed silently.</done>
</task>

<task type="auto">
  <name>Task 3: reporting.json + its loader, with every number traced</name>
  <files>src/pursuit/shared/reporting_config.py, config/police/reporting.json, config/thief/reporting.json, tests/unit/test_reporting_config.py</files>
  <action>
    Follow `shared/language_config.py`'s four-part convention exactly: `ReportingKey` str-Enum,
    frozen `ReportingParams`, a module-level minima table naming each Table-19 row, fail-loud
    `KeyError`/`TypeError`/`ValueError` naming the offending key.

    Numeric leaves: the Table-19 rows, with `wait_after_error_seconds` = 30 (OQ-3) and
    `parallel_requests` = 2, plus `requests_per_hour` = 500 carrying an explicit in-file comment
    naming `docs/SEGAL_GUIDELINES.md:173` as its source. No daily leaf (OQ-1).

    Structural leaves: `mode` (`dry_run` by default in BOTH shipped configs -- rule 31/39-40
    hazard otherwise), the recipient (exactly `rmisegal+uoh26finalgame@gmail.com`, `PARAMETERS.md:176`),
    the artifact directory, and the credential/token env-var NAMES (never values, never paths in
    git -- rules 39-40).

    **Decide and record the artifact directory deliberately.** Measured: `.gitignore` ignores
    `logs/` wholesale while claiming in its own comment that the four required artifacts 'MUST be
    committed'; `agent_step0_wiring.write_declaration` writes `declaration_<game_id>.json` into
    `logs/<role>/`, which is therefore unreachable to git today. Pick a directory that is NOT
    ignored, state why in the loader docstring, and record the contradiction for 07-02.
  </action>
  <verify>A config missing any key, or setting any minimum row below its Table-19 floor, raises and names the key. Both shipped files load. Both ship `dry_run`.</verify>
  <done>Every mail limit is configured, sourced, and floor-checked.</done>
</task>

<task type="auto">
  <name>Task 4: QuotaManager, DosDetector, and the Fig-13 chain</name>
  <files>src/pursuit/services/reporting/__init__.py, src/pursuit/services/reporting/quota.py, src/pursuit/services/reporting/dos.py, src/pursuit/services/reporting/chain.py, tests/unit/test_reporting_quota.py, tests/unit/test_reporting_dos.py, tests/unit/test_reporting_chain.py</files>
  <action>
    `quota.py` -- hourly ceiling from config, counted durably through `durable_write_json` /
    `load_json_with_fallback`, with the window boundary read off an INJECTED clock. Exhaustion
    returns a named refusal.

    `dos.py` -- the structural latch (OQ-2): observe each attempt's bucket readiness through the
    Task-1 seam; latch when the bucket has been continuously empty across
    `retries_before_failure` + 1 consecutive attempts. **Latching means latching** -- rule 29's
    sanction is an interface lock, so it never auto-clears within a process. Zero numeric literals
    in this module.

    `chain.py` -- the book's order (D-69): `QuotaManager` gate -> `Gatekeeper.submit()` (bucket
    inside, untouched) -> `DosDetector` observation -> sink. A refusal at any stage, and a
    retry-exhausted `submit()`, enqueue onto a FIFO bounded at `queue_depth` and return a
    caller-handled outcome. Expose an explicit `drain()`; start no thread and no task.
  </action>
  <verify>
    Quota exhaustion survives a simulated process restart (write, construct a fresh manager over
    the same path, assert the count). A latched lock refuses and stays latched. A permanently
    failing sink enqueues rather than raising. Nothing in this package raises out of the caller
    on any refusal path -- assert with `pytest.raises` NOT firing, and pair it with a control that
    proves the assertion can fail.
  </verify>
  <done>The three Figure-13 stages exist, are configured, and refuse without crashing.</done>
</task>

</tasks>

<verification>
1. `uv run ruff check .` -> 0 violations.
2. `uv run pytest tests/ --cov` -> all pass, coverage >= 85% and not below the inherited baseline.
   Every pre-existing Phase-4 gatekeeper/budget/bucket test passes UNMODIFIED -- if one needed an
   edit, the LLM path changed and that is a finding, not a fix.
3. `bash scripts/check_line_limit.sh` -> exit 0, PLUS every new file checked explicitly by path.
4. `uv run python scripts/check_no_llm_in_strategy.py` -> OK.
5. `uv run python scripts/dev_launch.py` -> exit 0, both sides `audit_verdict matched=true`, zero
   `technical_win`. The LLM path is expected to behave identically; say so with the run's numbers.
6. `git diff config/police/language.json config/thief/language.json` -> EMPTY.
</verification>

<success_criteria>
- A mail-shaped call with zero tokens completes without touching any `TokenBudget`.
- The LLM instance's reserve/settle order is pinned by a test with a recorded revert probe.
- Quota exhaustion and a latched DOS lock each return a caller-handled refusal; neither raises.
- The daily quota counter survives a simulated process restart.
- Zero invented numbers; OQ-1/OQ-2/OQ-3 carried verbatim with their citations.
</success_criteria>

<non_goals>
Any change to `config/*/language.json`, to Phase-4 LLM behaviour, or to `bucket.py`. Any mail
sending -- the sink arrives in 07-04. Setting or reading the games-played value (07-10).
</non_goals>

<output>
Create `.planning/phases/07-reporting-and-visualization-shell/07-01-SUMMARY.md` with the three
measured line counts, the Task-2 revert-probe failure counts, and the artifact-directory decision
with its reasoning.
</output>
