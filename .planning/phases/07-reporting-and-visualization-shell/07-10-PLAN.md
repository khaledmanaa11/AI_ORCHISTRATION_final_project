---
phase: 07-reporting-and-visualization-shell
plan: "10"
type: execute
wave: 5
depends_on: ["07-09"]
files_modified:
  - docs/phases/phase-7/GATE-7-MEASUREMENT.md
  - docs/phases/phase-7/GAMES-PLAYED-RECONSTRUCTION.md
  - docs/phases/phase-7/live-send-evidence/
  - docs/phases/phase-7/screenshots/
  - docs/phases/phase-7/PRD.md
  - docs/phases/phase-7/PLAN.md
  - docs/phases/phase-7/TODO.md
  - docs/TODO.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [REPORT-01, REPORT-08, REPORT-09, SEC-07]

must_haves:
  truths:
    - "TWO THINGS IN THIS PHASE GENUINELY REQUIRE A HUMAN AND CLAUDE MUST DO NEITHER: clicking through Google's OAuth consent screen and authorising the send-only client, and the one live send that proves Sec10.4 criterion 1 end to end. **Claude does not enter credentials and does not click consent.** A single live send from an unattended run is both a rules 39-40 hazard and unsolicited mail to the lecturer. Every config this repository ships reads `reporting.mode = dry_run`; only this checkpoint flips one, and it flips it back."
    - "THE GAMES-PLAYED VALUE IS DECIDED HERE, BEFORE ANY SEND, AND IT IS NOT CLAUDE'S TO CHOOSE (OQ-5). Rule 38 (`docs/RULES.md:79`): 'Falsely declare the number of games played -> **Absolute disqualification** for an ethical and integrity breach.' 07-00 closed the MECHANISM -- measured there: `config/police/games_played.json` read 1881 and `config/thief/` 1874, one `uv run pytest tests/` run advanced each by exactly 14, and two agents that only ever played each other stood 7 apart; after the fix a full suite advances them by 0/0 and one real game by 1/1. The VALUE is a judgement about how the USER is represented to the league, wrong in either direction is the same breach, and the counter file is gitignored so git history cannot settle it. `docs/phases/phase-7/GAMES-PLAYED-RECONSTRUCTION.md` states 'No option is selected here.' -- this checkpoint selects one, in writing, with its reasoning."
    - "CRITERION 1 FLIPS FROM PENDING ONLY ON DELIVERED-MESSAGE EVIDENCE. 07-09 wrote `GATE-7-MEASUREMENT.md` with criterion 1 as `dry_run` PASS + live PENDING and named exactly what must be attached. If the live send does not happen or does not arrive, the criterion STAYS PENDING and this plan ticks nothing -- `docs/phases/phase-5/GATE-5-MEASUREMENT.md` retaining Attempt 1's honest failure beside Attempt 2 is this project's own precedent, and a gate reading PASS without evidence is what rule 38 exists for."
    - "THE SCREENSHOTS ARE THE ONLY AESTHETIC JUDGEMENT IN THE PHASE, WHICH IS WHY THEY ARE HERE. Rule 42 (`RULES.md:83`) requires the academic report to include images; book Sec9.4.2 item 5 names them. Everything else about both GUIs is machine-checked by 07-06 and 07-08 and already measured by 07-09, so nothing but this waits on a person."
    - "THIS IS A SHORT SCRIPTED SESSION, NOT A FISHING TRIP. `OAUTH-RUNBOOK.md` (written by 07-09) is followed start to finish; four items, in order; anything found that is not on the list is recorded as a gap and not fixed here."
    - "SECRET DISCIPLINE APPLIES TO THE EVIDENCE, NOT JUST THE CODE (rules 39-40). `.gitignore` already carries `credentials.json`, `client_secret*.json`, `token.json`, `token.pickle`, `*.token` and `.env` with a comment stating they exist before the OAuth code does. Before committing ANY console capture, screenshot or evidence file, scan it for the client secret, the refresh token, the shared secret, `ANTHROPIC_API_KEY` and the ngrok authtoken -- consoles and screenshots are exactly where a token leaks -- and record in the SUMMARY what was redacted and where."
  artifacts:
    - path: docs/phases/phase-7/GATE-7-MEASUREMENT.md
      provides: "criterion 1 answered with delivered-message evidence, or retained as PENDING with the reason"
      contains: "the live-send record beside 07-09's dry_run measurement, neither overwriting the other"
    - path: docs/phases/phase-7/GAMES-PLAYED-RECONSTRUCTION.md
      provides: "the OQ-5 decision, selected and justified by the human"
      contains: "which reading was chosen, what it counts, and the value actually written to both counters"
    - path: docs/phases/phase-7/screenshots/
      provides: "the two README assets (Sec9.4.2 item 5, rule 42)"
      contains: "the live GUI, and the replay viewer showing Verified OK"
  key_links:
    - from: docs/phases/phase-7/GAMES-PLAYED-RECONSTRUCTION.md
      to: docs/RULES.md
      via: "rule 38 makes this value's honesty an absolute-disqualification question, which is why a human sets it"
      pattern: "games_played"
---

<objective>
Do the two things Claude must not do, once, with the evidence filed -- and set the one number in
this project whose sanction is absolute disqualification.

Purpose: Sec10.4 criterion 1's live half, the README's two images, and OQ-5 are the entire
human-gated surface of Phase 7; nothing else in the phase waits on a person.
Output: a delivered report, two screenshots, a written OQ-5 decision, and a gate record that says
what actually happened.
</objective>

<execution_context>
@C:\Users\Hp\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Hp\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@docs/phases/phase-7/OAUTH-RUNBOOK.md
@docs/phases/phase-7/GATE-7-MEASUREMENT.md
@docs/phases/phase-7/GAMES-PLAYED-RECONSTRUCTION.md

- `07-PLAN-OUTLINE.md` Sec7 (07-10 and its trap), Sec5 (OQ-5 stays with the human).
- `.planning/phases/07-reporting-and-visualization-shell/07-00-SUMMARY.md` -- what was measured, and
  why the value was deliberately not set there.
- `.planning/phases/05-cloud-exposure-and-tunneling/05-08-PLAN.md` -- the checkpoint-plan precedent
  this one follows.
- `docs/PARAMETERS.md:170-179` -- the reporting address, and that it is the single mandatory
  destination.
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: OAuth consent, one live send, two screenshots, and the OQ-5 decision</name>
  <files>docs/phases/phase-7/live-send-evidence/, docs/phases/phase-7/screenshots/</files>
  <action>
    HUMAN-RUN. Claude executes nothing in this task. It must not enter credentials, must not click
    consent, must not run a live send, and must not simulate, approximate or substitute any of it.
    Present the <how-to-verify> steps to the operator, then WAIT for the resume signal.
  </action>
  <what-built>
    The entire reporting stack is built and measured against `dry_run` with an injected fake Google
    client: the Figure-13 chain around the one gatekeeper, the four artifacts, token accounting,
    the rule-35 agreement record, 429 backoff, attachment shaping, the local-truth read model with
    its CI gate, the live dashboard as a separate process, and the verifying replay viewer.
    `uv run python scripts/measure_gate7.py` already reports criteria 2 and 3 PASS and criterion 1
    as `dry_run` PASS + live PENDING.
  </what-built>
  <how-to-verify>
    Follow `docs/phases/phase-7/OAUTH-RUNBOOK.md` end to end. Four items, in this order.

    **1. Authorise the send-only client.** In the Google Cloud console, create an OAuth client
    whose scope is `gmail.send` and nothing else (rule 30 -- a broader scope is 'a security breach
    that disqualifies the code'). Complete the consent screen yourself. Place the credential and
    token files at the paths named by the env vars in `config/<role>/reporting.json`. Confirm both
    paths are covered by `.gitignore` BEFORE the files exist -- `git status` must not list them.

    **2. Decide OQ-5 and write the counters. DO THIS BEFORE ANY SEND.** Read
    `docs/phases/phase-7/GAMES-PLAYED-RECONSTRUCTION.md`, choose one reading of "a game played",
    and record in that file: which reading, what it counts, what it excludes, and the value written
    to `config/police/games_played.json` and `config/thief/games_played.json`. Rule 38's sanction is
    absolute and applies to overstatement AND understatement. The two files may legitimately differ
    only if your reading says they should -- if it does not, make them equal and say so.

    **3. One live send.** Flip exactly ONE config to `reporting.mode = live`, run one game, and
    confirm the mail arrives at `rmisegal+uoh26finalgame@gmail.com` with the JSON **attached** and
    the body free of report content. Retain: the console output (stdout AND stderr, redirected to a
    file for the whole session), the sent `result_` artifact, and the received message's headers.
    **Then flip the config BACK to `dry_run`** and confirm with `git diff`.

    **4. The two screenshots.** Capture the live GUI mid-game and the replay viewer showing
    `Verified OK` (Sec9.4.2 item 5, rule 42). These are presentation-grade README assets -- your
    aesthetic call, which is why they are here and not in an automated plan. Record the refresh
    interval you launched the live GUI with (OQ-6).

    **What a PASS looks like -- check these before reporting back:**
    - the message ARRIVED at the reporting address, with the JSON as an attachment;
    - the OAuth client's scope is `gmail.send` and nothing else;
    - `git status` lists no credential, token or secret file;
    - both `reporting.json` files read `dry_run` again;
    - both counters carry the decided value, and the decision is written down.

    If any of these fails, that is a real result. Report exactly what happened with the retained
    evidence. Do not re-run until it looks good, and do not soften the criterion.
  </how-to-verify>
  <resume-signal>Report: the OQ-5 value and reading chosen; whether the live send arrived and with what evidence; the two screenshot paths; and anything that did not fit.</resume-signal>
  <verify>The operator reports back against the five PASS checks above, with the evidence directories populated.</verify>
  <done>Consent, one delivered report, two images and a written decision exist -- whatever the verdict.</done>
</task>

<task type="auto">
  <name>Task 2: record the result -- tick only what the evidence earns</name>
  <files>docs/phases/phase-7/GATE-7-MEASUREMENT.md, docs/phases/phase-7/GAMES-PLAYED-RECONSTRUCTION.md, docs/phases/phase-7/PRD.md, docs/phases/phase-7/PLAN.md, docs/phases/phase-7/TODO.md, docs/TODO.md, .planning/ROADMAP.md</files>
  <action>
    **SECRET SCAN FIRST**, before a single evidence file is staged: grep every retained console
    capture, header dump and artifact for the OAuth client secret, the refresh token, the shared
    secret, `ANTHROPIC_API_KEY` and the ngrok authtoken. Inspect the screenshots by eye for a
    visible token, address bar or console line. Redact any hit and say in the SUMMARY what was
    redacted and where. Commit evidence only after this passes.

    **UNCONDITIONAL** -- these describe what shipped and are written whatever the send did:
    - `docs/phases/phase-7/GAMES-PLAYED-RECONSTRUCTION.md` records the decision and its reasoning;
    - `docs/phases/phase-7/PRD.md` and `PLAN.md` record the phase's delivered shape, including the
      decisions made at plan time that the outline left open (D-76, the live GUI as a separate
      process) and the questions raised rather than invented (OQ-6, the GUI refresh interval);
    - the two screenshots are committed under `docs/phases/phase-7/screenshots/`.

    **CONDITIONAL -- only if the live send genuinely arrived:**
    - flip criterion 1 in `GATE-7-MEASUREMENT.md` from PENDING to PASS, with the delivered-message
      evidence linked, ADDING to 07-09's `dry_run` record rather than replacing it;
    - tick every row in `docs/phases/phase-7/TODO.md`, the matching Phase-7 rows in root
      `docs/TODO.md`, and the Phase 7 plan rows in `.planning/ROADMAP.md`. The three Sec10.4
      criteria themselves are quoted from the book and are NOT edited, ever.

    **If it did not arrive:** leave criterion 1 PENDING, record what happened with the evidence, and
    tick NOTHING. The unconditional half still ships.
  </action>
  <verify>
    No secret in any committed file. `GATE-7-MEASUREMENT.md` retains 07-09's `dry_run` record
    unchanged. If the send did not arrive, `git diff` shows ZERO checkbox transitions anywhere.
    Both `reporting.json` files read `dry_run`.
  </verify>
  <done>The record says what happened, the docs describe what shipped, and every tick is earned.</done>
</task>

</tasks>

<verification>
1. `uv run ruff check .` -> 0 violations.
2. `uv run pytest tests/ --cov` -> all pass, coverage >= 85%; this plan changes no source, so the
   figure must equal 07-09's closing number exactly -- any drift is a finding.
   **Then re-read both counter files and confirm the suite advanced them by ZERO** (07-00's
   invariant), so the value decided in Task 1 is still the value on disk.
3. `bash scripts/check_line_limit.sh` -> exit 0.
4. `uv run python scripts/check_no_llm_in_strategy.py` and `scripts/check_local_truth.py` -> OK.
5. `uv run python scripts/measure_gate7.py` -> exit 0; criteria 2 and 3 PASS.
6. `git ls-files docs/phases/phase-7/` lists the evidence and screenshots; `git status` lists no
   credential, token, `.env` or secret file.
</verification>

<success_criteria>
- The OAuth client is restricted to `gmail.send`, authorised by the human, with no credential in git.
- One live report arrived at `rmisegal+uoh26finalgame@gmail.com` with the JSON attached -- or the
  criterion stays PENDING and says exactly why.
- Two presentation-grade screenshots are committed.
- OQ-5 is resolved IN WRITING and the counters carry the decided value.
- Every shipped `reporting.json` reads `dry_run` again.
</success_criteria>

<non_goals>
Any code change. The repo split, the academic README, the Git tag and league play (Phase 8 --
this phase produces the screenshots and the report Phase 8 attaches). Re-running the send until it
looks better.
</non_goals>

<output>
Create `.planning/phases/07-reporting-and-visualization-shell/07-10-SUMMARY.md` recording the OQ-5
decision and its reasoning, the live-send evidence paths, what was redacted and where, the OQ-6
refresh interval used for the screenshot, and whether Phase 7 is ready for `/gsd:verify-work 7`.
</output>
