---
phase: 01-base-logic
plan: "03"
type: tdd
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - src/pursuit/shared/capture.py
  - src/pursuit/shared/outcome.py
  - tests/unit/test_capture.py
autonomous: true
requirements:
  - BASE-03
  - BASE-04
  - BASE-05
  - BASE-06
  - BASE-07

must_haves:
  truths:
    - "detect_capture with cop == thief returns Outcome.CAPTURE (BASE-03: coordinate overlap)"
    - "detect_capture after barrier placed on thief's cell (thief in state.barriers) returns Outcome.CAPTURE (BASE-04)"
    - "detect_capture when thief has zero legal moves at the start of the thief's turn returns Outcome.CAPTURE (BASE-05)"
    - "detect_capture when thief has at least one legal move returns None — game continues"
    - "score_outcome(Outcome.CAPTURE, params) returns (params.score_capture_cop, params.score_capture_thief) with no numeric literals"
    - "score_outcome(Outcome.SURVIVAL, params) returns (params.score_survival_cop, params.score_survival_thief) with no numeric literals"
    - "evaluate_turn_end returns Outcome.SURVIVAL when state.turn >= params.survival_threshold and no capture condition is active (BASE-06)"
    - "evaluate_turn_end returns None when state.turn < params.survival_threshold and no capture (game continues)"
    - "No numeric literal for survival_threshold, any score, or any quota appears anywhere in capture.py or outcome.py"
  artifacts:
    - path: src/pursuit/shared/capture.py
      provides: "detect_capture(state, params) -> Outcome | None; evaluate_turn_end(state, params) -> Outcome | None"
      exports: ["detect_capture", "evaluate_turn_end"]
      contains: "detect_capture"
    - path: src/pursuit/shared/outcome.py
      provides: "score_outcome(outcome, params) -> tuple[int, int] pure function"
      exports: ["score_outcome"]
      contains: "score_outcome"
    - path: tests/unit/test_capture.py
      provides: "Full BASE-03..07 test suite replacing Wave 0 stubs (8 named tests)"
      contains: "test_cop_on_thief_capture"
  key_links:
    - from: src/pursuit/shared/capture.py
      to: src/pursuit/shared/board.py
      via: "get_legal_moves(state, 'thief', params) for no-legal-move check"
      pattern: "get_legal_moves"
    - from: src/pursuit/shared/capture.py
      to: src/pursuit/shared/config.py
      via: "params.survival_threshold for survival detection"
      pattern: "params\\.survival_threshold"
    - from: src/pursuit/shared/outcome.py
      to: src/pursuit/shared/config.py
      via: "params.score_capture_cop / params.score_survival_cop for scoring"
      pattern: "params\\.score_"
    - from: tests/unit/test_capture.py
      to: src/pursuit/shared/capture.py
      via: "import detect_capture, evaluate_turn_end"
      pattern: "from pursuit\\.shared\\.capture import"
    - from: tests/unit/test_capture.py
      to: src/pursuit/shared/outcome.py
      via: "import score_outcome"
      pattern: "from pursuit\\.shared\\.outcome import"
---

<objective>
Implement capture detection (all three capture types) and outcome scoring using RED -> GREEN
-> REFACTOR TDD cycles.

Purpose: detect_capture is the pure predicate that 01-03 adds between cop's action and
thief's move, implementing the D-12 turn order exactly. Getting the three capture types
correct and the no-legal-move check properly evaluated at the START of the thief's turn
(D-13) is the invariant that prevents the bug described in RESEARCH Pitfall 3 (capture
missed when checked after thief moves). score_outcome is the only place where scoring
values are read from config — zero numeric literals are permitted anywhere in these modules.

The split into two source files (capture.py + outcome.py) keeps each under 150 lines and
gives a clean boundary: capture.py detects game state, outcome.py maps detected outcomes
to scores. Both remain pure functions over GameState and GameParams.

Output:
- src/pursuit/shared/capture.py — detect_capture(state, params) and evaluate_turn_end(state, params)
- src/pursuit/shared/outcome.py — score_outcome(outcome, params) -> tuple[int, int]
- tests/unit/test_capture.py — full BASE-03..07 test suite replacing Wave 0 stubs
</objective>

<execution_context>
@C:\Users\Hp\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Hp\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-base-logic/01-CONTEXT.md
@.planning/phases/01-base-logic/01-VALIDATION.md

<interfaces>
<!-- Contracts established by 01-01 and 01-02 that this plan builds on directly. -->
<!-- Do NOT re-derive these — use them verbatim. -->

From src/pursuit/shared/state.py (created in 01-01):
```python
from dataclasses import dataclass

Coord = tuple[int, int]

@dataclass(frozen=True)
class GameState:
    cop: Coord
    thief: Coord
    barriers: frozenset[Coord]
    barriers_placed: int
    turn: int
```

From src/pursuit/shared/config.py (created in 01-01):
```python
@dataclass(frozen=True)
class GameParams:
    board_size: int
    cop_start: tuple[int, int]
    thief_start: tuple[int, int]
    barrier_quota: int
    move_ceiling: int
    survival_threshold: int      # <-- used to detect SURVIVAL in evaluate_turn_end
    score_capture_cop: int       # <-- used in score_outcome(CAPTURE)
    score_capture_thief: int     # <-- used in score_outcome(CAPTURE)
    score_survival_cop: int      # <-- used in score_outcome(SURVIVAL)
    score_survival_thief: int    # <-- used in score_outcome(SURVIVAL)
    score_tie: int
    version: str

def load_game_params(path: Path | str) -> GameParams: ...
```

From src/pursuit/constants.py (created in 01-01):
```python
from enum import Enum

class Outcome(Enum):
    CAPTURE        = "capture"
    SURVIVAL       = "survival"
    TIE            = "tie"            # unreachable in Phase 1
    TECHNICAL_LOSS = "technical_loss" # unreachable in Phase 1
```

From src/pursuit/shared/board.py (created in 01-01):
```python
def get_legal_moves(state: GameState, agent: str, params: GameParams) -> list[tuple[int, int]]:
    # Returns orthogonal neighbors + stay, filtered by bounds and barriers.
    # Always returns at least the current position (stay) for an agent
    # whose own cell cannot be a barrier (barrier-on-thief = capture, enforced here).
    ...
```

From src/pursuit/shared/barrier.py (created in 01-02):
```python
def place_barrier(state: GameState, cell: tuple[int, int], params: GameParams) -> GameState:
    # Accepted: returns new GameState with cell added to barriers, barriers_placed +1.
    # Rejected (OOB, cop's own cell, already barriered, over quota): returns original state.
    # NOTE: placing on thief's cell IS accepted by place_barrier; capture detection
    # is detect_capture's responsibility (ownership boundary per D-10).
    ...
```

From tests/conftest.py (Wave 0 fixtures available to all test files):
```python
@pytest.fixture(scope="session")
def default_params() -> GameParams:
    # Loads and returns load_game_params("config/police/game_params.json")
    # default_params.survival_threshold == 35  (never hardcode 35 in tests)
    # default_params.score_capture_cop  == 20  (never hardcode 20 in tests)
    # default_params.score_capture_thief == 5  (never hardcode 5 in tests)
    # default_params.score_survival_cop == 5   (never hardcode 5 in tests)
    # default_params.score_survival_thief == 10 (never hardcode 10 in tests)

@pytest.fixture(scope="function")
def start_state(default_params) -> GameState:
    # GameState(cop=(0,0), thief=(3,3), barriers=frozenset(), barriers_placed=0, turn=0)
```

New signatures this plan must implement:

    detect_capture(state: GameState, params: GameParams) -> Outcome | None
      Checks, in order:
        1. cop == thief -> Outcome.CAPTURE  (BASE-03)
        2. thief in state.barriers -> Outcome.CAPTURE  (BASE-04)
        3. get_legal_moves(state, "thief", params) == [] -> Outcome.CAPTURE  (BASE-05, D-13)
      Otherwise -> None (no capture, thief may move)
      This function is called AFTER the cop acts and BEFORE the thief moves (D-12 step 2+3).

    evaluate_turn_end(state: GameState, params: GameParams) -> Outcome | None
      Called AFTER the thief moves and turn counter increments (D-12 step 4).
      If state.turn >= params.survival_threshold -> Outcome.SURVIVAL  (BASE-06, D-16)
      Otherwise -> None (game continues)

    score_outcome(outcome: Outcome, params: GameParams) -> tuple[int, int]
      Maps Outcome to (cop_score, thief_score) exclusively from params fields.
      Outcome.CAPTURE  -> (params.score_capture_cop,  params.score_capture_thief)
      Outcome.SURVIVAL -> (params.score_survival_cop, params.score_survival_thief)
      Outcome.TIE      -> (params.score_tie,          params.score_tie)
      Outcome.TECHNICAL_LOSS -> (0, 0)  -- only zero literal permitted; no named config key
      Raises ValueError for unrecognised Outcome members.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 RED: Fill test_capture.py with failing assertions</name>
  <files>tests/unit/test_capture.py</files>
  <read_first>
    .planning/phases/01-base-logic/01-VALIDATION.md (BASE-03..07 rows in
    Requirement -> Test detail table: test_cop_on_thief_capture,
    test_barrier_on_thief_capture, test_no_legal_move_capture,
    test_one_move_available_no_capture, test_survival_at_threshold,
    test_game_continues_below_threshold, test_capture_score, test_survival_score)
    .planning/phases/01-base-logic/01-CONTEXT.md (D-12, D-13, D-14, D-15, D-16)
    tests/conftest.py (default_params and start_state fixture signatures)
    config/police/game_params.json (key names so we never hardcode values)
  </read_first>
  <behavior>
    All eight tests below must be present. All game-parameter-derived values come from
    the default_params fixture — never hardcode 35, 20, 5, 10, or 14.

    test_cop_on_thief_capture(default_params):
      Build a GameState with cop == thief == (3, 3), barriers=frozenset(),
      barriers_placed=0, turn=0.
      Call result = detect_capture(state, default_params).
      Assert result is Outcome.CAPTURE.

    test_barrier_on_thief_capture(default_params, start_state):
      The thief is at (3, 3) (from start_state.thief).
      Build a new state with barriers=frozenset({start_state.thief}),
      cop=(0, 0), barriers_placed=1, turn=0
      (the barrier already sits on the thief's cell — place_barrier accepted it per D-10;
      now detect_capture reads that condition).
      Call result = detect_capture(state, default_params).
      Assert result is Outcome.CAPTURE.

    test_no_legal_move_capture(default_params):
      Place the thief at (0, 0) (corner). Surround all four orthogonal neighbors with
      barriers: barriers=frozenset({(1, 0), (0, 1)}) plus out-of-bounds on the other
      two sides (NW corner — (-1,0) and (0,-1) are OOB, so only two barrier cells needed).
      cop at (6, 6), barriers_placed=2, turn=0.
      Stay is NOT a legal move when the thief's own cell equals one of its barrier
      neighbors — but wait: stay is always legal UNLESS thief's own cell is in barriers.
      For this test, thief at (0,0) is NOT in barriers, so stay = (0,0) is always legal.
      Therefore to achieve zero legal moves, we must ALSO add (0, 0) to barriers.
      Revised setup: thief at (0, 0), barriers=frozenset({(0, 0), (1, 0), (0, 1)}),
      cop=(6, 6), barriers_placed=3, turn=0.
      (Having thief's own cell in barriers is the barrier-on-thief capture state, so this
      also triggers BASE-04. That is intentional: both BASE-04 and BASE-05 fire because
      detect_capture checks them in order — BASE-04 (thief in barriers) is checked before
      BASE-05 (no legal moves). Assert result is Outcome.CAPTURE.)
      Alternatively: use a position where stay IS blocked by barriers but the thief's own
      cell is NOT a barrier, making it a pure no-legal-move (no overlap with BASE-04).
      Use thief at (1, 1), barriers=frozenset({(0, 1), (2, 1), (1, 0), (1, 2), (1, 1)}),
      cop=(6, 6), barriers_placed=5, turn=0.
      This puts the thief's own cell (1,1) in barriers, which is a BASE-04 trigger.
      CLEANEST approach for a pure BASE-05 test: thief at (3, 3), barriers surrounding
      all four orthogonal neighbors ({(2,3),(4,3),(3,2),(3,4)}), thief NOT in barriers.
      Since stay = (3,3) is not in barriers, get_legal_moves returns [(3,3)] — stay is
      always legal if thief's own cell is not a barrier. Therefore for zero legal moves
      we MUST have the thief's own cell in barriers, which collapses into BASE-04.
      RESOLUTION (per D-13): "stay" counts as a legal move unless the thief's own cell
      is the capture condition (thief in barriers). So a pure BASE-05 state requires a
      board where the thief has no orthogonal neighbors open AND stay is still blocked.
      Since a thief-in-barrier is BASE-04, for test_no_legal_move_capture use a state
      where thief-in-barrier is NOT the case, but stay is blocked by an adjacent barrier.
      Per RESEARCH Pattern 1, stay = thief's current position. It passes the barrier
      check unless thief's own cell is in barriers. Therefore stay is always legal in
      a state where thief is not in barriers. A thief surrounded on all 4 orthogonal
      sides by barriers still has stay legal. get_legal_moves will return [thief_pos].
      CONCLUSION: It is geometrically impossible to produce zero legal moves (BASE-05)
      without also triggering BASE-04 (thief in barriers) — because stay is always legal
      when the thief's own cell is not a barrier, and if the thief's own cell IS a
      barrier then BASE-04 fires first. detect_capture checks BASE-04 before BASE-05,
      so test_no_legal_move_capture should set up a state where thief's own cell is in
      barriers (which IS the zero-legal-moves state since stay is then blocked too),
      and the assertion is still result is Outcome.CAPTURE (captured, regardless of
      which check triggers first).
      Setup: thief at (3, 3), barriers=frozenset({(3, 3)}), cop=(0, 0),
      barriers_placed=1, turn=0.
      Assert detect_capture(state, default_params) is Outcome.CAPTURE.
      (BASE-04 triggers; also demonstrates the zero-legal-moves case since stay is
      the thief's own cell which is now a barrier.)

    test_one_move_available_no_capture(default_params, start_state):
      Thief at (3, 3). Surround three of the four orthogonal neighbors with barriers:
      barriers=frozenset({(2, 3), (4, 3), (3, 2)}), leaving (3, 4) open.
      Cop at (0, 0), barriers_placed=3, turn=0.
      Call result = detect_capture(state, default_params).
      Assert result is None  (thief has at least one legal move: (3,4) and stay (3,3)).

    test_survival_at_threshold(default_params):
      Build a GameState with cop at (0, 0), thief at (6, 6), barriers=frozenset(),
      barriers_placed=0, turn=default_params.survival_threshold.
      (turn == survival_threshold, no capture condition active.)
      Call result = evaluate_turn_end(state, default_params).
      Assert result is Outcome.SURVIVAL.

    test_game_continues_below_threshold(default_params):
      Build a GameState with cop at (0, 0), thief at (6, 6), barriers=frozenset(),
      barriers_placed=0, turn=default_params.survival_threshold - 1.
      Call result = evaluate_turn_end(state, default_params).
      Assert result is None  (threshold not yet reached).

    test_capture_score(default_params):
      Call cop_score, thief_score = score_outcome(Outcome.CAPTURE, default_params).
      Assert cop_score == default_params.score_capture_cop.
      Assert thief_score == default_params.score_capture_thief.

    test_survival_score(default_params):
      Call cop_score, thief_score = score_outcome(Outcome.SURVIVAL, default_params).
      Assert cop_score == default_params.score_survival_cop.
      Assert thief_score == default_params.score_survival_thief.
  </behavior>
  <action>
    Replace the pytest.skip stub bodies in tests/unit/test_capture.py with the eight
    real assertions described in the behavior block above.

    Import structure at the top of the file:
      import pytest
      from pursuit.shared.state import GameState
      from pursuit.constants import Outcome
      from pursuit.shared.capture import detect_capture, evaluate_turn_end
      from pursuit.shared.outcome import score_outcome

    For tests that need a custom state (not start_state), construct GameState directly
    using keyword arguments:
      GameState(cop=(r,c), thief=(r,c), barriers=frozenset(...),
                barriers_placed=N, turn=T)

    NEVER write a numeric literal for any game parameter (do not write 35, 20, 5, 10,
    14, or 2 anywhere in this file when those values come from game_params.json).
    Use default_params.survival_threshold, default_params.score_capture_cop,
    default_params.score_capture_thief, default_params.score_survival_cop,
    default_params.score_survival_thief, etc.
    The only permitted integer literals are test-construction coordinates
    (e.g. cop=(0, 0), barriers=frozenset({(2, 3), (4, 3), (3, 2)})).

    After writing, run:
      uv run pytest tests/unit/test_capture.py -x
    Confirm the tests FAIL with ImportError or ModuleNotFoundError (RED gate).
    If they pass or skip, the RED gate is not met — ensure the imports are real, not mocked.

    Run ruff check tests/unit/test_capture.py and fix any violations before completing.
    Run bash scripts/check_line_limit.sh and confirm test_capture.py is within 150 lines.
    If the eight tests push the file over 150 lines, split into test_capture.py
    (detect_capture and evaluate_turn_end tests) and test_outcome.py (score_outcome tests).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_capture.py -x 2>&1 | grep -E "(FAILED|ERROR|ImportError|ModuleNotFoundError)" | head -5</automated>
    <automated>uv run ruff check tests/unit/test_capture.py</automated>
    <automated>bash scripts/check_line_limit.sh</automated>
  </verify>
  <done>
    test_capture.py contains all eight real assertions (no pytest.skip bodies). Running
    pytest exits non-zero because src/pursuit/shared/capture.py and
    src/pursuit/shared/outcome.py do not yet exist. ruff check exits 0.
    check_line_limit.sh passes test_capture.py (and test_outcome.py if split was needed).
    RED gate confirmed.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 GREEN: Implement capture.py and outcome.py</name>
  <files>
    src/pursuit/shared/capture.py,
    src/pursuit/shared/outcome.py
  </files>
  <read_first>
    .planning/phases/01-base-logic/01-CONTEXT.md (D-12, D-13, D-14, D-15, D-16)
    .planning/phases/01-base-logic/01-RESEARCH.md (Pitfall 3 "Capture Check Timing",
    Anti-Patterns "Capture check after thief moves", Pattern 4 "Turn Algorithm D-12 verbatim")
    tests/unit/test_capture.py (the eight RED assertions — implement exactly what they test)
    src/pursuit/shared/state.py (GameState fields)
    src/pursuit/shared/config.py (GameParams fields: survival_threshold, score_* fields)
    src/pursuit/constants.py (Outcome enum — import from here, do NOT redefine)
    src/pursuit/shared/board.py (get_legal_moves signature and semantics)
  </read_first>
  <behavior>
    capture.py — two public pure functions:

    detect_capture(state: GameState, params: GameParams) -> Outcome | None:
      Implements D-12 step 2 + D-13 (no-legal-move check at start of thief's turn).
      Three checks in order (stop at first match, return Outcome.CAPTURE):
        1. if state.cop == state.thief -> return Outcome.CAPTURE  (BASE-03)
        2. if state.thief in state.barriers -> return Outcome.CAPTURE  (BASE-04)
           Rationale: place_barrier accepted the placement (01-02 boundary); the
           consequence — capture — is this function's responsibility (D-10 / RESEARCH §capture.py).
        3. if not get_legal_moves(state, "thief", params) -> return Outcome.CAPTURE  (BASE-05, D-13)
           Note: get_legal_moves always includes stay (thief's current position) UNLESS
           the thief's own cell is in state.barriers — which is already caught by check 2.
           Therefore check 3 only reaches a state where stay IS legal (thief not in
           barriers). In practice, check 3 fires only if the thief is surrounded on all
           4 orthogonal sides by barriers AND... stay is at (thief_pos) which is NOT in
           barriers (caught by check 2). Actually, as noted in Task 1 behavior analysis:
           stay is always legal when thief's own cell is not a barrier, so check 3 will
           never fire independently of check 2. Include check 3 anyway for correctness
           completeness and future-proofing (e.g. if get_legal_moves semantics evolve).
        4. return None  (no capture condition — thief may move)

    evaluate_turn_end(state: GameState, params: GameParams) -> Outcome | None:
      Called after the thief moves and after the turn counter is incremented (D-12 step 4).
      if state.turn >= params.survival_threshold -> return Outcome.SURVIVAL  (BASE-06, D-16)
      return None  (game continues)
      No other outcome is produced here (D-15: Phase 1 never produces TIE or TECHNICAL_LOSS).

    Module docstring must explain:
    - detect_capture is called AFTER cop acts and BEFORE thief moves (D-12 step 2+3).
    - evaluate_turn_end is called AFTER thief moves and turn counter increments (D-12 step 4).
    - Check order in detect_capture: BASE-03 first, then BASE-04, then BASE-05.
    - Phase 1 only produces CAPTURE and SURVIVAL (D-15).

    outcome.py — one public pure function:

    score_outcome(outcome: Outcome, params: GameParams) -> tuple[int, int]:
      Returns (cop_score, thief_score) — both values come exclusively from params fields.
      Mapping (per D-14, PARAMETERS Table 17):
        Outcome.CAPTURE        -> (params.score_capture_cop,  params.score_capture_thief)
        Outcome.SURVIVAL       -> (params.score_survival_cop, params.score_survival_thief)
        Outcome.TIE            -> (params.score_tie,          params.score_tie)
        Outcome.TECHNICAL_LOSS -> (0, 0)
          The (0, 0) for TECHNICAL_LOSS uses literal zeros. This is the ONLY permitted
          numeric literal in outcome.py. There is no "score_technical_loss" field in
          GameParams (confirmed from the config schema — the json has no such key).
          PARAMETERS Table 17 lists 0/0 as fixed; using the literal 0 here is correct
          and preferred over adding a spurious config field for a value that cannot vary.
        _                      -> raise ValueError(f"Unrecognised outcome: {outcome!r}")

    No numeric literal for any score value appears in outcome.py EXCEPT the two zeros in
    TECHNICAL_LOSS. All other score values read exclusively from params.

    Both files use only stdlib imports (from pursuit.shared.*) plus the pursuit package.
    No PyPI imports. Each file must be ≤150 lines (blanks and comments excluded).

    After writing both files, run:
      uv run pytest tests/unit/test_capture.py -x -v
    All eight tests must pass (GREEN gate). Then:
      uv run ruff check src/pursuit/shared/capture.py src/pursuit/shared/outcome.py
      bash scripts/check_line_limit.sh
    Fix any violations before completing this task.

    Spot-check for forbidden numeric literals in capture.py:
      uv run python -c "
    import ast, sys
    src = open('src/pursuit/shared/capture.py').read()
    tree = ast.parse(src)
    nums = [n.value for n in ast.walk(tree)
            if isinstance(n, ast.Constant) and isinstance(n.value, (int, float))]
    print('numeric literals in capture.py:', nums)
    sys.exit(1 if nums else 0)
    "
    Expected: numeric literals in capture.py: [] (empty list — no literals permitted here).

    Spot-check for permitted numeric literals in outcome.py (only 0 allowed):
      uv run python -c "
    import ast, sys
    src = open('src/pursuit/shared/outcome.py').read()
    tree = ast.parse(src)
    nums = [n.value for n in ast.walk(tree)
            if isinstance(n, ast.Constant) and isinstance(n.value, (int, float))
            and n.value != 0]
    print('non-zero numeric literals in outcome.py:', nums)
    sys.exit(1 if nums else 0)
    "
    Expected: non-zero numeric literals in outcome.py: [] (only literal 0 is permitted).
  </behavior>
  <action>
    Create src/pursuit/shared/capture.py and src/pursuit/shared/outcome.py following
    the behavior block above.

    capture.py structure:
    - Module docstring (timing note: detect_capture called before thief moves; evaluate_turn_end
      called after turn counter increments; D-15 reminder: only CAPTURE and SURVIVAL produced).
    - Imports: from __future__ import annotations; from pursuit.shared.state import GameState;
      from pursuit.shared.config import GameParams; from pursuit.constants import Outcome;
      from pursuit.shared.board import get_legal_moves.
    - Coord type alias: Coord = tuple[int, int].
    - detect_capture(state: GameState, params: GameParams) -> "Outcome | None":
      Three-check cascade with early return on Outcome.CAPTURE; None on no match.
      Function docstring: Args (state, params); Returns (Outcome.CAPTURE or None);
      Note (called after cop acts, before thief moves).
    - evaluate_turn_end(state: GameState, params: GameParams) -> "Outcome | None":
      Single threshold check. Function docstring: Args; Returns (Outcome.SURVIVAL or None);
      Note (called after thief moves and turn counter increment).

    outcome.py structure:
    - Module docstring (maps Outcome enum to (cop_score, thief_score) from config params;
      TECHNICAL_LOSS uses literal 0 — no config field for this value).
    - Imports: from pursuit.shared.config import GameParams; from pursuit.constants import Outcome.
    - score_outcome(outcome: Outcome, params: GameParams) -> tuple[int, int]:
      Use if/elif/else or match statement (Python 3.10+; target-version py310 per D-01).
      All score values read from params.*. Only 0 is permitted as a literal (TECHNICAL_LOSS).
      Raise ValueError for unrecognised Outcome.
      Function docstring: Args (outcome, params); Returns tuple[int, int];
      Note (TECHNICAL_LOSS returns (0,0) — only literal permitted; all others from params).

    File must be ≤150 lines each (blanks and comments excluded). Only stdlib + internal imports.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_capture.py::test_cop_on_thief_capture -x -v</automated>
    <automated>uv run pytest tests/unit/test_capture.py::test_barrier_on_thief_capture -x -v</automated>
    <automated>uv run pytest tests/unit/test_capture.py::test_no_legal_move_capture -x -v</automated>
    <automated>uv run pytest tests/unit/test_capture.py::test_one_move_available_no_capture -x -v</automated>
    <automated>uv run pytest tests/unit/test_capture.py::test_survival_at_threshold -x -v</automated>
    <automated>uv run pytest tests/unit/test_capture.py::test_game_continues_below_threshold -x -v</automated>
    <automated>uv run pytest tests/unit/test_capture.py::test_capture_score -x -v</automated>
    <automated>uv run pytest tests/unit/test_capture.py::test_survival_score -x -v</automated>
    <automated>uv run pytest tests/unit/test_capture.py -x -v</automated>
    <automated>uv run ruff check src/pursuit/shared/capture.py src/pursuit/shared/outcome.py</automated>
    <automated>bash scripts/check_line_limit.sh</automated>
    <automated>uv run python -c "import ast, sys; src=open('src/pursuit/shared/capture.py').read(); tree=ast.parse(src); nums=[n.value for n in ast.walk(tree) if isinstance(n, ast.Constant) and isinstance(n.value, (int,float))]; print('numeric literals in capture.py:', nums); sys.exit(1 if nums else 0)"</automated>
    <automated>uv run python -c "import ast, sys; src=open('src/pursuit/shared/outcome.py').read(); tree=ast.parse(src); nums=[n.value for n in ast.walk(tree) if isinstance(n, ast.Constant) and isinstance(n.value, (int,float)) and n.value != 0]; print('non-zero literals in outcome.py:', nums); sys.exit(1 if nums else 0)"</automated>
  </verify>
  <done>
    All eight tests in test_capture.py pass, including all three capture types and both
    scoring tests. ruff check exits 0 on both source files. check_line_limit.sh passes
    both files. AST scan finds zero numeric literals in capture.py. AST scan finds zero
    non-zero numeric literals in outcome.py (only the literal 0 for TECHNICAL_LOSS is
    allowed). GREEN gate confirmed.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3 REFACTOR: Full suite clean + static audits + commit</name>
  <files>
    src/pursuit/shared/capture.py,
    src/pursuit/shared/outcome.py,
    tests/unit/test_capture.py
  </files>
  <read_first>
    .planning/phases/01-base-logic/01-RESEARCH.md (Pitfall 3 "Capture Check Timing",
    Pitfall 4 "Hardcoded Value Slippage in Tests", Anti-Patterns)
    .planning/phases/01-base-logic/01-CONTEXT.md (D-12, D-13, D-15, D-16)
  </read_first>
  <behavior>
    After GREEN, run the full quality gate and apply any refactors needed:
    - No test regression: all eight tests that passed in Task 2 must still pass.
    - No numeric literals in capture.py (AST scan returns empty list).
    - No non-zero numeric literals in outcome.py (AST scan returns empty list for non-zero).
    - Coverage of capture.py and outcome.py >= 85% individually (all branches exercised).
    - ruff check . exits 0 across the whole repo (including 01-01 and 01-02 files).
    - check_line_limit.sh passes capture.py, outcome.py, and test_capture.py.
    - Function and module docstrings present in both source files.
    - No code duplication: the D-12 check order (BASE-03 -> BASE-04 -> BASE-05) is
      expressed once in detect_capture; no copies or re-implementations elsewhere.
    - Full unit suite still green after refactoring (no regression against 01-01/01-02).
  </behavior>
  <action>
    Run the full unit suite to confirm no regression from 01-01 and 01-02:
      uv run pytest tests/unit/ -x -q

    Run coverage for both new modules:
      uv run pytest --cov=pursuit.shared.capture --cov=pursuit.shared.outcome \
        --cov-report=term-missing tests/unit/test_capture.py
    All branches of detect_capture, evaluate_turn_end, and score_outcome must be covered.
    Uncovered branches to check: the ValueError path in score_outcome (add a targeted test
    that passes an invalid/unknown Outcome and asserts ValueError is raised, if not already
    covered). evaluate_turn_end's "game continues" branch is covered by
    test_game_continues_below_threshold. detect_capture's None branch is covered by
    test_one_move_available_no_capture.

    Static audit — run each of the following and fix any issues:
    1. uv run ruff check .  (0 violations required across entire repo)
    2. bash scripts/check_line_limit.sh  (all files ≤150 lines)
    3. AST numeric literal scan for capture.py:
         uv run python -c "import ast, sys; src=open('src/pursuit/shared/capture.py').read(); tree=ast.parse(src); nums=[n.value for n in ast.walk(tree) if isinstance(n, ast.Constant) and isinstance(n.value, (int,float))]; print('numeric literals:', nums); sys.exit(1 if nums else 0)"
       Must print: numeric literals: []
    4. AST literal scan for outcome.py (only 0 permitted):
         uv run python -c "import ast, sys; src=open('src/pursuit/shared/outcome.py').read(); tree=ast.parse(src); nums=[n.value for n in ast.walk(tree) if isinstance(n, ast.Constant) and isinstance(n.value, (int,float)) and n.value != 0]; print('non-zero literals:', nums); sys.exit(1 if nums else 0)"
       Must print: non-zero literals: []

    Refactor targets (apply only if needed — do not change behavior):
    - If detect_capture has inline string comparisons for agent ("thief") duplicated
      elsewhere, verify get_legal_moves call uses the literal string "thief" exactly once.
    - If test_capture.py has multiple tests constructing the same GameState shape,
      consider adding a module-level helper build_state(cop, thief, barriers=None, turn=0)
      to reduce duplication (keep file under 150 lines; split if needed).
    - If module docstrings are missing or incomplete, add them now.
    - If score_outcome uses chained if/elif without a final else raising ValueError,
      add the else clause for safety (it's a required correctness guard, not optional polish).

    After all audits pass, run the full unit suite one final time:
      uv run pytest tests/unit/ -x -q

    Then commit with message:
      feat(01-03): capture detection + outcome scoring (BASE-03/04/05/06/07)
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_capture.py -v</automated>
    <automated>uv run pytest tests/unit/ -x -q</automated>
    <automated>uv run pytest --cov=pursuit.shared.capture --cov=pursuit.shared.outcome --cov-report=term-missing tests/unit/test_capture.py</automated>
    <automated>uv run ruff check .</automated>
    <automated>bash scripts/check_line_limit.sh</automated>
    <automated>uv run python -c "import ast, sys; src=open('src/pursuit/shared/capture.py').read(); tree=ast.parse(src); nums=[n.value for n in ast.walk(tree) if isinstance(n, ast.Constant) and isinstance(n.value, (int,float))]; print('numeric literals in capture.py:', nums); sys.exit(1 if nums else 0)"</automated>
    <automated>uv run python -c "import ast, sys; src=open('src/pursuit/shared/outcome.py').read(); tree=ast.parse(src); nums=[n.value for n in ast.walk(tree) if isinstance(n, ast.Constant) and isinstance(n.value, (int,float)) and n.value != 0]; print('non-zero literals in outcome.py:', nums); sys.exit(1 if nums else 0)"</automated>
  </verify>
  <done>
    All eight tests in test_capture.py pass. Full unit suite (tests/unit/) passes with no
    failures or errors (including 01-01 and 01-02 tests). ruff check exits 0. check_line_limit.sh
    passes capture.py, outcome.py, and test_capture.py. Coverage for capture.py and
    outcome.py is >= 85%. AST scan finds zero numeric literals in capture.py. AST scan
    finds zero non-zero numeric literals in outcome.py. Commit created.
    REFACTOR gate confirmed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> detect_capture | state argument is produced by apply_move + place_barrier; both are pure functions over frozen GameState so the input is structurally trusted, but barrier membership and position equality must still be evaluated correctly |
| caller -> score_outcome | outcome argument is a Python Outcome enum value from constants.py; unrecognised values raise ValueError immediately |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Tampering | src/pursuit/shared/capture.py (detect_capture) | mitigate | D-12 check order is enforced by the code structure (three sequential early-return guards); frozen=True GameState prevents state mutation between the three checks; no shared mutable state means the thief cannot "move" between check 1 and check 3 |
| T-01-03-02 | Tampering | barriers_placed and turn counters | mitigate | Both counters live inside the immutable GameState snapshot and are only updated by the respective pure functions (apply_move, place_barrier); detect_capture and evaluate_turn_end are read-only — they cannot increment or decrement any counter |
| T-01-03-03 | Tampering | score values in score_outcome | mitigate | All scores read exclusively from params.score_* fields loaded at startup from the config-validated GameParams; literal 0 for TECHNICAL_LOSS is the only exception; no other integer literal in outcome.py |
| T-01-03-04 | Repudiation | Capture type traceability | accept | Pure offline computation; all three capture types are distinguishable by state inspection (which check fired); replay-safe. Commit-reveal audit (Phase 6) will lock this further. Phase 1 has no audit trail requirement. |
| T-01-03-05 | Information Disclosure | GameState passed to detect_capture contains both cop and thief positions | accept | Phase 1 is a single-process test harness; cop and thief are not yet separated into processes (Phase 2). detect_capture receives full GameState; this is architecturally safe until Phase 2 enforces process separation |
| T-01-03-SC | Tampering | uv dependencies | mitigate | No new packages introduced in this plan; existing pytest/ruff packages already audited in 01-RESEARCH.md Package Legitimacy Audit (all [OK]) |

No HIGH-severity threats: detect_capture, evaluate_turn_end, and score_outcome are pure
offline computations with no network calls, no file I/O, no untrusted user input, and no
secrets. The immutable GameState pattern (frozen=True) is the primary correctness guarantee —
detect_capture reads state but cannot modify it, so there is no risk of a partially-applied
capture leaving the game in an inconsistent state.
</threat_model>

<verification>
Full plan-01-03 verification sequence (run in order after Task 3 completes):

1. RED gate (Task 1):
   uv run pytest tests/unit/test_capture.py -x
   exits non-zero with ImportError or ModuleNotFoundError — tests are real, not stubs.

2. GREEN gate (Task 2):
   uv run pytest tests/unit/test_capture.py::test_cop_on_thief_capture -x  -> PASS
   uv run pytest tests/unit/test_capture.py::test_barrier_on_thief_capture -x  -> PASS
   uv run pytest tests/unit/test_capture.py::test_no_legal_move_capture -x  -> PASS
   uv run pytest tests/unit/test_capture.py::test_one_move_available_no_capture -x  -> PASS
   uv run pytest tests/unit/test_capture.py::test_survival_at_threshold -x  -> PASS
   uv run pytest tests/unit/test_capture.py::test_game_continues_below_threshold -x  -> PASS
   uv run pytest tests/unit/test_capture.py::test_capture_score -x  -> PASS
   uv run pytest tests/unit/test_capture.py::test_survival_score -x  -> PASS
   uv run pytest tests/unit/test_capture.py -x  -> all 8 pass

3. REFACTOR gate (Task 3):
   uv run pytest tests/unit/ -x -q  -> full unit suite green (01-01 + 01-02 + 01-03)
   uv run ruff check .  -> 0 violations
   bash scripts/check_line_limit.sh  -> all files pass
   uv run pytest --cov=pursuit.shared.capture --cov=pursuit.shared.outcome \
     --cov-report=term-missing tests/unit/test_capture.py
   -> capture.py and outcome.py coverage >= 85%

4. Static audit:
   AST scan of capture.py finds zero int/float literals
   AST scan of outcome.py finds zero non-zero int/float literals (only literal 0 for TECHNICAL_LOSS)
   grep -c "detect_capture" src/pursuit/shared/capture.py -> >= 1
   grep -c "score_outcome" src/pursuit/shared/outcome.py -> >= 1
   grep -c "frozen=True" src/pursuit/shared/state.py -> >= 1 (inherited from 01-01)

5. Named test pins:
   uv run pytest tests/unit/test_capture.py::test_cop_on_thief_capture -x  -> PASS
   uv run pytest tests/unit/test_capture.py::test_barrier_on_thief_capture -x  -> PASS
   uv run pytest tests/unit/test_capture.py::test_no_legal_move_capture -x  -> PASS
   uv run pytest tests/unit/test_capture.py::test_one_move_available_no_capture -x  -> PASS
   uv run pytest tests/unit/test_capture.py::test_survival_at_threshold -x  -> PASS
   uv run pytest tests/unit/test_capture.py::test_game_continues_below_threshold -x  -> PASS
   uv run pytest tests/unit/test_capture.py::test_capture_score -x  -> PASS
   uv run pytest tests/unit/test_capture.py::test_survival_score -x  -> PASS
</verification>

<success_criteria>
- src/pursuit/shared/capture.py exports detect_capture(state, params) and evaluate_turn_end(state, params).
- detect_capture checks captures in D-12 order: (1) cop==thief -> CAPTURE, (2) thief in barriers -> CAPTURE,
  (3) get_legal_moves("thief") empty -> CAPTURE, (4) None.
- evaluate_turn_end returns Outcome.SURVIVAL when state.turn >= params.survival_threshold; None otherwise.
- src/pursuit/shared/outcome.py exports score_outcome(outcome, params) -> tuple[int, int].
- score_outcome returns scores exclusively from params.score_* fields; only literal 0 is permitted (TECHNICAL_LOSS).
- score_outcome raises ValueError for unrecognised Outcome members.
- capture.py contains zero numeric literals (AST scan returns empty list).
- outcome.py contains zero non-zero numeric literals (AST scan returns empty list for non-zero values).
- tests/unit/test_capture.py contains all eight real named assertions with no pytest.skip bodies.
- uv run pytest tests/unit/test_capture.py::test_cop_on_thief_capture -x  -> PASS.
- uv run pytest tests/unit/test_capture.py::test_barrier_on_thief_capture -x  -> PASS.
- uv run pytest tests/unit/test_capture.py::test_no_legal_move_capture -x  -> PASS.
- uv run pytest tests/unit/test_capture.py::test_one_move_available_no_capture -x  -> PASS.
- uv run pytest tests/unit/test_capture.py::test_survival_at_threshold -x  -> PASS.
- uv run pytest tests/unit/test_capture.py::test_game_continues_below_threshold -x  -> PASS.
- uv run pytest tests/unit/test_capture.py::test_capture_score -x  -> PASS.
- uv run pytest tests/unit/test_capture.py::test_survival_score -x  -> PASS.
- uv run pytest tests/unit/ -x -q  -> full unit suite green (no regression against 01-01 or 01-02).
- uv run ruff check .  -> 0 violations.
- bash scripts/check_line_limit.sh  -> passes capture.py, outcome.py, and test_capture.py.
- capture.py and outcome.py coverage >= 85%.
</success_criteria>

<output>
Create `.planning/phases/01-base-logic/01-03-SUMMARY.md` when done.
</output>
