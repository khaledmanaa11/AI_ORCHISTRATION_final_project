---
phase: 04-language-and-scent
plan: "02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/pursuit/network/handshake_wire.py
  - src/pursuit/network/handshake.py
  - src/pursuit/network/config_hash.py
  - tests/unit/test_handshake.py
  - tests/unit/test_handshake_wire.py
  - tests/unit/test_config_hash.py
autonomous: true
requirements:
  - LANG-07
  - SEC-01

must_haves:
  truths:
    - "D-46: the scent digest is exchanged in the EXISTING Phase-2 handshake, as a second key in the same payload dict, using the SAME hashing helper. handshake.py's docstring establishes the PATTERN - it reserves payload extension for Phase 6's Step-0 declaration and states the envelope shape does not change. The scent digest is the first user of that pattern; the docstring does not name it, and this plan updates the docstring to name both"
    - "Rule 23 makes this MUST: the scent-emission model is cryptographically locked BEFORE the game starts, and any deviation in the decay formula voids the game. The lock therefore has to be verified at handshake time, before move 1 - not audited at game end"
    - "A scent-digest mismatch aborts to State.ERROR exactly as a config-digest mismatch does (rule 11's existing path). Both digests are checked in the SAME _evaluate call so a peer cannot pass one and fail the other silently"
    - "Comparison uses secrets.compare_digest, never ==. This is already how digests_match works and the scent digest must not open a second, weaker path"
    - "Section 4.5's red box requires the WORKED NUMERIC EXAMPLE to be part of what both sides verify, not just the parameters. Plan 04-01 put the example inside the hashed payload; this plan must not hash a subset of it"
    - "Backwards compatibility is a deliberate NON-goal here. A peer that sends no scent digest has not locked its model, and rule 23 says the game is void without the lock - so a missing digest is a mismatch, and the abort message says which key was absent"
    - "The handshake stays one attempt with no retry, as today. Adding a retry would change Phase 2 behaviour this plan has no mandate to change"
  artifacts:
    - path: src/pursuit/network/handshake_wire.py
      provides: "build_offer carries both the config digest and the scent digest in one payload"
      contains: "SCENT_DIGEST"
    - path: src/pursuit/network/handshake.py
      provides: "_evaluate compares both digests and aborts naming the one that failed"
      contains: "compare_digest"
  key_links:
    - from: src/pursuit/network/handshake.py
      to: src/pursuit/shared/scent_config.py
      via: "the handshake hashes the loaded ScentModel through scent_digest (D-46, rule 23)"
      pattern: "scent_digest"
---

<objective>
Make rule 23 real: both peers commit to a byte-identical scent-emission and decay model,
cryptographically, before move 1 — inside the handshake that already exists.

This is a small plan on purpose. `handshake.py`'s own docstring established the pattern —
*"Phase 6 later adds a Step-0 declaration to this same handshake by extending the envelope
payload — the shape does not change"* — and the scent digest is the **first** user of that path,
arriving two phases earlier than the docstring anticipated. Update the docstring to say so.
</objective>

<execution_context>
@C:\Users\Hp\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Hp\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/04-language-and-scent/04-PLAN-OUTLINE.md
@.planning/phases/04-language-and-scent/04-01-SUMMARY.md

- **`docs/RULES.md` rule 23** — *MUST cryptographically lock the scent-emission model before the
  game starts; deviation in the decay formula voids the game.*
- **Book §4.5 red box, p.31 (PDF 47)** — the two groups must exchange the emission and decay
  model **in full, including a concrete numeric example**, verify they interpret it identically,
  and only then lock the agreement with a SHA-256 of the agreed formula together with the
  numeric example. Plan 04-01 put the example inside the payload for exactly this reason.
- `src/pursuit/network/handshake.py` (183 lines) — `perform_handshake` / `respond_to_handshake`,
  `_evaluate`, `_abort`. One attempt, no retry, abort to `State.ERROR` on mismatch (rule 11).
- `src/pursuit/network/handshake_wire.py` (51 lines) — `build_offer(local_digest, local_role)`,
  `HandshakeKey.DIGEST`. The payload dict that gains a second key.
- `src/pursuit/network/config_hash.py` (59 lines) — `canonical_json`, `config_digest`,
  `digests_match` (which already uses `secrets.compare_digest`).
</context>

<task type="auto">
  <name>Task 1: carry a second digest in the offer</name>
  <files>src/pursuit/network/handshake_wire.py, src/pursuit/network/config_hash.py</files>
  <read_first>
    src/pursuit/network/handshake_wire.py (whole file)
    src/pursuit/network/config_hash.py (whole file)
    src/pursuit/shared/scent_config.py (scent_digest, from plan 04-01)
  </read_first>
  <action>
    Add `SCENT_DIGEST` to `HandshakeKey` and a second parameter to `build_offer`. The payload
    becomes a two-key dict inside the same envelope — **no envelope-level change**.

    If `config_hash.py` needs a generic "compare a named pair of digests" helper so that
    `_evaluate` does not grow a second copy of the comparison, extract it here rather than
    duplicating (Table 5 forbids the second copy). Keep `digests_match` as-is for callers that
    already use it.

    The wire is a fixed vocabulary: define the key name once in the enum and reference it
    everywhere, so a typo cannot produce a silently-absent digest.
  </action>
  <verify>
    `build_offer` produces a payload with exactly the two documented keys and no others. A
    round-trip through `Envelope.to_dict`/`from_dict` preserves both. The envelope's
    `REQUIRED_KEYS` check is untouched and its test still passes.
  </verify>
  <done>The offer states both commitments in one message.</done>
</task>

<task type="auto">
  <name>Task 2: verify both, abort naming the failure</name>
  <files>src/pursuit/network/handshake.py</files>
  <read_first>
    src/pursuit/network/handshake.py (whole file — _evaluate and _abort in particular)
    src/pursuit/network/state_machine.py (the ERROR transition the abort uses)
    docs/RULES.md rules 11 and 23
  </read_first>
  <action>
    `_evaluate` compares **both** digests in one place, with `secrets.compare_digest`, and aborts
    to `State.ERROR` if either differs. The abort message names which digest failed — a config
    mismatch and a scent mismatch have different fixes and a grader reading the log should not
    have to guess.

    **A missing scent digest is a mismatch.** Rule 23 makes the lock mandatory; a peer that sends
    no digest has not locked its model, so the game is void either way. Abort with a message
    that says the key was absent rather than that it differed, so the operator can tell "opponent
    is on an older build" from "opponent uses a different decay law".

    The handshake stays one attempt with no retry. Do not add a retry, a negotiation round, or a
    fallback to a book-default model — none of those are in scope and all three would change
    Phase 2 behaviour this plan has no mandate to change.
  </action>
  <verify>
    Four cases, all tested: both digests match → handshake succeeds and the state machine
    advances; config differs → abort naming the config digest; scent differs → abort naming the
    scent digest; scent key absent → abort naming the absent key. In every failing case the
    state is `ERROR` and no move is ever sent. A mutated kernel entry in one peer's `scent.json`
    produces the scent-mismatch abort end-to-end.
  </verify>
  <done>Rule 23 is enforced before move 1, and the log says which commitment broke.</done>
</task>

<verification>
1. `uv run ruff check .` → 0; `uv run pytest tests/ --cov` → ≥ 85%.
2. `bash scripts/check_line_limit.sh` passes; `handshake.py` was 183 lines of source with
   comments — confirm the counted figure stays under the limit and split if it does not.
3. `grep -n "==" src/pursuit/network/handshake.py` shows no digest compared with `==`.
4. Existing handshake tests pass unmodified except where the offer's key set legitimately
   changed; **no assertion is deleted or weakened**.
5. Two peers with byte-identical `scent.json` complete the handshake; a one-character mutation
   in either file aborts both sides before move 1.
</verification>

<success_criteria>
- The scent model is locked cryptographically before the game starts, satisfying rule 23 and
  LANG-07.
- Both commitments — game config and scent model — are verified in the same place, with the same
  constant-time comparison, and a failure names itself.
- Phase 2's handshake semantics (one attempt, abort on mismatch) are unchanged.
</success_criteria>

<output>
Create `.planning/phases/04-language-and-scent/04-02-SUMMARY.md`. Record the exact payload key
names and the shipped digest value — the league opponent has to be sent both before a match.
</output>
