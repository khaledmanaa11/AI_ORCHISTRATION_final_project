---
phase: 04-language-and-scent
plan: "13"
type: execute
wave: 7
depends_on: ["04-12"]
files_modified:
  - docs/PRD_scent_map.md
  - docs/PRD_belief_map.md
  - docs/PRD_deception.md
  - docs/phases/phase-4/RULES-RESOLUTION-LANG.md
  - docs/phases/phase-4/PRD.md
  - docs/phases/phase-4/PLAN.md
  - docs/phases/phase-4/TODO.md
  - docs/STRATEGY.md
  - docs/TODO.md
  - .planning/ROADMAP.md
  - .planning/graphs/GRAPH_REPORT.md
autonomous: true
requirements:
  - DOC-01
  - LANG-05
  - LANG-07

must_haves:
  truths:
    - "CLAUDE.md makes the per-mechanism PRD mandatory for EVERY algorithm or central mechanism (Segal section 2.3). Phase 4 ships three mechanisms and therefore three PRDs: PRD_scent_map, PRD_belief_map, PRD_deception. Roadmap task 04-04 is this row"
    - "D-48: the section 5.3.2 vs section 6.4 contradiction and our choice are written up in docs/phases/phase-4/RULES-RESOLUTION-LANG.md, quoting BOTH sides with book and PDF page numbers. The book's preface (p. v) grants this freedom only on condition that the report states where the contradiction was found, what was chosen and why - so the document is a submission requirement, not a courtesy"
    - "D-49: the same document records why scent is derived locally and never transmitted, and why that is the only reading under which rule 23's lock has a purpose"
    - "The honesty clause from outline section 1 is written into PRD_belief_map.md in plain words: in Regime A the believed-state substitution is the identity and the belief map does not improve move scoring there. Overclaiming would be the kind of misreporting rule 38 exists to punish"
    - "NO BOX IS TICKED IN THIS PLAN. Ticking is /gsd:verify-work 4 (roadmap task 04-99) and only after 04-14 has measured GATE-4. Phase 3's plan 03-24 set this precedent for exactly the right reason"
    - "docs/STRATEGY.md carries exactly three TBD - Phase 4 rows: the belief map, when to lie vs tell the truth, and how opponent hints are weighted against scent. Find them by content with grep, never by line number, and leave the neighbouring TBD - Phase 3 rows untouched. They are filled with what was built, not with what was planned"
    - "D-51 is recorded as a DISCLOSED REVISION of D-40, not as an extension, in BOTH PRD_belief_map.md and RULES-RESOLUTION-LANG.md. 04-CONTEXT.md's locked decision reads 'hint trust: fixed discount weight'; the shipped code has a fixed weight AND an adaptive trust coefficient. A grader who reads the context then the code must find that difference already explained, with the section 4.4 evidence that motivated it - an unexplained gap between a locked decision and the build is exactly what rule 38 punishes"
    - "graphify.enabled is true and roadmap task 04-96 requires the graph refreshed after new code lands. The refresh command is in CLAUDE.md; only GRAPH_REPORT.md is committed"
    - "Every numeric claim in these documents is copied from a plan SUMMARY, never re-derived. A PRD that disagrees with the code is worse than no PRD"
  artifacts:
    - path: docs/PRD_belief_map.md
      provides: "The mechanism PRD: what the belief map is, the Bayes update, the two regimes, and the honest statement of what it does not do"
      contains: "Regime"
    - path: docs/phases/phase-4/RULES-RESOLUTION-LANG.md
      provides: "The documented contradiction and choice the book's preface requires (D-48, D-49)"
      contains: "PDF"
    - path: docs/phases/phase-4/TODO.md
      provides: "The phase task list, every row mapped to a plan, nothing ticked"
      contains: "04-14"
  key_links:
    - from: docs/phases/phase-4/PRD.md
      to: docs/phases/phase-4/RULES-RESOLUTION-LANG.md
      via: "the phase PRD routes a grader to the documented contradiction rather than letting them discover it (D-48)"
      pattern: "RULES-RESOLUTION-LANG"
---

<objective>
Write the documents a grader will actually open, and make them true.

Three per-mechanism PRDs, the rules-resolution note the book's preface demands as the price of
resolving a contradiction, the phase triplet, and the trackers. This plan writes nothing new
about the design — everything here is transcribed from what 04-01…04-12 built and measured.
</objective>

<execution_context>
@C:\Users\Hp\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Hp\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/04-language-and-scent/04-PLAN-OUTLINE.md

All twelve `04-NN-SUMMARY.md` files in this directory are the source for every number and every
signature below. Read them; do not re-derive.

- `CLAUDE.md` — the per-phase triplet rule (grader-facing, mandatory) and the per-mechanism PRD
  rule (Segal §2.3).
- `docs/phases/_TEMPLATE/{PRD,PLAN,TODO}.md` — the skeletons to copy.
- `docs/phases/phase-3/RULES-RESOLUTION.md` — the precedent for a rules document: BOOK vs
  NEGOTIATED rows, quotations with book and PDF pages, no silent inference. Match its shape.
- `docs/phases/phase-3/PRD.md` — the precedent for a phase PRD that routes the reader to a
  deviation defence.
- `docs/PRD_matrix_mover.md`, `docs/PRD_mcp_transport.md` — the house shape for a per-mechanism
  PRD.
- `docs/STRATEGY.md` — the three `TBD — Phase 4` rows this plan fills. Identify them by
  CONTENT, not by line number: the belief-map row, the when-to-lie row, and the
  hint-vs-scent-weighting row. (The neighbouring `TBD — Phase 3` rows are NOT ours to touch.)
</context>

<task type="auto">
  <name>Task 1: the rules-resolution note</name>
  <files>docs/phases/phase-4/RULES-RESOLUTION-LANG.md</files>
  <read_first>
    .planning/phases/04-language-and-scent/04-PLAN-OUTLINE.md section 1 (the whole section)
    docs/phases/phase-3/RULES-RESOLUTION.md (the shape to match)
  </read_first>
  <action>
    Quote **both sides** of the contradiction with book and PDF pages: §5.3.2 and Figure 6 on the
    per-turn Reveal of the Move; §6.4 on neither side seeing the opponent's real location. Quote
    the preface's academic-freedom clause that permits the choice and requires the write-up.

    State the choice (D-48) and the four reasons in outline §1: the protocol reading is required
    by rules 15/16/19/21/22/46–48; §6.4 stays meaningful because the *current* turn is unknown at
    commit time; rule 27 is satisfied by the direction token; and the design degrades to Regime B.

    State D-49 and its argument: rule 23's lock only has a purpose under local derivation.

    Add a **BOOK / NEGOTIATED / DERIVED** table in Phase 3's style for every Phase-4 rule call:
    the three Table 16 values (BOOK), the Figure 4 kernel (DERIVED — transcribed from a figure,
    with the Gaussian observation noted as an observation), the direction vocabulary
    (NEGOTIATED), the hint payload shape (NEGOTIATED), the locked-payload contents (BOOK, §4.5).

    Mark it **binding**, dated, with a "supersedes" line if it changes anything Phase 3 recorded.
  </action>
  <verify>
    Every quotation carries a book page and a PDF page and they differ by 16. Both sides of the
    contradiction are quoted, not paraphrased. Every Phase-4 rule call appears in the table with
    a status. `docs/phases/phase-4/PRD.md` links to this file.
  </verify>
  <done>The condition the book attaches to resolving a contradiction is met, in writing.</done>
</task>

<task type="auto">
  <name>Task 2: the three per-mechanism PRDs</name>
  <files>docs/PRD_scent_map.md, docs/PRD_belief_map.md, docs/PRD_deception.md</files>
  <read_first>
    docs/PRD_matrix_mover.md (house shape)
    all twelve 04-NN-SUMMARY.md files (the numbers)
  </read_first>
  <action>
    **`PRD_scent_map.md`** — the locked model: the three fixed Table 16 values with their source,
    the Figure 4 kernel as a table, the decay law, the worked example, the digest and how the
    handshake enforces it (rule 23), and D-49's local-derivation argument with a pointer to the
    rules note. Include the shipped digest verbatim from 04-01's summary; a league opponent has
    to be able to match it.

    **`PRD_belief_map.md`** — the grid, the motion model, both likelihoods, the reliability
    coefficient and §4.4's worked lie detection reproduced with our own measured numbers from
    04-09's summary. The two regimes as a table. **And the honesty clause, in plain words:** in
    Regime A the believed-state substitution is the identity, so the belief map does not improve
    move scoring there; its value is LANG-05 compliance, the deception channel, and Regime B
    survival.

    Two further disclosures belong in this PRD, both because a grader will otherwise find the gap
    unaided:
    - **D-51 is a revision of D-40, not an extension.** `04-CONTEXT.md` locked "hint trust: fixed
      discount weight"; the build ships a fixed weight `w` **and** an adaptive trust coefficient.
      Say which part survived, that §4.4's worked lie detection is the evidence, and that the
      autonomy directive is the mandate.
    - **We sample from the belief where §6.4 takes `argmax_s b(s)`** (D-43), with the reason.

    **`PRD_deception.md`** — the intent flag and its ordering relative to the text (LANG-03), the
    always-true carve-outs (rules 15/16/21/22) and how the type system enforces them, both role
    policies with the measured lie-rate curves from 04-08's summary, the style guide verbatim
    from 04-10's summary, and the structural rule-25 argument: the CI guard, its command, and its
    passing output.

    Each PRD states its acceptance criteria and links back to the plans that built it.
  </action>
  <verify>
    Every number traces to a summary file. Each PRD names its REQ IDs. `PRD_belief_map.md`
    contains the honesty clause and the D-40/D-51 reconciliation in prose a grader can follow
    without reading the plans.
  </verify>
  <done>Three mechanisms documented to the standard Segal §2.3 requires.</done>
</task>

<task type="auto">
  <name>Task 3: the phase triplet and the trackers</name>
  <files>docs/phases/phase-4/PRD.md, docs/phases/phase-4/PLAN.md, docs/phases/phase-4/TODO.md, docs/STRATEGY.md, docs/TODO.md, .planning/ROADMAP.md</files>
  <read_first>
    docs/phases/_TEMPLATE/{PRD,PLAN,TODO}.md
    docs/phases/phase-3/{PRD,PLAN,TODO}.md (the shape a grader has already been shown)
    CLAUDE.md (the triplet rule and what each file must contain)
  </read_first>
  <action>
    **`phase-4/PRD.md`** — what the phase delivers, the §10.4 milestone gate as the acceptance
    criteria, the LANG REQ-IDs covered, in/out of scope, and a prominent link to
    `RULES-RESOLUTION-LANG.md`.

    **`phase-4/PLAN.md`** — the components and files as built, the wave graph, and the
    phase ADRs **by reference** to `04-PLAN-OUTLINE.md` §2 rather than copied. One authoritative
    source for D-32…D-53.

    **`phase-4/TODO.md`** — one row per plan 04-01…04-14 plus roadmap tasks 04-96/97/99, each
    with priority, status, owner and definition of done. **Learn from Phase 3's namespace
    collision:** here the row IDs and the plan IDs are deliberately the same, so state that
    plainly at the top of the file so no future reader wonders.

    **`docs/STRATEGY.md`** — fill exactly the three `TBD — Phase 4` rows (belief map, when to
    lie vs tell the truth, how opponent hints are weighted against scent) with what was built and
    the measured weights. `grep -n 'TBD' docs/STRATEGY.md` first and leave every `TBD — Phase 3`
    row alone — they belong to a phase that is already closed.

    **`.planning/ROADMAP.md`** — replace Phase 4's `Plans: TBD` and the four placeholder rows
    with the real fourteen. Keep tasks 04-96/97/99. Phase 4's status stays **executing**; its
    success criteria are the book's §10.4 gate and are not ours to edit.

    **Tick nothing.** Status stays ☐ or ◐ everywhere. `/gsd:verify-work 4` ticks, after 04-14.
  </action>
  <verify>
    All three triplet files exist and describe what was built. Every plan 04-01…04-14 appears
    exactly once in `phase-4/TODO.md`. `grep -c "☑"` shows no box ticked by this plan.
    `ROADMAP.md`'s Phase 4 plan list matches the files on disk. No `TBD — Phase 4` remains in
    `docs/STRATEGY.md`.
  </verify>
  <done>A grader opening docs/phases/phase-4/ sees a complete, honest, unticked triplet.</done>
</task>

<task type="auto">
  <name>Task 4: refresh the knowledge graph (roadmap task 04-96)</name>
  <files>.planning/graphs/GRAPH_REPORT.md</files>
  <read_first>
    CLAUDE.md (the graphify section — the exact refresh command and what is committed)
  </read_first>
  <action>
    Run the refresh command from `CLAUDE.md` and copy the three outputs into `.planning/graphs/`.
    **Commit only `GRAPH_REPORT.md`** — `graph.json` and `graph.html` are gitignored build
    artefacts, regenerable at zero API cost.

    Sanity-check the report rather than trusting it: the new `strategy/belief*`, `strategy/scent*`
    and `services/llm/*` modules should appear, and `services/llm/` should have **no edge into**
    `strategy/`. If such an edge exists the layering is broken and that is a finding for the
    summary, not something to paper over.
  </action>
  <verify>
    `GRAPH_REPORT.md` is newer than the last source commit and names the new modules.
    `git status` shows no `graph.json` or `graph.html` staged.
  </verify>
  <done>The graph reflects Phase 4, and it corroborates the layering the CI guard asserts.</done>
</task>

<verification>
1. Three per-mechanism PRDs exist and every number in them traces to a plan summary.
2. `RULES-RESOLUTION-LANG.md` quotes both sides of the contradiction with book+PDF pages and
   states the choice and its reasons — the condition the book's preface attaches.
3. The phase triplet exists, is complete, and **nothing is ticked**.
4. `ROADMAP.md`'s Phase 4 plan list matches the phase directory; tasks 04-96/97/99 survive.
5. `docs/STRATEGY.md` has no remaining `TBD — Phase 4`.
6. `uv run ruff check .` → 0 and the test suite is unchanged — this plan touches no code.
</verification>

<success_criteria>
- Every mechanism this phase built has a PRD a grader can read on its own.
- The one place where the book contradicts itself is documented, with the choice and the reason,
  as the preface requires.
- The trackers are honest about an unmeasured gate rather than optimistic about it.
</success_criteria>

<output>
Create `.planning/phases/04-language-and-scent/04-13-SUMMARY.md`, noting that ticking is deferred
to `/gsd:verify-work 4` after 04-14 measures GATE-4, and recording any layering violation the
graph refresh exposed.
</output>
