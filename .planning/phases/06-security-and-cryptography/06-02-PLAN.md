---
phase: 06-security-and-cryptography
plan: "02"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/pursuit/network/envelope.py
  - src/pursuit/network/tools.py
  - src/pursuit/network/agent_context.py
  - src/pursuit/network/orchestrator.py
  - src/pursuit/network/agent_lifecycle.py
  - src/pursuit/network/agent_wiring.py
  - src/pursuit/network/turn_commit.py
  - src/pursuit/network/turn_commit_wait.py
  - src/pursuit/network/turn_actions.py
  - src/pursuit/network/turn_language.py
  - src/pursuit/network/turn_language_io.py
  - src/pursuit/network/turn_resolve.py
  - tests/unit/test_envelope.py
  - tests/unit/test_tools.py
  - tests/unit/test_agent_context.py
  - tests/unit/test_turn_commit.py
  - tests/unit/test_turn_language.py
  - tests/integration/test_commit_reveal_protocol.py
  - tests/integration/test_gate4.py
  - tests/integration/test_language_pipeline.py
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-03
  - SEC-04
  - SEC-07

must_haves:
  truths:
    - "D-58 (revised): reveal happens only once BOTH sides' commits are locked (book §5.3.2) — the initiator reveals after holding the opponent's ACK of its own commit AND the opponent's own commit; the responder decides its own action upon receiving the initiator's commit, commits+ACKs+sends its own commit inside its wait phase, and reveals only later, from its own take phase. Zero new State members, zero ALLOWED_TRANSITIONS rows."
    - "D-58: a missing ACK/COMMIT/REVEAL rides call_with_retry's existing NetworkParams ladder into the existing TechnicalWin(OPPONENT_UNRESPONSIVE) path; a late or duplicate COMMIT/ACK is dropped/overwritten (04-12 jitter lesson), never raised as HintProtocolError and never a spurious Outcome.TECHNICAL_LOSS"
    - "Deadlock-freedom (traced, not asserted): COMMIT_A arrives → responder decides+commits+sends COMMIT_B+ACK_A, both unconditional on anything the initiator has not already sent → initiator (still inside its own take_my_turn) receives COMMIT_B, ACKs it, and reveals (REVEAL_A) — gated only on conditions the responder's step already satisfied → responder's own later take_my_turn waits only for ACK_B, already sent by the initiator's step above, then reveals REVEAL_B → initiator's own later await_opponent_turn waits only for REVEAL_B. Every wait is satisfied by a step earlier in this same chain; no side ever waits on its own future action."
    - "Resolve-timing safety (the one place a naive implementation deadlocks the OPPONENT, not this side): the responder's OWN action is recorded via record_action/maybe_resolve ONLY in the responder's own later take_my_turn, AFTER its REVEAL has been sent — never during the wait phase where it was decided. Recording it early would let ctx.pending_thief_move and ctx.pending_cop_action both fill inside await_opponent_turn, fire maybe_resolve there, and — if that resolution ends the game — let run_turn_loop's existing 'outcome is not None -> break' short-circuit skip the responder's own take_my_turn entirely, so its REVEAL/COMMIT_B is never sent and the opponent hangs until OPPONENT_UNRESPONSIVE fires against the side that actually won or tied. The opponent's revealed action is still recorded immediately on receipt in await_opponent_turn, unchanged — only the RESPONDER'S OWN slot is deferred."
    - "D-58 side effect (intended, per book §6.4): the responder's decide-now step runs before the initiator's current-turn commit is revealed — only the hash has arrived. known_opponent_cell(ctx, 'thief') therefore returns None (Regime B) at decide time even on turns where the pre-Phase-6 code would have exposed the initiator's just-decided move (ctx.pending_cop_action was set by a full prior await_opponent_turn before choosing ran) — this closes a real blindness leak, not a regression. known_opponent_cell's docstring is updated to say so; the initiator's (police's) regime is unchanged (still D-48's one-turn-stale Regime A)."
    - "toggle-off (security.commit_reveal=false) is byte-equivalent to pre-Phase-6: single MOVE envelope, same call_with_retry, same resolve timing (opponent's action recorded on receipt, own action recorded and resolved in take_my_turn) — proven by a dedicated integration test, not code inspection"
    - "GATE-4 preserved, with one necessary, disclosed exception: turn_language_io.py still calls build_deception_plan() then (later) compose_outgoing() from ITS OWN module namespace, so the patch on those two bare names keeps firing regardless of which outer function (take_my_turn, or turn_commit's decide-now step) triggers them. But the responder's plan and compose calls are now separated by a real network round trip (multiple await points) instead of being back-to-back sync statements — the SAME gap already exists for the initiator once plan is hoisted before commit. tests/integration/test_gate4.py's strict pairwise order[i:i+2]==['plan','compose'] assertion relied on that back-to-back atomicity and is no longer safe under concurrent two-sided interleaving; it is replaced with an identity-based check (every compose's plan object was already produced by an earlier plan call) that proves the SAME real invariant (intent fixed before text exists) without depending on global call-order atomicity."
    - "D-59/D-66: the committed/revealed action is the composite dict {'move': move_payload.encode(pre,dest_or_pre,MOVE), 'barrier': move_payload.encode(cop_pre,cell,BARRIER) | None} — 'move' always present (encoding a real step, or 'stay' when a barrier is placed instead), 'barrier' present only for a cop turn that places one. This mirrors CopAction's own move-XOR-barrier invariant (sdk/actions.py:37-42, __post_init__ raises if both are set) — record_action must never construct CopAction(move=dest, barrier=cell) with both non-None; it branches."
    - "D-66/SEC-07: barrier placement travels over the wire inside the committed action for the first time — turn_language.py's choose surface stops discarding Decision.barrier (today's turn_language.py:81 `.move` and beliefadapter.py's own `decision.move` line both do), record_action gains an optional barrier, the receiver validates it through the ALREADY-SHIPPED move_payload.decode/is_legal(BARRIER) branch (quota via barrier_cells) — an illegal barrier is rejected exactly like an illegal move (technical loss for the sender). Toggle-off stays move-only, byte-equivalent — barriers exist only under commit-reveal (D-66 states this plainly, not an oversight)."
    - "SEC-04/rule 18: the nonce never appears in ctx.log_path's JSONL at any point during play — only h_commit, envelopes and acks do; the nonce lives solely in the CommitLedger file 06-01 built"
    - "orchestrator.py and agent_lifecycle.py were both AT their 150-code-line ceiling before this plan — security/commit_state fields could not be added to AgentContext in place. Splitting AgentContext + Coord + ChooseMove + build_context into src/pursuit/network/agent_context.py, re-exported unchanged from both origins, is this plan's own room-making move, not scope creep."
    - "General rule for Task 4 (state once, apply everywhere): Phase 6 changes BOTH the envelope TYPE (move -> reveal) AND the payload SHAPE (a flat move dict -> the composite {move, barrier|None} dict) of the action-carrying message. Any pre-existing code that reads an action off the wire log — a test's own coordinate-leak check, a replay reconstruction, anything added later — must handle BOTH shapes for as long as the toggle exists: a flat payload for a 'move'-typed record (toggle off, or a legacy peer), the nested payload['move']/payload['barrier'] for a 'reveal'-typed record. A ('move','reveal') type filter alone is necessary but not sufficient — anything that then reads INTO the payload needs the shape branch too."
    - "turn_commit.py's own 150-line pressure is pre-authorized, not improvised mid-execution: it carries three async entry points (initiate/await_and_respond/reveal_pending) plus a multi-condition jitter-tolerant wait loop and the shared commit+ledger-append helper — the wait/dispatch primitives and that helper split out into a sibling turn_commit_wait.py from day one, mirroring the handshake.py/handshake_wire.py/handshake_evaluate.py precedent (policy vs. wire/mechanism split)."
  artifacts:
    - path: src/pursuit/network/agent_context.py
      provides: "AgentContext (+ security: SecurityParams, + commit_state: CommitTurnState), Coord, ChooseMove, build_context, PendingAction, CommitTurnState"
      contains: "CommitTurnState"
    - path: src/pursuit/network/turn_commit.py
      provides: "initiate / await_and_respond / reveal_pending — the D-58 both-locked exchange, both roles (the three PUBLIC entry points only; wire/wait mechanism lives in the sibling)"
      contains: "PendingAction"
    - path: src/pursuit/network/turn_commit_wait.py
      provides: "the jitter-tolerant wait-for-message-type loop + the shared build-composite-payload/commit/ledger-append helper both turn_commit.py entry points call"
      contains: "CommitLedger"
    - path: src/pursuit/network/tools.py
      provides: "receive_commit / receive_ack / receive_reveal / receive_final_reveal via the existing _accept pattern"
      contains: "receive_reveal"
    - path: src/pursuit/network/envelope.py
      provides: "MessageType.COMMIT / ACK / REVEAL / FINAL_REVEAL"
      contains: "FINAL_REVEAL"
  key_links:
    - from: src/pursuit/network/turn_actions.py
      to: src/pursuit/network/turn_commit.py
      via: "take_my_turn branches on ctx.commit_state.pending_action: None -> decide+turn_commit.initiate; set -> turn_commit.reveal_pending. await_opponent_turn calls turn_commit.await_and_respond in place of turn_buffer.await_move."
      pattern: "turn_commit\\.(initiate|await_and_respond|reveal_pending)"
    - from: src/pursuit/network/turn_commit.py
      to: src/pursuit/network/turn_language_io.py
      via: "the responder's decide-now step calls decode_turn_hint / choose_destination / plan_turn_deception by their PUBLIC names in their OWN modules — never reimplemented in turn_commit.py — so the GATE-4/test_language_pipeline spies keep intercepting regardless of the caller"
      pattern: "plan_turn_deception|choose_destination|decode_turn_hint"
    - from: src/pursuit/network/turn_resolve.py
      to: src/pursuit/sdk/actions.py
      via: "record_action builds CopAction(barrier=cell) OR CopAction(move=dest), never both — CopAction's own __post_init__ enforces the XOR"
      pattern: "CopAction\\("
---

<objective>
Wire the 06-01 crypto core into the live turn loop as the book's ACTUAL both-locked exchange
(§5.3.2: reveal only once both sides are committed), carrying the cop's barrier placement inside
the committed action for the first time (D-66/SEC-07). When `security.commit_reveal` is off, the
wire and the resolve timing are byte-identical to today.

Purpose: SEC-01/02/03/04/07 — the four-phase protocol runs on the wire with the real gate the
book requires, and barrier declarations are finally open, not silently dropped.
Output: `turn_commit.py` (initiator + responder paths), `agent_context.py`, the widened choose
surface, four new MessageType members, a two-peer integration test proving all of it.
</objective>

<execution_context>
@C:\Users\Hp\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Hp\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/06-security-and-cryptography/06-PLAN-OUTLINE.md
@.planning/phases/06-security-and-cryptography/06-RESEARCH.md
@.planning/phases/06-security-and-cryptography/06-01-SUMMARY.md
- `src/pursuit/network/turn_actions.py` — read the WHOLE file first; 148/150 measured code
  lines. `run_turn_loop`'s existing `if outcome is not None or ctx.machine.state in
  TERMINAL_STATES: break` (orchestrator.py) is why the resolve-timing must_have above matters —
  a non-None outcome from the FIRST call of the pair skips the SECOND call outright.
- `src/pursuit/network/turn_resolve.py` — `record_action`/`maybe_resolve`, read in full. Today
  `record_action` for "police" is the ONLY call site building `CopAction`; the thief branch
  never does. `maybe_resolve` fires once per pair, clears both slots + `pending_hints`.
- `src/pursuit/sdk/actions.py` — `CopAction` (move XOR barrier, raises otherwise, lines 24-51),
  `cop_actions`/`barrier_cells`/`thief_actions`. Book Sec3.4 p.21 quoted in this file's own
  docstring: the cop moves XOR places one barrier on its own cell or one neighbour.
- `src/pursuit/strategy/base.py` — `Decision` (line 43-60): `move`, `source`, `barrier: Coord |
  None = None`, always None for the thief (only the cop's `_decide_move` may set it).
- `src/pursuit/network/turn_language.py` — `choose_destination` (line 56-81) discards
  `decision.barrier` on BOTH the `BeliefAdapter` branch (`return decision.move`) AND the raw-brain
  branch (line 81, `.move`). `known_opponent_cell`'s own docstring documents the Regime-A leak
  this plan closes for the thief — read it before editing.
- `src/pursuit/network/turn_language_io.py` / `tests/integration/test_gate4.py` /
  `tests/integration/test_language_pipeline.py` — read
  `test_intent_is_always_committed_before_the_hint_text_exists` and
  `test_figure_7_order_is_decode_then_move_then_deception_then_compose` in full BEFORE touching
  anything in `turn_language_io.py` or `turn_commit.py`. The first needs the identity-based fix
  this plan specifies (see must_haves); the second is untouched (single-side, single call, no
  concurrency, unaffected).
- `tests/integration/two_peer_game.py` — the harness Task 4's test reuses.
- `tests/integration/test_language_pipeline.py`'s `_replay_from_log` — filters
  `envelope["type"] != "move"`. Fixed the same way as `test_gate4.py`'s `_moves()` (see Task 4).
</context>

<tasks>

<task type="auto">
  <name>Task 1: four new wire message kinds</name>
  <files>src/pursuit/network/envelope.py, src/pursuit/network/tools.py, tests/unit/test_envelope.py, tests/unit/test_tools.py</files>
  <action>
    `envelope.py`: add `COMMIT = "commit"`, `ACK = "ack"`, `REVEAL = "reveal"`, `FINAL_REVEAL =
    "final_reveal"` to `MessageType`. No change to `Envelope`'s four-key shape, `EnvelopeKey`, or
    `from_dict`'s validation — additive only.

    `tools.py`: add four `@mcp.tool` handlers — `receive_commit`, `receive_ack`, `receive_reveal`,
    `receive_final_reveal` — each `return await _accept(queue, MessageType.X, turn, sender,
    payload)`, byte-for-byte mirroring `receive_hint`. No payload validation at this layer.
    `register_tools`'s signature is unchanged.
  </action>
  <verify>
    Each new MessageType round-trips through `to_dict`/`from_dict`; the nine-member set is
    exhaustive. Each new handler enqueues a decodable Envelope of the right type and returns the
    generic ack dict; a malformed payload raises `ToolError`.
  </verify>
  <done>Four new message kinds exist on the wire, decode correctly, and enqueue exactly like
  every existing message kind.</done>
</task>

<task type="auto">
  <name>Task 2: the AgentContext split — security, and the D-58/D-66 scratch state</name>
  <files>src/pursuit/network/agent_context.py, src/pursuit/network/orchestrator.py, src/pursuit/network/agent_lifecycle.py, src/pursuit/network/agent_wiring.py, tests/unit/test_agent_context.py</files>
  <action>
    Create `agent_context.py`: move `Coord`, `ChooseMove`, `AgentContext` (verbatim), and
    `build_context` (verbatim, moved out of `agent_lifecycle.py`) here. Add:
    - `security: SecurityParams` (from `pursuit.shared.security_config`, 06-01) — required, no
      default (every construction site must be explicit).
    - `commit_state: CommitTurnState` — `field(default_factory=CommitTurnState)`, defined in this
      same file:
      ```
      @dataclass
      class PendingAction:
          """The responder's fully-decided action for THIS turn (D-58),
          built once during await_opponent_turn's decide-now step and
          consumed once by this role's own later take_my_turn — never
          rebuilt, so a stateful brain (BeliefAdapter's RNG/belief) is
          invoked exactly once per turn."""
          move: Coord
          barrier: Coord | None
          plan: object | None   # DeceptionPlan | None — object to avoid a
                                 # shared/network import cycle here; the
                                 # real type lives in deception_types.py
          incoming_log: dict
          regime: str

      @dataclass
      class CommitTurnState:
          """Per-game mutable holder, one instance per AgentContext, never
          reconstructed mid-game. Every field returns to its idle default
          at turn resolution (D-58, D-66)."""
          pending_action: PendingAction | None = None
          own_ack_received: bool = False
          chosen_barrier: Coord | None = None
          """The barrier companion to choose_destination's last return
          value (D-66) — choose_destination stays a thin Coord-only
          wrapper (existing callers/tests need that), so its full Decision
          is stashed here as a side effect, read immediately after by
          whichever function just called it. Reset on every
          choose_destination call, never left stale across turns."""
      ```

    `orchestrator.py`: replace the removed definitions with `from pursuit.network.agent_context
    import AgentContext, ChooseMove, Coord` and re-export them (module-level import — no
    circularity, `agent_context.py` never imports `orchestrator.py`). Every existing
    `from pursuit.network.orchestrator import AgentContext` call site keeps working unchanged.

    `agent_lifecycle.py`: replace the removed `build_context` with `from
    pursuit.network.agent_context import build_context` and re-export it. `default_context` calls
    `build_context` exactly as before.

    `agent_wiring.py`: add `security: SecurityParams` to `AgentConfig`; `load_agent_config` adds
    `security = load_security_config(directory / "security.json")` and passes it through.
  </action>
  <verify>
    Constructing `AgentContext` without `security` raises TypeError (no default);
    `ctx.commit_state` defaults to an idle `CommitTurnState()` with no explicit construction
    needed by any existing test fixture. Every pre-existing test importing
    `AgentContext`/`Coord`/`ChooseMove`/`build_context` from their OLD module paths still passes
    with zero edits to those test files — run the FULL existing suite, not just the new file.
    `bash scripts/check_line_limit.sh` — `orchestrator.py`/`agent_lifecycle.py` both drop well
    under 150; `agent_context.py` itself is comfortably under 150.
  </verify>
  <done>AgentContext carries `security` and `commit_state`; every file that was at its line
  ceiling before this task has room again; no existing import path broke.</done>
</task>

<task type="auto">
  <name>Task 3: the widened choose surface, record_action's barrier, and turn_commit.py's D-58 exchange</name>
  <files>src/pursuit/network/turn_language.py, src/pursuit/network/turn_language_io.py, src/pursuit/network/turn_resolve.py, src/pursuit/network/turn_commit.py, src/pursuit/network/turn_commit_wait.py, src/pursuit/network/turn_actions.py, tests/unit/test_turn_language.py, tests/unit/test_turn_commit.py</files>
  <action>
    `turn_language.py` — `choose_destination` keeps its EXACT signature and Coord return type
    (test_turn_language.py's two direct tests, and turn_actions.py's own spied call site, both
    depend on this). Internally it now computes the FULL `Decision` on every branch (the
    `ctx.choose_move` override and `first_legal_move` placeholder paths wrap their bare `Coord`
    into a `Decision(move=cell, source=..., barrier=None)` — barrier-less by construction) and,
    before returning `.move`, sets `ctx.commit_state.chosen_barrier = decision.barrier`
    unconditionally (never leaving a prior turn's value stale). Fix the two lines that silently
    discarded `.barrier` today: the `BeliefAdapter` branch's `return decision.move` and the raw
    brain branch's `.move` (turn_language.py:81) — both now flow through this same
    decision-then-stash-then-return path. Update `known_opponent_cell`'s docstring: for the
    thief, Regime A no longer holds — the responder now decides before the initiator's
    current-turn commit is revealed (D-58), so `known_opponent_cell(ctx, "thief")` is Regime B on
    every turn, not just turn 0.

    `turn_language_io.py` — as already planned: `plan_turn_deception(ctx, agent, pre_turn_state,
    dest) -> DeceptionPlan` (thin wrapper calling the bare `build_deception_plan` name) and
    `compose_and_send_hint(ctx, plan, turn, started, budget, regime, incoming_log) -> None`
    (everything `send_turn_hint` did after the plan existed). Delete `send_turn_hint`.

    `turn_resolve.py` — `record_action(ctx, role, dest, barrier=None)`: for `role == "police"`,
    `ctx.pending_cop_action = CopAction(barrier=barrier) if barrier is not None else
    CopAction(move=dest)` — NEVER `CopAction(move=dest, barrier=barrier)` with both set
    (`__post_init__` raises). For the thief, `ctx.pending_thief_move = dest` unchanged (barrier
    is always None for the thief per `Decision`'s own contract; the parameter exists for
    signature symmetry, not because the thief ever uses it).

    `turn_commit.py` (new) + `turn_commit_wait.py` (new sibling, split from day one — pre-authorized,
    mirroring handshake.py/handshake_wire.py/handshake_evaluate.py's policy-vs-mechanism split, not
    improvised once the file approaches 150 lines): `turn_commit.py` holds ONLY the three public
    entry points below (`initiate`/`await_and_respond`/`reveal_pending`) and the policy decisions
    (what to send when, what gates a reveal); `turn_commit_wait.py` holds the jitter-tolerant
    wait-for-a-message-type loop (built once, parameterized by expected `MessageType`, reused for
    "wait ACK", "wait opponent COMMIT", "wait REVEAL" — tolerant of an interleaved HINT and of a
    late/duplicate COMMIT/ACK, mirroring `turn_buffer.await_move`'s own shape without touching that
    file) AND the shared "build composite payload → `commit_pack.commit` → `CommitLedger.append`"
    helper both `initiate` and `await_and_respond` call to commit their own action identically.
    `turn_commit.py` imports from `turn_commit_wait.py`, never the reverse. All three public
    entry points, `ctx.security.commit_reveal is False` delegates to today's exact byte-equivalent
    behavior in every case:

    `async def initiate(ctx, current, pre_cell, dest, barrier, plan) -> Outcome | None` —
    replaces `take_my_turn`'s old inline block, called ONLY when `ctx.commit_state.pending_action
    is None` (this side is the initiator this turn). Off: send the single MOVE envelope exactly
    as pre-Phase-6. On: build the composite action dict (D-59/D-66 — `move`:
    `move_payload.encode(pre_cell, pre_cell if barrier is not None else dest, MOVE)`, `barrier`:
    `move_payload.encode(cop_pre, barrier, BARRIER) if barrier is not None else None`), the state
    record, `intent = plan.intent.value if plan is not None else Intent.TRUTH.value`; call
    `commit_pack.commit`; append to `CommitLedger` at `ctx.log_path`'s `.ledger.jsonl` sibling
    BEFORE any send; send COMMIT (`{"h_commit": h_commit}`) via `call_with_retry` (existing
    technical-loss pattern); WAIT until BOTH (a) an ACK naming our `h_commit` has arrived AND (b)
    the opponent's own COMMIT has arrived — ACK the opponent's COMMIT the moment it arrives,
    regardless of whether our own ACK has arrived yet; once both hold, send REVEAL (the same
    composite dict) via `call_with_retry`; log `turn_record`s for COMMIT/ACK/REVEAL — h_commit is
    public, the nonce never appears in any of these. A late/duplicate ACK or COMMIT arriving
    after its condition is already satisfied is dropped, not re-processed.

    `async def await_and_respond(ctx) -> tuple[Envelope | None, TechnicalWin | None]` — replaces
    `await_opponent_turn`'s `turn_buffer.await_move` call. Off: `return await
    turn_buffer.await_move(ctx)` verbatim. On: wait for the opponent's COMMIT (tolerant of an
    interleaved HINT, same primitives `turn_buffer.py` itself uses — do not add a parameter to
    that file); the MOMENT it arrives, decide THIS side's own action for this turn by calling, in
    order, the public `decode_turn_hint` (fresh `started = time.monotonic()`,
    `budget = turn_budget_seconds(ctx.net) if ctx.language is not None else 0.0`),
    `choose_destination` (reads `ctx.commit_state.chosen_barrier` right after, per Task 3's own
    `turn_language.py` change), and — `if ctx.language is not None` — `plan_turn_deception`; do
    NOT call `record_action`/`maybe_resolve` here (resolve-timing safety, see must_haves) — only
    build a `PendingAction(move=dest, barrier=barrier, plan=plan, incoming_log=incoming_log,
    regime=regime)` and set `ctx.commit_state.pending_action = pending`; commit THIS side's own
    action the same way `initiate` does (build payload, `commit_pack.commit`, ledger append
    BEFORE any send); send this side's own COMMIT, then ACK the opponent's COMMIT (either order
    between these two is fine — both are unconditional at this point); then wait for the
    opponent's REVEAL (tolerant of interleaving, same primitives); return that REVEAL envelope in
    `await_move`'s own shape.

    `async def reveal_pending(ctx) -> Outcome | None` — called from `take_my_turn` when
    `ctx.commit_state.pending_action is not None` (this side is the responder this turn, already
    decided). Wait for the ACK of THIS side's own commit if it has not already arrived (tolerant,
    existing primitives); send REVEAL from `pending.move`/`pending.barrier`; return `None` on
    success, `Outcome.TECHNICAL_LOSS` on the existing failure pattern. Does NOT clear
    `ctx.commit_state.pending_action` itself — `take_my_turn` clears it after using the other
    `pending` fields for the hint.

    `turn_actions.py` — `take_my_turn`: branch on `ctx.commit_state.pending_action`.
    - `None` (initiator): unchanged decode/choose sequence through `dest = choose_destination(
      ctx, agent, inference, known)`; then `barrier = ctx.commit_state.chosen_barrier if agent ==
      "cop" else None`; `plan = plan_turn_deception(ctx, agent, pre_turn_state, dest) if
      ctx.language is not None else None`; `record_action(ctx, ctx.role, dest, barrier)`;
      `outcome = maybe_resolve(ctx)` (unchanged position/semantics — still a no-op here in normal
      play, since the opponent's slot isn't filled yet); `result = await turn_commit.initiate(
      ctx, current, pre_cell, dest, barrier, plan); if result is not None: return result`; `if
      ctx.language is not None: await compose_and_send_hint(ctx, plan, ctx.state.turn, started,
      budget, regime, incoming_log)`.
    - not `None` (responder, already decided): `pending = ctx.commit_state.pending_action`;
      `record_action(ctx, ctx.role, pending.move, pending.barrier)`; `outcome =
      maybe_resolve(ctx)` (this now legitimately fires here — mirrors today's exact position for
      this role); `result = await turn_commit.reveal_pending(ctx); if result is not None: return
      result`; `if ctx.language is not None: fresh_started = time.monotonic();
      await compose_and_send_hint(ctx, pending.plan, ctx.state.turn, fresh_started, budget_fresh,
      pending.regime, pending.incoming_log)` — a FRESH budget window, not the stale one from the
      decide-now step, since the commit/ack/reveal round trip already consumed real time between
      deciding and revealing; `ctx.commit_state.pending_action = None` (clear last).
    In BOTH branches, `ctx.machine.attempt(State.WAIT_OPPONENT)` at the end, unchanged.
    `await_opponent_turn`: replace `turn_buffer.await_move(ctx)` with
    `turn_commit.await_and_respond(ctx)`. Its decode/validate/`record_action` block (unchanged
    POSITION — still right after the wait call returns, still records the OPPONENT's revealed
    action on receipt) now branches explicitly on `ctx.security.commit_reveal`, since the payload
    SHAPE differs (SEC-07 — this is where a forged barrier would be caught, so the branch must not
    be under-scoped): off → today's single flat `move_payload.decode(move_envelope.payload,
    pre_cell, ctx.params)` call, unchanged. On → decode `move_envelope.payload["move"]` via
    `move_payload.decode`/`is_legal` exactly as today; separately, when
    `move_envelope.payload.get("barrier")` is not None, decode+validate THAT through the SAME
    `move_payload.decode`/`is_legal(BARRIER)` branch (quota via `barrier_cells`, already shipped).
    Either part failing legality (an illegal move OR an illegal/forged barrier) takes the existing
    `turn_buffer.reject_peer_payload` path — technical loss for the sender, exactly like an
    illegal move does today. Only once both parts (or the single move, when barrier is null) are
    legal does `record_action(ctx, move_envelope.sender, resolved_move.cell,
    resolved_barrier.cell if barrier present else None)` run.
  </action>
  <verify>
    `test_turn_language.py`: the two existing direct `choose_destination` tests pass unmodified
    (still return a bare Coord); a new test with a scripted barrier-returning brain confirms
    `ctx.commit_state.chosen_barrier` is set correctly and reset to `None` on a subsequent
    barrier-less call.
    `test_turn_commit.py`, fake-driven, no real network: off-toggle byte-equivalence for all
    three functions; on-toggle `initiate` sends COMMIT then (only after both conditions) REVEAL,
    with the ledger appended before either send; `await_and_respond` decides via the real
    `decode_turn_hint`/`choose_destination`/`plan_turn_deception` names (assert via spy, mirroring
    the GATE-4 technique) exactly once, builds a `PendingAction`, and does NOT call
    `record_action` (assert `ctx.pending_thief_move`/`ctx.pending_cop_action` unchanged by this
    call alone); `reveal_pending` sends REVEAL from the stashed pending action without deciding
    again; a faked duplicate ACK/COMMIT is dropped, never raised. `bash scripts/check_line_limit.sh`
    — re-check `turn_commit.py` AND `turn_commit_wait.py` both individually (this is the split
    pre-authorized above, not a fallback to reach for only if one file runs over).
  </verify>
  <done>Both roles drive the full both-locked Commit→Ack→Reveal exchange when the toggle is on,
  with the responder deciding blind during its wait and revealing from its own take — and the
  untouched pre-Phase-6 exchange when it is off.</done>
</task>

<task type="auto">
  <name>Task 4: prove it — two-peer protocol test, forced barrier, and the three foreseeable casualties</name>
  <files>tests/integration/test_commit_reveal_protocol.py, tests/integration/test_gate4.py, tests/integration/test_language_pipeline.py</files>
  <action>
    New `test_commit_reveal_protocol.py`, built on `two_peer_game.py`:
    1. Full game, real shipped configs (commit_reveal defaults true). Assert both sides' wire
       logs contain `commit`/`ack`/`reveal` envelope types and zero `move`-typed envelopes; zero
       `"nonce"` occurrences in either JSONL; a matching nonce-bearing record count in each side's
       own `.ledger.jsonl`.
    2. Both-locked-gate assertion (this plan's own core proof): in each side's own JSONL, for
       every turn, the record of THIS side's own REVEAL sent appears strictly after the record of
       the OPPONENT's COMMIT received, by JSONL line order.
    3. Forced cop barrier: wire a scripted fake brain (implementing `_decide_move`, NOT
       `ctx.choose_move` — the override seam never carries a barrier) that returns a barrier
       `Decision` on one specific cop turn. Assert: the committed/revealed composite dict on that
       turn carries a non-null `barrier` key; both sides' independently-resolved `ctx.state`
       after that turn show the SAME barrier cell in `barriers` and the SAME `barriers_placed`
       count; the quota (`barrier_quota`) is respected by both engines identically.
    4. Toggle-off byte-equivalence: `dataclasses.replace` both configs' `security.commit_reveal`
       to `False`; play; assert the wire log looks EXACTLY like a pre-Phase-6 game (only
       `move`/`hint`/`handshake`/`game_over` types, no barrier ever placed since the override seam
       never produces one — consistent with D-66's "barriers exist only under commit-reveal").
    5. Jitter tolerance: inject a duplicate ACK after the real one; assert the game still reaches
       a normal (non-technical-loss) outcome.

    Phase 6 changes BOTH the envelope TYPE (move → reveal) AND the payload SHAPE (a flat move dict
    → the composite `{move, barrier|None}` dict) of the action-carrying message under the
    (default-true) toggle. A type-filter widening ALONE is wrong wherever the found code also
    reads INTO the payload — three such sites exist, all fixed the same shape-aware way (flat
    payload for a `move`-typed record, `payload["move"]`/`payload["barrier"]` for a `reveal`-typed
    one):
    - `test_gate4.py`'s `_moves()` (its OWN filter widens to `envelope["type"] in ("move",
      "reveal")`) plus its call site in `test_a_hint_rides_every_turn_with_no_outgoing_coordinate`:
      today's flat `assert "x" not in payload; assert "y" not in payload` becomes: for a
      `reveal`-typed record, check `payload["move"]` and, when `payload.get("barrier")` is not
      None, `payload["barrier"]` too; for a `move`-typed record, keep today's flat check unchanged.
      Un-widened, this check would still technically pass (the composite dict's TOP level never
      has an "x"/"y" key either) but would stop examining the level a coordinate could actually
      leak at — checking the nested dicts explicitly is what keeps rule 27's real guarantee alive.
    - `test_language_pipeline.py:134-137`, inside
      `test_two_peer_game_carries_a_direction_move_and_a_hint_every_turn` — a THIRD, separate
      filter site (not `_replay_from_log`), currently `assert "x" not in payload; assert
      "direction" in payload`. Under the composite shape `"direction" in payload` is FALSE at the
      top level (a HARD failure, not a vacuous one, once the type filter alone is widened) — apply
      the identical shape-aware fix: `"direction" in payload["move"]` for a `reveal`-typed record
      (and the same check on `payload["barrier"]` when present), the flat check unchanged for a
      `move`-typed record.
    - `test_language_pipeline.py`'s `_replay_from_log()` — widening `envelope["type"] != "move"`
      to exclude `"reveal"` too is not enough: `decode_move(envelope["payload"], pre, params)`
      would then be handed the COMPOSITE dict, and `move_payload.decode` returns `ok=False`
      ("payload has neither direction nor x/y keys") for it, so the replay silently stops
      advancing (an `assert resolved.ok` failure, not a silent skip). Fix: for a `reveal`-typed
      record, decode `envelope["payload"]["move"]` for the move half; when
      `envelope["payload"].get("barrier")` is not None, ALSO decode `envelope["payload"]["barrier"]`
      (same `pre` cell — both sub-payloads encode a direction relative to the cop's OWN pre-turn
      cell) and build `CopAction(barrier=resolved_barrier.cell)` instead of
      `CopAction(move=resolved_move.cell)` for that turn — this keeps the replay genuinely
      faithful (rule 20's own requirement) rather than silently narrowing its scope to
      movement-only. The existing `cop_cell, thief_cell = pending_cop.move, pending_thief` line
      must change to `cop_cell = pending_cop.destination(cop_cell); thief_cell = pending_thief` —
      `CopAction.destination()` already encodes "unchanged when placing," and `.move` is `None` in
      the barrier case, so the old line would silently set `cop_cell = None` on any turn a barrier
      was placed.

    Fix `test_gate4.py`'s `test_intent_is_always_committed_before_the_hint_text_exists` per the
    must_haves entry above: replace the flat `order: list[str]` with entries carrying the plan
    object itself — `spy_plan` appends `("plan", result)` (the real `build_deception_plan`
    return value); `spy_compose` appends `("compose", args[0])` (the `plan` positional argument
    `compose_outgoing` was actually called with). After the game: every `("compose", plan_obj)`
    entry's `id(plan_obj)` must already be present among the `id()`s of every `("plan", ...)`
    entry recorded strictly earlier in the list — i.e. no compose ever runs on a plan this same
    spy has not already seen produced. Keep the existing `outcome_a == outcome_b` assertion and
    the "at least one plan/compose pair happened" assertion; only the pairing check changes.
  </action>
  <verify>
    `uv run pytest tests/integration/test_commit_reveal_protocol.py tests/integration/test_gate4.py
    tests/integration/test_language_pipeline.py -v` — all green. Re-run the full suite
    (`uv run pytest tests/`); anything else that broke is a real regression to fix here, not to
    paper over.
  </verify>
  <done>The both-locked four-phase protocol is observed end to end; a forced barrier round-trips
  identically on both engines; the toggle-off path is proven byte-equivalent; GATE-4's real
  invariant (intent before text) is proven under legitimate cross-side interleaving, not just
  under the old atomic-per-side assumption.</done>
</task>

</tasks>

<verification>
1. `uv run ruff check .` → 0; `uv run pytest tests/ --cov` → ≥ 85%, 0 failures, full suite.
2. `bash scripts/check_line_limit.sh` clean — re-check `turn_actions.py`, `turn_language.py`,
   `turn_language_io.py`, `turn_resolve.py`, `orchestrator.py`, `agent_lifecycle.py`,
   `agent_wiring.py`, `turn_commit.py`, `turn_commit_wait.py` (the pre-authorized split).
3. `git diff -- src/pursuit/network/state_machine.py src/pursuit/network/turn_buffer.py` is empty.
4. A manual trace check: `grep -n "CopAction(move=.*barrier=" src/pursuit/network/turn_resolve.py`
   matches nothing — `record_action` never constructs both fields non-None in one call.
</verification>

<success_criteria>
- Four new message kinds exist and are exercised by a real two-peer game.
- Reveal happens only after BOTH sides are locked, on both the initiator and responder paths,
  with a traced deadlock-freedom argument and a verified resolve-timing safety property.
- Barrier placement travels over the wire under commit-reveal, round-trips identically on both
  engines, and respects the quota; toggle-off stays move-only and byte-equivalent.
- GATE-4's real invariant survives under the new, legitimately concurrent timing.
</success_criteria>

<output>
Create `.planning/phases/06-security-and-cryptography/06-02-SUMMARY.md` with `turn_commit.py`'s
three function signatures, `PendingAction`/`CommitTurnState`'s shape, the composite action dict
shape, and the ledger filename convention — 06-03 reads its own ledger by this same convention.
</output>
