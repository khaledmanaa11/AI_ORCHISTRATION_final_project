---
phase: 06-security-and-cryptography
plan: "04"
type: execute
wave: 4
depends_on: ["06-03"]
files_modified:
  - scripts/measure_gate6.py
  - docs/phases/phase-6/GATE-6-MEASUREMENT.md
  - docs/phases/phase-6/gate6_measurement_evidence.json
  - docs/PRD_commit_reveal.md
  - .planning/graphs/GRAPH_REPORT.md
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-03
  - SEC-04
  - SEC-05
  - SEC-06
  - SEC-07
  - SEC-08

must_haves:
  truths:
    - "GATE-6 needs no credentials, no env vars, and no second machine (06-PLAN-OUTLINE.md §5) — measure_gate6.py runs entirely on localhost against the real, shipped config/police and config/thief, exactly like 06-02/06-03's own integration tests, and is invoked as `uv run python scripts/measure_gate6.py` with zero required arguments"
    - "The three §10.4 milestone-6 criteria are quoted VERBATIM from ROADMAP.md into GATE-6-MEASUREMENT.md, each with its own PASS/FAIL/PENDING marker and a method — mirroring GATE-5's own live-PENDING discipline (docs/phases/phase-5/GATE-5-MEASUREMENT.md), never a fabricated PASS"
    - "Criterion 1 (four phases run, valid nonce) is measured by counting commit/ack/reveal envelope types in a real played game's own JSONL logs, plus one final_reveal per side at game end — not asserted from source reading alone"
    - "D-58: Criterion 1's evidence also includes the both-locked-gate check — for every turn, in each side's own JSONL, the own-REVEAL-sent record appears strictly after the opponent-COMMIT-received record (06-02's own must_haves proof, re-measured here on the real shipped config rather than a fake-driven unit test)"
    - "D-66: the evidence honestly reports a barrier-placement count observed in the clean run (0 is an honest report if the real cop brain never chose to place one this run — never fabricated to look more thorough); the forced-barrier round-trip PROOF itself lives in 06-02's own integration test, not duplicated here"
    - "Criterion 2 (canonical hash, nonce secret till end, mismatch = technical loss) is measured by the SAME two tamper-harness proofs 06-03 already unit-tests, re-run here end to end through measure_gate6.py itself and recorded as evidence: (a) a corrupted ledger PAYLOAD produces AUDIT_HASH_MISMATCH and Outcome.TECHNICAL_LOSS in a real run; (b) D-67's own bypass case — an untouched hash paired with a swapped revealed action — ALSO produces AUDIT_HASH_MISMATCH, proving the hash-only check alone would have missed it; plus a grep-based nonce-absence check over the wire-mirroring logs"
    - "Criterion 3 (Step-0 verified before move 1) is measured by confirming declaration_<game_id>.json exists and is timestamped/ordered before the first move record in both sides' logs, plus a SEPARATE run with a deliberately mismatched step0 digest proving the abort happens via HandshakeOutcome.STEP0_MISMATCH before any move"
    - "docs/PRD_commit_reveal.md follows the house PRD structure (docs/PRD_mcp_transport.md's own section order: mechanism/scope, requirements table citing every SEC-ID, topology/design, interfaces, out of scope) and traces every number to 06-PLAN-OUTLINE.md §2 — nothing invented"
    - "Graph refresh (06-96): .planning/graphs/GRAPH_REPORT.md is regenerated after this phase's code lands, so it reflects security/, turn_commit.py, agent_context.py and the handshake extensions"
  artifacts:
    - path: scripts/measure_gate6.py
      provides: "the fully-scriptable localhost GATE-6 run + tamper harness"
      contains: "AUDIT_HASH_MISMATCH"
    - path: docs/phases/phase-6/GATE-6-MEASUREMENT.md
      provides: "criteria verbatim + measured PASS/FAIL evidence"
      contains: "§10.4"
    - path: docs/PRD_commit_reveal.md
      provides: "the per-mechanism PRD for the commit-reveal protocol"
      contains: "SEC-01"
  key_links:
    - from: scripts/measure_gate6.py
      to: tests/integration/two_peer_game.py
      via: "reuses the SAME real two-peer harness 06-02/06-03's tests already use — never a second, parallel game-runner"
      pattern: "play_two_peer_game"
    - from: docs/phases/phase-6/GATE-6-MEASUREMENT.md
      to: docs/phases/phase-6/gate6_measurement_evidence.json
      via: "every PASS claim in the markdown points at a field in the evidence JSON a human can re-open"
      pattern: "gate6_measurement_evidence.json"
---

<objective>
Close the phase: run the real, shipped configuration through the whole commit-reveal lifecycle
once, capture it as evidence, and write the two mandatory documents (the phase gate report and
the per-mechanism PRD) plus the graph refresh.

Purpose: prove — not just assert — that §10.4 milestone 6's three criteria hold.
Output: `measure_gate6.py`, `GATE-6-MEASUREMENT.md`, `docs/PRD_commit_reveal.md`, refreshed graph.
</objective>

<execution_context>
@C:\Users\Hp\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Hp\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/06-security-and-cryptography/06-PLAN-OUTLINE.md
@.planning/phases/06-security-and-cryptography/06-01-SUMMARY.md
@.planning/phases/06-security-and-cryptography/06-02-SUMMARY.md
@.planning/phases/06-security-and-cryptography/06-03-SUMMARY.md
- `.planning/ROADMAP.md` lines 166-187 — Phase 6's goal and the three §10.4 criteria, quoted
  verbatim into the gate report, not paraphrased.
- `docs/phases/phase-5/GATE-5-MEASUREMENT.md` + `docs/phases/phase-5/gate5_smoke_evidence.json` —
  the exact structure and PASS/PENDING discipline to mirror (criterion header, method, run
  command, evidence-field table).
- `docs/PRD_mcp_transport.md` — the house PRD structure/section order to mirror exactly (§1
  mechanism+scope+requirements table, topology, design, interfaces, out of scope). Read its
  first ~60 lines before writing.
- `scripts/gate4_report.py` / `scripts/measure_gate4.py` — the pattern for a script that plays a
  real game and writes a structured evidence artifact.
- `CLAUDE.md`'s `graphify` section — the exact refresh command:
  `graphify update . && cp graphify-out/{graph.json,graph.html,GRAPH_REPORT.md}
  .planning/graphs/`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: measure_gate6.py — the scriptable localhost run + tamper harness</name>
  <files>scripts/measure_gate6.py, docs/phases/phase-6/gate6_measurement_evidence.json</files>
  <action>
    Build on `tests/integration/two_peer_game.py`'s `play_two_peer_game`, the REAL
    `config/police`/`config/thief` (commit_reveal true, the shipped default — no synthetic
    override). The script:
    1. Plays one full clean game; counts commit/ack/reveal/final_reveal envelope occurrences per
       side from the JSONL logs; confirms zero `"nonce"` occurrences in either wire-mirroring
       log and a matching nonce-bearing record in each side's own `.ledger.jsonl`; confirms
       `declaration_<game_id>.json` exists on both sides with the SAME `game_id` and predates
       every move-turn record; confirms the D-58 both-locked-gate ordering — for every turn, in
       each side's own log, the own-REVEAL-sent record's line number is strictly greater than the
       opponent-COMMIT-received record's line number for that same turn; reports a barrier
       occurrence count (how many turns, across both sides, revealed a non-null `barrier` in
       their composite action dict this run) — an honest count, 0 is a valid outcome of a clean
       run where the scripted cop brain never happened to place one; the forced-round-trip PROOF
       that a barrier CAN be committed/revealed/applied correctly is 06-02's own integration test,
       cited here by name, not re-run.
    2. Runs the tamper harness TWICE, mirroring 06-03's own two unit-level proofs end to end
       through a real run: (a) replays the setup, corrupts one ledger entry's PAYLOAD before the
       Final-Reveal exchange, confirms the honest side's returned `Outcome` is `TECHNICAL_LOSS`
       with an `AUDIT_VERDICT` event naming `AUDIT_HASH_MISMATCH`; (b) replays again, this time
       leaving the hash/payload untouched but swapping in a different legal action as the
       revealed `move` for one turn (D-67's own bypass case), confirms the SAME
       `AUDIT_HASH_MISMATCH`/`TECHNICAL_LOSS` outcome even though the hash alone would have
       verified.
    3. Runs a Step-0-mismatch handshake (a forged local step0 digest on one side) and confirms
       the abort happens via `HandshakeOutcome.STEP0_MISMATCH` before either side's turn loop
       ever starts (mirrors 06-03's `test_handshake_step0.py`, run here as a live measurement,
       not just a unit assertion).
    Writes `docs/phases/phase-6/gate6_measurement_evidence.json` with one section per criterion
    (1/2/3), each carrying the measured counts/booleans above and a UTC timestamp — mirroring
    `gate5_smoke_evidence.json`'s own field style. No numeric threshold in this script is
    invented; every count is simply reported, not compared against an unsourced target.
  </action>
  <verify>
    `uv run python scripts/measure_gate6.py` exits 0 with no env vars set, writes the evidence
    JSON, and prints a one-line PASS/FAIL per criterion. Re-running it is idempotent (overwrites
    the same evidence file, does not append or duplicate log directories).
  </verify>
  <done>The whole GATE-6 lifecycle — clean game, tamper harness, Step-0 mismatch — runs
  end-to-end from one command with real evidence to show for it.</done>
</task>

<task type="auto">
  <name>Task 2: docs/PRD_commit_reveal.md</name>
  <files>docs/PRD_commit_reveal.md</files>
  <action>
    Mirror `docs/PRD_mcp_transport.md`'s exact section order. §1 mechanism+scope: the four-phase
    both-locked commit-reveal protocol (D-58), barrier-over-the-wire (D-66), Step-0, and the
    Final-Reveal audit including its D-67 revealed-vs-played cross-check; requirements table
    covers SEC-01 through SEC-08 verbatim from `.planning/REQUIREMENTS.md`; in-scope/out-of-scope
    lists (out: emailing the declaration/reports — Phase 7; the replay viewer — Phase 7; repo
    split/league play — Phase 8). §2 topology/design: the four phases in order — explicitly note
    the initiator/responder asymmetry (D-58: police always initiates, thief always responds and
    decides blind during its own wait phase, per book §6.4) — the composite action dict shape
    (D-59/D-66), the hash recipe (`canonical_json({state,move,intent,nonce})` → SHA-256), the
    nonce rule, Step-0's signed-when-possible rule, the audit's reuse of the existing
    `TechnicalWin` pathway and its two-check design (hash re-verify, then revealed-vs-played,
    D-67). §3 interfaces: the exact signatures of `commit_pack.commit/verify_reveal`,
    `state_record.build_state_record`, `ledger.CommitLedger`, `turn_commit.initiate/
    await_and_respond/reveal_pending`, `step0_collect.collect_declaration`, `step0_sign.
    sign_declaration/verify_declaration`, `audit.audit_peer_records` (now three positional
    dicts) — copied from the prior SUMMARY files, not re-derived. §4 out of scope, phrased as
    future-phase extensions, matching the house style. Every number cites 06-PLAN-OUTLINE.md §2
    or is labelled a structural/engineering default.
  </action>
  <verify>
    A human (or a grep) can find every SEC-01..08 requirement ID inside the document, each next
    to the section that satisfies it. No number appears without a citation or a "structural"
    label.
  </verify>
  <done>The per-mechanism PRD exists, matches house structure, and traces every claim back to a
  requirement or a decision ID.</done>
</task>

<task type="auto">
  <name>Task 3: GATE-6-MEASUREMENT.md + graph refresh</name>
  <files>docs/phases/phase-6/GATE-6-MEASUREMENT.md, .planning/graphs/GRAPH_REPORT.md</files>
  <action>
    Write `GATE-6-MEASUREMENT.md` mirroring `GATE-5-MEASUREMENT.md`'s structure exactly: quote
    the three §10.4 criteria verbatim from ROADMAP.md in a blockquote (labelled "not ours to
    edit"), then one section per criterion with Method / Run command / What a PASS looks like /
    the actual measured result pulled from `gate6_measurement_evidence.json` (Task 1's output).
    Mark each PASS honestly — if any of Task 1's three checks came back FAIL, this document
    reports FAIL, and the phase is not done until the underlying plan (06-01/02/03) is fixed, not
    until the report is reworded.

    Graph refresh: run `graphify update . && cp graphify-out/{graph.json,graph.html,
    GRAPH_REPORT.md} .planning/graphs/` so the committed `GRAPH_REPORT.md` reflects
    `src/pursuit/security/`, `turn_commit.py`, `agent_context.py`, and the extended handshake
    modules (task 06-96).
  </action>
  <verify>
    `GATE-6-MEASUREMENT.md` contains all three criteria verbatim and a PASS/FAIL line for each,
    each traceable to a field in the evidence JSON. `git diff --stat .planning/graphs/
    GRAPH_REPORT.md` shows a real change (the graph actually moved, not a no-op refresh).
  </verify>
  <done>The phase's gate is measured, not asserted; the graph reflects what actually shipped.</done>
</task>

</tasks>

<verification>
1. `uv run ruff check .` → 0; `uv run pytest tests/ --cov` → ≥ 85%, 0 failures, full project suite.
2. `bash scripts/check_line_limit.sh` clean on `scripts/measure_gate6.py`.
3. `uv run python scripts/measure_gate6.py` runs clean with no environment variables set (GATE-6
   needs none — 06-PLAN-OUTLINE.md §5).
4. Every SEC-01..SEC-08 requirement ID appears at least once across
   `docs/PRD_commit_reveal.md` + `docs/phases/phase-6/GATE-6-MEASUREMENT.md`.
</verification>

<success_criteria>
- `measure_gate6.py` proves all three §10.4 criteria with real, localhost-only evidence.
- `GATE-6-MEASUREMENT.md` and `docs/PRD_commit_reveal.md` exist, are honest about PASS/FAIL, and
  cite every number back to the outline's table.
- The committed graph reflects Phase 6's actual shipped code.
</success_criteria>

<output>
Create `.planning/phases/06-security-and-cryptography/06-04-SUMMARY.md`. This is the last plan in
the phase — the summary should also note any criterion that came back FAIL/PENDING so
`/gsd:verify-work 6` does not tick the phase triplet on a false PASS (rule 38's own spirit,
applied to our own reporting).
</output>
