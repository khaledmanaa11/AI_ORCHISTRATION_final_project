---
phase: 06-security-and-cryptography
plan: "03"
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - pyproject.toml
  - uv.lock
  - src/pursuit/security/step0_collect.py
  - src/pursuit/security/step0_sign.py
  - src/pursuit/security/audit.py
  - src/pursuit/network/handshake_wire.py
  - src/pursuit/network/handshake_evaluate.py
  - src/pursuit/network/handshake.py
  - src/pursuit/network/agent_wiring.py
  - src/pursuit/network/agent_entrypoint.py
  - src/pursuit/network/agent_audit_wiring.py
  - src/pursuit/network/verdict.py
  - src/pursuit/network/event_log.py
  - tests/unit/test_step0_collect.py
  - tests/unit/test_step0_sign.py
  - tests/unit/test_audit.py
  - tests/unit/test_handshake_step0.py
  - tests/integration/test_step0_and_audit.py
autonomous: true
requirements:
  - SEC-05
  - SEC-06
  - SEC-07
  - SEC-08

must_haves:
  truths:
    - "D-63: the Step-0 declaration is auto-collected (platform + psutil for OS/CPU/RAM, best-effort nvidia-smi for GPU with an honest 'not detected' — never a fabricated value), never typed by hand; the sanity display is a print/log line, never input() — league games run unattended"
    - "D-63: games_played_so_far is read from a persisted per-role counter file BEFORE this game (never hand-edited, rule 37) and incremented exactly once, at game end, via pursuit.shared.durable_write — the same crash-safe write/read pair Phase 3's checkpoints already use, not a new durability scheme"
    - "D-62: the declaration is SHA-256 digested unconditionally; it is ADDITIONALLY HMAC-SHA256-signed only when resolve_shared_secret(config_dir) returns a value — absent secret (every local/CI run) produces an explicit signed:false, never a silently-assumed verified"
    - "D-62: Step-0 verification rides the handshake's existing third-digest seam (SCENT_DIGEST's own precedent) — a new HandshakeOutcome.STEP0_MISMATCH aborts to State.ERROR before move 1, exactly like CONFIG_MISMATCH/SCENT_MISMATCH already do; no new State member, no new transition row (D-58 still holds)"
    - "D-61: game_id adoption is computed from EACH side's own perform_handshake() result only — never a mutable box shared with the responder closure, which is constructed before AgentContext exists and cannot safely be mutated from it. Both directions of build_offer carry local_game_id unconditionally in real play; the police role's own game_uid wins (police keeps it, thief adopts result.peer_game_id) — scoped ONLY to declaration_<game_id>.json's filename, never reassigning ctx.game_uid or moving ctx.log_path mid-game"
    - "SEC-05/rule 19: any Final-Reveal hash mismatch overrides the outcome to Outcome.TECHNICAL_LOSS via the EXISTING TechnicalWin(reason=AUDIT_HASH_MISMATCH, ...) dataclass and the EXISTING technical-loss log/return pattern — never a second, parallel verdict type"
    - "SEC-08/rule 36: the mutual audit runs at every game's end, both directions — I send my ledger's per-turn records, I receive the opponent's, and I verify THEIRS against what I logged as their h_commit turn by turn; a turn with no observed commit for a revealed record is itself a mismatch, not skipped"
    - "D-67: hash-verifying the final-revealed payload against H_commit ALONE is bypassable (commit honestly to X, play Y in the in-game REVEAL, present X at final reveal — hash matches, forgery survives). audit_peer_records therefore also cross-checks the revealed composite action dict against the action THIS SIDE actually observed in that turn's in-game REVEAL envelope (its own wire log) — a turn where the hash verifies but the revealed action differs from what was actually played is ALSO matched=False. Per-turn move-match transitively pins the whole trajectory (our own engine derives positions from actions), so no deeper position cross-check is needed."
    - "CONTEXT (locked, symmetric honesty): the audit also re-verifies MY OWN ledger against what I actually sent (hash AND the in-game-observed-action cross-check, same function), and a self-mismatch (a bug, not tampering) is logged with the same AUDIT_HASH_MISMATCH honesty — never suppressed to protect our own score"
    - "SEC-07 (revised): barrier declarations are now GENUINELY open on the wire (06-02's D-66 — the committed/revealed composite action dict carries the barrier cell, and the receiver validates it through move_payload's shipped BARRIER branch) — this is SEC-07's substance, not a byproduct. DeceptionPlan.__post_init__ (Phase 4, unmodified) is the complementary HINT-layer honesty guarantee (a verbal claim about a barrier/capture can never be a lie), not the sole enforcement mechanism. A false barrier claim is caught two ways now: structurally at the hint layer (DeceptionPlan), and cryptographically at Final Reveal via D-67's action cross-check if the WIRE action itself were ever forged."
    - "No new numeric literal anywhere in this plan — HMAC/SHA-256 are algorithm names, not numbers; retry/backoff on the FINAL_REVEAL exchange reuse ctx.net exactly like every other network call (D-17)"
    - "agent_entrypoint.py's own 150-line pressure is pre-authorized, not improvised mid-execution: it measured 47 code lines before this plan, but Task 4 adds Step-0 collect/sign/declare AND the whole Final-Reveal audit wiring (send/receive FINAL_REVEAL, two JSONL scans building observed_commits/observed_reveals in both directions, two audit calls, AUDIT_VERDICT append, outcome override) — a sibling agent_audit_wiring.py absorbs all of it from day one, mirroring the exact reason agent_entrypoint.py itself was split out of agent_lifecycle.py in 05-01; run_agent stays the thin caller."
    - "D-61 verification split (revised): the two-peer integration test's 'both declarations share the same game_id' assertion is corroboration only — play_two_peer_game passes ONE shared game_uid to both default_context(cfg_a, game_uid=game_uid, ...) / default_context(cfg_b, game_uid=game_uid, ...) calls by construction, so that assertion would hold even if peer_game_id negotiation were entirely broken. The load-bearing check is test_handshake_step0.py's own unit test, which MUST construct the two sides with deliberately DIFFERENT local_game_id values (mirroring real deployment, where each process independently generates its own secrets.token_hex(8)) and confirm the responder's HandshakeResult.peer_game_id resolves to the INITIATOR's value, not its own."
  artifacts:
    - path: src/pursuit/security/step0_collect.py
      provides: "collect_declaration (full §5.5 field set) + the persisted games-played counter"
      contains: "games_played_so_far"
    - path: src/pursuit/security/step0_sign.py
      provides: "digest_declaration / sign_declaration / verify_declaration"
      contains: "signed"
    - path: src/pursuit/security/audit.py
      provides: "audit_peer_records(observed_commits, observed_reveals, peer_records) / all_matched — the Final-Reveal re-hash PLUS the D-67 revealed-vs-played cross-check"
      contains: "observed_reveals"
    - path: src/pursuit/network/handshake_wire.py
      provides: "HandshakeKey.STEP0_DIGEST / GAME_ID, build_offer's two new optional params"
      contains: "STEP0_DIGEST"
    - path: src/pursuit/network/verdict.py
      provides: "TechnicalWinReason.AUDIT_HASH_MISMATCH"
      contains: "AUDIT_HASH_MISMATCH"
    - path: src/pursuit/network/agent_audit_wiring.py
      provides: "Step-0 collect/sign/declare + the whole Final-Reveal/audit send-receive-verdict sequence — the sibling that keeps agent_entrypoint.py a thin caller"
      contains: "run_final_audit"
  key_links:
    - from: src/pursuit/network/agent_entrypoint.py
      to: src/pursuit/network/agent_audit_wiring.py
      via: "run_agent calls this sibling's two entry points (Step-0 before the handshake, the audit after run_turn_loop) — it does not build declarations, scan JSONL, or call audit.py itself"
      pattern: "agent_audit_wiring"
    - from: src/pursuit/network/agent_audit_wiring.py
      to: src/pursuit/security/step0_collect.py
      via: "collects and signs the declaration BEFORE the handshake, so the digest is ready in time to ride the handshake payload"
      pattern: "collect_declaration"
    - from: src/pursuit/network/handshake_evaluate.py
      to: src/pursuit/network/verdict.py
      via: "a Final-Reveal mismatch (found later, at game end, by audit.py) reuses TechnicalWinReason — NOT HandshakeOutcome, which only ever covers pre-move-1 aborts"
      pattern: "AUDIT_HASH_MISMATCH"
    - from: src/pursuit/security/audit.py
      to: src/pursuit/security/commit_pack.py
      via: "verify_reveal is the ONLY re-hash function the audit calls — same function 06-01's own round-trip test already proved correct"
      pattern: "verify_reveal"
    - from: src/pursuit/network/agent_entrypoint.py
      to: src/pursuit/security/audit.py
      via: "D-67 — observed_reveals is built from THIS side's own message_received 'reveal' envelope records, the same records 06-02's turn_commit.py already logs, never re-derived or re-decoded a second way"
      pattern: "observed_reveals"
---

<objective>
Everything that happens once per game rather than once per turn: the Step-0 hardware/identity
declaration (signed, verified before move 1 via a third handshake digest), the negotiated
game_id used only for the declaration's filename, and the Final-Reveal mutual audit that turns
any hash mismatch into a technical loss through the existing pathway.

Purpose: SEC-05/06/07/08 — fairness is declared up front and honesty is checked at the end.
Output: `step0_collect.py`, `step0_sign.py`, `audit.py`, the handshake's third digest, `psutil`.
</objective>

<execution_context>
@C:\Users\Hp\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Hp\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/06-security-and-cryptography/06-PLAN-OUTLINE.md
@.planning/phases/06-security-and-cryptography/06-RESEARCH.md
@.planning/phases/06-security-and-cryptography/06-01-SUMMARY.md
@.planning/phases/06-security-and-cryptography/06-02-SUMMARY.md
- `src/pursuit/network/handshake_wire.py` / `handshake_evaluate.py` / `handshake.py` — read the
  SCENT_DIGEST precedent in full before touching any of the three: `local_scent_digest: str |
  None = None` optional param, `_compare_offer`'s "only compare if THIS call site opted in"
  rule, `HandshakeOutcome.SCENT_MISMATCH`'s abort path. STEP0_DIGEST and GAME_ID both extend the
  SAME payload the SAME way — do not invent a second extension mechanism.
- `src/pursuit/network/agent_wiring.py::make_handshake_responder` — the responder closure is
  bound BEFORE `AgentContext` exists (design note 12); it cannot close over `ctx`. Every value it
  needs (`local_game_id`, `local_step0_digest`) must be plain parameters computed before it is
  built, exactly like `local_digest`/`local_scent_digest` already are.
- `src/pursuit/network/secret_wiring.py::resolve_shared_secret` — returns `(header, value) |
  None`; pass only the VALUE into `step0_sign.sign_declaration`, never the tuple, never the
  header name into anything hashed.
- `src/pursuit/shared/durable_write.py` — `durable_write_json`/`load_json_with_fallback`, reused
  verbatim for the games-played counter file. Do not write a new durability scheme.
- `src/pursuit/network/turn_actions.py`'s existing 3-step technical-loss pattern (log evidence →
  the machine is already at GAME_OVER by the time audit runs → return `Outcome.TECHNICAL_LOSS`) —
  the audit's own override reuses this shape as a POST-HOC append, per RESEARCH's own finding
  (`GAME_OVER` is terminal with no outgoing edges — do not call `machine.attempt` again).
- `docs/PARAMETERS.md` §"Required JSON artifacts" — `declaration_<game_id>.json`'s field list and
  the four-artifact naming convention this plan's `game_id` resolution feeds.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Step-0 collection, signing, and the games-played counter</name>
  <files>pyproject.toml, uv.lock, src/pursuit/security/step0_collect.py, src/pursuit/security/step0_sign.py, tests/unit/test_step0_collect.py, tests/unit/test_step0_sign.py</files>
  <action>
    `uv add psutil`.

    `step0_collect.py` — `collect_declaration(*, role: str, team_code: str, llm_name: str,
    code_version: str, games_played: int) -> dict` assembles the full §5.5 field set: `role`,
    `team_code`, `os` (`platform.platform()`), `cpu` (`psutil.cpu_count()`, best-effort
    `psutil.cpu_freq()`), `ram_gb` (`psutil.virtual_memory().total`), `gpu` (best-effort
    `nvidia-smi` via `subprocess.run`, `{"present": False, "detail": "not detected"}` on any
    failure — NEVER a fabricated GPU), `llm_name`, `code_version`, `games_played_so_far`,
    `commit_hash` (`git rev-parse HEAD` via `subprocess.run`, the FIRST `subprocess` caller in
    `src/pursuit/`, per RESEARCH §12 — raise loudly if the command fails, never a blank hash).
    Print one non-blocking sanity line summarizing the declaration (never `input()`).

    Also in this file: `read_games_played(path) -> int` (via `load_json_with_fallback`, `0` when
    the file/field is absent — turn 1 of a fresh team) and `record_game_played(path) -> None`
    (read, increment by 1, `durable_write_json` back) — both plain functions over a caller-given
    path, no `pursuit.network` dependency.

    `step0_sign.py` — `digest_declaration(declaration: dict) -> str` (SHA-256 of
    `canonical_json(declaration)` — the SAME narrow `pursuit.network.config_hash` exception
    06-01's `commit_pack.py` already documents; note it here too, do not re-justify it a second
    way). `sign_declaration(declaration: dict, *, secret: str | None) -> dict` returns `{"digest":
    ..., "signed": secret is not None, "hmac": hmac.new(secret.encode(), canonical_json(
    declaration).encode(), hashlib.sha256).hexdigest() if secret is not None else None}`.
    `verify_declaration(declaration: dict, *, digest: str, hmac_value: str | None, secret: str |
    None) -> bool` recomputes and compares BOTH the digest (always, via `digests_match`) and the
    HMAC (only when both `hmac_value` and `secret` are present) — signed vs. unsigned peers are
    never conflated.
  </action>
  <verify>
    `test_step0_collect.py`: the returned dict has exactly the documented keys; a faked failing
    `nvidia-smi` subprocess yields the honest not-detected shape, never raises; `games_played`
    round-trips through `record_game_played`→`read_games_played` (0 → 1 → 2); a missing counter
    file reads as 0, never raises. `test_step0_sign.py`: `sign_declaration(d, secret=None)` →
    `signed: False, hmac: None`; with a secret, `verify_declaration` on the untampered dict is
    True and False after flipping any one field; a digest-only (unsigned) declaration still
    verifies its digest correctly.
  </verify>
  <done>A full declaration can be collected offline, deterministically, and signed when a
  shared secret exists — never silently treated as verified when it doesn't.</done>
</task>

<task type="auto">
  <name>Task 2: the handshake's third digest and game_id</name>
  <files>src/pursuit/network/handshake_wire.py, src/pursuit/network/handshake_evaluate.py, src/pursuit/network/handshake.py, src/pursuit/network/agent_wiring.py, tests/unit/test_handshake_step0.py</files>
  <action>
    `handshake_wire.py`: add `HandshakeKey.STEP0_DIGEST = "step0_digest"` and `HandshakeKey.
    GAME_ID = "game_id"`. `build_offer` gains `local_step0_digest: str | None = None,
    local_game_id: str | None = None` — each key is included in the payload only when its value
    is given, mirroring `SCENT_DIGEST`'s own omit-don't-null rule.

    `handshake_evaluate.py`: add `HandshakeOutcome.STEP0_MISMATCH`. `HandshakeResult` gains
    `peer_game_id: str | None = None`. `_compare_offer` gains the same opt-in step0 comparison
    `SCENT_DIGEST` already has (compare only if the LOCAL call site supplied
    `local_step0_digest`; a missing remote counts as mismatch, via `compare_named_digest`).
    `evaluate()` reads `envelope.payload.get(HandshakeKey.GAME_ID)` into `peer_game_id`
    UNCONDITIONALLY (whether the outcome agrees or not — a mismatch report should still surface
    who proposed what), passing it through `build_result`/`_abort`.

    `handshake.py`: thread `local_step0_digest: str | None = None, local_game_id: str | None =
    None` through both `perform_handshake` and `respond_to_handshake`, passed straight into
    `build_offer`/`evaluate` — no new logic in this file beyond parameter plumbing.

    `agent_wiring.py::make_handshake_responder`: add the same two parameters, passed straight
    into its internal `build_offer`/`respond_to_handshake` calls. Every real caller (Task 4) will
    always supply both; `None` (every pre-existing test) keeps the exact pre-Phase-6 behavior.
  </action>
  <verify>
    `test_handshake_step0.py`, mirroring `test_handshake_scent.py`'s own structure: matching
    step0 digests agree; a mismatch aborts to `State.ERROR` with `HandshakeOutcome.
    STEP0_MISMATCH` before move 1 (the same assertion shape as
    `test_mismatched_digests_abort_before_move_1`); a call site that never opts in (`
    local_step0_digest=None`) is unaffected, matching `SCENT_DIGEST`'s own opt-out test.
    THE LOAD-BEARING game_id test (D-61 — see must_haves; this is the ONLY test in the whole
    phase that actually proves negotiation works, not just that a shared value survives a
    harness that already shares it): construct the initiator's offer with
    `local_game_id="police-uid-aaa"` and the responder's own local value as a DELIBERATELY
    DIFFERENT `"thief-uid-bbb"` — never the same string on both sides, or the test cannot
    distinguish "negotiation worked" from "both sides happened to agree already". Assert the
    responder's resulting `HandshakeResult.peer_game_id == "police-uid-aaa"` (the INITIATOR's
    value, not the responder's own `"thief-uid-bbb"`) — present even on a MISMATCH result
    (evidence, not just success).
  </verify>
  <done>A hardware-declaration mismatch aborts before move 1 exactly like a config or scent
  mismatch; every existing handshake test keeps passing with zero edits (both new parameters
  default away).</done>
</task>

<task type="auto">
  <name>Task 3: the Final-Reveal audit</name>
  <files>src/pursuit/security/audit.py, src/pursuit/network/verdict.py, src/pursuit/network/event_log.py, tests/unit/test_audit.py</files>
  <action>
    `verdict.py`: add `TechnicalWinReason.AUDIT_HASH_MISMATCH = "audit_hash_mismatch"` — additive,
    `TechnicalWin`/`CallOutcome` unchanged.

    `event_log.py`: add `EventType.AUDIT_VERDICT = "audit_verdict"` — the record shape for the
    game-end audit result, following `game_over_record`'s own reasoning (no existing `EventType`
    member fits an end-of-game summary; assemble the four common fields directly, per that
    function's own docstring precedent, rather than forcing it through `build_event`).

    `audit.py` — `AuditRecord` (frozen dataclass): `turn: int, matched: bool, detail: str`.
    `audit_peer_records(observed_commits: dict[int, str], observed_reveals: dict[int, dict],
    peer_records: list[dict]) -> list[AuditRecord]` (D-67, revised) — for each `peer_records`
    entry (`{"turn", "h_commit", "payload": {"state","move","intent","nonce"}}`), check, in
    order: (1) `entry["turn"]` has a matching `observed_commits` entry — absent = `matched=False`
    naming the missing turn, never silently skipped; (2) `commit_pack.verify_reveal(
    observed_commits[turn], **entry["payload"])` — the hash re-verifies against what we actually
    saw at Commit time; (3) `entry["payload"]["move"]` (the composite action dict) EQUALS
    `observed_reveals.get(turn)` — the action THIS SIDE actually saw revealed in-game for that
    turn (a turn missing from `observed_reveals` also fails this check, naming it). ALL THREE
    must hold for `matched=True`; the FIRST failing check's detail is what the `AuditRecord`
    reports (do not compute all three and hide which one failed). `all_matched(records:
    list[AuditRecord]) -> bool`.

    Symmetric-honesty self-check: `audit_peer_records` is also the function the caller uses on
    ITS OWN ledger records against its OWN observed (self-sent) commits/reveals — a self-mismatch
    means a serialization bug, not tampering, and Task 4's wiring treats it with the SAME
    `AUDIT_HASH_MISMATCH` honesty (CONTEXT, locked) rather than suppressing it.
  </action>
  <verify>
    `test_audit.py`: an untampered `(observed_commits, observed_reveals, peer_records)` triple
    from a genuine `commit_pack.commit()` call, with `observed_reveals[turn] ==
    entry["payload"]["move"]`, audits clean (`all_matched` True). Three DISTINCT tamper cases,
    each flipping only its own turn: (a) flip a `payload` field before hashing — check 2 fails;
    (b) leave the hash and payload untouched but make `observed_reveals[turn]` differ from
    `entry["payload"]["move"]` (the D-67 case: honest commit, forged claim of what was played) —
    check 3 fails even though check 2 passes; (c) a `peer_records` turn missing from either
    `observed_commits` or `observed_reveals` — check 1 or 3 fails, naming the missing turn. Every
    other turn in the same batch stays `matched=True`.
  </verify>
  <done>Given what one side observed on the wire (both the commit hash AND the actually-revealed
  action) and what the other side reveals at game end, the audit function alone — no network, no
  ctx — proves agreement or names the exact turn that disagrees, including the D-67 case where
  the hash checks out but the claimed final action does not match what was actually played.</done>
</task>

<task type="auto">
  <name>Task 4: agent_audit_wiring.py (new sibling), and run_agent as its thin caller</name>
  <files>src/pursuit/network/agent_audit_wiring.py, src/pursuit/network/agent_entrypoint.py, tests/integration/test_step0_and_audit.py</files>
  <action>
    Pre-authorized split (see must_haves): `agent_entrypoint.py` measured 47 code lines before
    this plan and stays the thin caller; `agent_audit_wiring.py` (new) holds two entry points and
    everything behind them:

    `async def declare_step0(cfg, *, local_game_id: str) -> tuple[str, str | None]` — reads
    `games_played = step0_collect.read_games_played(<per-role counter path>)`; builds the
    declaration via `step0_collect.collect_declaration(role=cfg.role, team_code=cfg.security.
    team_code, llm_name=..., code_version=VERSION, games_played=games_played)`; resolves
    `secret = resolve_shared_secret(cfg.config_dir)`; signs via `step0_sign.sign_declaration`;
    returns `(step0_digest, declaration_json_ready_to_write)` — `run_agent` passes
    `step0_digest` straight into `perform_handshake`'s `local_step0_digest` and into
    `default_context`/`build_context`'s responder wiring (thread it the same way
    `local_scent_digest` already flows in), and calls a second small helper,
    `write_declaration(ctx, cfg, result, declaration) -> None`, ONLY after a successful
    (`result.agreed`) handshake — this is where `declared_game_id` is resolved (D-61, scoped
    only to the filename): `game_uid if cfg.role == "police" else (result.peer_game_id or
    game_uid)` — never reassigning `ctx.game_uid`/`ctx.log_path`. Writes
    `declaration_<declared_game_id>.json` beside `ctx.log_path`'s directory, then
    `step0_collect.record_game_played(...)` once.

    `async def run_final_audit(ctx) -> Outcome | None` — called by `run_agent` after
    `run_turn_loop(ctx)` returns and BEFORE `shutdown_cleanly`, only when
    `ctx.security.commit_reveal` is True. Reads this side's own ledger; sends it as a
    FINAL_REVEAL envelope (`{"records": [...]}`) via `call_with_retry` (existing technical-loss
    pattern on failure); receives the opponent's FINAL_REVEAL the same bounded-wait way
    `turn_commit.py`'s own waits work; extracts `observed_commits`/`observed_reveals` from
    `ctx.log_path`'s own JSONL (received-direction, keyed by `envelope.turn`, from the `commit`/
    `reveal`-typed `message_received` records 06-02 already logs); runs `audit.
    audit_peer_records(observed_commits, observed_reveals, peer_records)` against the opponent's
    FINAL_REVEAL records, AND separately again against THIS side's own ledger using
    sent-direction `observed_commits`/`observed_reveals` (the symmetric self-check — same
    function, only the sent/received direction differs); appends one `AUDIT_VERDICT` event with
    the full result from both checks; returns `Outcome.TECHNICAL_LOSS` on any mismatch (either
    direction), else `None`.

    `run_agent` (`agent_entrypoint.py`): three new lines — call `declare_step0`/thread its
    digest into the handshake and responder as above, call `write_declaration` after a successful
    handshake, and — right after `outcome = await run_turn_loop(ctx)`, still inside the existing
    `try` — `audit_outcome = await run_final_audit(ctx); if audit_outcome is not None: outcome =
    audit_outcome`. No JSONL scanning, no audit-record construction, and no declaration-file I/O
    happens in `agent_entrypoint.py` itself.
  </action>
  <verify>
    `bash scripts/check_line_limit.sh` — re-check BOTH `agent_entrypoint.py` (should stay close
    to its pre-plan 47 lines plus the three call lines above) and `agent_audit_wiring.py`
    individually. A dedicated integration test (folded into `tests/integration/
    test_step0_and_audit.py`, this task's own proof): a two-peer game via the existing harness
    produces a `declaration_<game_id>.json` on both sides BEFORE any move is logged, both files
    sharing the SAME `game_id` — noted explicitly as CORROBORATION, not the load-bearing D-61
    proof (see must_haves and Task 2's `test_handshake_step0.py`, which is where negotiation is
    actually exercised with differing inputs — `play_two_peer_game` hands both sides the SAME
    `game_uid` by construction, so this assertion would hold even with negotiation entirely
    broken); a clean game's audit reports no mismatch and the original outcome stands. TWO
    tamper cases, both must be caught: (a) a test that corrupts one entry's PAYLOAD in one side's
    ledger before the Final-Reveal exchange (the hash itself now disagrees) causes the OTHER
    side's audit to report `AUDIT_HASH_MISMATCH` (D-67 check 2) and its returned `Outcome`
    becomes `TECHNICAL_LOSS`; (b) the D-67 case specifically — a test that leaves one entry's
    hash/payload untouched (still verifies) but swaps in a DIFFERENT already-legal action as that
    entry's revealed `move` (so it no longer matches what the other side actually observed
    in-game) ALSO causes `AUDIT_HASH_MISMATCH` (D-67 check 3), proving the hash-only bypass
    D-67's own rationale describes is genuinely closed. Both are the exact proof 06-04's gate
    measurement will point back to.
  </verify>
  <done>Step-0 is verified before move 1; the declaration file is named by the negotiated
  game_id; a clean game's outcome is unaffected by the audit; a tampered one is not;
  `agent_entrypoint.py` stays a thin caller throughout.</done>
</task>

</tasks>

<verification>
1. `uv run ruff check .` → 0; `uv run pytest tests/ --cov` → ≥ 85%, 0 failures, full suite.
2. `bash scripts/check_line_limit.sh` clean — re-check `handshake_evaluate.py` and
   `agent_wiring.py` (least remaining margin going into this plan), plus `agent_entrypoint.py`
   and `agent_audit_wiring.py` (the pre-authorized split, Task 4).
3. `git diff -- src/pursuit/network/state_machine.py` is empty (Step-0 abort reuses
   `HandshakeOutcome`/`State.ERROR`, no new State member).
4. `uv run python -c "import psutil"` succeeds; `grep -n psutil pyproject.toml` shows it as a
   real dependency, not a dev-only one (Step-0 runs in production, not just tests).
</verification>

<success_criteria>
- Step-0 declarations are collected, signed (when a secret exists), exchanged, and verified
  before move 1 — a mismatch aborts exactly like every other handshake abort.
- `declaration_<game_id>.json` uses one game_id both peers agree on.
- The mutual Final-Reveal audit runs at every game's end in both directions, including a
  self-check, and a mismatch reliably produces `Outcome.TECHNICAL_LOSS` through the existing
  pathway.
</success_criteria>

<output>
Create `.planning/phases/06-security-and-cryptography/06-03-SUMMARY.md` with the declaration file
naming convention, the `HandshakeResult.peer_game_id` contract, and the `AuditRecord` shape —
06-04's `measure_gate6.py` drives this exact flow end to end for the gate report.
</output>
