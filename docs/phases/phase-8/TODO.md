# Phase 8 TODO — Submission and League Operations

**Owner:** Khaled (solo) · **Updated:** 2026-08-17 · **Status: ◐ in progress — 08-01 … 08-06
executed 2026-08-17; eight plans remain, three of them human-gated.**

> Phase task list. ROADMAP rows 08-01…08-04 are deliverable **groups**; §17 is wider than those four
> rows, so this phase needs fourteen plans and row IDs and plan IDs are **not** 1:1 here. The
> mapping is in
> [08-PLAN-OUTLINE.md §12](../../../.planning/phases/08-submission-and-league-operations/08-PLAN-OUTLINE.md).
> `/gsd:verify-work 8` marks every row `☑` and ticks the matching rows in the root
> [docs/TODO.md](../../TODO.md).
> **Status:** ☐ not started · ◐ in progress · ☑ done · **Priority:** P0/P1/P2

| Task | Pri | Wave | Auto | Status | Owner | Definition of Done |
|------|-----|------|------|--------|-------|--------------------|
| 08-01 The §17 + Table-5 audit as a runnable gate, with `docs/SUBMISSION-CHECKLIST.md` as its living gap register | P0 | 1 | yes | ☑ | Khaled | `scripts/check_submission.py` exits 0 all-pass / 1 any GAP / **2 on an evidence set that judged nothing**, proven by an emptied-list probe; the mechanism inventory comes from the `src/pursuit/` package tree, not a `docs/PRD_*.md` glob, proven by planting an empty package; renaming one satisfied artifact flips exactly one row; every GAP names the exact path where the fix lands; unjudgeable rows print `UNJUDGED`, never PASS (QUAL-01…13, DOC-01). **DONE 2026-08-17:** 86 rows, 41 PASS / 32 GAP / 13 UNJUDGED, exit 1; `--empty-probe` exits 2 and all four emptiness reasons pin independently; 13 mutation probes, one counter-control per group, each asserted landed before the verdict was read; the planted empty package adds exactly one GAP row |
| 08-02 `.planning/REQUIREMENTS.md`, `docs/TODO.md`, the ROADMAP Progress table and the phase-1 triplet reconciled **project-wide in one pass** | P0 | 1 | yes | ☑ | Khaled | Header total equals the actual checkbox count (**77**, not 74); every `[x]` cites the `NN-VERIFICATION.md` / `GATE-N-MEASUREMENT.md` that proves it and every `[ ]` names what is outstanding; all ten traceability rows (lines 179-188) carry an evidenced verdict, `Pending` surviving only where an artifact says so; STRAT-01/02 reworded to the mechanism that shipped; LANG-01/06, REPORT-01…07 and all twelve SUB-* left honestly open with the reason written in; `check_requirements_ledger.py` fails on a flipped row and does **not** print OK on an empty ledger; one commit (DOC-01). **DONE 2026-08-17:** header 74→77 (counted, not asserted); 6→48 ticks, every one citing a `path` "verbatim quote" the gate reads back; all ten traceability rows carry an evidenced verdict and a self-declared `N/M ticked` count the gate cross-checks; STRAT-01/02/06 reworded to the matrix mover; LANG-01/06, all nine REPORT-* and all twelve SUB-* held open with the reason written in. **The flip probe found a HOLE in the first version of the gate and it was closed, not reported.** One commit |
| 08-03 Publication hygiene made machine-checkable; `LICENSE`, `CONTRIBUTING.md`, packaging metadata | P0 | 1 | yes | ☐ | Khaled | The secret scan runs over the **tracked** set and is paired with a **planted dummy secret it must catch**; `.env`, `.venv/`, `logs/`, `graphify-out/`, `graph.json`, `graph.html`, `run.log`, `.coverage`, `scratchpad/`, `police_thief_p2p.pdf` each proven ignored by an individual `git check-ignore` assertion; D7-19's remaining half becomes a rule — the four rule-50 artifact names stay un-ignored under `game_artifacts/` and every other shape there is ignored; `pyproject.toml` gains `license`/`authors` and `uv sync` still resolves (SUB-04, QUAL-12, rules 39–40) |
| 08-04 League machinery: the opponent ledger, `config/*/league.json`, and the **first production caller** for the declaration artifact; the D7-17 question drafted | P0 | 1 | yes | ☑ | Khaled | `write_declaration_artifact` has a grep-proven production caller (today the grep returns only its own module and the `__init__` re-export — it is dead code); a real `dev_launch.py` game leaves a `declaration_<game_id>.json` carrying `repo_urls`, `mcp_server_addresses`, `token_ceiling`, `start_time`, `end_time`; rule 49's **four** links carried when the peer supplies theirs and their absence recorded honestly; the ledger refuses a second scoring game per opponent (rule 52) and refuses beyond `max games per team` = 10 (Table 18 row 5, fixed); the games-played count is derived from the ledger with a counter-control pair — honest passes, inflated fails; zero hardcoded URLs (SUB-07, SUB-08, SUB-12, REPORT-06, rules 38, 49, 52, 53). **DONE 2026-08-17:** a real `dev_launch.py` game (`game_id` `397b3503b1bfa996`, exit 0) wrote `declaration_<game_id>.json` on BOTH seats, carrying `repo_urls` (rule 49's four named slots), `mcp_server_addresses`, `token_ceiling` 200000, `start_time`/`end_time` and both signed Step-0 envelopes embedded verbatim — artifacts kept at `docs/phases/phase-8/declaration-evidence/`. `grep -rn "write_declaration_artifact|DeclarationContext|build_declaration_artifact" src/ scripts/ training/` now returns `end_of_game_declaration.py` as a real caller; probe F removed the call site and 7 tests failed. Ledger refuses a rule-52 rematch while permitting warm-ups, and refuses beyond Table 18 row 5's fixed 10 — that bound is asserted against `docs/PARAMETERS.md` itself, because probe E moved the constant to 11 and the whole ledger suite stayed green. `declared_count_matches` passes the honest count and fails an inflated one. **The games-played VALUE is left UNSET**, unparameterised, naming `GAMES-PLAYED-RECONSTRUCTION.md`. Repo URLs are absent-marked, never placeheld; `league_config` refuses an absence in `live` mode. D7-17 drafted at `docs/phases/phase-8/D7-17-QUESTION-FOR-THE-LECTURER.md`, unsent, no id scheme invented. Counters: suite 0/0, one real game +1/+1 (1922→1923, 1915→1916). Suite 2188→2293, coverage 97.37%→97.43% |
| 08-05 Deferred #13 and #19 closed, or formally accepted with re-measured evidence | P1 | 1 | yes | ☐ | Khaled | Either both closed with a revert probe that fails against pre-plan code, or both accepted in `docs/SUBMISSION-CHECKLIST.md` with `config/{police,thief}/security.json` re-read at HEAD and the existing scope-asserting test — the one that **fails deliberately when the defect is closed** — left in place. `turn_buffer.py` is at 146/150: a **split**, never a compression (NET-06, SEC-05) |
| 08-06 The root README rebuilt to §2.1's seven items **and** §9.4.2's six sections | P0 | 2 | yes | ☑ | Khaled | All seven §2.1 items present and non-empty, checked individually by 08-01's gate; all six §9.4.2 sections present; the false "trained tabular **Q-learning** policy (Bayes + BFS fallback)" opening replaced by what actually shipped (matrix-game mover over a learned 15-weight evaluation, `docs/phases/phase-3/PRD.md` §2) — rule 42 governs this file; curves from `artifacts/curves/*.png` with the generating command recorded; GUI + `Verified OK` screenshots from 07-10; the cross-link a config-sourced placeholder that 08-01 reports as a GAP until 08-12 fills it; OQ8-8 (English vs Hebrew) confirmed, not assumed (SUB-03, rule 42) **DONE 2026-08-17:** `check_submission.py` 49 PASS / 24 GAP → **58 PASS / 15 GAP**, closing exactly the nine rows this plan owns (G1-01…G1-09) and moving no other row in either direction. Unqualified `Q-Learning` mentions **3 → 0**; the strategy section describes the matrix-game mover over the learned 15-weight evaluation, and the status table is derived from the `NN-VERIFICATION.md` / `GATE-N-MEASUREMENT.md` files (phase 3 no longer "in progress"). Curves: the plan found the documented command named `training/plot_curves.py`, **deleted with the run-1 stack in f3d9847**, and that the only figures in the tree were the WITHDRAWN run's — so `scripts/plot_run2_curves.py` now draws `run2_selfplay.png` / `run2_evolution.png` from tracked artefacts, reproducible with no training run. G1-03b and G5-04 **stay GAP**: 07-10's screenshots do not exist, and they are marked-absent SLOTS, never placeholders — `test_readme_contract.py` fails if any README image link resolves to nothing. Nothing overstated: mail `dry_run` + live PENDING, phase 4 `human_needed`, phases 7/8 NOT verified, no league game, **no games-played number anywhere** (rule 38). The rule-49 cross-link is a stated absence naming 08-12. **OQ8-8 is NOT confirmed** — the extracts carry no language requirement and the book is Hebrew, so English is recorded as an ASSUMPTION, not a confirmation. New assertions proven RED on the pre-fix file: 6 failed / 11 passed against `git show HEAD:README.md`. Counters: suite 0/0, one real game +1/+1 |
| 08-07 Architecture documentation: C4 ×4, deployment, commit-reveal sequence, ISO/IEC 25010 map, extension points | P1 | 2 | yes | ☑ | Khaled | All four C4 levels as **rendered** mermaid (today `grep -rl '```mermaid' docs/` returns nothing — every diagram is ASCII art); a deployment diagram covering two processes, two tunnels and the Gmail path; a Commit → Acknowledge → Reveal → Final Reveal/Audit sequence diagram; **every component label resolves to a real module path**, checked by a script; the eight ISO 25010 characteristics each mapped to concrete repo evidence, not prose (today the whole repo has one line, `docs/PRD.md:94`); extension points documented against the real seams — `BrainBase`, `MailSink`, the provider registry, `[strategy]` (QUAL-07, DOC-01) **DONE 2026-08-17:** `check_submission.py` 15 GAP -> **11 GAP**, closing exactly the four rows this plan owns (G1-13, G6-01, G6-02, G6-05) and moving no other row in either direction. `docs/ARCHITECTURE.md` carries the repository's **first six rendered mermaid diagrams** — C4 context / container / component / code, a deployment view and the four-phase commit-reveal sequence — plus §5.1 deployment instructions. The gate counted **0** rendered blocks before this. `scripts/check_diagrams.py` (exit 0/1/2, D-82's contract) resolves **29** distinct `src/pursuit` labels against `git ls-files`, and `scripts/diagram_graph.py` reads the container diagram AS A GRAPH so the three binding facts are structural, not prose: identical module sets plus an edge each way (symmetric peers), no node id inside both subgraphs (rule 2), the `gui` node outside both (D-76). **Rendering proven, not assumed:** all six blocks rendered to real SVGs with `@mermaid-js/mermaid-cli` 11.16.0 out of tree, no `Syntax error` box in any of them, and the two mutations the unit tests use were REJECTED by that renderer. `docs/QUALITY-25010.md` maps all eight characteristics, each in its own section with its own repo evidence; `docs/EXTENSION-POINTS.md` documents five real seams plus what is deliberately NOT extensible. New assertions proven RED against the pre-change documents: **16 failed / 4 passed**. A backticked-path citation check found **three fabrications in this plan's own first draft**, one a script name that does not exist. Suite 2366 -> 2413, coverage 97.44% unchanged |
| 08-08 The three missing per-mechanism PRDs — SDK layer, tunnel, `gui/` rendering | P1 | 2 | yes | ☑ | Khaled | `docs/PRD_sdk.md`, `docs/PRD_tunnel.md`, `docs/PRD_gui.md` committed; the mechanism inventory comes from the package tree and a probe adding an empty package makes the check fail; each PRD documents every number **and its source**; `PRD_rl_strategy.md`'s superseded banner verified still intact and pointing at `PRD_matrix_mover.md` (DOC-02, §2.3) **DONE 2026-08-17:** `check_submission.py` 11 GAP -> **8 GAP**, closing exactly the three rows this plan owns and moving no other row. `docs/PRD_sdk.md` covers both halves of the SDK — the game rules as callable functions and the D-74 read model — and states the limit honestly: nothing automated prevents a FUTURE rule landing in `network/`. `docs/PRD_tunnel.md` covers the genuine hole `docs/PRD_mcp_transport.md:28` declares: ngrok exposure, the D-56 shared-secret ASGI door, 05-11's bounded repair and the drop that proved it had no caller, and GATE-5's two criteria including attempt 4 — with the three things attempt 4 does NOT prove copied across rather than summarised away. **`docs/PRD_gui.md` existed at `aa75852` but `docs/mechanism-prd-map.json` still answered `prds: []`, so its row was still open**; the register now cites it, and still refuses `PRD_display_belief.md` as coverage. `tests/unit/test_mechanism_prd_contract.py` pins what the walk cannot see: no cited PRD carries a SUPERSEDED banner, `PRD_rl_strategy.md`'s banner is intact and still points at `PRD_matrix_mover.md`, `PRD_mcp_transport.md` still excludes the tunnel (the derivation `PRD_tunnel.md` rests on), and every backticked path in all three PRDs is in `git ls-files` — which caught a second fabricated script name. RED before GREEN: **5 failed / 6 passed** |
| 08-09 Research and visualization: sensitivity analysis, offline analysis notebook, token-cost analysis, prompt log | P1 | 2 | yes | ☑ | Khaled | The sweep varies only `negotiable` or labelled-default parameters and **lists the fixed ones it did not touch** (Table 16, Table 17 and the 5×5 scent window are fixed); `notebooks/analysis.ipynb` executes offline from **tracked** inputs (`nbconvert --execute` exits 0) with a probe proving it never reads the gitignored `logs/`; `docs/TOKEN-COST.md` fed by real `TokenBudget.report()` output with a stated optimization strategy; `docs/PROMPT_LOG.md` records the actual `services/llm/` prompts and their revisions (§8.3, §17 group 5). **DONE 2026-08-17:** `check_submission.py` 8 GAP -> **4 GAP**, closing exactly the four rows this plan owns (G1-14, G5-02, G5-03, G5-05) and moving no other row in either direction. The sweep varies only `minimum` (upward only) or `negotiable` rows plus three LABELLED engineering defaults; the Status of every knob is PARSED out of `docs/PARAMETERS.md` (32 rows, 14 fixed) and `refuse_fixed`/`refuse_downward` fail the sweep otherwise — the fixed list the document prints is derived from that same parse, not typed. 13 configurations x 3 matchups x 200 games, 755.6s, offline, counters 1927/1920 -> 1927/1920. Separable at 95% Wilson: board 11 **+35.0pp**, horizon 70 **-29.0pp**, swap-as-capture **-25.0pp**, fitted vector over prior **+18.0pp**; search depth and extra barriers move nothing separably and the cop matchup is flagged SATURATED. `notebooks/analysis.ipynb` executes offline (`nbconvert --execute` exit 0, 4.6s, three embedded figures); its offline property is PARSED — manifest `ast.literal_eval`ed and each path put to `git ls-files`, imports held to `{json, pathlib, matplotlib}` — because a substring scan for `logs/` would fire on the notebook's own prose. `docs/TOKEN-COST.md` is fed by the ONE real Haiku 4.5 game and states S1-S4 ranked by measured leverage, with S2 blocked on a named missing measurement and the 'call less often' lever listed as REJECTED by `language_model_config.py:78-83`. `docs/PROMPT_LOG.md` R1 is a measured prompt revision: 1 third-person hint in the 10 before `50ac2fe`, 0 in the 69 after. **Two honest findings kept rather than smoothed:** the sweep contradicts ENGINEERING-LOG Act 4.3's 89%/1% pair (measured 32.0%/7.5%; eight arms re-measured, cause NOT established, direction of the shipped decision unchanged), and this plan's own first empty-evidence probe was vacuous — it failed on `ZeroDivisionError`, not its assertion, and was rebuilt. Suite 2413 -> 2459, coverage 97.44% unchanged, ruff 0, line-limit 0 |
| 08-10 Two split repos built LOCALLY from `git ls-files`, each independently passing Table 5 inside its own tree | P0 | 3 | yes | ☑ | Khaled | **Done 2026-08-17** — `docs/phases/phase-8/SPLIT-VERIFICATION.md`, 12/12 rows in both outputs, `split_build_evidence.json`. Built into a destination **outside** this tree, with a planted untracked `secret.txt` appearing in neither output; each output has one initial commit, **zero remotes**, and no commit in common with `origin`; each passes ruff 0, `uv sync`, `pytest --cov` ≥ 85% **compared against the mono-repo's 97.37%**, and `check_line_limit.sh` reporting a **scanned-file count > 0** (its no-argument form enumerates via `git ls-files`, empty before the first commit — the vacuity already on record in `05-18-SUMMARY.md`); rule 50 checked per output; each README cross-links the other; `scripts/hooks` present with the `git config core.hooksPath` step documented; `.github/workflows/` jobs reference only paths that exist there; 08-03's scan re-run on both outputs. **Nothing pushed** (SUB-01, SUB-02, rules 49–50) |
| 08-11 Tag prepared and verified (**not pushed**), graph refreshed, `GATE-8-MEASUREMENT.md`, three runbooks, self-assessment evidence | P0 | 4 | yes | ☐ | Khaled | `shared/version.py` (`1.00`) and `pyproject.toml` (`1.00.0`) reconciled and the tag name **derived** from the reconciled value; the tag created in each split output and its content verified, and the plan states it is not pushed; `.planning/graphs/GRAPH_REPORT.md` refreshed and committed (ROADMAP 08-96); 08-01's gate re-run at HEAD with every GAP closed or carrying a dated recorded reason; GATE-8 reports the human-dependent halves as **PENDING**, never a blanket PASS (the GATE-7 criterion-1 precedent); `docs/SELF-ASSESSMENT.md` drafted with the score field **blank**; `SPLIT-`/`LEAGUE-`/`SUBMISSION-RUNBOOK.md` written (SUB-05, SUB-11) |
| 08-12 **HUMAN — publish:** create the two public repos, push them, push the tag, fill the real URLs, send the lecturer question | P0 | 5 | **NO** | ☐ | Khaled | `origin`'s public/private status determined first (OQ8-9); both repos reachable and cross-linking each other; the tag visible on both; `config/*/league.json` holds real URLs and 08-01's placeholder GAP is closed; the D7-17 question sent to `rmisegal@gmail.com` with the date recorded; 08-03's scan re-run against the **pushed** trees. *Claude must not create accounts, push to public repos, push tags, or mail the lecturer* (SUB-01, SUB-05, rules 41, 49) |
| 08-13 **HUMAN — league:** ≥ 2 scored games against different teams, live-reported. **Also needs 07-10** | P0 | 6 | **NO** | ☐ | Khaled | One unscored warm-up per opponent then the **one** scoring game (rule 52); `reporting.mode = live` only for the scored games and flipped back; each report confirmed arrived at `rmisegal+uoh26finalgame@gmail.com` with the JSON **attached** and the commit hash inside; each game recorded in the ledger with its four artifacts committed under per-game names; ≥ 2 vs different teams (Table 18 row 3, fixed) and ≤ 10 total (row 5, fixed); a verdict disagreement is **reported as a disagreement**, never edited to match (SUB-07, SUB-08, SUB-12, rules 32, 35, 38, 52, 53) |
| 08-14 **HUMAN — submit:** form PDF, self-assessment score, games-played sign-off, per-member submission | P0 | 7 | **NO** | ☐ | Khaled | The form obtained (OQ8-3 — its location is recorded in no project document), filled, saved as PDF, **unaltered**; the two repo links and team code `khm-mn17` entered; the self-assessment score set for **code quality only** (rule 55); the games-played value signed off and equal to the ledger's derived figure and to what went on the wire (rule 38); submitted per team member (rule 44); the tag moved to the final commit if code changed after 08-12 (SUB-05, SUB-09, SUB-10, SUB-11) |
| 08-96 Refresh the graphify graph and commit `.planning/graphs/` for the submission showcase | P2 | 4 | yes | ☐ | Khaled | `graphify update .` run and `GRAPH_REPORT.md` committed; `graph.json`/`graph.html` remain gitignored build artifacts |
| 08-97 Create/refresh `docs/phases/phase-8/{PRD,PLAN,TODO}.md` at plan-phase | P1 | — | yes | ☑ | Khaled | This triplet exists and is approved before execution (§2.5 step 5) |
| 08-99 On verify-work: mark all rows ☑ + tick the matching rows in root `docs/TODO.md` | P1 | — | yes | ☐ | Khaled | Submission gate met; all phase-8 TODOs checked (DOC-01) |

## Phase gate (the submission gate)
- [ ] **1.** Two cross-linked public repos (cop, thief), each carrying README / `config/` / PRD /
      PLAN / TODO, with a Git tag on the submitted version.
      *(Prepared and measured by 08-10/08-11; completed by 08-12.)*
- [ ] **2.** Academic README with its six §9.4.2 sections; submission form filled and saved as PDF,
      submitted per team member.
      *(Drafted in full by 08-06; completed by 08-14.)*
- [ ] **3.** At least 2 scored league games against different teams, reported, each game emailing
      the commit hash it ran on.
      *(Machinery by 08-04; played by 08-13 — **which also needs 07-10**.)*

## The three human-gated plans, and exactly what each needs from a person
Everything through wave 4 runs unattended. Claude must not create or push to a public repository,
push a git tag, mail the lecturer, play a league game, or enter a credential.

| Plan | What only a human can do |
|---|---|
| **08-12** | Create two public GitHub repos · push each prepared output · push the tag · paste the two real URLs into `league.json` · send the D7-17 question to the lecturer |
| **08-13** | Arrange real opponent teams · run the scored games with `reporting.mode = live` · confirm each report arrived · judge a verdict disagreement honestly |
| **08-14** | Obtain and fill the submission form · set the self-assessment **score** · sign off the games-played **value** · submit per team member |

## Open questions blocking parts of execution
Recorded rather than invented — full text in
[08-PLAN-OUTLINE.md §4](../../../.planning/phases/08-submission-and-league-operations/08-PLAN-OUTLINE.md):

| # | Question | Blocks | Who decides |
|---|---|---|---|
| OQ8-1 | **D7-17** — `game_id` is minted per GAME while `PARAMETERS.md:168` reads it as the SERIES id; `:72` settles ties on the cross-sub-game aggregate and `:86` (rule 52) pulls the other way. `game_id` is peer-negotiated (D-61), so this is a **protocol** decision. Three costed options + "ask the lecturer" in `GATE-7-MEASUREMENT.md` | 08-04 drafts the question; 08-13 may need the answer | **human, not Claude** — no scheme invented |
| OQ8-2 | The games-played **VALUE**. `config/police/games_played.json` = **1922**, `config/thief/` = **1915** — one team, two counters, disagreeing by seven. Options A/B/C in `GAMES-PLAYED-RECONSTRUCTION.md` §6; under Option A the value must lie in `[0, 10]` (Table 18 row 5, fixed) | 08-13, 08-14 | **human, not Claude** — rule 38 is absolute |
| OQ8-3 | Where the **submission form** lives. Rule 43 says "download the submission form"; neither `PARAMETERS.md` §Addresses nor `RULES.md` gives a URL, a Moodle location or a file name | 08-14 | **human** — no location guessed |
| OQ8-4 | The self-assessment **SCORE** (rule 55, code quality only) | 08-14 | **human** — 08-11 drafts the evidence table with the field blank |
| OQ8-5 | Which **licence**. §17 requires one and none exists; publishing a licence is a legal declaration | 08-03 | **human** — a `LICENSE` placeholder stays a GAP until named |
| OQ8-6 | The two public **repo URLs** — they do not exist until 08-12 | 08-04, 08-06 | resolved by D-81: config, never hardcoded |
| OQ8-7 | The agreed per-series **token ceiling**. Table 18 row 4's ~200,000 is an *example*, status negotiable; the real figure is agreed with the lead team | 08-04, 08-13 | agreed with the opponent — `DeclarationContext` already refuses to default it |
| OQ8-8 | Does §9.4.2 mandate a **Hebrew** README? `08-CONTEXT.md` assumes English and instructs a check; the extracts do not settle it | 08-06 | confirm against the book before writing |
| OQ8-9 | Is the existing `origin` (`main` is **122 commits ahead**) public or private? If public, 08-03's scan is urgent rather than preventive | 08-12 | **human** — cannot be determined offline |
