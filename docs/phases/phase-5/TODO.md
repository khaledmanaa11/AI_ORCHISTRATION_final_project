# Phase 5 TODO — Cloud Exposure and Tunneling

**Owner:** Khaled (solo) · **Updated:** 2026-08-16 (**GATE-5 MET** — remote-round attempt 4 closed criterion 2: two machines on two networks, agreeing verdicts and matched audits on **both** sides, live `claude-haiku-4-5` hints, evidence in `remote-round-2026-08-16-attempt4/`. A post-closure adversarial audit then found five further gaps **G6–G10** — three blocker-class, none of them a §10.4 criterion — recorded in `.planning/phases/05-cloud-exposure-and-tunneling/05-UAT.md` Round 2 and awaiting their own plan.)

> Phase task list. Row IDs and plan IDs are deliberately the same (the Phase-4 convention).
> `/gsd:verify-work 5` marks every row `[x]` and ticks the matching rows in the root
> [docs/TODO.md](../../TODO.md).
> **Status:** ☐ not started · ◐ in progress · ☑ done · **Priority:** P0/P1/P2

| Task | Pri | Status | Owner | Definition of Done |
|------|-----|--------|-------|--------------------|
| 05-01 Tunnel lifecycle — pyngrok dep, tunnel.json + loader, TunnelManager (DI, reconnect via Table 19 reuse), lifecycle wiring, exchange printout | P0 | ☑ | Khaled | Unit tests all faked; gates green; no secret/number in config (CLOUD-01) |
| 05-02 Shared-secret channel — ASGI middleware, explicit client transport headers, env plumbing, .env-example | P0 | ☑ | Khaled | 403 without header through loopback integration; secret-off default leaves all existing tests untouched (CLOUD-01) |
| 05-03 Gate 5 — smoke script, GATE-5-MEASUREMENT.md (remote round procedure, PENDING item), LOCALTONET-FALLBACK.md, graph refresh | P1 | ☑ | Khaled | Smoke script env-gated + evidence JSON; both §10.4 criteria quoted with evidence paths (CLOUD-02, rule 10) |
| 05-04 G1 — verdict honesty + bounded teardown grace: a failed OWN final-reveal send stops accusing the peer; technical losses get a corrected `game_over`; `linger_for_peer` (Table 19 `response_timeout` cap / `backoff_seconds` quiet interval, zero new numbers) | P0 | ☑ | Khaled | Send-failure matrix + 3 paired fairness controls green; GATE-6 re-run all three criteria PASS (rules 16/22) — **executed 2026-08-14** (3/3 tasks, commits 8f35721 / 6fd4fb9 / 142c4b4); suite 1262 passed / 96.30%, GATE-6 all three PASS; ☑ at verify-work |
| 05-05 G2 — the negotiated game id governs log, ledger, declaration AND committed `state.game_id`; audit validates the peer's committed role/turn, and game_id when one was negotiated | P0 | ☑ | Khaled | Four artifacts share one stem across two machines; forged-record probe no longer matched; honest peer publishing no game_id still matched; GATE-6 PASS (D-61, SEC-05/08) — **executed 2026-08-14** (3/3 tasks, commits ead48df / b06b4b3 / 01ff8ed); real loopback game produces one stem `f21a1071045f801c` on BOTH sides across log+ledger+declaration and both ledgers commit that same `state.game_id`; `ctx.candidate_game_ids` measured two-element on BOTH roles; suite 1293 passed / 96.35%, GATE-6 all three PASS; ☑ at verify-work |
| 05-06 G3+G4 — inbound HINTs written to the wire log (D-11/D-14, rule 20); relaxed receive window AND responder `pending.turn` stamp landing together; no hint composed for an already-resolved turn | P0 | ☑ | Khaled | Two-peer test: both sides stamp the turn played, both sides decode ≥1 non-`no_hint` hint; the 3 tests that froze the bugs re-specified, none deleted (LANG-01/03) — **executed 2026-08-14** (3/3 tasks, commits 7619758 / ead48df / f32bb3a; Task 2's six files landed inside `ead48df` via a shared-index collision with the parallel 05-05 executor — content verified file-by-file, see 05-06-SUMMARY.md); real loopback game: police hints `0,1,2,3,4` and thief hints `0,1,2,3` (both = turn played), BOTH sides carry `message_received`+`hint` records and BOTH have ≥1 non-`no_hint` incoming hint; five tests re-specified, none deleted or loosened; both non-vacuity reverts recorded; suite 1293 passed / 96.35%, GATE-6 all three PASS; ☑ at verify-work |
| 05-07 G5 — keyless LLM made legible: startup WARNING, honest declared `llm_name`, first-person compose prompt | P1 | ☑ | Khaled | Fallback BEHAVIOUR unchanged (Phase-4 sanctioned); 10 declaration keys unchanged; PRD_deception.md STYLE_GUIDE in sync (rule 38) — **executed 2026-08-14** (2/2 tasks, commits `951d87d` / `50ac2fe`); keyless probe prints the WARNING to bare stderr with no `logging.basicConfig` (measured); declared `llm_name` is `template-fallback (no LLM calls)` for template-or-keyless and `claude-haiku-4-5` only with a key present; declaration still exactly 10 keys, pinned by name; the 4 Phase-4 degradation tests pass **unedited**; `agent_audit_wiring.py` 134 lines after the helper was split into `language_wiring.py` (129); suite 1327 passed / 96.37%, GATE-6 all three PASS; ☑ at verify-work |
| 05-08 Remote round attempts — HUMAN-RUN on two machines/networks; runbook amended with attempt-1's missing evidence (machine B console, ngrok agent log, clock skew) | P0 | ☑ | Khaled | **Runbook half landed 2026-08-16 (`85a7171`), after this row was first ticked.** Stated plainly per rule 38: at verify-work the row was ticked on the ROUND alone — the runbook amendment it also names had not been written, and `REMOTE-ROUND-RUNBOOK.md` contained zero occurrences of "ngrok agent log" and no clock note until 05-08 executed. It now carries both, plus the expected-difference table, the pre-flight commit/`llm_name` checks and a pre-`git add` secret grep, informed by attempts **2–4** as well as attempt 1. **Round half CLOSED by attempt 4 (2026-08-16)**: games `b22361aa93ccf310` + `d265603c116a9f99`, hotspot ↔ wired, agreeing `capture` + `audit_verdict matched=true` on BOTH machines, one shared UID each, live `claude-haiku-4-5` both sides; 26/26 cross-checks re-derived independently. Evidence `remote-round-2026-08-16-attempt4/`, narrative in GATE-5-MEASUREMENT.md Attempt 4. Attempt 3 (`9c1cf313482719d4`, template-fallback, honestly declared) also completed. Original DoD: Two AGREEING verdicts + one shared game UID across all four artifacts, or criterion 2 stays PENDING with the new gaps stated (CLOUD-02) — **attempt 2 ran 2026-08-16** (game `5efbc5811fabfac4`, hotspot ↔ wired, B on runbook Path C after Smart App Control blocked pyngrok's binary): shared UID + byte-identical cross declarations + turns 0–3 live both ways, then A's ingress dropped and stayed down → B `technical_loss {opponent_unresponsive}`, A killed by its own watchdog with NO verdict; criterion stays PENDING; the repair gap became 05-11; evidence `remote-round-2026-08-16/`, narrative in GATE-5-MEASUREMENT.md Attempt 2 |
| 05-11 Tunnel watch — `ensure_connected()` gets its production caller: a `run_with_tunnel` watch task polls `healthy()` every `watchdog_poll_seconds` (D-55/D-18 reuse, zero new numbers) via `to_thread`, repairs a drop with the existing Table-19 bound, gives up honestly after one exhausted repair; a raising probe/connect is one spent attempt (attempt-2's ERR_NGROK_334 shape) | P0 | ☑ | Khaled | Watch runs for exactly the game's lifetime; tunnel-off flows start no task and change nothing; suite green with cadence/repair/give-up/cancellation pinned; ruff 0, ≤150 gate, coverage ≥85 (CLOUD-01/02) |
| 05-09 Deferred item #1 — a transport-level connection failure becomes a bounded, measured, correctly-attributed verdict instead of killing the agent (NET-06, CLOUD-02) | P0 | ☑ | Khaled | No connection failure escapes `call_with_retry`; in-game exhaustion accuses, a teardown-time failure after a board outcome does not; the 403 path provably untouched; no catch-all in `deadline.py` — **executed 2026-08-14** (2/2 tasks, commits `f31ece5` / `f602eb3`); `httpx.TransportError` joined the ladder AND `unwraps_to_retryable` closed fastmcp's `RuntimeError` connect-path wrapper, which a widened tuple alone left open (measured, deviation 3); `late_peer_round(linger=False)` now ends `audit_incomplete` → `audit_verdict{matched: true}` with **zero** `technical_win` on either side, where pre-fix the late peer ended on `game_over` and nothing after it; suite 1308 passed / 96.36%, GATE-6 all three PASS, `dev_launch.py` exit 0 with both sides `audit_verdict matched=true`; ☑ at verify-work |
| 05-10 Deferred items #6 + #7 — the last peer-data crash paths: a malformed peer FINAL_REVEAL is a named mismatch instead of a process kill, a 5xx/429 is retried while a 4xx still raises, and the `board_outcome` production wiring gains a test (NET-06, CLOUD-02, SEC-05) | P0 | ☑ | Khaled | No peer-supplied shape prevents a verdict; an unparseable peer still loses and an honest peer is untouched; a 502/429 costs a backoff and a 403 accuses nobody; no catch-all — **executed 2026-08-14** (3/3 tasks, commits `ab4951b` / `49b58ac` / `6a9df51`); nine hostile shapes that RAISED at `495aa9d` now each return a named mismatch and still lose (before/after verbatim in 05-10-SUMMARY.md); the `turn` test is join-key USABILITY, so an honest peer sending the float `3.0` is STILL MATCHED and the control FAILS against an `isinstance` rule; a **sixth** instance found and closed (`handshake_step0._step0_verified` raising `AttributeError` at the handshake); `RETRYABLE_STATUS_CODES = {429, 500, 502, 503, 504}` ENUMERATED, with 501/505 as the any-5xx control; `tests/integration/test_secret_channel.py` passes **unedited** 3/3; six revert probes recorded; three files SPLIT rather than compressed; suite 1361 passed / 96.51%, GATE-6 all three PASS, `dev_launch.py` exit 0 with one shared uid `ac3a02d87460b28e`; ☑ at verify-work |
| 05-12 G9+G7 — a malformed peer cannot kill us at the handshake: a non-str peer digest is a named non-agreement, and `peer_game_id` is safety-validated before it reaches a set, a Path or the audit's membership key | P0 | ☐ | Khaled | Probe matrix (non-str digest; unhashable / traversal / empty / over-long id) each ends in a named outcome, never a traceback — **and the control that an honest peer using a FOREIGN id convention is still MATCHED** (rules 16/22); `digests_match` keeps its strict contract; boundary-rule comment names instances 7–8 (D-46, D-61, rule 23, rule 36) — **EXECUTED 2026-08-16** (commits `52a6b47` / `11e9978` / `2826d3a` + `5c92ec7` / `765d8a6`). Box left ☐ deliberately: `/gsd:verify-work` ticks it, per this table's own 05-10 precedent. Every probe recorded verbatim in `05-12-SUMMARY.md`. The corridor sweep found **two MORE** instances than the plan named — a non-str `step0_digest` and a non-str declaration `hmac`, both reaching `digests_match` inside `verify_declaration` — so the boundary-rule comment names instances **7, 8 AND 9**, and both new ones were **downgraded, not aborted**, because an abort would have been a new false-accusation path. A first draft that DID abort was caught by its own no-new-accusation control and reversed. Fairness control: 8 honest foreign conventions (UUID, upper-case hex, 64-hex, team-code label, 2-char, non-ASCII, interior dot, exactly at the bound) still MATCH along the whole chain — gate → adoption → candidate set → `audit_state` membership — with a third-game negative control. 5 revert probes; P3 (our own 16-lower-hex convention as the rule) fails 18 fairness cases. `game_identity.py` hit 162/150 → **SPLIT** into `game_identity_validate.py`; `test_config_hash.py` hit 161/150 → SPLIT into `test_config_hash_peer.py`. Suite **1479 passed / 0 failed / 96.57%** (baseline 1374 / 96.54%), ruff 0, ≤150 exit 0, no-llm OK, GATE-6 all three PASS, `dev_launch.py` exit 0 with one shared uid `57db320c3383f405` and both sides `audit_verdict matched=true` |
| 05-13 G6 — the audit survives long enough to be honest: it touches the watchdog per bounded attempt, and BOTH legs stop accusing a peer that answered | P0 | ☐ | Khaled | A push ladder outliving `watchdog_threshold` still reaches `audit_incomplete` + writes an `audit_verdict` with zero `technical_win`; mirrored receive-leg case; three paired fairness controls still fire; NET-07 freeze detection preserved; injected clock, zero real sleeps (NET-06/07, rules 16/22) — **EXECUTED 2026-08-16** (commits `d5f2f36` / `a2f7034` / `6920d4d` / `ce89f02` / `2633df4` / `da58bc2`). Box left ☐ deliberately: `/gsd:verify-work` ticks it, per this table's own 05-10/05-12 precedent. Measured: the push ladder runs **4 attempts / 140 s on the injected clock in 0.000 s of real time** against `watchdog_threshold` = 60 s and still reaches `audit_incomplete` + `audit_verdict` with zero `technical_win`; the receive ladder likewise **reaches** rule 36's TECHNICAL_LOSS where before the process died partway down it with nothing written. NET-07 proven preserved by TEST, not prose: a single attempt overrunning the threshold still fires `["freeze", "exit"]` in that order, and `test_the_audit_never_disarms_the_watchdog` refutes the tempting wrong fix. **FOUR revert probes**, P4 being the wrong fix itself (touches removed + `ctx.watchdog.stop()` across the audit): 3 failed / 6 passed. The receive leg's discrimination is `send_verdict is not None` — a push that LANDED proves the channel worked, so the withheld-nonces sanction is unchanged; only when BOTH of our own legs failed is the evidence about us. All three pre-existing paired controls pass **byte-unedited**. `agent_audit_wiring.py` hit **exactly 150/150** → SPLIT into `agent_step0_wiring.py`, never compressed. Suite **1484 passed / 0 failed / 96.58%** (baseline 1479 / 96.57%), ruff 0, ≤150 exit 0, no-llm OK, GATE-6 all three PASS, `dev_launch.py` exit 0 with one shared uid `59dc79e25016a0d8` and both sides `audit_verdict matched=true` |
| 05-14 G8 — the hint channel is correct on every supported path: one inbound hint is decoded at most once, both branches stamp the turn played | P1 | ☐ | Khaled | Re-sent hint after a pop decoded ONCE (revert probe fails without the marker); `commit_reveal=False` initiator stamp == turn played; `_HINT_LOOKBACK_TURNS` recorded in PARAMETERS.md with its derivation; the confounded `token_spend` argument removed; window width unchanged (LANG-01/03) — **EXECUTED 2026-08-16** (commits `f6bbc0f` / `91dbc86` / `54619a1` / `914662c` + `9852b3f` / `5371d0d` / `f108a8b`). Box left ☐ deliberately: `/gsd:verify-work` ticks it, per this table's own 05-10/05-12/05-13 precedent. **THE PLAN'S OWN FIX WAS HALF RIGHT AND THE PROBE IS WHAT SAID SO.** Its consumed marker is written at ARRIVAL and relies on `maybe_resolve` clearing `pending_hints`; measured, that survives to the RESPONDER's decode (attempt-4 machine B, 6 of 6 records at `record_turn == envelope_turn + 1`) but is destroyed before the INITIATOR's (machine A, 5 of 5 at delta 0 — the hint arrives during the turn, the resolve clears the marker, `take_my_turn` decodes a turn later), leaving the initiator decoding a re-send **twice** (`decodes=2`). `turn_hint_store.consume_hint` writes it at CONSUMPTION instead: initiator `decodes=1`, responder `decodes=1`. **Window WIDTH byte-unchanged** — `test_hint_freshness.py`'s two boundary cases pass unedited, and revert probe **P4, the tempting wrong fix** (re-narrow to the pre-05-06 `turn < ctx.state.turn`), **fails 6 tests**. Five probes total: P1 guard removed 3 fail, **P2 the plan's own arrival-time marker 1 fail** (exactly the initiator case), P3 the inclusive `>=` reused unexamined 3 fail, P4 6 fail, P5 `ctx.state.turn` restored 2 fail. `_HINT_LOOKBACK_TURNS` recorded in `docs/PARAMETERS.md` under a NEW **"Derived protocol constants — not Appendix F values"** section with no Status column, and both the doc and the source say why it is treated differently from 05-12's structural `_MAX_PEER_GAME_ID`. The confounded `token_spend.calls = 0` sentence **deleted** and replaced by two unconfounded proofs. `turn_hint_buffer.py` hit 141/150 → **SPLIT** into `turn_hint_store.py` (88 + 91), never compressed. One deferred item logged, not fixed (**#13**: on the same toggle-off path the second mover's MOVE envelope is stamped 1..16 for turns 0..15 — the repair changes `turn_commit.initiate`'s signature, which on the commit-reveal-ON path feeds the D-59 hash input and the D-64 ledger join key). Suite **1491 passed / 0 failed / 96.59%** (baseline 1484 / 96.58%), all three changed hint modules at **100%**; ruff 0, ≤150 exit 0, no-llm OK, GATE-6 all three PASS, `dev_launch.py` exit 0 with one shared uid `83143c7b54bdbfbc`, both sides `audit_verdict matched=true`, zero `technical_win` and zero `watchdog_incident` |
| 05-15 G10 — the declaration story settled and written down: dead `declare_truthfully` removed, stale PRD line corrected, capture Claim de-risked | P1 | ☐ | Khaled | Rules 15/16 confirmed satisfied by the committed action (`PRD_commit_reveal.md` §2.2, D-66/SEC-07) with the quoted rule text recorded; no unreachable BARRIER/CAPTURE branches left; `PRD_mcp_transport.md:65` carries a dated superseded-by note; capture announced via the EXISTING `GAME_OVER` envelope driven by the resolved outcome, or deferred with a measurement (rules 21/22, DOC-01) — **EXECUTED 2026-08-16** (commits `4acee3b` / `1c90869` / `0930aac` / `886772d` / `f80e81b`). Box left ☐ deliberately: `/gsd:verify-work` ticks it, per this table's own 05-10/05-12/05-13/05-14 precedent. `declare_truthfully` deleted on RECORDED grep evidence (`src/` held only its own definition and its own error string; the dynamic/getattr/config-string sweep returned 0), and the plan under-counted the call-site test files by TWO — probe **P1** (deleted, tests unedited) = **5 COLLECTION ERRORS**, both directories interrupted, 0 tests run. The three deleted tests are RE-SPECIFIED, never dropped. Rules **15/16/21/22 recorded with the rule text QUOTED VERBATIM** in `deception.py`'s module docstring and a new `PRD_deception.md` §2.1.1 table, citing `PRD_commit_reveal.md` §2.2 / D-66 / SEC-07. `PRD_mcp_transport.md:65` carries a dated superseded-by note, APPENDED not rewritten, which also states why `MessageType.BARRIER` is deliberately retained. **THE PLAN'S PREFERRED REMOVAL OF THE BARRIER/CAPTURE ROWS IS WRONG AND THE MEASUREMENT SAYS SO** — probe **X** (rows deleted) = **4 fail / 42 pass**, one a `KeyError` escaping `bluff.compose()`, whose contract is having no failure mode; `DeceptionPlan(intent=TRUTH, kind=BARRIER)` stays constructible, so `BANK` must stay TOTAL. Kept and documented as reserved-and-total-by-design instead (the plan's second sanctioned option). Capture Claim SENT: `network/capture_declaration.py`, cop only, on the resolved `Outcome`, via the Phase-2 `GAME_OVER` envelope and `game_over` tool — no new MessageType, tool, number or payload vocabulary. **BUILDING IT EXPOSED A PRE-EXISTING FALSE-ACCUSATION PATH** (rules 16/22): `receive_final_reveal` read `records` off whatever arrived FIRST, so a stray envelope gave `records=[]` / `verdict=None` — indistinguishable from the `{"records": []}` rule-36 evasion — and lost the game to an HONEST peer. Reachable before this plan added a sender, because `game_over` is on our published league surface. Measured pre-fix, closed in `886772d`. **SELF-AUDIT FOUND A SHADOWED ASSERTION IN MY OWN CONTROL**: `records ==` fired first and hid `verdict is None`, so both wrong fixes produced the IDENTICAL failure line; re-pinned as one tuple, and probe **P8** (the defensive wrong fix that returns the ledger correctly but still accuses) now fails 2. **ELEVEN probes** recorded verbatim. TWO 150-line SPLITS, never compressions (`agent_audit_exchange.py` 153 → `agent_audit_observed.py`, code byte-identical; `test_agent_audit_exchange.py` 152 → `test_agent_audit_receive.py`, every surviving body byte-unedited). Live `dev_launch.py` exit 0, one shared uid `6ee059af51c508f5`: police `game_over message_sent turn=6`, thief `message_received turn=6`, payloads byte-identical, both `audit_verdict matched=true`, zero `technical_win`, zero `watchdog_incident`. GATE-6 all three PASS (evidence differs in 3 timestamp lines plus exactly two new counters — `game_over: 1` under `police_sent` and under `thief_received`, nowhere else; all 32 verdict fields byte-identical). Suite **1507 passed / 0 failed / 96.62%** (baseline 1491 / 96.59%), all four new/changed network modules at 100%; ruff 0, ≤150 exit 0, no-llm OK. Two deferred items logged (#14, #15) |
| 05-16 Deferred item #10 — the TURN LOOP survives a stalled peer: every bounded attempt is marked, so a 140 s ladder ends in D-13's honest verdict instead of `os._exit(1)` at t=60 s mid-game (NET-06, NET-07, CLOUD-02) | P0 | ☐ | Khaled | A turn-loop ladder outliving `watchdog_threshold` reaches a verdict with zero `watchdog_incident`; NET-07 still kills a genuinely wedged loop, proven by a test that FAILS against the blanket `stop()`; no Table-19 value moved — **EXECUTED 2026-08-16** (commits `a010a55` / `4e3d42e` / `0993b05`). Box left ☐ deliberately: `/gsd:verify-work` ticks it, per this table's own 05-10/05-12/05-13/05-14/05-15 precedent. **Measured before** on the injected clock in 0.000 s of real time: `wait/push/move attempts=2 elapsed=70.0s touches=0 checks=[False, True] fired=['freeze','exit'] verdict=None` — the D-13 ladder due at t=140 s never spoke, and `LethalWatchdog` makes that a MEASURED fact by raising where production calls `os._exit(1)`. **After:** `attempts=4 elapsed=140.0s touches=5 checks=[F,F,F,F] fired=[] verdict=TechnicalWin` on every leg; widest touch gap = `response_timeout + backoff_seconds` = 35 s < 60 s. **THE DEFERRED ITEM UNDER-COUNTED ITS OWN EXPOSURE BY TWO LADDERS**: it names the four `wait_for_*` legs, but a stalled peer stalls our PUSH first — `turn_commit_send.push` sends the COMMIT the wait leg awaits a reply to — and `send_move_only`, `send_hint` and 05-15's `capture_declaration` (which runs INSIDE `run_turn_loop`, so an unmarked ladder there killed us before `run_final_audit` could publish our nonces) carried it too. Six ladders, all marked; `grep -rn "watchdog\.touch" src/` returns **18** = 12 calls + 5 hook passes + 1 comment. **TEN PROBES**: P1–P6 each revert one leg and fail EXACTLY one case (1 failed / 7 passed, six times); **W1 the WRONG FIX of widening `watchdog_threshold` 60 → 150 fails 6 of 8** on `assert 140.0 > 150` / "a Table-19 NUMBER was moved", because every case reads the bound from config and asserts the ladder OUTLIVES it; **W2 the WRONG FIX of a blanket `ctx.watchdog.stop()` fails 8 of 8**, including "DID NOT RAISE ProcessKilledError" and "the turn loop left the watchdog disarmed"; **P7 probes MY OWN HARNESS** — strip the freeze poll out of the stalled peer and 7 of 8 fail, so the suite is not certifying its own silence. Two SPLITS, never compressions (`_turn_loop_fixtures.py` 74, `test_turn_push_watchdog.py` 85, leaving `test_turn_loop_watchdog.py` at 110). Suite **1523 passed / 96.62%** (+8 on the 1516 baseline, coverage unchanged), all four changed modules at **100%**; ruff 0, ≤150 exit 0, no-llm OK, GATE-6 all three PASS, `dev_launch.py` exit 0 with one shared uid `0f803ac4058d208a`, both sides `audit_verdict matched=true`, zero `technical_win`, zero `watchdog_incident`. The one suite failure is deferred #4's late-peer flake, re-attributed by paired measurement (identical 1-fail/2-pass with and without this plan's source changes) |
| 05-17 An early peer FINAL_REVEAL is never lost — the turn loop buffers it instead of eating it, and the audit reads that buffer before it waits (NET-06, NET-07, CLOUD-02, SEC-05, rules 16/22/36) | P0 | ☐ | Khaled | A peer that finishes first and pushes is MATCHED, never `OPPONENT_UNRESPONSIVE`; a genuinely silent peer is STILL accused; no Table-19 value moved — **EXECUTED 2026-08-17** (commits `b29a098` / `28adc4d` / `0a896aa` / `0cb9aea`). Box left ☐ deliberately: `/gsd:verify-work` ticks it, per this table's own 05-10/05-12..05-16 precedent. **Measured at HEAD**, verbatim: `TURN LOOP LEG: queued=2 -> returned type='reveal' verdict=None queue_left=0`; `AUDIT: our push landed? 1 call(s)`; `AUDIT OUTCOME: <Outcome.TECHNICAL_LOSS: 'technical_loss'>`; `technical_win{reason: 'opponent_unresponsive', retries_attempted: 2}` then `game_over{outcome: 'technical_loss'}`, and **no `audit_verdict` ever written**. `queue_left=0` is the defect in one number — the peer's ledger was consumed off our own queue and destroyed by the tolerated-jitter drop — and the landed push is what took `agent_audit_wiring.py:97`'s accusatory branch against a peer that DEMONSTRABLY DID PUSH. **The PREMISE broke, not the inference**: `:90-93` is sound and was not edited by one character. Fixed by ROUTING — `next_protocol_message` records every FINAL_REVEAL into `ctx.commit_state.early_final_reveal` and **still returns it** (a HINT is buffered-and-SKIPPED because no caller wants one; this is buffered-and-RETURNED because exactly one caller does), and `receive_final_reveal` consults the buffer **before** the wait so an early ledger short-circuits the ladder. **RULE 36 PROVEN INTACT**: the silent-peer counter-control still earns `TECHNICAL_LOSS` + `technical_win{opponent_unresponsive}` and passed UNCHANGED in all six revert probes. **SEVEN PROBES, every one with a real count**: P1 routing reverted **8 failed / 2 passed**; **P2 THE WRONG FIX — widen the receive ladder — ran 21 attempts (10x the shipped 4) for a BYTE-IDENTICAL accusation**, because the envelope was already eaten; P3 audit stops reading the buffer 4 failed / 6 passed; P4 first-wins → last-wins 1 failed / 9 passed; **P5 the DEFENSIVE wrong fix (buffer read AFTER the ladder) 1 failed / 9 passed on `2 != 0` QUEUE PULLS** — it returns the same records and accuses nobody while costing 135 s against a 60 s `watchdog_threshold`, and counting pulls is the only thing that sees it; P6 `take` → peek 1 failed / 9 passed. **SELF-AUDIT caught a false justification of my own** (`take_final_reveal` claimed a peek "would spin forever"; it would not) — replaced with the reason P6 makes testable, recorded as a correction. TWO 150-line SPLITS at seams deferred #11 had already NAMED (`turn_commit_pull.py`, moved body byte-identical bar two lines, 145 → 135; `final_reveal_buffer.py`), plus a test-file split (133 + 111 + 111). **A THIRD false-accusation door found by GREPPING PRODUCTION CALLERS**: `turn_commit.py:103`, the police branch, pulls bare with no type test and turns an early FINAL_REVEAL into `TECHNICAL_LOSS` / `'payload must be a dict, got NoneType'` — measured byte-identical with the routing reverted, so pre-existing; filed as **#18**, NOT fixed. Both named non-goals filed WITH measurements (**#16** the linger's quiet interval is derived from the wrong clock: 5 s quiet, 30 s cap, 35 s worst-case arrival→retry; **#17** the linger drains via a plain `queue.get()` so the buffer does not cover it: `queue 1 → 0`, `buffered None`, nothing logged). Suite **1530 passed / 0 failed / 96.64%** (baseline 1524 / 96.62%), all six changed/new modules at **100%**; ruff 0, ≤150 exit 0 **tree-wide and again on the five new files named explicitly** (the no-argument form enumerates via `git ls-files` and passes vacuously over untracked files), no-llm OK, GATE-6 all three PASS, `dev_launch.py` exit 0 with one shared uid `a97adb14e48fc621`, both sides `audit_verdict matched=true`, zero `technical_win`, zero `watchdog_incident` |
| 05-96 Refresh the graphify graph at plan-phase and after execute | P2 | ☑ | Khaled | GRAPH_REPORT.md current with tunnel modules (plan-phase refresh done at 04-13/14; post-execute refresh in 05-03; refreshed again at plan-phase 2026-08-16 for the 05-12..05-15 set; refreshed after 05-16 executed — 7832 nodes / 14141 edges / 479 communities; refreshed again after 05-17 — **7934 nodes / 14322 edges / 481 communities**, `graph.html` skipped by the tool's own 5000-node viz ceiling, which is pre-existing and gitignored) |
| 05-97 Create/refresh docs/phases/phase-5/{PRD,PLAN,TODO}.md at plan-phase | P1 | ☑ | Khaled | Triplet exists and matches the plan set — refreshed 2026-08-13 for the 05-04..05-08 gap-closure set; re-checked at verify-work 2026-08-16 (PRD/PLAN/TODO all present) |
| 05-99 On verify-work: mark all rows ☑ + tick root docs/TODO.md | P1 | ☑ | Khaled | Phase gate met incl. the human remote round; all TODOs checked (DOC-01) — **done 2026-08-16**: criterion 2 closed by attempt 4, G1–G5 confirmed present in live code with real production callers, every row above ☑, duplicate 05-09/05-10 rows removed, root `docs/TODO.md` row 05-02 ticked. Table-5 gate re-measured green (ruff 0, 1374 passed, 96.54%, ≤150 clean, 0 secrets, `.env` untracked). Five NEW gaps **G6–G10** recorded in 05-UAT.md Round 2 — none is a §10.4 criterion, so the gate stands. **Scope note:** this row covers the 05-01..05-11 set. Rows 05-12..05-15 were PLANNED on 2026-08-16 (plan-checker verified, 2 iterations) and are ☐ pending execution; they get their own verify-work pass, which is when this row's "all rows ☑" claim is re-asserted for them |

## Phase gate (§10.4)
- [x] Each peer is reachable on the public internet through ngrok/Localtonet
      (real run 2026-08-09T09:41:20Z, `verdict: PASS` — gate5_smoke_evidence.json)
- [x] An agent on a remote machine connects through the tunnel and plays a full round
      against the local agent (human-run; procedure in REMOTE-ROUND-RUNBOOK.md).
      **Attempt 1, 2026-08-13: a full 5-turn game to a real capture across two machines
      on two networks — but the two sides' verdicts disagreed and the two logs carried
      different game UIDs, so the criterion did not close.** Five gaps diagnosed
      (05-UAT.md), planned as 05-04..05-08.
      **Attempt 2, 2026-08-16: every attempt-1 fix held live (one shared game UID,
      cross-verified declarations, honest failure records) — but machine A's tunnel
      ingress died at turn 4 and the designed reconnect had no production caller, so
      no agreeing verdicts exist.** That gap closed same-day as 05-11 (the tunnel
      watch); attempt 3 ran on it.
      **CLOSED by attempts 3–4, 2026-08-16 (hotspot ↔ wired ethernet): attempt 3 —
      full round to capture, audit matched, template-fallback hints declared honestly;
      attempt 4 — TWO full rounds with live `claude-haiku-4-5` hints on both sides,
      both `capture` with `audit_verdict matched=true` on BOTH machines, one shared
      game UID each, declarations byte-identical cross copies. 26/26 cross-checks
      (recomputed commit hashes, wire delivery, verdicts, digests) pass. Evidence:
      GATE-5-MEASUREMENT.md Attempts 3–4 + `remote-round-2026-08-16-attempt{3,4}/`.**
