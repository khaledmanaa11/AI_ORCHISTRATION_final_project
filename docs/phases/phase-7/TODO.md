# Phase 7 TODO — Reporting and Visualization Shell

**Owner:** Khaled (solo) · **Updated:** 2026-08-17 · **Status: ☐ planned — outline + triplet written
at plan-phase; no plan executed yet.**

> Phase task list. ROADMAP rows 07-01…07-04 are deliverable **groups**; this phase needs ten plans
> to keep every file under the 150-line gate, so row IDs and plan IDs are **not** 1:1 here (they
> were in Phases 4 and 6). The mapping is in
> [07-PLAN-OUTLINE.md §10](../../../.planning/phases/07-reporting-and-visualization-shell/07-PLAN-OUTLINE.md).
> `/gsd:verify-work 7` marks every row `☑` and ticks the matching rows in the root
> [docs/TODO.md](../../TODO.md).
> **Status:** ☐ not started · ◐ in progress · ☑ done · **Priority:** P0/P1/P2

| Task | Pri | Wave | Status | Owner | Definition of Done |
|------|-----|------|--------|-------|--------------------|
| 07-00 Games-played counter — increment moved to a genuine game end (`record_completed_game`), test suite structurally barred from the shipped counter (`tests/_shipped_config_guard.py`), the false Phase-6 claim corrected, evidence reconstruction written | P0 | 0 | ☑ | Khaled | A full `uv run pytest tests/` advances both shipped counters by **zero** (measured 1910→1910 / 1903→1903, was +14/+14); `dev_launch.py` advances each by exactly **one** (1909→1910 / 1902→1903); an aborted handshake counts nothing and an audit-decided technical loss counts one; `GATE-6-MEASUREMENT.md`'s "not a bug" claim corrected in place; `GAMES-PLAYED-RECONSTRUCTION.md` written with options and evidence and **no value set** (rules 37/38, REPORT-01/SEC-07/DOC-01) |
| 07-01 Gatekeeper chain — `GatekeeperParams` extraction + injected budget, `QuotaManager`, `DosDetector`, `chain.py`, `reporting.json` + loader | P0 | 1 | ☑ | Khaled | A zero-token mail call never touches a `TokenBudget`; the LLM path's reserve/settle order provably unchanged (Phase-4 tests pass unmodified); bucket admits ⌊C⌋ then blocks; quota exhaustion and a latched DOS lock return caller-handled refusals; daily counter survives a simulated restart (REPORT-02, REPORT-03) |
| 07-02 Artifact spine — names/`<NN>`/`game_uid` join + canonical signing, `config_<game_id>_g<NN>.json`, the `declaration_` wrapper | P0 | 1 | ☑ | Khaled | All four names match PARAMETERS exactly (pinned to `:165-168` as literals, with both negative halves); one shared `game_uid`, cross-checked against the filename's `<NN>`; `config_` round-trips to the same digest `config_hash` already computes (`23f86a93…` game_params, `c0e63220…` scent, both roles); both roles produce byte-identical `config_` (1526 bytes); the **signed** Step-0 payload is unchanged (`git diff` over `security/` + `network/` EMPTY, zero `STEP0_MISMATCH`) and the handshake still passes (REPORT-06). **D7-1 resolved**: artifacts move to `game_artifacts/`, the `logs/` ignore rule is byte-unchanged, `write_artifact` refuses a `logs/` path |
| 07-03 `LocalView` firewall — read model + view builder + CI local-truth gate | P0 | 1 | ☑ | Khaled | Opponent cell `(5,3)` — chosen distinct from own `(0,0)`, from both barriers and from the belief argmax `(6,6)`, and with both flat indices (38 / 26) distinct from every other integer in the view — appears **nowhere** in either serialised form, in any of five encodings; the identical scanner **does** report a deliberately leaky view (the counter-control) and every one of its five encoding branches, and **does** find the four cells the view may legally carry (anti-vacuity); measured that with an always-clean scanner test (a) alone still passes 2/2 while exactly those three counter-controls fail. `scripts/check_local_truth.py` reports imports **and** `.state.<cop|thief|barriers>` chains and **exits 2 on an empty/missing root** rather than printing OK; `view_builder` proven by AST scan to be the only `sdk/` reader of the true position (a planted new reader fails it). All three failed against pre-plan code (2 collection ERRORs + 8 failed). 32 tests; 30 revert probes, all non-zero. Suite 1794→1826, coverage 96.90→96.95%, counters 0/0 on the suite and 1/1 on one real game (REPORT-08, D-74) |
| 07-11 Display belief — rules 8–9 **recovery** (not absence), option (a), `strategy/display_belief.py` + `docs/PRD_display_belief.md` | P0 | 2 | ☑ | Khaled | 07-03's firewall was green **because its fixtures were vacuous**: `scent_field` never called `emit_opponent` (all-zero grid in all 30 probes) and belief was seeded with a cell production never supplies. Corrected to run one real `decide(known_cell=ctx.state.thief)`, 07-03's own load-bearing absence test FAILED at `$.belief.argmax: pair [5, 3]`. Measured leak: argmax `(5,3)`; support `[(4,3),(5,2),(5,3),(5,4),(6,3)]`, 5/49, P 0.5556, inverting geometrically to `[(5,3)]`; `scent.opponent` peak `(5,3)` at **0.9** = `scent.json`'s `source`; sealed thief (2 barriers of quota 14) argmax `(0,0)`, entropy `-0.0`, lit `[(0,0,1.0)]`. **Option (a) chosen** (display-only `BeliefMap`, never fed `ctx.state.thief`) over (b) (a permanently blank cop panel), reasoned in the PRD and the source. After: argmax `(1,3)`, entropy 5.5469, support 47/49, P(truth) 0.0223, inversion `[]`, scent peak `(4,4)` at 0.154; sealed argmax `(1,2)`, entropy 5.5472. Strategy belief **unchanged** (argmax still `(5,3)`, entropy 1.88) — provenance is legitimate, rule 9 governs display. Thief published belief **byte-identical**, sha256 `0b046a94…e3b0`, 2565 bytes. The argmax-only "fix" measured: coordinate scanner **PASSES 2/2** while the geometric, scent and floor tests all FAIL. 19 tests; 12 revert probes, all non-zero (9/3/3/2/14/9/3/3/2/2/22/3). Floors derived, never invented. Suite 1826→1845, coverage 96.95%, counters 0/0 suite and 1/1 on one real game (REPORT-02, STRAT-05, QUAL-11, D-74) |
| 07-04 Mail transport — `MailSink`, `DryRunSink`, `GmailSink` (fake transport), MIME attachment, 429 backoff, `.env-example` | P0 | 2 | ☑ | Khaled | JSON is an **attachment** and the body is a fixed boilerplate constant — asserted by re-parsing the rendered bytes, with paired controls that plant a distinctive report value in the body and in a header and require the leak check to FAIL (probes: report inlined into the body 2 failed; JSON sent inline instead of attached 4 failed; attachment renamed off 07-02's namer 2 failed). Recipient from **both** shipped configs equals the literal `rmisegal+uoh26finalgame@gmail.com` transcribed from `docs/PARAMETERS.md:178`. Rule 30 enforced **twice** — the requested scopes before any credential is read (6 forbidden sets, all refused) and the granted scopes on the loaded token before any send (probes: gate weakened to membership 4 failed; granted check removed 1 failed; gate ordered after the credential read 7 failed). 429→429→200 succeeds after **exactly two injected sleeps of 30 s**, asserted against a transcribed literal, not against the params under test (probes: `GmailSink` swallows 429 → 3 failed; backoff lowered to Table 19's bare 5 s → 3 failed). Permanent 429 exhausts 4 attempts, queues, and the report is **recovered from the queue** — the same object reaches the wire on `drain()`. `google-*` imported by exactly **one** module in `src/` (grepped). Zero network: non-loopback connect and DNS both blocked by an autouse fixture with two control tests. LLM path still 5 s; `git diff config/` empty. 71 tests; suite 1846→1919, coverage 96.95→97.02%, counters 0/0 on the suite and 1/1 on one real game (REPORT-01, REPORT-04, REPORT-05) |
| 07-05 `log_<game_id>_g<NN>.json` builder — wire JSONL × nonce ledger, joined on local turn truth | P0 | 2 | ☑ | Khaled | Every turn carries commitment, revealed move, intent, hint, verdict, nonce and hash, with the five `verify_reveal` inputs at the top level. Re-hash on a real `dev_launch` game (`521519a78f96c255`): **5/5 = 100%** on BOTH seats, and **100% again with the `.jsonl` and `.ledger.jsonl` DELETED** — self-containment proven by removing the sources, not by asserting it. Join keyed on LOCAL turn truth (06-05's discipline, reused): the adversarial fixture stamps the peer's COMMIT turn 99 and REVEAL turn 7 on one local turn 3 and the artifact still pairs them; re-keying on `envelope.turn` **loses 1 of 4 peer pairs and invents 2 phantom turns (5 entries → 7, keys `[0,1,2,3,4,7,99]`)**, while the honest fixture is unchanged — a happy path alone proves nothing. Even with no adversary, **every real game carries 5–6 received records whose local turn differs from the peer's stamp** (measured across 20 logs; every incoming HINT), so the wrong key misfiles them all. Truncated tail → artifact + explicit sealed `truncated_tail` marker; mid-file corruption → `CorruptLogError`; `read_all`'s fail-loud audit contract byte-unchanged. **Rules 8–9: zero internal state** — the `language_turn` record is copied by ALLOW-LIST, and all six `LANGUAGE_INTERNAL_FIELDS` (incl. D7-8's `belief_argmax`) are absent from both real artifacts, with a counter-control proving the scan finds a planted one. D-61 found on a real thief log (42 records, **two** game_uids): fixed by carrying `prior_game_uids` inside the seal rather than refusing the thief an artifact. Nonce separation enforced as a scan over 173 `src/` files, not a grep. 55 tests; 21 revert probes, all non-zero. Suite 1919→1974, coverage 97.02→97.12%, all four new modules at 100%; counters 0/0 on the suite and 1/1 on one real game (REPORT-06, REPORT-09) |
| 07-06 Live GUI — Tk dashboard over a PUBLISHED `LocalView` snapshot, separate process (D-76) | P0 | 2 | ☑ | Khaled | **D-76: a separate PROCESS, not `after()` off the turn loop.** Three measured reasons: `tk.mainloop()` blocks its calling thread and `Watchdog` `os._exit`s at the configured 60 s threshold; Tk is not thread-safe, and a polling thread could sample the pure 1.0/0.0 delta window inside `decide()`; a separate process CANNOT HOLD the true joint position, turning D-74's type-level firewall into a PROCESS-level one. The agent's only cost is one best-effort `durable_write_json` at the single `maybe_resolve` publication site — it never raises, never changes a return value, and `dev_launch` logged **zero `watchdog_incident`, zero `technical_win`, `"matched":true` on both seats, outcome capture** (`2db6cc8b039c82e7`). **OQ-6 (refresh interval) has NO source and is not written into `src/`**: `--refresh-ms` is `required=True` with no `default=`, and `LiveDashboard.__init__` takes it keyword-only with no default (the `Watchdog`/`TokenBucket` precedent); 07-10 records the value used for the screenshot. **`gui/` is 5 files / 200 code lines with ZERO derivation** — every colour, shade, pixel bound, grid position and string lives in `sdk/view_render.py` + `sdk/view_text.py` (both 100% covered), because `pyproject.toml:38` omits `*/gui/*` from coverage; enforced structurally by `test_gui_structural.py` (zero arithmetic BinOps, zero f-strings/`.join`/`.format`, every `pursuit` import under `pursuit.sdk`/`pursuit.gui`), each scan paired with a control proving it can fail. **D7-6 CLOSED honestly**: `check_local_truth.py` prints `OK: 5 module(s) scanned`, exit 0, because the code passes — and D7-9's three blind spots were MEASURED OPEN then closed (a bare `__init__.py` printed `OK: 1 module(s) scanned` exit 0 → now exit 2; a `panel.pyw` reading `ctx.state.thief` was never scanned → now reported; `s = ctx.state; s.thief` + `getattr(ctx.state,'thief')` + `asdict(s)['cop']` returned `violations=[]` exit 0 → now 3 violations). **A RUNTIME recovery test over what the panels PAINT** found a leak channel the numbers could not: **quantisation** — a ramp that rounded small probabilities to the background would draw a support smaller than `display.min_support_cells` guards, and a 5-cell drawn plus names its centre; `shade` therefore reserves the background for exactly zero and the drawn support is asserted EQUAL to the published one. Both counter-controls fire (a leaky panel inverts to `(5,3)`; the peak-cell-line-deleted sidebar earns a clean scan while the heatmap still inverts). Verified on the LIVE game: true thief cell `(2,3)`, published argmax `(1,1)`, entropy 5.5587, drawn support 49/49 == published, inversion `[]`, scent peak `(2,2)`, no forward pair `[2,3]` and no flat index 17/23 anywhere in the file. 18 revert probes, every count non-zero. Suite 1974→2046, coverage 97.12→97.18%, counters 0/0 on the suite and 1/1 on one real game (REPORT-08, D-74, D-76, D7-6, D7-7, D7-9) |
| 07-07 End-of-game reporting — `result_<game_id>.json`, token totals, rule-35 agreement, send via the chain in `dry_run` | P0 | 3 | ☐ | Khaled | `result_` carries this game's **and** the series' LLM token totals (proven: series total > one game's after two games) plus the git commit hash; honest game → `agreed: true`, fabricated disagreement → `agreed: false` **and still reported**; a send failure leaves outcome and exit code untouched (REPORT-01, REPORT-06, REPORT-07) |
| 07-08 Replay viewer — load `log_`, recompute hashes, verdict banner, step/play/pause | P0 | 3 | ☐ | Khaled | Clean log → `Verified OK`; single-bit tamper in `state`/`move`/`intent`/`nonce` → `FAILED` naming the turn; **empty log → neither verdict**, an explicit "nothing to verify"; verification goes through `commit_pack`, never a second serializer; the verdict function has a grep-proven production caller (REPORT-09) |
| 07-09 GATE-7 measurement + `docs/PRD_gatekeeper.md` + `OAUTH-RUNBOOK.md` | P1 | 4 | ☐ | Khaled | One command, zero credentials, honest PASS/FAIL per criterion; criterion 1 reported as **`dry_run` PASS + live PENDING**, never a blanket PASS; per-mechanism PRD documents the chain and the source of every number; no `gui/` import at module scope in the measurement script (DOC-02) |
| **07-10 HUMAN CHECKPOINT — `autonomous: false`** — OAuth consent + send-only client, one live send, README screenshots, OQ-5 decision | P0 | 5 | ☐ | **Khaled (human)** | Consent completed by a person (Claude must not enter credentials or click consent); one live message delivered to `rmisegal+uoh26finalgame@gmail.com` with the JSON attached; criterion 1 flips PENDING→PASS with delivered-message evidence; presentation-grade GUI + `Verified OK` screenshots committed; OQ-5 resolved in writing; `reporting.mode` returned to `dry_run` (REPORT-01) |
| 07-96 Refresh the graphify graph at plan-phase and after execute | P2 | — | ◐ | Khaled | `GRAPH_REPORT.md` current with `services/reporting/`, `sdk/local_view.py`, `gui/`. Refreshed after 07-06: **9734 nodes / 17412 edges / 579 communities**; `graphify explain publish_view` resolves to `view_publish.py:90` (degree 17, edge in from `maybe_resolve`) and `LiveDashboard` to `live_app.py:47`. `gui/` is now in the graph |
| 07-97 Create/refresh `docs/phases/phase-7/{PRD,PLAN,TODO}.md` at plan-phase | P1 | — | ☑ | Khaled | This triplet exists and matches the plan outline (created 2026-08-17) |
| 07-99 On verify-work: mark all rows ☑ + tick root `docs/TODO.md` | P1 | — | ☐ | Khaled | Phase gate met on measured evidence; all TODOs checked (DOC-01) |

## Phase gate (§10.4)
- [ ] **1.** A game summary is sent by mail — send-only OAuth, through the gatekeeper, attached
      JSON, never free text. *(Measured in two halves: the whole stack end to end under
      `reporting.mode = dry_run` with a fake Google client — 07-09; plus one live send that needs a
      human at the consent screen — 07-10.)*
- [ ] **2.** The live GUI displays state — **only local truth**, never the full objective board.
      *(Machine-measurable: renders + the D-74 firewall tests with their counter-control. Only the
      presentation-grade README screenshot is a human aesthetic call.)*
- [ ] **3.** The replay app reconstructs a recorded round and shows `Verified OK`.
      *(Machine-measurable: clean → OK, tampered → FAILED, empty → neither.)*

## The one human-gated item
Everything through wave 4 runs unattended. **07-10 is the only `autonomous: false` plan**, and it
exists because Claude must not enter credentials or click through Google's OAuth consent screen,
and cannot judge whether a screenshot is presentation-grade. It is a short scripted checkpoint
against `OAUTH-RUNBOOK.md`, following the GATE-4 live-run and GATE-5 remote-round precedent.

## Open questions blocking parts of execution
Recorded rather than invented — full text in
[07-PLAN-OUTLINE.md §4](../../../.planning/phases/07-reporting-and-visualization-shell/07-PLAN-OUTLINE.md):

| # | Question | Blocks | Who decides |
|---|---|---|---|
| OQ-1 | The Quota Manager's **daily send ceiling** has no value in PARAMETERS.md (SEGAL §4 gives an *hourly* 500) | 07-01 | confirm, then D-18-style labelled engineering default |
| OQ-2 | The **DOS detector's** trip threshold and lock duration have no source at all | 07-01 | same — or define it structurally, with no new number |
| OQ-3 | "Take the stricter value" is ambiguous for backoff (Table 19 ≥ 5 s vs SEGAL 30 s) | 07-01 | one-line confirmation |
| OQ-4 | One `result_` per **series** vs one email per **game** (rule 32 + mandatory-rule 5) | 07-07 | interpretation, flagged not quoted |
| OQ-5 | `config/police/games_played.json` reads **1881** — incremented once per agent start — and this phase mails that number. **Rule 38: a false games-played declaration is an absolute disqualification.** *(**Updated 2026-08-17 by 07-00.** The MECHANISM is fixed: the increment now happens once per completed game and the suite can no longer touch the counter, measured +0 per full suite run and +1 per real game. The **VALUE is still open and still human-only** — the files now read 1910 / 1903 and both are known-wrong. Two decisions are needed, not one: which reading of "a game played", and per-team vs per-agent counting. Evidence, candidate readings and derivations in [GAMES-PLAYED-RECONSTRUCTION.md](GAMES-PLAYED-RECONSTRUCTION.md); do not let any agent pick the number.)* | 07-10 | **human, not Claude** |
